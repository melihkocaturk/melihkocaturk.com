<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.4">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2024-05-22T00:42:59+03:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Melih Kocatürk</title><subtitle>10 yılı aşkın süredir yazılım geliştiriyor. Lise ve Üniversite eğitimini Elektrik-Elektronik üzerine aldı. Teknolojiye olan ilgisi,  bilgisayar programcılığını kariyer olarak seçmesini sağladı. Birçok ulusal ve büyük çaplı projede çalıştı. İşini seven bir yazılımcı  olarak, sürekli kendimi nasıl daha iyi geliştirebilirim sorusuna cevap arıyor...
</subtitle><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><entry><title type="html">Ruby.new</title><link href="http://localhost:4000/2024/05/21/ruby-new.html" rel="alternate" type="text/html" title="Ruby.new" /><published>2024-05-21T00:00:00+03:00</published><updated>2024-05-21T00:00:00+03:00</updated><id>http://localhost:4000/2024/05/21/ruby-new</id><content type="html" xml:base="http://localhost:4000/2024/05/21/ruby-new.html"><![CDATA[<p>Programlama dilleri üzerine birçok kitap hemen hemen aynı görünür. Temel türlerle ilgili bölümlerle başlarlar: integer, string vb. Daha sonra <code class="language-plaintext highlighter-rouge">if</code> ve <code class="language-plaintext highlighter-rouge">while</code> ifadelerine ve döngülerine geçmeden önce 2 + 3 gibi ifadelere bakarlar. Daha sonra belki 7. veya 8. Bölüm civarında sınıflardan bahsetmeye başlayacaklar. Bunu biraz sıkıcı buluyoruz.</p>

<p>Bunun yerine, bu kitabı tasarlarken büyük bir planımız vardı. Dili yukarıdan aşağıya, sınıflardan ve nesnelerden başlayarak, en ince ayrıntısına kadar söz dizimi ayrıntılarıyla sona erdirerek belgelemek istedik. O zamanlar iyi bir fikir gibi görünüyordu. Sonuçta Ruby’deki her şey bir nesnedir, bu yüzden önce nesnelerden bahsetmek mantıklıydı.</p>

<p>Ya da biz öyle düşündük.</p>

<p>Ne yazık ki bir dili yukarıdan aşağıya anlatmak çok zor. Eğer dizeleri, <code class="language-plaintext highlighter-rouge">if</code> ifadelerini, atamaları ve diğer detayları ele almadıysanız sınıf örnekleri yazmak zordur. Yukarıdan aşağıya açıklamamız boyunca, örnek kodun anlamlı olması için gerekli olan düşük düzeyli ayrıntıları vermeye karar verdik.</p>

<p>Böylece başka bir büyük plan hazırladık (bize boşuna pragmatik demiyorlar). Ruby’yi hâlâ en yukarıdan başlayarak tanımlıyoruz. Ancak bunu yapmadan önce, Ruby’de kullanılan özel sözcüklerin yanı sıra örneklerde kullanılan tüm ortak dil özelliklerini açıklayan kısa bir bölüm ekledik; bu, bizi kitabın geri kalanına yönlendirecek mini bir eğitimdir. Ve bu mini eğitim bu bölümdür.</p>

<h2 id="ruby-nesne-yönelimli-bir-dildir">Ruby Nesne Yönelimli Bir Dildir</h2>

<p>Tekrar söyleyelim. Ruby nesne yönelimli bir dildir. Programlama açısından bir nesne, verileri, bu verileri işleyen mantıkla birleştiren bir şeydir. Ve bir dil, nesnelerin yaratılmasını kolaylaştıran dil yapıları sağlıyorsa “nesne yönelimlidir”. Tipik olarak nesne yönelimli diller, nesnelerin, verilerinin ne olduğunu tanımlamasına, işlevlerini tanımlamasına ve diğer nesnelerin bu işlevselliğe erişmesine izin vermek için ortak bir sözdizimi sağlamasına olanak tanır.</p>

<p>Java, JavaScript ve Python gibi diğer nesne yönelimli dillerin aksine, tüm Ruby türleri nesnelerdir ve farklı davranan nesne olmayan temel türler yoktur. Ancak birçok dil nesne yönelimli olduğunu iddia eder ve bu diller genellikle nesne yönelimli nin ne anlama geldiği konusunda farklı bir yoruma ve kullandıkları kavramlar için farklı bir terminolojiye sahiptir.</p>

<p>O halde ayrıntılara çok fazla girmeden önce Ruby hakkında konuşmak için kullanacağımız terimlere ve gösterimlere kısaca göz atalım.</p>

<p>Nesneye yönelik programlar yazdığınızda, kavramları dış dünyadan veya mantıksal alanınızdan modellemeye çalışırsınız. Bu modelleme süreci sırasında, kodda temsil edilmesi gereken ilgili veri ve davranış kategorilerini keşfedeceksiniz. Müzik kutusunu temsil eden bir sistemde “şarkı” kavramı böyle bir kategori olabilir. Bir şarkı, durumu (örneğin şarkının adı) ve bu durumu (state) kullanan metotları (belki de şarkıyı çalmaya yönelik bir metodu) birleştirebilir. Ruby’de şarkıların genel durumunu temsil etmek için <code class="language-plaintext highlighter-rouge">Song</code> adında bir sınıf tanımlarsınız.</p>

<p>Bu sınıflara sahip olduğunuzda, genellikle her birinin birkaç ayrı örneğini oluşturmak isteyeceksiniz. <code class="language-plaintext highlighter-rouge">Song</code> adlı bir sınıf içeren müzik kutusu sistemi için, “Ruby Tuesday”, “Enveloped in Python”, “String of Pearls”, “Small Talk” vb. gibi farklı adlara sahip popüler hitler için ayrı örnekleriniz olacaktır. Bu örneklerin her birinin kendi durumu vardır ancak sınıfın ortak davranışını paylaşırlar. Nesne (object) kelimesi sıklıkla örnek (instance) kelimesiyle birbirinin yerine kullanılır.</p>

<p>Ruby’de örnekler, sınıfla ilişkili özel bir metot olan kurucunun (constructor) çağrılması yoluyla oluşturulur. Standart kurucuya <code class="language-plaintext highlighter-rouge">new</code> denir. <code class="language-plaintext highlighter-rouge">new</code> metodu sizin için Ruby tarafından tanımlanır ve bunu kendi başınıza tanımlamanıza gerek yoktur. Bunun gibi örnekler oluşturabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>song1 = Song.new("Ruby Tuesday")
song2 = Song.new("Enveloped in Python") 
# and so on
</code></pre></div></div>

<p>Bu örneklerin her ikisi de aynı sınıftan türetilmiştir ancak her birinin benzersiz özellikleri vardır. Her nesnenin, <code class="language-plaintext highlighter-rouge">object_id</code> özelliği aracılığıyla erişilebilen benzersiz bir nesne tanımlayıcısı vardır. Yukarıdaki örnekte, <code class="language-plaintext highlighter-rouge">song1.object_id</code> ve <code class="language-plaintext highlighter-rouge">song2.object_id</code>‘yi kontrol ederseniz, bunların farklı değerlere sahip olduğunu görürsünüz.</p>

<p>Her örnek için, o örneğe özgü değerlere sahip değişkenler tanımlayabilirsiniz. Bu örnek değişkenleri nesnenin durumunu tutar. Örneğin şarkılarımızın her biri, o şarkının adını taşıyan bir örnek değişkene sahip olacak.</p>

<p>Her sınıf içinde örnek metotları tanımlayabilirsiniz. Her metot, sınıf bağlamında ve genellikle sınıfın dışından çağrılabilen bir işlevsellik yığınıdır; ancak hangi metotların harici olarak kullanılabileceğine ilişkin kısıtlamalar belirleyebilirsiniz. Bu örnek metotların nesnenin örnek değişkenlerine ve dolayısıyla nesnenin durumuna erişimi vardır. Örneğin bir <code class="language-plaintext highlighter-rouge">Song</code> sınıfı, <code class="language-plaintext highlighter-rouge">play</code> adı verilen bir örnek metodu tanımlayabilir. Bir değişken belirli bir <code class="language-plaintext highlighter-rouge">Song</code> örneğine referans veriyorsa, o örneğin <code class="language-plaintext highlighter-rouge">play</code> metodunu çağırabilir ve o şarkıyı çalabilirsiniz.</p>

<p>Sözdizimsel olarak, bir metot nokta sözdizimi kullanılarak çağrılır; işte bazı örnekler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># intro/puts_examples.rb
"gin joint".length # =&gt; 9
"Rick".index("c") # =&gt; 2
42.even? # =&gt; true
sam.play(song) # =&gt; "duh dum, da dum de dum ..."
</code></pre></div></div>

<p>Her satır çağrılan metodu gösterir. Noktadan önceki öğeye metodun alıcısı denir ve noktadan sonra gelen öğe çağrılacak metodun adıdır. İlk örnekte “gin joint” dizesinin uzunluğu sorulur; ikincisi farklı bir dizeden c harfinin indeksini bulmasını ister. Üçüncü satır 42 sayısının çift olup olmadığını sorar (soru işareti <code class="language-plaintext highlighter-rouge">even?</code> metot adının bir parçasıdır). Son olarak, <code class="language-plaintext highlighter-rouge">sam</code> adlı bir nesneden bize bir şarkı çalmasını istiyoruz (başka bir yerde tanımladığımız bir nesneye referans veren <code class="language-plaintext highlighter-rouge">sam</code> adında bir değişkenin olduğunu varsayarak).</p>

<p>Gönderilen metotlardan bahsettiğimizde genellikle nesneye bir mesaj gönderdiğimizi söyleriz. Mesaj, metodun adını ve metodun bekleyebileceği tüm argümanları içerir. Nesne, mesaja bu isimdeki metodu çağırarak yanıt verir. Metot çağrılarını nesnelere mesaj şeklinde ifade etme fikri Smalltalk programlama dilinden gelmektedir. Bir nesne bir mesaj aldığında, karşılık gelen bir metot için kendi sınıfını arar. Bulunursa bu metot yürütülür. Metot bulunamazsa, Ruby onu arar.</p>

<p>Burada Ruby ile diğer nesne yönelimli diller arasındaki büyük farka dikkat çekmekte yarar var. Örneğin Java’da, ayrı bir fonksiyonu çağırıp o sayıyı ileterek, sayının mutlak değerini bulursunuz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>num = Math.abs(num); // Java code
</code></pre></div></div>

<p>Ruby’de mutlak değer belirleme yeteneği, ayrıntılarla dahili olarak ilgilenen numbers sınıfına yerleştirilmiştir. Basitçe <code class="language-plaintext highlighter-rouge">abs</code> mesajını bir sayı nesnesine gönderirsiniz ve işi onun yapmasına izin verirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>num = -1234 # =&gt; -1234 
positive = num.abs # =&gt; 1234
</code></pre></div></div>

<p>Aynı durum tüm Ruby nesneleri için geçerlidir. Python’da <code class="language-plaintext highlighter-rouge">len(name)</code> yazarsınız, ancak Ruby’de bu <code class="language-plaintext highlighter-rouge">name.length</code> vb. şeklinde olur. Ruby’nin temel türleri olmayan saf nesne yönelimli bir dil olduğunu söylerken kastettiğimiz şey budur.</p>

<h2 id="temel-ruby">Temel Ruby</h2>

<p>Pek çok insan yeni bir dil öğrenirken sıkıcı sözdizimi kurallarını okumaktan hoşlanmaz, bu yüzden hile yapacağız. Bu bölümde öne çıkanlara değineceğiz - Ruby programları yazacaksanız bilmeniz gereken şeyler var.</p>

<p>Basit bir Ruby programıyla başlayalım. Kişiselleştirilmiş selamlama döndüren bir metot yazacağız. Daha sonra bu metodu birkaç kez çağıracağız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># intro/hello1.rb
def say_hello_goodbye(name)
  result = "I don't know why you say goodbye, " + name + ", I say hello"
  return result
end
# call the method
puts say_hello_goodbye("John")
puts say_hello_goodbye("Paul")
# I don't know why you say goodbye, John, I say hello
# I don't know why you say goodbye, Paul, I say hello
</code></pre></div></div>

<p>Örnekte görüldüğü gibi Ruby’nin sözdizimi düzenlidir. Her bir ifadeyi ayrı bir satıra koyduğunuz sürece ifadelerin sonunda noktalı virgül kullanmanıza gerek yoktur. Ruby yorumları <code class="language-plaintext highlighter-rouge">#</code> karakteriyle başlar ve satırın sonuna kadar devam eder. Kod düzeni size kalmış; girinti önemli değildir. Bununla birlikte, iki karakterli girinti Ruby topluluğunun en çok tercih ettiği seçenektir.</p>

<p>Metotlar, <code class="language-plaintext highlighter-rouge">def</code> anahtar sözcüğü, ardından metot adı (bu durumda ad <code class="language-plaintext highlighter-rouge">say_hello_goodbye</code>) ve ardından parantez içindeki metot parametreleri ile tanımlanır (Aslında parantezler isteğe bağlıdır, ancak bunları kullanmanızı öneririz). Ruby, bileşik ifadelerin ve tanımların gövdelerini sınırlamak için parantez kullanmaz. Bunun yerine, gövdeyi <code class="language-plaintext highlighter-rouge">end</code> anahtar sözcüğüyle tamamlamanız yeterlidir. Metodun gövdesi oldukça basittir. İlk satır, “I don’t know why you say goodbye, “ dizesini ve parametre adı ile “, I say hello” dizesini birleştirir ve sonucu local değişken <code class="language-plaintext highlighter-rouge">result</code>‘a atar. Bir sonraki satırda bu sonucu döndürür. <code class="language-plaintext highlighter-rouge">result</code> değişkenini bildirmemize gerek olmadığını unutmayın; biz ona değer atadığımızda ortaya çıktı.</p>

<p>Metodu tanımladıktan sonra onu iki kez çağırıyoruz. Her iki durumda da sonucu <code class="language-plaintext highlighter-rouge">puts</code> metoduna aktarıyoruz, bu metot basitçe argümanı ve ardından yeni satır verir (sonraki çıktı satırına geçer):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>puts say_hello_goodbye("John")
puts(say_hello_goodbye("John"))
</code></pre></div></div>

<p>Hayat her zaman basit değildir ve öncelik kuralları, hangi argümanın hangi metot çağrısına uygun olduğunu bilmeyi zorlaştırabilir; bu nedenle en basit durumlar dışında tüm durumlarda parantez kullanmanızı öneririz. Metodun açık bir alıcısı olmadığı ve yalnızca bir argümanı olduğu zaman Ruby programlarının sıklıkla parantezleri atladığını göreceksiniz.</p>

<p>Bu örnek aynı zamanda Ruby string nesnelerini de göstermektedir. String nesnesi oluşturmanın birçok yolu vardır, ancak en yaygın olanı, tek veya çift tırnak işaretleri arasındaki karakter dizileri kullanmaktır. İki form, Ruby’nin literal oluştururken string üzerinde yaptığı işlem miktarı açısından farklılık gösterir. Tek tırnaklı durumda Ruby çok az şey yapar. Birkaç istisna dışında, string literal’e girdiğiniz şey string’in değeri olur.</p>

<p>Çift tırnaklı durumda Ruby daha fazla iş yapar. Öncelikle ters eğik çizgi karakteriyle başlayan ikame dizilerini arar ve bunları bir binary değerle değiştirir. Bu ikamelerden en yaygın olanı, yeni satır karakteriyle değiştirilen <code class="language-plaintext highlighter-rouge">\n</code>‘dir. Yeni satır içeren bir string çıktısı alındığında, bu yeni satır, satır sonu haline gelir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>puts "Hello and goodbye to you,\nGeorge"

# Hello and goodbye to you,
# George
</code></pre></div></div>

<p>Ruby’nin çift tırnaklı dizelerle yaptığı ikinci şey ifade enterpolasyonudur. Dize içinde <code class="language-plaintext highlighter-rouge">#{EXPRESSION}</code> dizisinin yerini <code class="language-plaintext highlighter-rouge">EXPRESSION</code> değeri alır. Bunu önceki metodumuzu yeniden yazmak için kullanabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def say_hello_goodbye(name)
  result = "I don't know why you say goodbye, #{name}, I say hello"
  return result
end
puts say_hello_goodbye("Ringo")
# I don't know why you say goodbye, Ringo, I say hello
</code></pre></div></div>

<p>Ruby bu string nesnesini oluşturduğunda, <code class="language-plaintext highlighter-rouge">name</code>‘in mevcut değerine bakar ve onu string’in yerine koyar. <code class="language-plaintext highlighter-rouge">#{...}</code> yapısında karmaşık ifadelere izin verilir. Aşağıdaki örnekte, parametremizi büyük harfle başlatmak için <code class="language-plaintext highlighter-rouge">capitalize</code> metodunu çağırıyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def say_hello_goodbye(name)
  result = "I don't know why you say goodbye, #{name.capitalize}, I say hello"
  return result
end
puts say_hello_goodbye("john")
# I don't know why you say goodbye, John, I say hello
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">say_hello_goodbye</code> metodumuzu biraz daha basitleştirebiliriz. Açık bir <code class="language-plaintext highlighter-rouge">return</code> ifadesinin yokluğunda, Ruby metodunun döndürdüğü değer, değerlendirilen son ifadenin değeridir, böylece geçici değişkenden ve <code class="language-plaintext highlighter-rouge">return</code> ifadesinden tamamen kurtulabiliriz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def say_hello_goodbye(name)
  "I don't know why you say goodbye, #{name}, I say hello"
end
puts say_hello_goodbye("Paul")
# I don't know why you say goodbye, Paul, I say hello
</code></pre></div></div>

<p>Bu sürüm daha deyimsel (idiomatic) olarak kabul edilir; bununla, uzman Ruby programcılarının Ruby programlarını yazmayı seçme şekliyle daha uyumlu olduğunu kastediyoruz. Idiomatic Ruby, Ruby’nin kısayollarına ve belirli sözdizimine güvenme eğilimindedir. Idiomatic Ruby stiline ilişkin yönergeleri <a href="https://github.com/testdouble/standard">https://github.com/testdouble/standard</a> adresinde bulabilirsiniz, ki bu kitaptaki kodlar için kullanılmıştır (bir noktaya değinmek için kurallarını kasıtlı olarak ihlal ettiğimiz durumlar hariç).</p>

<p>Bu bölümün kısa olacağına söz vermiştik. Ele almamız gereken bir konu daha var: Ruby adları. Kısaca açıklamak gerekirse, burada tanımlamayacağımız bazı terimleri (sınıf değişkeni gibi) kullanacağız. Ancak, şimdi kurallar hakkında konuşarak, sınıf değişkenlerini ve benzerlerini daha sonra tartışmaya başladığımızda oyunun ilerisinde olacaksınız.</p>

<p>Ruby ilk başta tuhaf görünebilecek bir kural kullanıyor: Bir ismin ilk karakterleri değişkenin ne kadar görünür olduğunu gösterir. Local değişkenler, metot parametreleri ve metot adlarının tümü küçük harfle veya alt çizgiyle başlamalıdır (Ruby’nin kendisinde büyük harfle başlayan birkaç metot vardır, ancak genel olarak bu kendi kodunuzda yapılacak bir şey değildir).</p>

<p>Global değişkenlere dolar işareti <code class="language-plaintext highlighter-rouge">$</code> eklenir ve örnek değişkenler “et” işareti <code class="language-plaintext highlighter-rouge">@</code> ile başlar. Sınıf değişkenleri iki “et” <code class="language-plaintext highlighter-rouge">@@</code> işaretiyle başlar. Her ne kadar burada konuyu tamamlamak adına global ve sınıf değişkenlerinden bahsetsek de, bunların Ruby programlarında nadiren kullanıldığını göreceksiniz. Global değişkenlerin programların sürdürülmesini zorlaştırdığına dair pek çok kanıt var. Sınıf değişkenleri global değişkenler kadar tehlikeli değildir, ancak güvenli bir şekilde kullanılması hala zordur; sadece insanlar bunları pek kullanmama eğilimindedir çünkü benzer işlevleri elde etmenin genellikle daha kolay yolları vardır. Son olarak sınıf adları, modül adları ve diğer sabitler büyük harfle başlamalıdır.</p>

<p>Bu başlangıç karakterinden sonra ad, herhangi bir harf, rakam ve alt çizgi kombinasyonunu içerebilir, istisna olarak <code class="language-plaintext highlighter-rouge">@</code> işaretini takip eden karakter rakam olamaz. Bununla birlikte, geleneksel olarak, çok kelimeli örnek değişkenler, <code class="language-plaintext highlighter-rouge">first_name</code> veya <code class="language-plaintext highlighter-rouge">zip_code</code> gibi kelimelerin arasına alt çizgilerle yazılır ve çok kelimeli sınıf adları, <code class="language-plaintext highlighter-rouge">FirstName</code> veya <code class="language-plaintext highlighter-rouge">ZipCode</code> gibi her kelime büyük harfle yazılacak şekilde MixedCase (bazen CamelCase olarak da adlandırılır) yazılır. Sabitler, <code class="language-plaintext highlighter-rouge">FIRST_NAME</code> veya <code class="language-plaintext highlighter-rouge">ZIP_CODE</code> gibi sözcükler alt çizgilerle ayrılmış şekilde tamamen büyük harflerle yazılır. Metot adları <code class="language-plaintext highlighter-rouge">?</code>, <code class="language-plaintext highlighter-rouge">!</code> ve <code class="language-plaintext highlighter-rouge">=</code> karakterleriyle bitebilir.</p>

<h2 id="diziler-ve-hashler">Diziler ve Hash’ler</h2>

<p>Ruby, nesneleri koleksiyonlarda birleştirmenin birkaç farklı yolunu sunar. Çoğu zaman bunlardan ikisini kullanacaksınız: Diziler ve Hash’ler. Dizi, nesnelerin doğrusal bir listesidir; bunları, ilk slot için sıfırdan başlayarak dizideki yerlerinin sayısı olan indeksleri aracılığıyla alırsınız. Hash bir ilişkilendirmedir, yani her değerin isteğe bağlı bir anahtara sahip olduğu anahtar/değer deposudur ve değeri bu anahtar aracılığıyla alırsınız. Hem diziler hem de hash’ler, yeni öğeleri tutacak şekilde gerektiği kadar büyür. Herhangi bir dizi veya hash, farklı türdeki nesneleri tutabilir; Birazdan göreceğimiz gibi, önce tamsayı, sonra dize, sonra da kayan noktalı sayı içeren bir diziniz olabilir.</p>

<p>Bir diziyi (köşeli parantezler arasındaki öğeler kümesi) kullanarak yeni bir dizi nesnesi oluşturabilir ve başlatabilirsiniz. Bir dizi nesnesi verildiğinde, sonraki örnekte gösterildiği gibi, indeks ile tek tek öğelere erişebilirsiniz. Ruby dizi indekslerinin sıfırdan başladığını unutmayın.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a = [1, 'cat', 3.14] # array with three elements 
puts "The first element is #{a[0]}"
# set the third element
a[2] = nil
puts "The array is now #{a.inspect}"

# The first element is 1
# The array is now [1, "cat", nil]
</code></pre></div></div>

<p>Bu örnekte <code class="language-plaintext highlighter-rouge">nil</code> özel değerini kullandığımızı fark etmiş olabilirsiniz. Pek çok dilde <code class="language-plaintext highlighter-rouge">nil</code> (veya <code class="language-plaintext highlighter-rouge">null</code>) kavramı “nesne yok” anlamına gelir. Ruby’de durum böyle değildir; <code class="language-plaintext highlighter-rouge">nil</code>, tıpkı diğerleri gibi bir nesnedir. Bu sadece hiçlik kavramını temsil eden bir nesne. Neyse, dizilere ve hash’lere geri dönelim.</p>

<p>Ruby hash sözdizimi dizi sözdizimine benzer. Hash köşeli parantez yerine süslü parantez kullanır. Literal değer her giriş için iki nesne sağlamalıdır: biri anahtar, diğeri değer için. Genel olarak, anahtar ve değer <code class="language-plaintext highlighter-rouge">=&gt;</code> ile ayrılır, ancak çok yaygın olarak kullanılan bir kısayolun da olduğunu göreceğiz.</p>

<p>Örneğin, müzik enstrümanlarını orkestral bölümlerle eşlemek için hash kullanabilirsiniz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>instrument_section = {
  'cello' =&gt; 'string',
  'clarinet' =&gt; 'woodwind',
  'drum' =&gt; 'percussion',
  'oboe' =&gt; 'woodwind',
  'trumpet' =&gt; 'brass',
  'violin' =&gt; 'string'
}
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">=&gt;</code> işaretinin solundaki şey anahtardır, sağındaki şey ise karşılık gelen değerdir. Belirli bir hash’teki anahtarlar benzersiz olmalıdır; “drum” için iki girişiniz olamaz. Hash’teki anahtarlar ve değerler isteğe bağlı nesneler olabilir. Değerlerin diziler, diğer hash’ler vb. olduğu hash’lere sahip olabilirsiniz. Hash’teki anahtarların sırası sabittir ve her zaman anahtarların eklendiği sırayla eşleşecektir. Bir anahtara yeni değer atarsanız eski değer silinir.</p>

<p>Hash’ler dizilerle aynı köşeli parantez gösterimini kullanılarak indekslenir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p instrument_section["oboe"] # =&gt; "woodwind"\n"string"
p instrument_section["cello"] # =&gt; "string"\nnil
p instrument_section["bassoon"] # =&gt; nil
</code></pre></div></div>

<p>Hash’in içermediği bir anahtarı kullandığınızda varsayılan davranışı, değerin yokluğunu temsil eden <code class="language-plaintext highlighter-rouge">nil</code> değerini döndürmektir.</p>

<p>Bazen bu varsayılan davranışı değiştirmek isteyebilirsiniz. Örneğin, bir dosyada her kelimenin kaç kez geçtiğini saymak için hash kullanıyorsanız, varsayılan değerin sıfır olması uygun olur. Daha sonra kelimeyi anahtar olarak kullanabilir ve o kelimeyi daha önce görüp görmediğiniz konusunda endişelenmeden karşılık gelen hash değerini artırabilirsiniz. Bu, yeni, boş bir hash oluşturduğunuzda varsayılan değer belirtilerek yapılabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>histogram = Hash.new(0) # The default value is zero 
histogram["ruby"] # =&gt; 0
histogram["ruby"] = histogram["ruby"] + 1 
histogram["ruby"] # =&gt; 1
</code></pre></div></div>

<h2 id="semboller">Semboller</h2>

<p>Çoğu zaman programlama sırasında aynı dizeyi tekrar tekrar kullanmanız gerekir. Belki de dize hash’teki bir anahtardır veya belki de dize bir metodun adıdır. Bu durumda, muhtemelen dizeye erişimin sabit olmasını, böylece değerinin değişmemesini istersiniz ve ayrıca dizeye erişimin olabildiğince hızlı olmasını ve mümkün olduğunca az bellek kullanmasını istersiniz.</p>

<p>Bu da bizi Ruby’nin sembollerine getiriyor. Semboller tam olarak optimize edilmiş dizeler değildir, ancak çoğu amaç için bunları sabit olan, yalnızca bir kez oluşturulan ve aramanın çok hızlı olduğu özel dizeler olarak düşünebilirsiniz. Sembollerin anahtarlar ve tanımlayıcılar olarak kullanılması amaçlanırken, dizelerin veriler için kullanılması amaçlanmaktadır.</p>

<p>Bir sembol iki nokta üst üste ile başlar ve ardından ad gelir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>walk(:north)
look(:east)
</code></pre></div></div>

<p>Bu örnekte, koddaki sabit değerleri temsil etmek için <code class="language-plaintext highlighter-rouge">:north</code> ve <code class="language-plaintext highlighter-rouge">:east</code> sembollerini kullanıyoruz. Sembolleri bildirmemize veya onlara bir değer atamamıza gerek yok; Ruby bunu sizin için halleder. Bir sembolün değeri ismine eşdeğerdir.</p>

<p>Ruby ayrıca programınızda nerede görünürlerse görünsün, aynı ada sahip sembollerin aynı değere sahip olacağını, aslında aynı nesne olacağını garanti eder. Sonuç olarak, aşağıdakileri güvenle yazabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def walk(direction)
  nil unless direction == :north
  # ...
end
</code></pre></div></div>

<p>Değerleri değişmediği için semboller sıklıkla hash’lerde anahtar olarak kullanılır. Önceki hash örneğimizi sembolleri anahtar olarak kullanarak yazabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>instrument_section = { 
  :cello =&gt; "string", 
  :clarinet =&gt; "woodwind", 
  :drum =&gt; "percussion", 
  :oboe =&gt; "woodwind", 
  :trumpet =&gt; "brass", 
  :violin =&gt; "string"
}

instrument_section[:oboe] # =&gt; "woodwind"
instrument_section[:cello] # =&gt; "string"
# Note that strings aren"t the same as symbols...
instrument_section["cello"] # =&gt; nil
</code></pre></div></div>

<p>Son satırda, sembol anahtarının dize anahtarından farklı olduğunu ve bir anahtar üzerinden erişimin diğeriyle ilişkili bir değerle sonuçlanmayacağını unutmayın.</p>

<p>Semboller hash anahtarları olarak o kadar sık kullanılır ki Ruby’nin bunun için kısayol söz dizimi vardır: anahtar bir sembol ise <code class="language-plaintext highlighter-rouge">name =&gt; value</code> yerine hash oluşturmak için <code class="language-plaintext highlighter-rouge">name: value</code> çiftlerini kullanabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>instrument_section = {
  cello: "string",
  clarinet: "woodwind",
  drum: "percussion",
  oboe: "woodwind",
  trumpet: "brass",
  violin: "string"
}

puts "An oboe is a #{instrument_section[:oboe]} instrument"
# An oboe is a woodwind instrument
</code></pre></div></div>

<p>Bu sözdizimi kısmen, her ikisi de anahtar/değer çiftlerinde ayırıcı olarak iki nokta üst üste kullanan JavaScript ve Python’a aşina olan programcılar için eklenmiştir.</p>

<h2 id="kontrol-yapıları">Kontrol Yapıları</h2>

<p>Ruby, <code class="language-plaintext highlighter-rouge">if</code> ifadeleri ve <code class="language-plaintext highlighter-rouge">while</code> döngüleri gibi tüm olağan kontrol yapılarına sahiptir. Java veya JavaScript programcıları bu ifadelerin gövdelerinin etrafındaki parantezlerin olmayışı karşısında şaşırabilirler. Bunun yerine Ruby, kontrol yapısının gövdesinin sonunu belirtmek için <code class="language-plaintext highlighter-rouge">end</code> anahtar sözcüğünü kullanır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>today = Time.now

if today.saturday?
  puts "Do chores around the house"
elsif today.sunday?
  puts "Relax"
else
  puts "Go to work"
end

# Relax
</code></pre></div></div>

<p>Alışılmadık bulabileceğiniz bir şey, Ruby’nin ikinci cümlede “else if”i belirtmek için <code class="language-plaintext highlighter-rouge">elsif</code> anahtar kelimesini (bir “e” eksik, tek kelime) kullanmasıdır. Bu anahtar kelimeyi <code class="language-plaintext highlighter-rouge">else if</code> şeklinde kullanmak sözdizimi hatasına neden olur.</p>

<p>Benzer şekilde, <code class="language-plaintext highlighter-rouge">while</code> ifadeleri, while satırındaki koşul doğru olduğu sürece <code class="language-plaintext highlighter-rouge">end</code> ile sonlandırılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>num_pallets = 0
weight = 0

while weight &lt; 100 &amp;&amp; num_pallets &lt;= 5
  pallet = next_pallet()
  weight += pallet.weight
  num_pallets += 1
end
</code></pre></div></div>

<p>Ruby’deki ifadelerin çoğu değer döndürür; bu da ifadeleri koşul olarak kullanabileceğiniz anlamına gelir. Örneğin, <code class="language-plaintext highlighter-rouge">gets</code> metodu, bir sonraki satırı veya dosyanın sonuna ulaşıldığında <code class="language-plaintext highlighter-rouge">nil</code> değerini döndürür. Ruby, koşullarda <code class="language-plaintext highlighter-rouge">nil</code> değerini <code class="language-plaintext highlighter-rouge">false</code> olarak ele aldığından, bir dosyadaki satırları işlemek için aşağıdakileri yazabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>while (line = gets)
  puts line.downcase
end
</code></pre></div></div>

<p>Atama ifadesi <code class="language-plaintext highlighter-rouge">line</code> değişkenini, metnin bir sonraki satırı veya <code class="language-plaintext highlighter-rouge">nil</code> olacak olan gets çağrısının sonucuna ayarlar. Daha sonra <code class="language-plaintext highlighter-rouge">while</code> ifadesi, atama ifadesinin döndürdüğü değeri, yani atanan değeri test eder. Değer <code class="language-plaintext highlighter-rouge">nil</code> olduğunda bu, çıktıda başka satır olmadığı ve <code class="language-plaintext highlighter-rouge">while</code> döngüsünün sona erdiği anlamına gelir. Ruby ifade değiştiricileri (statement modifiers), <code class="language-plaintext highlighter-rouge">if</code> veya <code class="language-plaintext highlighter-rouge">while</code> ifadesinin gövdesi yalnızca tek bir ifadeden oluşuyorsa kullanışlı bir kısayoldur. Basitçe ifadeyi yazın, ardından if, while ve koşulu yazmanız yeterlidir. Örneğin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if radiation &gt; 3000
  puts "Danger, Will Robinson"
end
</code></pre></div></div>

<p>İfade değiştirici kullanılarak yeniden yazıldığında:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>puts "Danger, Will Robinson" if radiation &gt; 3000
</code></pre></div></div>

<p>Benzer şekilde bu <code class="language-plaintext highlighter-rouge">while</code> döngüsü:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>square = 4
while square &lt; 1000
  square = square * square
end
</code></pre></div></div>

<p>şu daha kısa versiyona dönüşür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>square = 4
square = square * square while square &lt; 1000
</code></pre></div></div>

<p>Bu değiştiricilerin <code class="language-plaintext highlighter-rouge">if</code> sürümü belki de en yaygın olarak, <code class="language-plaintext highlighter-rouge">return nil if user.nil?</code> örneğinde olduğu gibi, bir metodun başlangıcında koruma ifadesi olarak kullanılır. <code class="language-plaintext highlighter-rouge">while</code> versiyonu çok daha az kullanılır.</p>

<h2 id="düzenli-i̇fadeler-regular-expressions">Düzenli İfadeler (Regular Expressions)</h2>

<p>Ruby’nin yerleşik türlerinin çoğu tüm programcılara tanıdık gelecektir. Dillerin çoğunda dizeler, tamsayılar, kayan değerler, diziler vb. bulunur. Ancak tüm diller, Ruby veya JavaScript’in aksine, düzenli ifadeler için yerleşik desteğe sahip değildir. Bu üzücü, çünkü düzenli ifadeler, şifreli olmasına rağmen metinle çalışmak için güçlü bir araçtır. Ve bunları bir kütüphane arayüzü aracılığıyla oluşturmak yerine yerleşik hale getirmek büyük bir fark yaratıyor.</p>

<p>Düzenli ifadeler hakkında kitaplar yazılmıştır (örneğin Jeffrey Friedl’in Mastering Regular Expressions adlı kitabı), bu nedenle bu kısa bölümde her şeyi kapsamaya çalışmayacağız. Bunun yerine, düzenli ifadelerin yalnızca birkaç örneğine bakacağız. Düzenli ifadelerle ilgili daha fazla bilgiyi ilerleyen bölümlerde bulacaksınız.</p>

<p>Düzenli ifade, bir dizede eşleştirilecek karakterlerin patern’ini belirtmenin bir yoludur. Ruby’de genellikle eğik çizgi karakterleri (/pattern/) arasına patern yazarak düzenli ifade oluşturursunuz. Ve Ruby, Ruby olduğundan, düzenli ifadeler nesnelerdir ve bu şekilde değiştirilebilirler.</p>

<p>Örneğin, aşağıdaki düzenli ifadeyi kullanarak <code class="language-plaintext highlighter-rouge">Ruby</code> metnini veya <code class="language-plaintext highlighter-rouge">Rust</code> metnini içeren dizeyle eşleşen bir patern yazabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/Ruby|Rust/
</code></pre></div></div>

<p>Eğik çizgiler, eşleştirdiğimiz iki şeyden oluşan patern’i dikey çizgi karakteriyle (<code class="language-plaintext highlighter-rouge">|</code>) ayırır. Düzenli ifadelerde dikey çizgi karakteri “ya sağdaki şey ya da soldaki şey” anlamına gelir, bu durumda ya <code class="language-plaintext highlighter-rouge">Ruby</code> ya da <code class="language-plaintext highlighter-rouge">Rust</code>. Tıpkı aritmetik ifadelerde olduğu gibi patern’lerin içinde parantezleri kullanabilirsiniz, böylece bu patern aynı dize kümesiyle eşleşir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/Ru(by|st)/
</code></pre></div></div>

<p>Ayrıca patern içindeki tekrarı da belirleyebilirsiniz. <code class="language-plaintext highlighter-rouge">/ab+c/</code>, bir a ve ardından bir veya daha fazla b ve ardından bir c içeren dizeyle eşleşir. Artıyı yıldız işaretiyle değiştirdiğinizde <code class="language-plaintext highlighter-rouge">/ab*c/</code>, bir a, sıfır veya daha fazla b ve bir c ile eşleşen düzenli ifade oluşturur.</p>

<p>Ayrıca patern içindeki bir grup karakterden birini de eşleştirebilirsiniz. Bazı yaygın örnekler, boşluk karakteriyle (boşluk, sekme, yeni satır vb.) eşleşen <code class="language-plaintext highlighter-rouge">\s</code> gibi karakter sınıflarıdır; Herhangi bir rakamla eşleşen <code class="language-plaintext highlighter-rouge">\d</code>; ve <code class="language-plaintext highlighter-rouge">\w</code>, tipik bir sözcükte bulunabilecek herhangi bir karakterle eşleşir. Nokta (<code class="language-plaintext highlighter-rouge">.</code>), (neredeyse) her karakterle eşleşir.</p>

<p>Bazı faydalı düzenli ifadeler üretmek için tüm bunları bir araya getirebiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/\d\d:\d\d:\d\d/ # a time such as 12:34:56
/Ruby.*Rust/ # Ruby, zero or more other chars, then Rust
/Ruby Rust/ # Ruby, exactly one space, and Rust
/Ruby *Rust/ # Ruby, zero or more spaces, and Rust
/Ruby +Rust/ # Ruby, one or more spaces, and Rust
/Ruby\s+Rust/ # Ruby, one or more whitespace characters, then Rust
/Java (Ruby|Rust)/ # Java, a space, and either Ruby or Rust
</code></pre></div></div>

<p>Match operatörü <code class="language-plaintext highlighter-rouge">=~</code>, bir dizeyi düzenli ifade ile eşleştirmek için kullanılabilir. Patern dizede bulunursa, başlangıç konumunu döndürür; aksi takdirde <code class="language-plaintext highlighter-rouge">nil</code> değerini döndürür. Bu, <code class="language-plaintext highlighter-rouge">if</code> ve <code class="language-plaintext highlighter-rouge">while</code> ifadelerinde koşul olarak düzenli ifadeleri kullanabileceğiniz anlamına gelir. Örneğin, aşağıdaki kod parçası, bir dizenin <code class="language-plaintext highlighter-rouge">Ruby</code> veya <code class="language-plaintext highlighter-rouge">Rust</code> metnini içermesi durumunda ekrana bir mesaj yazar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>line = gets
if line =~ /Ruby|Rust/
  puts "Programming language mentioned: #{line}"
end
</code></pre></div></div>

<p>Hem dizeler hem de düzenli ifadeler, <code class="language-plaintext highlighter-rouge">=~</code> operatörüyle eşanlamlı olan <code class="language-plaintext highlighter-rouge">match?</code> metoduna sahiptir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>line = gets
if line.match?(/Ruby|Rust/)
  puts "Scripting language mentioned: #{line}"
end
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">match?</code> formu muhtemelen Ruby’de daha yaygın kullanılır.</p>

<p>Bir dizenin düzenli ifade ile eşleşen kısmı, Ruby’nin değiştirme metotlarından biri kullanılarak farklı bir metinle değiştirilebilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>line = gets
newline = line.sub(/Python/, 'Ruby') # replace first 'Python' with 'Ruby'
newerline = line.gsub(/Python/, 'Ruby') # replace every 'Python' with 'Ruby'
</code></pre></div></div>

<p>Bunu kullanarak her JavaScript ve Python kelimesini Ruby ile değiştirebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>line = gets
newline = line.gsub(/JavaScript|Python/, 'Ruby')
</code></pre></div></div>

<p>Kitabı okudukça düzenli ifadeler hakkında daha çok şey öğreneceksiniz.</p>

<h2 id="bloklar">Bloklar</h2>

<p>Bu bölümde Ruby’nin güçlü yönlerinden biri olan bloklar kısaca anlatılmaktadır. Kod bloğu, sanki blok başka bir parametreymiş gibi, bir metoda iletebileceğiniz kod yığınıdır. Bu, Ruby metotlarının son derece esnek olmasını sağlayan inanılmaz derecede güçlü bir özelliktir. Bu kitabı ilk inceleyenlerden biri bu noktada şu yorumu yaptı: “Bu oldukça ilginç ve önemli, dolayısıyla daha önce dikkat etmediyseniz muhtemelen şimdi başlamalısınız.” Hala aynı fikirdeyiz.</p>

<p>Sözdizimsel olarak kod blokları iki şekilde sınırlandırılabilen kod parçalarıdır: parantez arasında veya do ile end arasında. Bu, bir mesaj çağrısının sonundaki kod bloğudur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>foo.each { puts "Hello" }
</code></pre></div></div>

<p>Bu da aynı zamanda bir mesaj çağrısının sonundaki kod bloğudur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>foo.each do
  club.enroll(person)
  person.socialize
end
</code></pre></div></div>

<p>İki tür blok sınırlayıcının önceliği farklıdır: parantezler <code class="language-plaintext highlighter-rouge">do/end</code> çiftlerinden daha sıkı bağlanır, bu da kodunuzda neredeyse hiçbir zaman fark yaratmayacak bir gerçektir. Pratikte en sık göreceğiniz standart, tek satırlı bloklar için parantez, çok satırlı bloklar için <code class="language-plaintext highlighter-rouge">do/end</code> kullanmaktır.</p>

<p>Metot blokla hiçbir şey yapmasa bile, bir bloğu herhangi bir metot çağrısına argüman olarak iletebilirsiniz. Bunu, bloğu metot çağrısının sonunda, diğer parametrelerden sonra başlatarak yaparsınız. Örneğin, aşağıdaki kodda, <code class="language-plaintext highlighter-rouge">puts "Hi"</code> içeren blok, <code class="language-plaintext highlighter-rouge">greet</code> metoduna yapılan çağrıyla ilişkilidir (bunu burada göstermiyoruz):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>greet { puts "Hi" }
</code></pre></div></div>

<p>Metodun parametreleri varsa, bunlar bloktan önce görünür ve metot çağrısı başına yalnızca bir blok verebilirsiniz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>verbose_greet("Dave", "loyal customer") { puts "Hi" }
</code></pre></div></div>

<p>Daha sonra bir metot, Ruby <code class="language-plaintext highlighter-rouge">yield</code> ifadesini kullanarak ilişkili bloğu bir veya daha fazla kez çağırabilir. <code class="language-plaintext highlighter-rouge">yield</code> ifadesi, metodu iletilen bloğu çağırarak kontrolü bloğun içindeki koda aktarır.</p>

<p>Aşağıdaki örnekte blok çağrısının çalışması gösterilmektedir. İki kez <code class="language-plaintext highlighter-rouge">yield</code> çağıran bir metot tanımlıyoruz. Daha sonra bu metodu çağırıp, herhangi bir argümandan sonra aynı satıra bir blok yerleştirin. Bloğun metotla ilişkilendirilmesini bir tür argüman aktarımı olarak düşünebilirsiniz. Aslında hikayenin tamamı bu değil. Blok, başka bir metoda argüman olarak çağrılabilen veya iletilebilen, etkili bir şekilde tamamen başka bir metottur. Örneğin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def call_block
  puts "Start of method"
  yield
  yield
  puts "End of method"
end

call_block { puts "In the block" }

# Start of method
# In the block
# In the block
# End of method
</code></pre></div></div>

<p>Bu örnekte, bloktaki kod (<code class="language-plaintext highlighter-rouge">puts "In the block"</code>), kontrolü bloğa aktaran <code class="language-plaintext highlighter-rouge">yield</code> çağrısı için bir kez olmak üzere iki kez yürütülür.</p>

<p><code class="language-plaintext highlighter-rouge">yield</code> için argümanlar sağlayabilirsiniz ve bunlar bloğa iletilecektir. Blok içinde, bu argümanları almak için parametrelerin adlarını dikey çubuklar (<code class="language-plaintext highlighter-rouge">|params...|</code>) arasında listeleyin. Aşağıdaki örnek, ilişkili bloğu iki kez çağıran ve her seferinde bloğa iki argüman ileten bir metodu gösterir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def who_says_what
  yield("Dave", "hello")
  yield("Andy", "goodbye")
end
who_says_what { |person, phrase| puts "#{person} says #{phrase}" }

# Dave says hello
# Andy says goodbye
</code></pre></div></div>

<p>Daha sonra callback uygulamak amacıyla kodu paketlemek için kod bloklarını kullanabilirsiniz. Kod blokları kod parçalarını aktarmak için kullanılabilir ve Ruby standart kütüphanesi boyunca, metotların dizi gibi bir koleksiyondaki ardışık öğeler üzerinde eylem gerçekleştirmesine izin vermek için kullanılır. Koleksiyondaki tüm nesnelere benzer bir şey yapma eylemine Ruby’de <em>enumeration</em> denir; diğer diller buna <em>iteration</em> adını verir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>animals = ["ant", "bee", "cat", "dog"] # create an array
animals.each { |animal| puts animal } # iterate over the contents

# ant
# bee
# cat
# dog
</code></pre></div></div>

<p>Java ve JavaScript gibi dillerde yerleşik olan döngü yapılarının çoğu, Ruby’deki metot çağrılarıdır ve metotlar ilişkili bir bloğu sıfır veya daha fazla kez çağırır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>["cat", "dog", "horse"].each { |name| print name, " " }
5.times { print "*" }
3.upto(6) { |i| print i }
("a".."e").each { |char| print char }
("a".."e").each { print _1 }

# cat dog horse *****3456abcdeabcde
</code></pre></div></div>

<p>İlk satırda bir diziden, bloğun her elemanı için bir kez çağrı yapmasını istiyoruz. Daha sonra nesne 5, her seferinde * yazdırarak bloğu beş kez çağırır. Üçüncü örnek, <code class="language-plaintext highlighter-rouge">for</code> döngülerini kullanmak yerine, Ruby’de 3 rakamından bir blok çağırmasını isteyebileceğimizi ve 6’ya ulaşana kadar ardışık değerler iletebileceğimizi gösterir. Son olarak, değer aralıkları için Ruby’nin literal sözdizimini kullanarak a’dan e’ye kadar olan karakter aralığının her biri için  bloğu çağırmasını sağlıyoruz: bu örneği iki kez gösteriyoruz; bir kez Ruby’nin normal blok parametresi sözdizimini kullanarak ve ikinci olarak blok parametreleri için Ruby’nin kısayolunu kullanarak.</p>

<h2 id="okuma-ve-yazma">Okuma ve Yazma</h2>

<p>Ruby, input-output (I/O) işlemlerini yönetmek için kapsamlı bir kütüphane ile birlikte gelir. Ancak bu kitaptaki örneklerin çoğunda birkaç basit metoda sadık kalacağız. Çıktı yazan metotlarla zaten karşılaştık: <code class="language-plaintext highlighter-rouge">puts</code> argümanlarını her birinin arkasına yeni satır ekleyerek yazar; <code class="language-plaintext highlighter-rouge">p</code> de argümanlarını da yazar ancak daha fazla hata ayıklanabilir çıktı üretecektir. Her ikisi de herhangi bir I/O nesnesine yazmak için kullanılabilir, ancak varsayılan olarak standart ouput’a yazarlar.</p>

<p>Programınızda input’u okumanın birçok yolu vardır. Muhtemelen en geleneksel olanı, standart input’tan bir sonraki satırı döndüren <code class="language-plaintext highlighter-rouge">gets</code> (“get string”in kısaltmasıdır) metodunu kullanmaktır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>line = gets
print line
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">gets</code> input’un sonuna ulaştığında <code class="language-plaintext highlighter-rouge">nil</code> değerini döndürdüğü için, dönüş değerini döngü koşulunda kullanabilirsiniz. Aşağıdaki kodda <code class="language-plaintext highlighter-rouge">while</code> koşulunun bir değer ataması olduğuna dikkat edin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>while (line = gets)
  print line
end
</code></pre></div></div>

<p>İlerleyen bölümlerde bir dosyadan veya başka bir veri kaynağından nasıl okunup yazılacağı hakkında daha fazla konuşacağız.</p>

<h2 id="komut-satırı-argümanları">Komut Satırı Argümanları</h2>

<p>Bir Ruby programını komut satırından çalıştırdığınızda argümanları iletebilirsiniz. Bunlara Ruby kodunuzdan iki farklı şekilde erişilebilir.</p>

<p>İlk olarak, global <code class="language-plaintext highlighter-rouge">ARGV</code> dizisi, çalışan programa aktarılan argümanların her birini içerir. Aşağıdaki kodu <em>cmd_line.rb</em> adında bir dosyaya yerleştirin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>puts "You gave #{ARGV.size} arguments"
p ARGV
</code></pre></div></div>

<p>Argümanlarla çalıştırdığımızda, bunların aktarıldığını görebiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ruby cmd_line.rb ant bee cat dog
You gave 4 arguments
["ant", "bee", "cat", "dog"]
</code></pre></div></div>

<p>Genellikle bir programın argümanları, işlemek istediğiniz dosyaların adlarıdır. Bu durumda ikinci bir teknik kullanabilirsiniz: <code class="language-plaintext highlighter-rouge">ARGF</code> değişkeni özel bir tür I/O nesnesidir, adları komut satırında iletilen tüm dosyaların içerikleri gibi davranır (veya herhangi bir dosya adını iletmezseniz standart input). Bunu ilerleyen bölümlerde daha detaylı inceleyeceğiz.</p>

<h2 id="rubyde-yorumlar">Ruby’de Yorumlar</h2>

<p>Ruby koduna yorum eklemenin iki yolu vardır; bunlardan birini kullanacaksınız ve birini neredeyse hiç kullanmayacaksınız. Yaygın olanı <code class="language-plaintext highlighter-rouge">#</code> sembolüdür; bu sembolden sonra satırın sonuna kadar olan her şey bir yorumdur ve yorumlayıcı tarafından dikkate alınmaz. Bir sonraki satır yoruma devam ediyorsa kendi <code class="language-plaintext highlighter-rouge">#</code> sembolüne ihtiyacı vardır.</p>

<p>Ruby ayrıca nadiren kullanılan çok satırlı bir yoruma da sahiptir; burada ilk satır <code class="language-plaintext highlighter-rouge">=begin</code> ile başlar ve kod <code class="language-plaintext highlighter-rouge">=end</code>‘e ulaşana kadar her şey bir yorumdur. Hem <code class="language-plaintext highlighter-rouge">=begin</code> hem de <code class="language-plaintext highlighter-rouge">=end</code> satırın en başında olmalıdır; girintili olamazlar.</p>

<p>Az önce Ruby’nin yorumları göz ardı ettiğini söylemiş olsak da Ruby, dosya bazında yapılandırma seçenekleri için çok az sayıda “sihirli yorum” kullanır. Bu yorumlar <code class="language-plaintext highlighter-rouge"># directive: value</code> biçimindedir ve dosyada asıl Ruby kodunun ilk satırından önce görünmelidir.</p>

<p>En sık kullanılan sihirli yorum <code class="language-plaintext highlighter-rouge"># frozen_string_literal: true</code>‘dur. Bu yönerge <code class="language-plaintext highlighter-rouge">true</code> ise, içinde enterpolasyon bulunmayan her dize literali, sanki üzerinde <code class="language-plaintext highlighter-rouge">frozen</code> çağrılmış gibi otomatik olarak dondurulacaktır.</p>

<p>Ayrıca, belirli bir dosyanın içindeki dize ve düzenli ifade literal lerinin kodlamasını belirten <code class="language-plaintext highlighter-rouge"># encoding: VALUE</code> yönergesini de görebilirsiniz. Ruby bir dosyanın girintisinin eşleşmemesi durumunda kod uyarıları verecek <code class="language-plaintext highlighter-rouge"># warn_indent: BOOLEAN</code> flag’ine sahiptir. <code class="language-plaintext highlighter-rouge"># sharable_constant_value</code> adlı deneysel bir yönerge de vardır: Ractor multithreading aracı kullanılarak değerlerin nasıl paylaşıldığını etkiliyor.</p>

<h2 id="sıradaki">Sıradaki</h2>

<p>Ruby’nin bazı temel özelliklerine ışık hızında turumuzu tamamladık. Nesnelere, metotlara, dizelere, container ve düzenli ifadelere göz attık; basit kontrol yapılarını gördük; ve oldukça şık iteratör’lere baktık. Bu bölümün size bu kitabın geri kalanına saldırmanıza yetecek kadar mühimmat verdiğini umuyoruz.</p>

<p>Artık ilerlemenin ve daha yüksek bir seviyeye çıkmanın zamanı geldi. Daha sonra, Ruby’deki en üst düzey yapılar hem de tüm dilin temeli olan sınıflara ve nesnelere bakacağız.</p>

<p><strong>Kaynak:</strong> “Programming Ruby 3.2: The Pragmatic Programmers’ Guide”, Noel Rappin with Dave Thomas</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Programlama dilleri üzerine birçok kitap hemen hemen aynı görünür. Temel türlerle ilgili bölümlerle başlarlar: integer, string vb. Daha sonra if ve while ifadelerine ve döngülerine geçmeden önce 2 + 3 gibi ifadelere bakarlar. Daha sonra belki 7. veya 8. Bölüm civarında sınıflardan bahsetmeye başlayacaklar. Bunu biraz sıkıcı buluyoruz.]]></summary></entry><entry><title type="html">Shell’i Öğrenmek</title><link href="http://localhost:4000/2024/05/14/shell-i-ogrenmek.html" rel="alternate" type="text/html" title="Shell’i Öğrenmek" /><published>2024-05-14T00:00:00+03:00</published><updated>2024-05-14T00:00:00+03:00</updated><id>http://localhost:4000/2024/05/14/shell-i-ogrenmek</id><content type="html" xml:base="http://localhost:4000/2024/05/14/shell-i-ogrenmek.html"><![CDATA[<p>Neden komut satırını öğrenmeniz gerekiyor? Peki, size bir hikaye anlatayım. Yıllar önce çalıştığım yerde bir sorun yaşadık. Dosya sunucularımızdan birinde sürekli dolmaya başlayan bir ortak sürücü vardı. Bu eski işletim sisteminin kullanıcı kotalarını desteklemediğinden bahsetmeyeceğim; bu başka bir hikaye. Ancak sunucu dolmaya devam etti ve insanların çalışmasını engelledi. Yazılım mühendislerimizden biri, tüm kullanıcı dizinlerini tarayacak, kullandıkları alanı toplayacak ve sonuçların listesini yapacak bir C++ programı yazmak için birkaç saat harcadı. İşteyken eski işletim sistemini kullanmak zorunda kaldığım için Linux benzeri bir komut satırı ortamı kurdum. Sorunu duyduğumda bu görevi şu tek satırla yapabileceğimi fark ettim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>du -s * | sort -nr &gt; $HOME/user_space_report.txt
</code></pre></div></div>

<p>Grafik kullanıcı arayüzleri (GUI’ler) birçok görev için faydalıdır ancak tüm görevler için iyi değildir. Uzun zamandır günümüz bilgisayarlarının çoğunun elektrikle çalışmadığını hissettim. Bunun yerine farenin “pompalama” hareketiyle güç alıyor gibi görünüyorlar. Bilgisayarların bizi manuel işlerden kurtarması gerekiyordu, ancak kaç kez bilgisayarın yapabileceğinden emin olduğunuz bir görevi yerine getirdiniz, fareyi sıkıcı bir şekilde kullanarak işi kendiniz yapmak zorunda kaldınız?</p>

<p>Bir keresinde bir yazarın, çocukken bilgisayarı resimlere bakarak kullandığını söylediğini duymuştum. Büyüyünce okumayı ve yazmayı öğrenirsin. Bilgisayar Okuryazarlığı 101’e hoş geldiniz. Şimdi işe koyulalım.</p>

<h2 id="shell-nedir">“Shell” nedir?</h2>

<p>Basitçe söylemek gerekirse shell, klavyeden komutları alan ve bunları gerçekleştirmesi için işletim sistemine veren bir programdır. Eski günlerde Linux gibi Unix benzeri sistemlerde kullanılabilen tek kullanıcı arayüzüydü. Günümüzde shell gibi komut satırı arayüzlerinin (CLI’ler) yanı sıra grafik kullanıcı arayüzlerine (GUI’ler) de sahibiz.</p>

<p>Çoğu Linux sisteminde, <strong>bash</strong> adı verilen bir program (Bourne Again SHell anlamına gelir; orijinal Unix shell programının geliştirilmiş versiyonu olan <strong>sh</strong>, Steve Bourne tarafından yazılmıştır) shell görevi görür. Linux sistemleri için bash’ın yanı sıra başka shell programları da mevcuttur: <strong>ksh</strong>, <strong>tcsh</strong> ve <strong>zsh</strong>.</p>

<h3 id="terminal-nedir">“Terminal” nedir?</h3>

<p>Terminal emülatörü adı verilen bir programdır. Bu, bir pencere açan ve shell ile etkileşime girmenizi sağlayan bir programdır. Kullanabileceğimiz bir sürü farklı terminal emülatörü var. Bazı Linux dağıtımları birkaç tane yükler. Bunlar <strong>gnome-terminal</strong>, <strong>konsole</strong>, <strong>xterm</strong>, <strong>rxvt</strong>, <strong>kvt</strong>, <strong>nxterm</strong> ve <strong>eterm</strong>‘i içerebilir.</p>

<h3 id="terminali-başlatmak">Terminali Başlatmak</h3>

<p>Genellikle menüden terminal başlatmanın bir yolu vardır. Terminal emülatörüne benzeyen bir şey olup olmadığını görmek için program listesine bakın. Çok sayıda farklı terminal emülatörü olmasına rağmen hepsi aynı şeyi yapar. Bizim için shell oturumuna erişim sağlıyorlar. Sağladığı farklı özelliklere göre muhtemelen bir tanesini tercih edeceksiniz.</p>

<h3 id="klavyeyi-test-etmek">Klavyeyi Test Etmek</h3>

<p>Tamam, biraz yazmayı deneyelim. Bir terminal penceresi açın. Görmemiz gereken ilk şey, kullanıcı adınız, makinenin adı ve ardından dolar işaretini içeren bir shell prompt udur. Bunun gibi bir şey:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$
</code></pre></div></div>

<p>Harika! Şimdi bazı rastgele karakterler yazın ve enter tuşuna basın.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ kdkjflajfks
</code></pre></div></div>

<p>Her şey yolunda giderse, komutun anlaşılamadığından şikayet eden bir hata mesajı almış olmalıyız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ kdkjflajfks
bash: kdkjflajfks: command not found
</code></pre></div></div>

<p>Müthiş! Şimdi yukarı ok tuşuna basın. Önceki komutumuz “kdkjflajfks”in nasıl döndüğünü izleyin. Evet, komut geçmişimiz var. Aşağı oka bastığımızda tekrar boş satıra ulaşıyoruz.</p>

<p>Gerekirse yukarı ok tuşunu kullanarak “kdkjflajfks” komutunu geri çağırın. Şimdi sol ve sağ ok tuşlarını deneyin. İmleci komut satırında herhangi bir yere konumlandırabiliriz. Bu, hataları kolayca düzeltmemize olanak tanır.</p>

<p><strong>Root olarak çalışmadığınıza emin olun!</strong></p>

<p>Shell prompt unuzun son karakteri $ yerine # ise, superuser olarak çalışıyorsunuz demektir. Bu, yönetici ayrıcalıklarına sahip olduğunuz anlamına gelir. Sistemdeki herhangi bir dosyayı silebileceğiniz veya üzerine yazabileceğiniz için bu tehlikeli olabilir. Kesinlikle yönetici ayrıcalıklarına ihtiyacınız olmadığı sürece superuser olarak çalışmayın.</p>

<h3 id="fareyi-kullanmak">Fare’yi Kullanmak</h3>

<p>Shell bir komut satırı arayüzü olmasına rağmen fare hala kullanışlıdır.</p>

<p>Terminal penceresinin içeriğini kaydırmak için fareyi kullanmanın yanı sıra, metni kopyalamak için de kullanabiliriz. Sol düğmeyi basılı tutarken fareyi bir metnin üzerine sürükleyin. Metin vurgulanmalıdır. Sol düğmeyi bırakın, fare işaretçisini terminal penceresine getirin ve orta fare düğmesine basın (alternatif olarak, dokunmatik yüzey üzerinde çalışırken hem sol hemde sağ düğmelere aynı anda basın). Pencerede vurguladığımız metin, komut satırına kopyalanacaktır.</p>

<h2 id="navigasyon">Navigasyon</h2>

<p>Bu derste ilk üç komutumuzu tanıtacağız: <strong>pwd</strong> (print working directory), <strong>cd</strong> (change directory) ve <strong>ls</strong> (list files and directories).</p>

<p>Kavramlara alışmak biraz zaman alacağından, komut satırına yeni başlayanlar bu derse çok dikkat etmelidir.</p>

<h3 id="dosya-sistemi-organizasyonu">Dosya Sistemi Organizasyonu</h3>

<p>Windows gibi, Linux sistemindeki dosyalar da hiyerarşik dizin yapısı adı verilen şekilde düzenlenir. Bu, dosyaları ve alt dizinleri içerebilen, ağaç benzeri bir dizin düzeninde (diğer sistemlerde klasörler olarak adlandırılır) organize edildikleri anlamına gelir. Dosya sistemindeki ilk dizine kök (root) dizin denir. Kök dizin, daha fazla dosya ve alt dizin vb. içeren dosyalar ve alt dizinler içerir.</p>

<p>Çoğu grafik ortam, dosya sisteminin içeriğini görüntülemek ve değiştirmek için kullanılan bir dosya yöneticisi programı içerir. Çoğu zaman dosya sisteminin şu şekilde temsil edildiğini göreceğiz:</p>

<p><img src="/assets/images/2024/05/file-manager.jpg" alt="File Manager" title="File Manager" class="img-fluid" /></p>

<p>Windows ile Linux gibi Unix benzeri işletim sistemleri arasındaki önemli bir fark, Linux’un sürücü harfleri kavramını kullanmamasıdır. Windows sürücü harfleri dosya sistemini bir dizi farklı ağaca (her aygıt için bir tane) bölerken, Linux’te her zaman tek bir ağaç bulunur. Farklı depolama aygıtları ağacın farklı dalları olabilir ancak her zaman tek bir ağaç vardır.</p>

<h3 id="pwd">pwd</h3>

<p>Komut satırı arayüzü dosya sistemi yapısının grafik resimlerini sağlayamadığı için onu farklı bir şekilde temsil etmemiz gerekir. Bunu yapmak için dosya sistemi ağacını bir labirent olarak ve bizim de onun içinde durduğumuzu düşünün. Herhangi bir anda tek bir dizinde bulunuruz. Bu dizinin içinde, dosyalarını ve ana dizinine giden yolu ve içinde bulunduğumuz dizinin alt dizinlerine giden yolları görebiliriz.</p>

<p>İçinde bulunduğumuz dizine çalışma dizini denir. Çalışma dizininin adını görmek için <code class="language-plaintext highlighter-rouge">pwd</code> komutunu kullanıyoruz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ pwd
/home/me
</code></pre></div></div>

<p>Linux sistemimize ilk giriş yaptığımızda çalışma dizini home olarak ayarlanmıştır. Dosyalarımızı buraya koyacağız. Çoğu sistemde, giriş dizini <em>/home/kullanıcı_adı</em> olarak adlandırılacaktır, ancak sistem yöneticisinin isteğine göre herhangi bir şey olabilir.</p>

<p>Çalışma dizinindeki dosyaları listelemek için <code class="language-plaintext highlighter-rouge">ls</code> komutunu kullanırız.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ls
Desktop    Downloads         foo.txt  Pictures  Templates
Documents  examples.desktop  Music    Public    Videos
</code></pre></div></div>

<p>Bir sonraki derste <code class="language-plaintext highlighter-rouge">ls</code> konusuna geri döneceğiz. Bununla yapabileceğiniz pek çok eğlenceli şey var, ancak önce biraz yol adları ve dizinler hakkında konuşmamız gerekiyor.</p>

<h3 id="cd">cd</h3>

<p>Çalışma dizinini (labirentte durduğumuz yer) değiştirmek için <code class="language-plaintext highlighter-rouge">cd</code> komutunu kullanırız. Bunu yapmak için <code class="language-plaintext highlighter-rouge">cd</code> yazıp ardından istenen çalışma dizininin yol adını yazıyoruz. Yol adı, istediğimiz dizine ulaşmak için ağacın dalları boyunca izlediğimiz yoldur. Yol adları iki farklı şekilde belirtilebilir; mutlak yol adları veya göreli yol adları. Önce mutlak yol adlarına bakalım.</p>

<p>Mutlak bir yol adı kök dizinle başlar ve istenen dizine veya dosyaya giden yol tamamlanana kadar ağaç dalını takip eder. Örneğin sisteminizde çoğu programın kurulu olduğu bir dizin var. Dizinin yol adı <em>/usr/bin</em>‘dir. Bu, kök dizinde (yol adının başındaki eğik çizgiyle temsil edilir) “bin” adlı bir dizini içeren “usr” adlı bir dizin olduğu anlamına gelir.</p>

<p>Hadi şunu deneyelim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ cd /usr/bin
[me@linuxbox bin]$ pwd
/usr/bin
[me@linuxbox bin]$ ls
'['                                   mshortname
 2to3-2.7                             mshowfat
 411toppm                             mtools
 a2ps                                 mtoolstest
 a2ps-lpr-wrapper                     mtr
 aa-enabled                           mtrace
 aa-exec                              mtr-packet
 aclocal                              mtvtoppm
 aclocal-1.15                         mtype
 aconnect                             mutter
 acpi_listen                          mxtar
 add-apt-repository                   mzip
 addpart                              namei

and many more...
</code></pre></div></div>

<p>Artık mevcut çalışma dizinini <em>/usr/bin</em> olarak değiştirdiğimizi ve dosyalarla dolu olduğunu görebiliriz. Shell prompt unun nasıl değiştiğine dikkat ettiniz mi? Kolaylık olması açısından genellikle çalışma dizininin adını görüntüleyecek şekilde ayarlanır.</p>

<p>Mutlak yol adı kök dizinden başlayıp hedefine gittiğinde, göreceli yol adı çalışma dizininden başlar. Bunu yapmak için dosya sistemi ağacındaki göreli konumları temsil eden birkaç özel gösterim kullanır. Bu özel gösterimler “.” (nokta) ve “..” (nokta nokta).</p>

<p>”.” gösterimi çalışma dizininin kendisini, “..” gösterimi ise çalışma dizininin üst dizinini belirtir. Nasıl çalıştığını görelim. Çalışma dizinini tekrar <em>/usr/bin</em> olarak değiştirelim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ cd /usr/bin
[me@linuxbox bin]$ pwd
/usr/bin
</code></pre></div></div>

<p>Tamam, şimdi diyelim ki çalışma dizinini <em>/usr/bin</em>‘in ebeveyni olan <em>/usr</em> ile değiştirmek istedik. Bunu iki farklı şekilde yapabiliriz. İlk olarak, mutlak yol adıyla:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox bin]$ cd /usr
[me@linuxbox usr]$ pwd
/usr
</code></pre></div></div>

<p>Veya göreceli yol adıyla:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox bin]$ cd ..
[me@linuxbox usr]$ pwd
/usr
</code></pre></div></div>

<p>Aynı sonuçlara sahip iki farklı metot. Hangisini kullanmalıyız? En az yazmayı gerektireni!</p>

<p>Aynı şekilde çalışma dizinini <em>/usr</em>‘dan <em>/usr/bin</em>‘e iki farklı şekilde değiştirebiliriz. İlk önce mutlak yol adı kullanarak:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox usr]$ cd /usr/bin
[me@linuxbox bin]$ pwd
/usr/bin
</code></pre></div></div>

<p>Veya göreceli yol adıyla:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox usr]$ cd ./bin
[me@linuxbox bin]$ pwd
/usr/bin
</code></pre></div></div>

<p>Burada belirtmemiz gereken önemli bir şey var. Çoğu durumda “./” işaretini atlayabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox usr]$ cd bin
</code></pre></div></div>

<p>Genel olarak, eğer bir şeye yol adı belirtmezsek, çalışma dizini varsayılacaktır. Bunun önemli bir istisnası var ama buna bir süre değinmeyeceğiz.</p>

<h3 id="birkaç-kısayol">Birkaç Kısayol</h3>

<p>Eğer <code class="language-plaintext highlighter-rouge">cd</code> ve ardından hiçbir şey yazmazsak, çalışma dizinini ana dizinimize değiştirecektir.</p>

<p>İlgili bir kısayol <code class="language-plaintext highlighter-rouge">cd ~kullanıcı_adı</code> yazmaktır. Bu durumda, çalışma dizinini belirtilen kullanıcının ana dizinine değiştirecektir.</p>

<p><code class="language-plaintext highlighter-rouge">cd -</code> yazmak çalışma dizinini bir öncekine değiştirir.</p>

<h3 id="dosya-adlarıyla-i̇lgili-önemli-bilgiler">Dosya Adlarıyla İlgili Önemli Bilgiler</h3>

<ol>
  <li>Nokta karakteriyle başlayan dosya adları gizlenir. Bu, <code class="language-plaintext highlighter-rouge">ls -a</code> kullanmadığınız sürece <code class="language-plaintext highlighter-rouge">ls</code>‘nin onları listelemeyeceği anlamına gelir. Hesabınız oluşturulduğunda, hesabınızla ilgili ayarları yapılandırmak için ana dizininize birkaç gizli dosya yerleştirildi. Daha sonra ortamımızı nasıl özelleştirebileceğinizi görmek için bu dosyalardan bazılarına daha yakından bakacağız. Ayrıca bazı uygulamalar, yapılandırma ve ayar dosyalarını gizli dosyalar olarak ana dizininize yerleştirir.</li>
  <li>Unix gibi Linux’taki dosya adları büyük/küçük harfe duyarlıdır. “Dosya1” ve “dosya1” dosya adları farklı dosyaları ifade eder.</li>
  <li>Linux’un Windows sistemleri gibi bir “dosya uzantısı” kavramı yoktur. Dosyaları istediğiniz şekilde adlandırabilirsiniz. Ancak Linux’un kendisi dosya uzantılarını umursamasa da birçok uygulama bunu önemser.</li>
  <li>Linux, boşluklar ve noktalama işaretleri içerebilen uzun dosya adlarını desteklese de, noktalama işaretlerini nokta, kısa çizgi ve alt çizgi ile sınırlandırın. En önemlisi dosya adlarında boşluk kullanmayın. Bir dosya adındaki sözcükler arasındaki boşlukları temsil etmek istiyorsanız alt çizgi karakterlerini kullanın. Daha sonra kendinize teşekkür edeceksiniz.</li>
</ol>

<h2 id="etrafa-gözatın">Etrafa Gözatın</h2>

<p>Artık çalışma dizininden çalışma dizinine nasıl geçeceğimizi bildiğimize göre, Linux sistemimizde bir tur atacağız ve bu arada onu neyin harekete geçirdiği hakkında bazı şeyler öğreneceğiz. Ancak başlamadan önce yolculuğumuz sırasında işimize yarayacak bazı araçları öğrenmemiz gerekiyor. Bunlar:</p>

<ul>
  <li><strong>ls</strong> (dosyaları ve dizinleri listele)</li>
  <li><strong>less</strong> (metin dosyalarını görüntüle)</li>
  <li><strong>file</strong> (bir dosyanın içeriğini sınıflandırın)</li>
</ul>

<h3 id="ls">ls</h3>

<p><code class="language-plaintext highlighter-rouge">ls</code> komutu bir dizinin içeriğini listelemek için kullanılır. Muhtemelen en yaygın kullanılan Linux komutudur. Birçok farklı şekilde kullanılabilir. İşte bazı örnekler:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls</code></td>
      <td>Çalışma dizinindeki dosyaları listele</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls /bin</code></td>
      <td><em>/bin</em> dizinindeki (veya başka bir dizindeki) dosyaları listeleyin</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls -l</code></td>
      <td>Çalışma dizinindeki dosyaları uzun formatta listeleyin</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls -l /etc /bin</code></td>
      <td><em>/bin</em> dizinindeki ve <em>/etc</em> dizinindeki dosyaları uzun formatta listeleyin</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls -la ..</code></td>
      <td>Çalışma dizininin üst öğesindeki tüm dosyaları (nokta karakteriyle başlayan, normalde gizli olanlar dahil) uzun formatta listeleyin</td>
    </tr>
  </tbody>
</table>

<p>Bu örnekler aynı zamanda komutlarla ilgili önemli bir kavrama da işaret etmektedir. Çoğu komut şu şekilde çalışır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>command -options arguments
</code></pre></div></div>

<p>burada <code class="language-plaintext highlighter-rouge">command</code> komutun adıdır, <code class="language-plaintext highlighter-rouge">-options</code> komutun davranışında yapılan bir veya daha fazla ayarlamadır ve <code class="language-plaintext highlighter-rouge">arguments</code> ise komutun üzerinde çalıştığı bir veya daha fazla “şey”dir.</p>

<p><code class="language-plaintext highlighter-rouge">ls</code> söz konusu olduğunda <code class="language-plaintext highlighter-rouge">ls</code>‘nin komutun adı olduğunu, <code class="language-plaintext highlighter-rouge">-a</code> ve <code class="language-plaintext highlighter-rouge">-l</code> gibi bir veya birden fazla seçeneğe sahip olabileceğini, bir veya birden fazla dosya veya dizin üzerinde çalışabildiğini görüyoruz.</p>

<p><strong>Uzun Formata Yakından Bir Bakış</strong></p>

<p><code class="language-plaintext highlighter-rouge">ls</code> ile birlikte <code class="language-plaintext highlighter-rouge">-l</code> seçeneğini kullanırsak, listelenen dosyalar hakkında zengin bilgiler içeren bir dosya listesi elde edersiniz. İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>-rw-------   1 me       me            576 Apr 17  2019 weather.txt
drwxr-xr-x   6 me       me           1024 Oct  9  2019 web_page
-rw-rw-r--   1 me       me         276480 Feb 11 20:41 web_site.tar
-rw-------   1 me       me           5743 Dec 16  2018 xmas_file.txt

----------     -------  -------  -------- ------------ -------------
    |             |        |         |         |             |
    |             |        |         |         |         File Name
    |             |        |         |         |
    |             |        |         |         +---  Modification Time
    |             |        |         |
    |             |        |         +-------------   Size (in bytes)
    |             |        |
    |             |        +-----------------------        Group
    |             |
    |             +--------------------------------        Owner
    |
    +----------------------------------------------   File Permissions
</code></pre></div></div>

<p><strong>File Name</strong>
Dosyanın veya dizinin adı.</p>

<p><strong>Modification Time</strong>
Dosyanın en son değiştirildiği zaman. Son değişiklik altı aydan daha uzun bir süre önce yapılmışsa tarih ve yıl görüntülenir. Aksi takdirde günün saati gösterilir.</p>

<p><strong>Size</strong>
Dosyanın bayt cinsinden boyutu.</p>

<p><strong>Group</strong>
Dosyanın sahibinin yanı sıra dosya izinlerine de sahip olan grubun adı.</p>

<p><strong>Owner</strong>
Dosyanın sahibi olan kullanıcının adı.</p>

<p><strong>File Permissions</strong>
Dosyanın erişim izinlerinin temsili. İlk karakter dosyanın türüdür. “-“ normal (sıradan) bir dosyayı belirtir. “d” bir dizini belirtir. Üç karakterden oluşan ikinci grup, dosya sahibinin okuma, yazma ve yürütme haklarını temsil eder. Sonraki üçü dosya grubunun haklarını, son üçü ise diğer herkese verilen hakları temsil eder. Bunu daha sonraki bir derste daha ayrıntılı olarak tartışacağız.</p>

<h3 id="less">less</h3>

<p><strong>less</strong> metin dosyalarını görüntülememizi sağlayan bir programdır. Linux’u kontrol etmek ve yapılandırmak için kullanılan dosyaların çoğu insan tarafından okunabildiğinden bu çok kullanışlıdır.</p>

<p><strong>“Text” nedir?</strong></p>

<p>Bilgisayarda bilgiyi temsil etmenin birçok yolu vardır. Tüm metotlar, bilgi ile onu temsil etmek için kullanılacak bazı sayılar arasındaki ilişkinin tanımlanmasını içerir. Sonuçta bilgisayarlar yalnızca sayıları anlar ve tüm veriler sayısal gösterime dönüştürülür.</p>

<p>Bu gösterim sistemlerinden bazıları oldukça karmaşıktır (sıkıştırılmış multimedya dosyaları gibi), diğerleri ise oldukça basittir. En eski ve en basitlerinden biri ASCII metnidir. ASCII (“As-Key” olarak telaffuz edilir) “American Standard Code for Information Interchange” in kısaltmasıdır. Bu, ilk olarak Teletype makinelerinde klavye karakterlerini sayılarla eşleştirmek için kullanılan basit bir kodlama şemasıdır.</p>

<p>Metin, karakterlerin sayılara basitçe bire bir eşleştirilmesidir. Çok kompakttır. Elli karakterlik metin, elli baytlık veriye karşılık gelir. Bir Linux sisteminde birçok dosya metin biçiminde saklanır ve metin dosyalarıyla çalışan birçok Linux aracı vardır. Windows sistemleri bile bu formatın öneminin farkındadır. Tanınmış Notepad.exe programı, düz ASCII metin dosyaları için bir düzenleyicidir.</p>

<p><code class="language-plaintext highlighter-rouge">less</code> programı basitçe şunu yazarak çağrılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>less text_file
</code></pre></div></div>

<p>Bu dosyayı gösterecektir.</p>

<p><strong>less’i kontrol etmek</strong></p>

<p>Bir kez başlatıldığında, <code class="language-plaintext highlighter-rouge">less</code> metin dosyasını her seferinde bir sayfa olarak görüntüler. Metin dosyasında ilerlemek için “Page Up” ve “Page Down” tuşlarını kullanabiliriz. Çıkmak için “q” yazıyoruz. <code class="language-plaintext highlighter-rouge">less</code>’in kabul edeceği bazı komutlar şunlardır:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Page Up veya b</td>
      <td>Bir sayfa geriye git</td>
    </tr>
    <tr>
      <td>Page Down veya space</td>
      <td>Bir sayfa ileri kaydır</td>
    </tr>
    <tr>
      <td>G</td>
      <td>Metin dosyasının sonuna git</td>
    </tr>
    <tr>
      <td>1G</td>
      <td>Metin dosyasının başına git</td>
    </tr>
    <tr>
      <td>/characters</td>
      <td>Belirtilen karakterleri metin dosyasında ileriye doğru arayın</td>
    </tr>
    <tr>
      <td>n</td>
      <td>Önceki aramayı tekrarla</td>
    </tr>
    <tr>
      <td>h</td>
      <td>Komut ve seçeneklerin tam listesini görüntüleyin</td>
    </tr>
    <tr>
      <td>q</td>
      <td>Çıkış</td>
    </tr>
  </tbody>
</table>

<h3 id="file">file</h3>

<p>Linux sistemimizde dolaşırken, bir dosyayı görüntülemeye çalışmadan önce ne tür veriler içerdiğini belirlemek yararlı olacaktır. İşte bu noktada <code class="language-plaintext highlighter-rouge">file</code> komutu devreye giriyor. <code class="language-plaintext highlighter-rouge">file</code> bir dosyayı inceleyecek ve bize onun ne tür bir dosya olduğunu söyleyecektir.</p>

<p><code class="language-plaintext highlighter-rouge">file</code> programını kullanmak için şunu yazmamız yeterli:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>file name_of_file
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">file</code> programı aşağıdakiler gibi çoğu dosya türünü tanıyabilir:</p>

<table>
  <thead>
    <tr>
      <th>Dosya Tipi</th>
      <th>Açıklama</th>
      <th>Metin olarak görüntülenebilir mi?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ASCII text</td>
      <td>Adı her şeyi söylüyor</td>
      <td>Evet</td>
    </tr>
    <tr>
      <td>Bourne-Again shell script text</td>
      <td>Bir bash scripti</td>
      <td>Evet</td>
    </tr>
    <tr>
      <td>ELF 64-bit LSB executable</td>
      <td>Yürütülebilir binary program</td>
      <td>Hayır</td>
    </tr>
    <tr>
      <td>ELF 64-bit LSB shared object</td>
      <td>Paylaşılan bir kütüphane</td>
      <td>Hayır</td>
    </tr>
    <tr>
      <td>GNU tar archive</td>
      <td>Arşiv dosyası. Dosya gruplarını depolamanın yaygın bir yolu</td>
      <td>Hayır, listeyi görüntülemek için <strong>tar tvf</strong>‘yi kullanın.</td>
    </tr>
    <tr>
      <td>gzip compressed data</td>
      <td>gzip ile sıkıştırılmış arşiv</td>
      <td>Hayır</td>
    </tr>
    <tr>
      <td>HTML document text</td>
      <td>Web sayfası</td>
      <td>Evet</td>
    </tr>
    <tr>
      <td>JPEG image data</td>
      <td>Sıkıştırılmış bir JPEG görüntüsü</td>
      <td>Hayır</td>
    </tr>
    <tr>
      <td>PostScript document text</td>
      <td>PostScript dosyası</td>
      <td>Evet</td>
    </tr>
    <tr>
      <td>Zip archive data</td>
      <td>Zip ile sıkıştırılmış arşiv</td>
      <td>Hayır</td>
    </tr>
  </tbody>
</table>

<p>Çoğu dosya metin olarak görüntülenemiyor gibi görünse de, şaşırtıcı bir sayı olabilir. Bu özellikle önemli yapılandırma dosyaları için geçerlidir. Maceramız sırasında işletim sisteminin birçok özelliğinin metin yapılandırma dosyaları ve shell script dosyaları tarafından kontrol edildiğini göreceğiz. Linux’ta sır yoktur!</p>

<h2 id="rehberli-tur">Rehberli Tur</h2>

<p>Turumuza çıkmanın vakti geldi. Aşağıda keşfedilecek bazı ilginç yerler listelenmektedir. Bu kesinlikle tam bir liste değil, ancak ilginç bir macera olduğu kanıtlanmalıdır. Aşağıda listelenen dizinlerin her biri için aşağıdakileri yapın:</p>

<ul>
  <li>Her dizine <code class="language-plaintext highlighter-rouge">cd</code> ile ulaşın.</li>
  <li>Dizinin içeriğini listelemek için <code class="language-plaintext highlighter-rouge">ls</code> kullanın.</li>
  <li>İlginç bir dosya varsa içeriğini belirlemek için <code class="language-plaintext highlighter-rouge">file</code> komutunu kullanın.</li>
  <li>Metin dosyalarını görüntülemek için <code class="language-plaintext highlighter-rouge">less</code> kullanın.</li>
</ul>

<table>
  <thead>
    <tr>
      <th>Dizin</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>/</td>
      <td>Dosya sisteminin başladığı kök dizin. Kök dizin muhtemelen yalnızca alt dizinleri içerecektir.</td>
    </tr>
    <tr>
      <td>/boot</td>
      <td>Linux çekirdeği ve önyükleyici dosyalarının saklandığı yer burasıdır. Çekirdek vmlinuz adlı bir dosyadır.</td>
    </tr>
    <tr>
      <td>/etc</td>
      <td>/etc dizini sistemin yapılandırma dosyalarını içerir. /etc içindeki tüm dosyalar metin dosyaları olmalıdır.</td>
    </tr>
    <tr>
      <td>/bin, /usr/bin</td>
      <td>Bu iki dizin sistemdeki programların çoğunu içerir. /bin dizini sistemin çalışması için gereken temel programları içerirken, /usr/bin sistem kullanıcılarına yönelik uygulamaları içerir.</td>
    </tr>
    <tr>
      <td>/sbin, /usr/sbin</td>
      <td>sbin dizinleri, çoğunlukla superuser tarafından kullanılmak üzere sistem yönetimine yönelik programlar içerir.</td>
    </tr>
    <tr>
      <td>/usr</td>
      <td>/usr dizini kullanıcı uygulamalarını destekleyen çeşitli şeyleri içerir.</td>
    </tr>
    <tr>
      <td>/usr/local</td>
      <td>/usr/local ve alt dizinleri, local makinede kullanılacak yazılımın ve diğer dosyaların kurulumu için kullanılır. Bunun gerçekte anlamı, resmi dağıtımın parçası olmayan (genellikle /usr/bin dizinine giren) yazılımın buraya gelmesidir. Sisteminize kurabileceğiniz ilgi çekici programlar bulduğunuzda, bunların /usr/local dizinlerinden birine kurulması gerekir. Çoğunlukla tercih edilen dizin /usr/local/bin’dir.</td>
    </tr>
    <tr>
      <td>/var</td>
      <td>/var dizini sistem çalışırken değişen dosyaları içerir. /var/log log dosyaları içeren dizin. Bunlar sistem çalıştıkça güncellenir. Sisteminizin sağlığını izlemek için bu dizindeki dosyaları zaman zaman görüntülemek iyi bir fikirdir. /var/spool dizini, e-mail mesajları ve yazdırma işleri gibi bazı işlemler için kuyruğa alınan dosyaları tutmak için kullanılır.</td>
    </tr>
    <tr>
      <td>/lib</td>
      <td>Paylaşılan kütüphaneler (diğer işletim sistemindeki DLL’lere benzer) burada tutulur.</td>
    </tr>
    <tr>
      <td>/home</td>
      <td>/home kullanıcıların kişisel çalışmalarını sakladıkları yerdir. Genel olarak kullanıcıların dosya yazmasına izin verilen tek yer burasıdır. Bu, her şeyin güzel ve temiz kalmasını sağlar.</td>
    </tr>
    <tr>
      <td>/root</td>
      <td>Bu, superuser’ın ana dizinidir.</td>
    </tr>
    <tr>
      <td>/tmp</td>
      <td>/tmp, programların geçici dosyalarını yazabileceği bir dizindir.</td>
    </tr>
    <tr>
      <td>/dev</td>
      <td>/dev dizini özel bir dizindir, çünkü alışılagelmiş anlamda dosya içermez. Bunun yerine, sistem tarafından kullanılabilen cihazları içerir. Linux’ta (Unix gibi), cihazlara dosyalar gibi davranılır. Cihazları sanki dosyalarmış gibi okuyabilir ve yazabilirsiniz. Örneğin /dev/fd0 ilk disket sürücüsüdür, /dev/sda ise ilk sabit sürücüdür. Kernel’in anladığı tüm cihazlar burada temsil edilir.</td>
    </tr>
    <tr>
      <td>/proc</td>
      <td>/proc dizini de özeldir. Bu dizin dosya içermiyor. Aslında dizin mevcut değil, tamamen sanaldır. /proc dizini kernel’e küçük gözetleme delikleri içerir. Bu dizinde sistemde çalışan tüm proseslere karşılık gelen bir grup numaralı giriş (entry) vardır. Ek olarak, sistemin mevcut konfigürasyonuna erişime izin veren çok sayıda adlandırılmış giriş vardır. Bu girişlerin çoğu görüntülenebilir. /proc/cpuinfo dosyasını görüntülemeyi deneyin. Bu giriş size kernel’in sistem CPU’su hakkında ne düşündüğünü söyleyecektir.</td>
    </tr>
    <tr>
      <td>/media</td>
      <td>Son olarak, özel bir şekilde kullanılan normal bir dizin olan /media’ya geliyoruz. /media dizini bağlantı noktaları için kullanılır. İkinci derste öğrendiğimiz gibi, farklı fiziksel depolama aygıtları (sabit disk sürücüleri gibi) dosya sistemi ağacının çeşitli yerlerine eklenir. Ağaca bir cihaz bağlama işlemine mounting denir. Bir cihazın kullanılabilmesi için öncelikle mount edilmesi gerekir. Hangi aygıtların ve bağlantı noktalarının kullanıldığını görmek için mount yazın.</td>
    </tr>
  </tbody>
</table>

<p><strong>Tuhaf bir dosya türü…</strong></p>

<p>Turunuz sırasında muhtemelen özellikle <em>/lib</em> dizininde garip bir dizin girişi fark etmişsinizdir. <code class="language-plaintext highlighter-rouge">ls -l</code> ile listelendiğinde şunun gibi bir şey görmüş olabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lrwxrwxrwx     25 Jul  3 16:42 System.map -&gt; /boot/System.map-4.0.36-3
-rw-r--r-- 105911 Oct 13  2018 System.map-4.0.36-0.7
-rw-r--r-- 105935 Dec 29  2018 System.map-4.0.36-3
-rw-r--r-- 181986 Dec 11  2019 initrd-4.0.36-0.7.img
-rw-r--r-- 182001 Dec 11  2019 initrd-4.0.36.img
lrwxrwxrwx     26 Jul  3 16:42 module-info -&gt; /boot/module-info-4.0.36-3
-rw-r--r--  11773 Oct 13  2018 module-info-4.0.36-0.7
-rw-r--r--  11773 Dec 29  2018 module-info-4.0.36-3
lrwxrwxrwx     16 Dec 11  2019 vmlinuz -&gt; vmlinuz-4.0.36-3
-rw-r--r-- 454325 Oct 13  2018 vmlinuz-4.0.36-0.7
-rw-r--r-- 454434 Dec 29  2018 vmlinuz-4.0.36-3
</code></pre></div></div>

<p><em>System.map</em>, <em>module-info</em> ve <em>vmlinuz</em> dosyalarına dikkat edin. Dosya adlarından sonraki garip gösterimi görüyor musunuz?</p>

<p>Bunun gibi dosyalara sembolik link denir. Sembolik linkler, başka bir dosyaya işaret eden özel bir dosya türüdür. Sembolik linkler ile tek bir dosyanın birden fazla isme sahip olması mümkündür. Şu şekilde çalışır: Sisteme sembolik bir link olan dosya adı verildiğinde, sistem bunu şeffaf bir şekilde işaret ettiği dosyayla eşleştirir.</p>

<p>Peki bu ne işe yarar? Bu çok kullanışlı bir özelliktir. Yukarıdaki dizin listesine (eski bir sistemin <em>/boot</em> dizini) bakalım. Bu sistemde Linux çekirdeğinin birden fazla sürümü kuruludur. Bunu vmlinuz-4.0.36-0.7 ve vmlinuz-4.0.36-3 dosyalarından görebiliriz. Bu dosya adları hem 4.0.36-0.7 hem de 4.0.36-3 sürümünün yüklü olduğunu göstermektedir. Dosya adları sürümü içerdiğinden, dizin listesindeki farklılıkları görmek kolaydır. Ancak bu, kernel dosyasının adını kullanan programlar için kafa karıştırıcı olabilir. Bu programlar kernel’in basitçe “vmlinuz” olarak adlandırılmasını bekleyebilir. Sembolik link’in güzelliği burada devreye giriyor. vmlinuz-4.0.36-3’e işaret eden vmlinuz adında sembolik bir link oluşturarak sorunu çözdük.</p>

<p>Sembolik link oluşturmak için <code class="language-plaintext highlighter-rouge">ln</code> komutunu kullanın.</p>

<h2 id="dosyaları-düzenlemek">Dosyaları Düzenlemek</h2>

<p>Bu derste aşağıdaki komutlar tanıtılacaktır:</p>

<ul>
  <li><strong>cp</strong> - dosyaları ve dizinleri kopyala</li>
  <li><strong>mv</strong> - dosyaları ve dizinleri taşıyın veya yeniden adlandır</li>
  <li><strong>rm</strong> - dosyaları ve dizinleri kaldır</li>
  <li><strong>mkdir</strong> - dizin oluştur</li>
</ul>

<p>Bu dört komut en sık kullanılan Linux komutları arasındadır. Bunlar hem dosyaları hem de dizinleri değiştirmek için kullanılan temel komutlardır.</p>

<p>Açıkçası bu komutların gerçekleştirdiği bazı görevleri grafiksel dosya yöneticisi ile daha kolay bir şekilde gerçekleştirebilirsiniz. Dosya yöneticisiyle, bir dosyayı bir dizinden diğerine sürükleyip bırakabilir, dosyaları kesip yapıştırabilir, dosyaları silebilirsiniz vb. Peki neden bu eski komut satırı programlarını kullanasınız ki?</p>

<p>Cevap güç ve esnekliktir. Grafiksel dosya yöneticisiyle basit dosya işlemlerini gerçekleştirmek kolay olsa da, komut satırı programlarıyla karmaşık görevler daha kolay olabilir. Örneğin, tüm HTML dosyalarını bir dizinden diğerine nasıl kopyalarsınız, ancak yalnızca hedef dizinde bulunmayan veya hedef dizindeki sürümlerden daha yeni olan dosyaları kopyalarsınız? Dosya yöneticisiyle oldukça zor. Komut satırıyla oldukça kolay:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ cp -u *.html destination
</code></pre></div></div>
<h3 id="joker-karakterler">Joker Karakterler</h3>

<p>Komutlarımıza başlamadan önce, bu komutları bu kadar güçlü kılan shell özelliğine bakacağız. Shell dosya adlarını çok fazla kullandığından, dosya adı gruplarını hızla belirlemenize yardımcı olacak özel karakterler sağlar. Bu özel karakterlere joker karakterler denir. Joker karakterler, karakter paternlerine göre dosya adlarını seçmenize olanak tanır. Aşağıdaki tabloda joker karakterler ve bunların seçtikleri listelenmektedir:</p>

<table>
  <thead>
    <tr>
      <th>Joker Karakter</th>
      <th>Anlamı</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">*</code></td>
      <td>Herhangi bir karakterle eşleşir</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">?</code></td>
      <td>Herhangi bir tek karakterle eşleşir</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">[characters]</code></td>
      <td>Ayarlanan karakterlerin üyesi olan herhangi bir karakterle eşleşir. Karakter kümesi ayrıca aşağıdakilerden gibi bir POSIX karakter sınıfı olarak da ifade edilebilir: <code class="language-plaintext highlighter-rouge">[:alnum:]</code> Alfanümerik karakterler <code class="language-plaintext highlighter-rouge">[:alpha:]</code> Alfabetik karakterler <code class="language-plaintext highlighter-rouge">[:digit:]</code> Rakamlar <code class="language-plaintext highlighter-rouge">[:upper:]</code> Büyük harfli alfabetik karakterler <code class="language-plaintext highlighter-rouge">[:lower:]</code> Küçük harfli alfabetik karakterler</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">[!characters]</code></td>
      <td>Ayarlanan karakterlerin üyesi olmayan herhangi bir karakterle eşleşir</td>
    </tr>
  </tbody>
</table>

<p>Joker karakterleri kullanarak dosya adları için çok karmaşık seçim kriterleri oluşturmak mümkündür. İşte bazı patern örnekleri ve bunların eşleştiği şeyler:</p>

<table>
  <thead>
    <tr>
      <th>Patern</th>
      <th>Eşleşme</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">*</code></td>
      <td>Tüm dosya adları</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">g*</code></td>
      <td>“g” karakteriyle başlayan tüm dosya adları</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">b*.txt</code></td>
      <td>“b” karakteriyle başlayan ve “.txt” karakteriyle biten tüm dosya adları</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Data???</code></td>
      <td>“Data” karakterleriyle başlayan ve ardından tam olarak 3 karakter daha gelen herhangi bir dosya adı</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">[abc]*</code></td>
      <td>“a”, “b” veya “c” ile başlayan ve ardından başka karakterler gelen herhangi bir dosya adı</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">[[:upper:]]*</code></td>
      <td>Büyük harfle başlayan herhangi bir dosya adı. Bu bir karakter sınıfı örneğidir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">BACKUP.[[:digit:]][[:digit:]]</code></td>
      <td>Karakter sınıflarına başka bir örnek. Bu patern, “BACKUP.” karakterleriyle başlayan ve ardından tam olarak iki rakam gelen herhangi bir dosya adıyla eşleşir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">*[![:lower:]]</code></td>
      <td>Küçük harfle bitmeyen herhangi bir dosya adı.</td>
    </tr>
  </tbody>
</table>

<p>Dosya adı argümanı kabul eden herhangi bir komutta joker karakterler kullanabiliriz.</p>

<h3 id="cp">cp</h3>

<p><code class="language-plaintext highlighter-rouge">cp</code> programı dosyaları ve dizinleri kopyalar. En basit haliyle tek bir dosyayı kopyalar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ cp file1 file2
</code></pre></div></div>

<p>Birden fazla dosyayı (ve/veya dizini) farklı bir dizine kopyalamak için de kullanılabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ cp file... directory
</code></pre></div></div>

<p><strong>Gösterimle ilgili not:</strong> … bir öğenin bir veya daha fazla kez tekrarlanabileceğini belirtir.</p>

<p><code class="language-plaintext highlighter-rouge">cp</code> ve seçeneklerinin diğer kullanışlı örnekleri şunları içerir:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cp file1 file2</code></td>
      <td>file1’in içeriğini file2’ye kopyalar. file2 mevcut değilse oluşturulur; aksi takdirde, file1’in içeriği file2’nin üzerine sessizce yazılır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cp -i file1 file2</code></td>
      <td>“-i” (etkileşimli) seçeneği belirtildiğinden, eğer file2 mevcutsa, file1’in içeriğinin üzerine yazılmadan önce kullanıcıya bilgi verilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cp file1 dir1</code></td>
      <td>file1’in içeriğini (file1 adlı bir dosyaya) dir1 dizininin içine kopyalayın.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cp -R dir1 dir2</code></td>
      <td>dir1 dizininin içeriğini kopyalayın. dir2 mevcut değilse oluşturulur. Aksi takdirde, dir2 dizini içerisinde dir1 adında bir dizin oluşturur.</td>
    </tr>
  </tbody>
</table>

<h3 id="mv">mv</h3>

<p><code class="language-plaintext highlighter-rouge">mv</code> komutu, nasıl kullanıldığına bağlı olarak dosyaları ve dizinleri taşır veya yeniden adlandırır. Bir veya daha fazla dosyayı farklı bir dizine taşıyacak veya bir dosya veya dizini yeniden adlandıracaktır. Bir dosyayı yeniden adlandırmak için şu şekilde kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ mv filename1 filename2
</code></pre></div></div>

<p>Dosyaları (ve/veya dizinleri) farklı bir dizine taşımak için:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ mv file... directory
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mv</code> ve seçeneklerinin örnekleri şunları içerir:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mv file1 file2</code></td>
      <td>file2 mevcut değilse, file1, file2 olarak yeniden adlandırılır. file2 mevcutsa, içeriği sessizce file1’in içeriğiyle değiştirilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mv -i file1 file2</code></td>
      <td>“-i” (etkileşimli) seçeneği belirtildiğinden, eğer file2 mevcutsa, file1’in içeriği üzerine yazılmadan önce kullanıcıya bilgi verilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mv file1 file2 dir1</code></td>
      <td>file1 ve file2 dosyaları dir1 dizinine taşınır. dir1 mevcut değilse, mv bir hatayla çıkacaktır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mv dir1 dir2</code></td>
      <td>dir1 mevcut değilse, dir1, dir2 olarak yeniden adlandırılır. dir2 mevcutsa, dir1 dizini dir2’nin içine taşınır.</td>
    </tr>
  </tbody>
</table>

<h3 id="rm">rm</h3>

<p><code class="language-plaintext highlighter-rouge">rm</code> komutu dosyaları ve dizinleri kaldırır (siler).</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ rm file...
</code></pre></div></div>

<p>Özyinelemeli seçeneği (-r) kullanarak, dizinleri silmek için de kullanılabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ rm -r directory...
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">rm</code> ve seçeneklerinin örnekleri şunları içerir:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm file1 file2</code></td>
      <td>file1 ve file2’yi siler.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm -i file1 file2</code></td>
      <td>“-i” (etkileşimli) seçeneği belirtildiğinden, her dosya silinmeden önce kullanıcıya bilgi verilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm -r dir1 dir2</code></td>
      <td>dir1 ve dir2 dizinleri tüm içerikleriyle birlikte silinir.</td>
    </tr>
  </tbody>
</table>

<p><strong>rm’yi dikkati kullanın</strong></p>

<p>Linux’un silmeyi geri alma komutu yoktur. <code class="language-plaintext highlighter-rouge">rm</code> ile bir şeyi sildiğinizde, o şey gider. Özellikle joker karakterleri kullanırken dikkat etmezseniz <code class="language-plaintext highlighter-rouge">rm</code> ile sisteminize büyük zararlar verebilirsiniz.</p>

<p><code class="language-plaintext highlighter-rouge">rm</code>‘yi joker karakterlerle kullanmadan önce şu yararlı yöntemi deneyebilirsiniz: komutunuzu <code class="language-plaintext highlighter-rouge">ls</code> kullanarak oluşturun. Bunu yaparak, dosyaları silmeden önce joker karakterlerinizin etkisini görebilirsiniz. Komutunuzu <code class="language-plaintext highlighter-rouge">ls</code> ile test ettikten sonra yukarı ok tuşuyla komutu çağırın ve <code class="language-plaintext highlighter-rouge">ls</code> yerine <code class="language-plaintext highlighter-rouge">rm</code> yazın.</p>

<h3 id="mkdir">mkdir</h3>

<p><code class="language-plaintext highlighter-rouge">mkdir</code> komutu dizin oluşturmak için kullanılır. Kullanmak için şunu yazmanız yeterlidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ mkdir directory...
</code></pre></div></div>

<h3 id="komutları-joker-karakterlerle-kullanmak">Komutları Joker Karakterlerle Kullanmak</h3>

<p>Burada ele aldığımız komutlar birden fazla dosya ve dizin adını argüman olarak kabul ettiğinden, bunları belirtmek için joker karakterler kullanabilirsiniz. İşte birkaç örnek:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Sonuç</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cp *.txt text_files</code></td>
      <td>Geçerli çalışma dizinindeki adları “.txt” karakterleriyle biten tüm dosyaları, text_files adlı mevcut bir dizine kopyalar.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mv dir1 ../*.bak dir2</code></td>
      <td>dir1 alt dizinini ve geçerli çalışma dizininin üst dizinindeki “.bak” ile biten tüm dosyaları dir2 adlı mevcut bir dizine taşır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm *~</code></td>
      <td>Geçerli çalışma dizinindeki “~” karakteriyle biten tüm dosyaları siler. Bazı uygulamalar bu adlandırma şemasını kullanarak yedekleme dosyaları oluşturur. Bu komutu kullanmak onları dizinden temizleyecektir.</td>
    </tr>
  </tbody>
</table>

<h2 id="komutlarla-çalışmak">Komutlarla Çalışmak</h2>

<p>Şu ana kadar çok sayıda komutu ve bunların gizemli seçeneklerini ve argümanlarını gördük. Bu derste bu gizemin bir kısmını ortadan kaldırmaya çalışacağız. Aşağıdaki komutları tanıtacağız.</p>

<ul>
  <li><strong>type</strong> - Komut türüyle ilgili bilgileri görüntüler</li>
  <li><strong>which</strong> - Bir komut bulun</li>
  <li><strong>help</strong> - Shell yerleşik referans sayfasını görüntüler</li>
  <li><strong>man</strong> - Çevrimiçi komut referansını görüntüler</li>
</ul>

<h3 id="komut-nedir">“Komut” Nedir?</h3>

<p>Komutlar 4 farklı türden biri olabilir:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">/usr/bin</code>‘de gördüğümüz tüm dosyalar gibi <strong>çalıştırılabilir bir program</strong>. Bu kategoride programlar, C ve C++ ile yazılmış programlar gibi derlenmiş binary dosyalar veya shell, Perl, Python, Ruby vb. script dillerinde yazılmış programlar olabilir.</li>
  <li><strong>Shell’in kendisinde yerleşik bir komut</strong>. bash, dahili olarak shell yerleşikleri (builtin) adı verilen bir dizi komut sağlar. Örneğin <code class="language-plaintext highlighter-rouge">cd</code> komutu shell yerleşiğidir.</li>
  <li><strong>Shell fonksiyonu</strong>. Bunlar ortama dahil edilen minyatür shell script dosyalarıdır. Daha sonraki derslerde ortamı (environment) yapılandırmayı ve shell fonksiyonları yazmayı ele alacağız, ancak şimdilik bunların var olduğunu unutmayın.</li>
  <li><strong>Takma ad (alias)</strong>. Kendimiz tanımlayabileceğimiz, diğer komutlardan oluşan komutlar. Bu daha sonra ele alınacaktır.</li>
</ul>

<h3 id="komutları-tanımlamak">Komutları Tanımlamak</h3>

<p>Dört tür komuttan hangisinin kullanıldığını bilmek genellikle faydalıdır ve Linux bunu öğrenmenin birkaç yolunu sunar.</p>

<p><strong>type</strong></p>

<p><code class="language-plaintext highlighter-rouge">type</code> komutu, belirli bir komut adı verildiğinde shell’in yürüteceği komut türünü görüntüleyen shell yerleşiğidir. Şu şekilde çalışır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type command
</code></pre></div></div>

<p>burada “command” incelemek istediğimiz komutun adıdır. İşte bazı örnekler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ type type
type is a shell builtin
[me@linuxbox me]$ type ls
ls is aliased to 'ls --color=auto'
[me@linuxbox me]$ type cp
cp is /bin/cp
</code></pre></div></div>

<p>Burada üç farklı komut için sonuçları görüyoruz. ls komutunun aslında “– color=auto” seçeneğinin eklendiği ls komutunun takma adı olduğuna dikkat edin. Artık ls çıktısının neden renkli görüntülendiğini biliyoruz!</p>

<p><strong>which</strong></p>

<p>Bazen bir sistemde programın birden fazla sürümü kurulu olabilir. Bu, masaüstü sistemlerde çok yaygın olmasa da, büyük sunucularda alışılmadık bir durum değildir. Belirli bir dosyanın tam konumunu belirlemek için <code class="language-plaintext highlighter-rouge">which</code> komutu kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ which ls
/bin/ls
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">which</code> yalnızca çalıştırılabilir programlar için işe yarar, gerçek çalıştırılabilir programların yerine geçen yerleşikler veya takma adlar için geçerli değildir.</p>

<h3 id="komut-dokümantasyonunun-alınması">Komut Dokümantasyonunun Alınması</h3>

<p>Komutun ne olduğuna dair bu bilgiyle, artık her komut türü için mevcut belgeleri arayabiliriz.</p>

<p><strong>help</strong></p>

<p>bash, shell yerleşiklerinin her biri için yardım olanağına sahiptir. Kullanmak için “help” ve ardından shell yerleşiğinin adını yazın. İsteğe bağlı olarak çıktının formatını değiştirmek için <code class="language-plaintext highlighter-rouge">-m</code> seçeneğini ekleyebiliriz. Örneğin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ help -m cd
NAME
    cd - Change the shell working directory.

SYNOPSIS
    cd [-L|-P] [dir]

DESCRIPTION
    Change the shell working directory.
    
    Change the current directory to DIR.  The default DIR is the value of the
    HOME shell variable.
    
    The variable CDPATH defines the search path for the directory containing
    DIR.  Alternative directory names in CDPATH are separated by a colon (:).
    A null directory name is the same as the current directory.  If DIR begins
    with a slash (/), then CDPATH is not used.
    
    If the directory is not found, and the shell option `cdable_vars' is set,
    the word is assumed to be  a variable name.  If that variable has a value,
    its value is used for DIR.
    
    Options:
        -L  force symbolic links to be followed
        -P  use the physical directory structure without following symbolic
      links
    
    The default is to follow symbolic links, as if `-L' were specified.
    
    Exit Status:
    Returns 0 if the directory is changed; non-zero otherwise.

SEE ALSO
    bash(1)

IMPLEMENTATION
    GNU bash, version 4.1.5(1)-release (i486-pc-linux-gnu)
    Copyright (C) 2009 Free Software Foundation, Inc.
</code></pre></div></div>

<p><strong>Gösterimle ilgili not</strong>: Bir komutun sözdiziminin açıklamasında köşeli parantezler göründüğünde, isteğe bağlı öğeleri belirtir. Dikey çubuk karakteri birbirini dışlayan öğeleri belirtir. Yukarıdaki <code class="language-plaintext highlighter-rouge">cd</code> komutu durumunda:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd [-L|-P] [dir]
</code></pre></div></div>

<p>Bu gösterim, <code class="language-plaintext highlighter-rouge">cd</code> komutunun ardından isteğe bağlı olarak “-L” veya “-P” ve ayrıca isteğe bağlı olarak “dir” argümanının gelebileceğini belirtir.</p>

<p><strong>–help</strong></p>

<p>Yürütülebilir programların çoğu, komutun desteklenen söz dizimi ve seçeneklerinin açıklamasını görüntüleyen “–help” seçeneğini destekler. Örneğin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ mkdir --help
Usage: mkdir [OPTION] DIRECTORY...
Create the DIRECTORY(ies), if they do not already exist.
Mandatory arguments to long options are mandatory for short options
too.

   -Z, --context=CONTEXT (SELinux) set security context to CONTEXT
   -m, --mode=MODE   set file mode (as in chmod), not a=rwx - umask
   -p, --parents     no error if existing, make parent directories as
                     needed
   -v, --verbose     print a message for each created directory
   --help            display this help and exit
   --version         output version information and exit
</code></pre></div></div>

<p>Bazı programlar “–help” seçeneğini desteklemez ama yine de deneyin.</p>

<p><strong>man</strong></p>

<p>Komut satırı kullanımına yönelik çoğu yürütülebilir program, manual veya man sayfası adı verilen resmi bir dökümantasyon sağlar. Bunları görüntülemek için <code class="language-plaintext highlighter-rouge">man</code> programı kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>man program
</code></pre></div></div>

<p>burada “program” görüntülenecek komutun adıdır. Man sayfaları format olarak biraz farklılık gösterir ancak genellikle başlık, komut sözdiziminin özetini, komut amacının açıklamasını ve komut seçeneklerinin her birinin listesini ve açıklamasını içerir. Ancak Man sayfaları genellikle örnekler içermez ve bir tutorial olarak değil, referans olarak tasarlanmıştır. <code class="language-plaintext highlighter-rouge">ls</code> komutu için man sayfasını görüntülemeyi deneyelim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ man ls
</code></pre></div></div>

<p>Çoğu Linux sisteminde <code class="language-plaintext highlighter-rouge">man</code>, kılavuz sayfasını görüntülemek için <code class="language-plaintext highlighter-rouge">less</code> kullanır, böylece bilinen <code class="language-plaintext highlighter-rouge">less</code> komutlarının tümü sayfayı görüntülerken çalışır.</p>

<h2 id="io-yönlendirmesi">I/O Yönlendirmesi</h2>

<p>Bu derste, input/output yönlendirmesi adı verilen, komut satırı programlarının kullandığı güçlü bir özelliği inceleyeceğiz. Gördüğümüz gibi <code class="language-plaintext highlighter-rouge">ls</code> benzeri birçok komut çıktılarını ekrana yazdırır. Ancak böyle olmak zorunda değildir. Bazı özel gösterimler kullanarak birçok komutun çıktısını dosyalara, aygıtlara ve hatta diğer komutların girişine yönlendirebiliriz.</p>

<h3 id="standart-output">Standart Output</h3>

<p>Sonuçlarını görüntüleyen çoğu komut satırı programı, sonuçlarını standart output adı verilen bir araca göndererek bunu yapar. Varsayılan olarak standart output, içeriği ekrana yönlendirir. Standart output’u bir dosyaya yönlendirmek için “&gt;” karakteri şu şekilde kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ls &gt; file_list.txt
</code></pre></div></div>

<p>Bu örnekte <code class="language-plaintext highlighter-rouge">ls</code> komutu çalıştırılıyor ve sonuçlar <em>file_list.txt</em> adlı bir dosyaya yazılıyor. <code class="language-plaintext highlighter-rouge">ls</code> çıktısı dosyaya yönlendirildiği için ekranda hiçbir sonuç görünmüyor.</p>

<p>Yukarıdaki komut her tekrarlandığında, <code class="language-plaintext highlighter-rouge">ls</code> komutunun çıktısı baştan itibaren <em>file_list.txt</em> dosyasının üzerine yazılır. Bunun yerine yeni sonuçların dosyaya eklenmesini sağlamak için “»” ifadesini şu şekilde kullanırız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ls &gt;&gt; file_list.txt
</code></pre></div></div>

<p>Yeni sonuçlar dosyanın sonuna eklenir, böylece komut her tekrarlandığında dosya daha uzun hale gelir. Dosya mevcut değilse, dosya oluşturulacaktır.</p>

<h3 id="standard-input">Standard Input</h3>

<p>Birçok komut, standart input adı verilen bir özellikten giriş kabul edebilir. Varsayılan olarak standart input, içeriğini klavyeden alır ancak standart output gibi yeniden yönlendirilebilir. Standart input’u klavye yerine bir dosyadan yönlendirmek için “&lt;” karakteri şu şekilde kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ sort &lt; file_list.txt
</code></pre></div></div>

<p>Yukarıdaki örnekte <em>file_list.txt</em> içeriğini işlemek için <code class="language-plaintext highlighter-rouge">sort</code> komutunu kullandık. Standart output yönlendirilmediği için sonuçlar ekranda görüntülenir. Standart output’u şu şekilde başka bir dosyaya yönlendirebiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ sort &lt; file_list.txt &gt; sorted_file_list.txt
</code></pre></div></div>

<p>Gördüğümüz gibi, bir komutun hem girişi hem de çıkışı yeniden yönlendirilebilir. Yönlendirme sırasının önemli olmadığını unutmayın. Tek gereksinim, yeniden yönlendirme işleçlerinin (“&lt;” ve “&gt;”) komuttaki diğer seçeneklerden ve parametrelerden sonra görünmesi gerektiğidir.</p>

<h3 id="pipeline">Pipeline</h3>

<p>I/O yönlendirmeyle yapabileceğimiz en kullanışlı ve güçlü şey, birden çok komutu birbirine bağlayarak pipeline adı verilen şeyi oluşturmaktır. Pipeline ile bir komutun standart output’u diğerinin standart input’unu besler. İşte çok yararlı bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ls -l | less
</code></pre></div></div>

<p>Bu örnekte <code class="language-plaintext highlighter-rouge">ls</code> komutunun çıktısı <code class="language-plaintext highlighter-rouge">less</code>‘e aktarılmıştır. Bu <code class="language-plaintext highlighter-rouge">| less</code> hilesini kullanarak herhangi bir komutun kayan çıktıya sahip olmasını sağlayabiliriz.</p>

<p>Komutları birbirine bağlayarak inanılmaz başarılar elde edebiliriz. İşte deneyebileceğiniz bazı örnekler:</p>

<table>
  <thead>
    <tr>
      <th>Komut</th>
      <th>Ne Yapar</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ls -lt | head</code></td>
      <td>Geçerli dizindeki en yeni 10 dosyayı görüntüler.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">du | sort -nr</code></td>
      <td>Dizinlerin listesini ve ne kadar alan tükettiklerini büyükten küçüğe doğru sıralanmış olarak görüntüler.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">find . -type f -print | wc -l</code></td>
      <td>Geçerli çalışma dizinindeki ve onun tüm alt dizinlerindeki toplam dosya sayısını görüntüler.</td>
    </tr>
  </tbody>
</table>

<h3 id="filtreler">Filtreler</h3>

<p>Pipeline’da sıklıkla kullanılan program türlerinden birine filtre adı verilir. Filtreler standart input’u alıp üzerinde işlem yapar ve sonuçları standart output’a gönderir. Bu şekilde, bilgiyi işlemek için birleştirilebilirler. Filtre olarak kullanılan yaygın programlardan bazıları şunlardır:</p>

<table>
  <thead>
    <tr>
      <th>Program</th>
      <th>Ne Yapar</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sort</code></td>
      <td>Standart input’u sıralar ve sıralanan sonucu standart output’a verir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">uniq</code></td>
      <td>Standart input’tan sıralanmış bir veri akışı verildiğinde, yinelenen veri satırlarını kaldırır (yani her satırın benzersiz olmasını sağlar).</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">grep</code></td>
      <td>Standart input’tan aldığı her veri satırını inceler ve belirli bir karakter patern’ini içeren her satırın çıktısını alır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">fmt</code></td>
      <td>Standart input’taki metni okur, ardından biçimlendirilmiş metni standart output’a verir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pr</code></td>
      <td>Metin girişini standart input’tan alır ve yazdırmaya hazırlık amacıyla verileri sayfa sonları, üstbilgiler ve altbilgilerle sayfalara böler.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">head</code></td>
      <td>İlk birkaç satırının çıktısını verir. Bir dosyanın başlığını almak için kullanışlıdır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tail</code></td>
      <td>Son birkaç satırının çıktısını verir. Log dosyasından en son girişleri almak gibi şeyler için kullanışlıdır.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tr</code></td>
      <td>Karakterleri çevirir. Büyük/küçük harf dönüştürme veya satır sonlandırma karakterlerini bir türden diğerine değiştirme (örneğin, DOS metin dosyalarını Unix tarzı metin dosyalarına dönüştürme) gibi görevleri gerçekleştirmek için kullanılabilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sed</code></td>
      <td>Akış düzenleyicisi. <code class="language-plaintext highlighter-rouge">tr</code>‘ye göre daha karmaşık metin çevirileri gerçekleştirebilir.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">awk</code></td>
      <td>Filtreler oluşturmak için tasarlanmış eksiksiz bir programlama dili. Son derece güçlüdür.</td>
    </tr>
  </tbody>
</table>

<h2 id="i̇zinler">İzinler</h2>

<p>Linux gibi Unix benzeri işletim sistemleri, yalnızca çoklu görev değil aynı zamanda çok kullanıcılı olmaları açısından diğer sistemlerden farklıdır.</p>

<p>Bu tam olarak ne anlama geliyor? Bu, birden fazla kullanıcının aynı anda bilgisayarı çalıştırabileceği anlamına gelir. Masaüstü veya dizüstü bilgisayarda yalnızca bir klavye ve monitör bulunurken yine de birden fazla kullanıcı tarafından kullanılabilir. Örneğin bilgisayar bir ağa veya internete bağlıysa uzaktaki kullanıcılar ssh (secure shell) aracılığıyla giriş yapıp bilgisayarı çalıştırabilirler. Aslında uzaktaki kullanıcılar grafik uygulamaları çalıştırabilir ve çıktının uzaktaki bir bilgisayarda görüntülenmesini sağlayabilir. X Window sistemi bunu desteklemektedir.</p>

<p>Unix benzeri sistemlerin çok kullanıcılı özelliği, işletim sisteminin tasarımına derinlemesine yerleşmiş bir özelliktir. Unix’in yaratıldığı ortamı hatırlarsak, bu çok mantıklıdır. Yıllar önce bilgisayarlar “kişisel” olmadan önce büyük, pahalı ve merkeziydi. Tipik bir üniversite bilgisayar sistemi, kampüsteki bazı binalarda bulunan büyük bir ana bilgisayardan ve kampüs boyunca her biri büyük merkezi bilgisayara bağlanan terminallerden oluşuyordu. Bilgisayar aynı anda birçok kullanıcıyı destekleyecektir.</p>

<p>Bunu pratik hale getirebilmek için kullanıcıları birbirinden koruyacak bir yöntemin geliştirilmesi gerekiyordu. Sonuçta ne bir kullanıcının eylemlerinin bilgisayarın çökmesine neden olmasını, ne de bir kullanıcının başka bir kullanıcıya ait dosyalara müdahale etmesine izin veremeyiz.</p>

<p>Bu ders aşağıdaki komutları kapsayacaktır:</p>

<ul>
  <li><strong>chmod</strong> - dosya erişim haklarını değiştirin</li>
  <li><strong>su</strong> - geçici olarak superuser olun</li>
  <li><strong>sudo</strong> - geçici olarak superuser olun</li>
  <li><strong>chown</strong> - dosya sahipliğini değiştirin</li>
  <li><strong>chgrp</strong> - dosyanın grup sahipliğini değiştirin</li>
</ul>

<h3 id="dosya-i̇zinleri">Dosya İzinleri</h3>

<p>Bir Linux sisteminde, her dosya ve dizine, dosyanın sahibine, ilgili kullanıcı grubunun üyelerine ve diğer herkese erişim hakları atanır. Bir dosyayı okumak, bir dosyayı yazmak ve bir dosyayı yürütmek (yani dosyayı bir program olarak çalıştırmak) için haklar atanabilir.</p>

<p>Bir dosyanın izin ayarlarını görmek için <code class="language-plaintext highlighter-rouge">ls</code> komutunu kullanabiliriz. Örnek olarak <em>/bin</em> dizininde bulunan bash programına bakacağız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ls -l /bin/bash
-rwxr-xr-x 1 root root 1113504 Jun  6  2019 /bin/bash
</code></pre></div></div>

<p>Burada şunları görebiliriz:</p>

<ul>
  <li>“/bin/bash” dosyası “root” kullanıcısına aittir</li>
  <li>superuser’ın bu dosyayı okuma, yazma ve yürütme hakkı vardır</li>
  <li>Dosyanın sahibi “root” grubudur</li>
  <li>“root” grubunun üyeleri de bu dosyayı okuyabilir ve çalıştırabilir</li>
  <li>Herkes bu dosyayı okuyabilir ve çalıştırabilir</li>
</ul>

<p>Aşağıdaki şemada listenin ilk bölümünün nasıl yorumlandığını görüyoruz. Dosya türünü belirten bir karakterden ve ardından dosya sahibi, grup ve diğer herkes için okuma, yazma ve yürütme iznini gösteren üç karakterli üç setten oluşur.</p>

<p><img src="/assets/images/2024/05/file-permissions.png" alt="File Permissions" title="File Permissions" class="img-fluid" /></p>

<h3 id="chmod">chmod</h3>

<p><code class="language-plaintext highlighter-rouge">chmod</code> komutu bir dosyanın veya dizinin izinlerini değiştirmek için kullanılır. Kullanmak için istenen izin ayarlarını ve değiştirmek istediğimiz dosya veya dosyaları belirtiyoruz. İzinleri belirtmenin iki yolu vardır. Bu derste bunlardan biri olan sekizli notasyon yöntemine odaklanacağız.</p>

<p>İzin ayarlarını bir dizi bit olarak düşünmek kolaydır (bilgisayarda onlar hakkında böyle düşünür). Şöyle çalışıyor:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rwx rwx rwx = 111 111 111
rw- rw- rw- = 110 110 110
rwx --- --- = 111 000 000

ve benzeri...

rwx = 111 in binary = 7
rw- = 110 in binary = 6
r-x = 101 in binary = 5
r-- = 100 in binary = 4
</code></pre></div></div>

<p>Şimdi, eğer üç izin kümesinin (sahip, grup ve diğer) her birini tek basamaklı olarak temsil edersek, izin ayarlarını ifade etmenin oldukça uygun bir yoluna sahip oluruz. Örneğin, <em>some_file</em>‘ın sahibi için okuma ve yazma iznine sahip olacak şekilde ayarlamak istiyor ancak dosyayı diğerlerinden gizli tutmak istiyorsak, şunu yaparız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ chmod 600 some_file
</code></pre></div></div>

<p>Burada tüm ortak ayarları kapsayan bir sayı tablosu verilmiştir. “7” ile başlayan dosyalar programlarla (yürütmeyi mümkün kıldıkları için) kullanılır, geri kalanlar ise diğer dosya türleri içindir.</p>

<table>
  <thead>
    <tr>
      <th>Değer</th>
      <th>Anlamı</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>777</td>
      <td>(rwxrwxrwx) İzinlerde herhangi bir kısıtlama yoktur. Herkes her şeyi yapabilir. Genellikle arzu edilen bir ayar değildir.</td>
    </tr>
    <tr>
      <td>755</td>
      <td>(rwxr-xr-x) Dosyanın sahibi dosyayı okuyabilir, yazabilir ve çalıştırabilir. Diğerleri dosyayı okuyabilir ve çalıştırabilir. Bu ayar, tüm kullanıcılar tarafından kullanılan programlar için ortaktır.</td>
    </tr>
    <tr>
      <td>700</td>
      <td>(rwx——) Dosyanın sahibi dosyayı okuyabilir, yazabilir ve çalıştırabilir. Başka hiç kimsenin hiçbir hakkı yoktur. Bu ayar yalnızca sahibinin kullanabileceği ve diğerlerinden gizli tutulması gereken programlar için kullanışlıdır.</td>
    </tr>
    <tr>
      <td>666</td>
      <td>(rw-rw-rw-) Tüm kullanıcılar dosyayı okuyabilir ve yazabilir.</td>
    </tr>
    <tr>
      <td>644</td>
      <td>(rw-r–r–) Dosyanın sahibi bir dosyayı okuyup yazabilir, diğer herkes ise yalnızca dosyayı okuyabilir. Veri dosyaları için herkesin okuyabileceği, ancak yalnızca sahibinin değiştirebileceği ortak bir ayar.</td>
    </tr>
    <tr>
      <td>600</td>
      <td>(rw——-) Sahibi bir dosyayı okuyabilir ve yazabilir. Diğerlerinin hiçbir hakkı yoktur. Sahibinin gizli tutmak istediği veri dosyaları için ortak bir ayar.</td>
    </tr>
  </tbody>
</table>

<h3 id="dizin-i̇zinleri">Dizin İzinleri</h3>

<p>chmod komutu, dizinlere erişim izinlerini kontrol etmek için de kullanılabilir. İzinleri ayarlamak için yine sekizli gösterimi kullanabiliriz ancak r, w ve x niteliklerinin anlamı farklıdır:</p>

<ul>
  <li><strong>r</strong> - x niteliği de ayarlanmışsa dizin içeriğinin listelenmesine izin verir.</li>
  <li><strong>w</strong> - x niteliği de ayarlanmışsa, dizin içindeki dosyaların oluşturulmasına, silinmesine veya yeniden adlandırılmasına izin verir.</li>
  <li><strong>x</strong> - Bir dizinin girilmesine izin verir (örn. <strong>cd dir</strong>).</li>
</ul>

<p>Dizinler için bazı kullanışlı ayarlar şunlardır:</p>

<table>
  <thead>
    <tr>
      <th>Değer</th>
      <th>Anlamı</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>777</td>
      <td>(rwxrwxrwx) İzinlerde herhangi bir kısıtlama yoktur. Herkes dosyaları listeleyebilir, dizinde yeni dosyalar oluşturabilir ve dizindeki dosyaları silebilir. Genellikle iyi bir ayar değildir.</td>
    </tr>
    <tr>
      <td>755</td>
      <td>(rwxr-xr-x) Dizin sahibinin tam erişimi vardır. Diğerleri dizini listeleyebilir ancak dosya oluşturamaz veya silemez. Bu ayar, diğer kullanıcılarla paylaşmak istediğiniz dizinler için ortaktır.</td>
    </tr>
    <tr>
      <td>700</td>
      <td>(rwx——) Dizin sahibinin tam erişimi vardır. Başka hiç kimsenin hiçbir hakkı yoktur. Bu ayar, yalnızca sahibinin kullanabileceği ve diğerlerinden gizli tutulması gereken dizinler için kullanışlıdır</td>
    </tr>
  </tbody>
</table>

<h3 id="kısa-süreliğine-superuser-olmak">Kısa Süreliğine Superuser Olmak</h3>

<p>Önemli sistem yönetimi görevlerini gerçekleştirmek için genellikle superuser olmak gerekir, ancak bildiğimiz gibi superuser olarak oturum açmamalıyız. Çoğu dağıtımda, superuser ayrıcalıklarına geçici olarak erişmenizi sağlayacak bir program vardır. Bu programa <code class="language-plaintext highlighter-rouge">su</code> (substitute user kelimesinin kısaltması) adı verilir ve az sayıda görev için superuser olmanızın gerektiği durumlarda kullanılabilir. Superuser olmak için <code class="language-plaintext highlighter-rouge">su</code> komutunu yazmanız yeterlidir. Superuser şifresi istenecektir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ su
Password:
[root@linuxbox me]#
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">su</code> komutunu çalıştırdıktan sonra superuser olarak yeni bir shell oturumu açılır. Superuser oturumundan çıkmak için, <code class="language-plaintext highlighter-rouge">exit</code> yazın ve önceki oturumunuza dönün.</p>

<p>Çoğu modern dağıtımda alternatif bir yöntem kullanılır. Bu sistemler <code class="language-plaintext highlighter-rouge">su</code> kullanmak yerine <code class="language-plaintext highlighter-rouge">sudo</code> komutunu kullanır. sudo ile bir veya daha fazla kullanıcıya gerektiğinde superuser ayrıcalıkları verilir. Bir komutu superuser olarak yürütmek için istenen komutun önüne <code class="language-plaintext highlighter-rouge">sudo</code> komutu gelir. Komut girildikten sonra kullanıcıdan superuser şifresi yerine kendi şifresi istenir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ sudo some_command
Password for me:
[me@linuxbox me]$
</code></pre></div></div>

<p>Aslında, modern dağıtımlar root hesap şifresini bile ayarlamadığından, root kullanıcı olarak oturum açmayı imkansız hale getirir. “-i” seçeneği kullanılarak sudo ile root shell hala mümkündür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ sudo -i
Password for me:
root@linuxbox:~#
</code></pre></div></div>

<h3 id="dosya-sahipliğini-değiştirmek">Dosya Sahipliğini Değiştirmek</h3>

<p><code class="language-plaintext highlighter-rouge">chown</code> komutunu kullanarak bir dosyanın sahibini değiştirebiliriz. İşte bir örnek: Diyelim ki <em>some_file</em>’ın sahibini “me” yerine “you” olarak değiştirmek istiyoruz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ sudo chown you some_file
</code></pre></div></div>

<p>Bir dosyanın sahibini değiştirmek için superuser ayrıcalıklarına sahip olmamız gerektiğine dikkat edin. Bunu yapmak için örneğimizde <code class="language-plaintext highlighter-rouge">sudo</code> komutunu kullandık.</p>

<p><code class="language-plaintext highlighter-rouge">chown</code>, dosyalarda olduğu gibi dizinlerde de aynı şekilde çalışır.</p>

<h3 id="grup-sahipliğini-değiştirmek">Grup Sahipliğini Değiştirmek</h3>

<p>Bir dosyanın veya dizinin grup sahipliği <code class="language-plaintext highlighter-rouge">chgrp</code> ile değiştirilebilir. Bu komut şu şekilde kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ chgrp new_group some_file
</code></pre></div></div>

<p>Yukarıdaki örnekte, <em>some_file</em>‘ın grup sahipliğini “new_group” olarak değiştirdik. <code class="language-plaintext highlighter-rouge">chgrp</code> işlemini gerçekleştirmek için dosyanın veya dizinin sahibi olmamız gerekir.</p>

<h2 id="i̇ş-kontrolü">İş Kontrolü</h2>

<p>Önceki derste Linux’un çok kullanıcılı bir işletim sistemi olmasının bazı sonuçlarına baktık. Bu dersimizde Linux’un çoklu görev yapısını ve komut satırı arayüzü ile nasıl kontrol edildiğini inceleyeceğiz.</p>

<p>Herhangi bir çoklu görev işletim sisteminde olduğu gibi, Linux’te birden fazla eşzamanlı proses yürütür. Aslında, tek bir işlemci çekirdeği aynı anda yalnızca bir proses yürütebilir, ancak Linux kernel’i işleme sırasını işlemciye vermeyi başarır ve her biri aynı anda çalışıyor gibi görünür.</p>

<p>Süreçleri kontrol etmek için kullanılan çeşitli komutlar vardır. Bunlar:</p>

<ul>
  <li><strong>ps</strong> - sistemde çalışan prosesleri listeleyin</li>
  <li><strong>kill</strong> - bir veya daha fazla prosese sinyal gönderin (genellikle prosesi “sonlandırmak” için)</li>
  <li><strong>jobs</strong> - kendi proseslerinizi listelemenin alternatif bir yolu</li>
  <li><strong>bg</strong> - prosesi arka plana koyar</li>
  <li><strong>fg</strong> - prosesi ön plana koyar</li>
</ul>

<h3 id="pratik-bir-örnek">Pratik Bir Örnek</h3>

<p>Bu konu oldukça belirsiz gibi görünse de çoğunlukla grafik kullanıcı arayüzü ile çalışan ortalama bir kullanıcı için oldukça pratik olabilir. Görünür olmasa da, grafik programların çoğu komut satırından başlatılabilir. İşte bir örnek: X Window sistemiyle birlikte sağlanan <strong>xload</strong> adında, sistem yükünü temsil eden bir grafik görüntüleyen küçük bir program var. Bu programı aşağıdakileri yazarak çalıştırabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ xload
</code></pre></div></div>

<p>Küçük <strong>xload</strong> penceresinin göründüğüne ve sistem yük grafiğini görüntülemeye başladığına dikkat edin. Xload’ın kullanılamadığı sistemlerde bunun yerine <strong>gedit</strong>‘i deneyin. Program başlatıldıktan sonra prompt un yeniden görünmediğine de dikkat edin. Shell, geri dönmeden önce programın bitmesini bekliyor. Eğer <strong>xload</strong> penceresini kapatırsak <strong>xload</strong> programı sonlandırılır ve prompt geri döner.</p>

<h3 id="programı-arka-plana-yerleştirmek">Programı Arka Plana Yerleştirmek</h3>

<p>Şimdi hayatı biraz daha kolaylaştırmak için xload programını tekrar başlatacağız ancak bu sefer prompt un geri dönmesi için onu arka plana koyacağız. Bunu yapmak için xload’ı şu şekilde çalıştırıyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ xload &amp;
[1] 1223
[me@linuxbox me]$
</code></pre></div></div>

<p>Bu durumda proses arka planda tutulduğu için prompt geri döndü.</p>

<p>Şimdi programı arka plana yerleştirmek için “&amp;” sembolünü kullanmayı unuttuğumuzu hayal edin. Hala umut var. Ctrl-z ile proses askıya alınacaktır. Bunu programın penceresinin donmuş olduğunu görerek doğrulayabiliriz. Proses hala mevcut ancak boşta. Proses i arka planda sürdürmek için <code class="language-plaintext highlighter-rouge">bg</code> komutunu yazın (background’un kısaltması). İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ xload
[2]+ Stopped xload

[me@linuxbox me]$ bg
[2]+ xload &amp;
</code></pre></div></div>

<h3 id="çalışan-proseslerin-listelenmesi">Çalışan Proseslerin Listelenmesi</h3>

<p>Artık arka planda bir prosesimiz olduğuna göre, başlattığımız proseslerin listesini görüntülemek faydalı olacaktır. Bunu yapmak için <code class="language-plaintext highlighter-rouge">jobs</code> komutunu veya daha güçlü olan <code class="language-plaintext highlighter-rouge">ps</code> komutunu kullanabiliriz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ jobs
[1]+ Running xload&amp;

[me@linuxbox me]$ ps
PID  TTY   TIME     CMD
1211 pts/4 00:00:00 bash
1246 pts/4 00:00:00 xload
1247 pts/4 00:00:00 ps

[me@linuxbox me]$
</code></pre></div></div>

<h3 id="prosesi-sonlandırmak">Proses’i Sonlandırmak</h3>

<p>Yanıt vermeyen bir programımız olduğunu varsayalım; ondan nasıl kurtuluruz? Elbette <code class="language-plaintext highlighter-rouge">kill</code> komutunu kullanıyoruz. Bunu xload’da deneyelim. Öncelikle sonlandırmak istediğimiz prosesi tanımlamamız gerekiyor. Bunu yapmak için <code class="language-plaintext highlighter-rouge">jobs</code> veya <code class="language-plaintext highlighter-rouge">ps</code> komutunu kullanabiliriz. Eğer <code class="language-plaintext highlighter-rouge">jobs</code> kullanırsak bir job numarası alacağız. <code class="language-plaintext highlighter-rouge">ps</code> ile bize bir proses id (PID) verilir. Bunu her iki şekilde de yapalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ xload &amp;
[1] 1292

[me@linuxbox me]$ jobs
[1]+ Running xload&amp;

[me@linuxbox me]$ kill %1

[me@linuxbox me]$ xload &amp;
[2] 1293
[1] Terminated xload

[me@linuxbox me]$ ps
PID  TTY   TIME     CMD
1280 pts/5 00:00:00 bash
1293 pts/5 00:00:00 xload
1294 pts/5 00:00:00 ps

[me@linuxbox me]$ kill 1293
[2]+ Terminated xload
[me@linuxbox me]$
</code></pre></div></div>

<h3 id="kill-hakkında-biraz-daha-fazla-bilgi">kill Hakkında Biraz Daha Fazla Bilgi</h3>

<p><code class="language-plaintext highlighter-rouge">kill</code> komutu prosesleri “sonlandırmak” için kullanılsa da asıl amacı proseslere sinyal göndermektir. Çoğu zaman sinyalin amacı proses’in sona erdiğini söylemektir ancak bundan daha fazlası vardır. Programlar (eğer doğru şekilde yazılmışlarsa) işletim sisteminden gelen sinyalleri dinler ve bunlara yanıt verirler; çoğu zaman zarif bir sonlandırma yöntemine izin verirler. Örneğin bir metin düzenleyici, kullanıcının oturumu kapattığını veya bilgisayarın kapandığını gösteren herhangi bir sinyali dinleyebilir. Bu sinyali aldığında, devam eden işi çıkmadan önce kaydedebilir. <code class="language-plaintext highlighter-rouge">kill</code> komutu proseslere çeşitli sinyaller gönderebilir. Şu komutla:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kill -l
</code></pre></div></div>

<p>desteklediği sinyallerin bir listesini yazdıracaktır. Birçoğu oldukça belirsizdir, ancak bazılarını bilmek faydalıdır:</p>

<table>
  <thead>
    <tr>
      <th>Sinyal #</th>
      <th>Ad</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>SIGHUP</td>
      <td>Kapatma sinyali. Programlar bu sinyali dinleyebilir ve ona göre hareket edebilir. Bu sinyal, terminali kapattığınızda terminalde çalışan proseslere gönderilir.</td>
    </tr>
    <tr>
      <td>2</td>
      <td>SIGINT</td>
      <td>Kesinti (interrupt) sinyali. Bu sinyal proseslere onları kesmeleri için verilir. Programlar bu sinyali işleyebilir ve ona göre hareket edebilir. Bu sinyali programın çalıştığı terminal penceresine Ctrl-c yazarak doğrudan verebiliriz.</td>
    </tr>
    <tr>
      <td>15</td>
      <td>SIGTERM</td>
      <td>Sonlandırma sinyali. Bu sinyal proseslere onları sonlandırmak için verilir. Yine programlar bu sinyali işleyebilir ve ona göre hareket edebilir. Bu, herhangi bir sinyal belirtilmemişse kill komutunun gönderdiği varsayılan sinyaldir.</td>
    </tr>
    <tr>
      <td>9</td>
      <td>SIGKILL</td>
      <td>Kill sinyali. Bu sinyal, proses’in Linux kerneli tarafından anında sonlandırılmasına neden olur. Programlar bu sinyali dinleyemez.</td>
    </tr>
  </tbody>
</table>

<p>Şimdi askıda kalan bir programımız olduğunu ve ondan kurtulmak istediğimizi varsayalım. İşte yaptığımız şey:</p>

<ul>
  <li>Sonlandırmak istediğimiz proses’in id’sini (PID) almak için <code class="language-plaintext highlighter-rouge">ps</code> komutunu kullanın.</li>
  <li>Bu PID için <code class="language-plaintext highlighter-rouge">kill</code> komutu verin.</li>
  <li>Proses sonlandırılmayı reddediyorsa (yani sinyali görmezden geliyorsa), sonlandırılana kadar giderek daha sert sinyaller gönderin.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ ps x | grep bad_program
PID  TTY   STAT TIME COMMAND
2931 pts/5 SN   0:00 bad_program

[me@linuxbox me]$ kill -SIGTERM 2931

[me@linuxbox me]$ kill -SIGKILL 2931
</code></pre></div></div>

<p>Yukarıdaki örnekte, tüm proseslerimizi (mevcut terminalden başlatılmayanlar dahil) listelemek için <code class="language-plaintext highlighter-rouge">ps</code> komutunu <code class="language-plaintext highlighter-rouge">x</code> seçeneğiyle birlikte kullandık. Ek olarak, yalnızca ilgilendiğimiz programı listelemek için ps komutunun çıktısını grep’e aktardık. Daha sonra, sorunlu programa SIGTERM sinyali vermek için <code class="language-plaintext highlighter-rouge">kill</code>‘i kullandık. Gerçek uygulamada kill tarafından gönderilen varsayılan sinyal SIGTERM olduğundan ve <code class="language-plaintext highlighter-rouge">kill</code> sinyal adı yerine sinyal numarasını da kullanabileceğinden, bunu aşağıdaki şekilde yapmak daha yaygındır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ kill 2931
</code></pre></div></div>

<p>Ardından, proses sonlandırılmazsa SIGKILL sinyaliyle zorlayın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[me@linuxbox me]$ kill -9 2931
</code></pre></div></div>

<p><strong>Kaynak:</strong> <a href="https://linuxcommand.org/">LinuxCommand.org</a></p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Neden komut satırını öğrenmeniz gerekiyor? Peki, size bir hikaye anlatayım. Yıllar önce çalıştığım yerde bir sorun yaşadık. Dosya sunucularımızdan birinde sürekli dolmaya başlayan bir ortak sürücü vardı. Bu eski işletim sisteminin kullanıcı kotalarını desteklemediğinden bahsetmeyeceğim; bu başka bir hikaye. Ancak sunucu dolmaya devam etti ve insanların çalışmasını engelledi. Yazılım mühendislerimizden biri, tüm kullanıcı dizinlerini tarayacak, kullandıkları alanı toplayacak ve sonuçların listesini yapacak bir C++ programı yazmak için birkaç saat harcadı. İşteyken eski işletim sistemini kullanmak zorunda kaldığım için Linux benzeri bir komut satırı ortamı kurdum. Sorunu duyduğumda bu görevi şu tek satırla yapabileceğimi fark ettim:]]></summary></entry><entry><title type="html">Ruby’ye Başlarken</title><link href="http://localhost:4000/2024/04/30/ruby-ye-baslarken.html" rel="alternate" type="text/html" title="Ruby’ye Başlarken" /><published>2024-04-30T00:00:00+03:00</published><updated>2024-04-30T00:00:00+03:00</updated><id>http://localhost:4000/2024/04/30/ruby-ye-baslarken</id><content type="html" xml:base="http://localhost:4000/2024/04/30/ruby-ye-baslarken.html"><![CDATA[<p>Bu kitapta Ruby dili hakkında konuşarak çok zaman harcayacağız. Bunu yapmadan önce Ruby’yi bilgisayarınıza kurup çalıştırabildiğinizden emin olmak istiyoruz. Bu şekilde örnek kodları deneyebilir ve okurken kendi başınıza denemeler yapabilirsiniz. Ruby’yi öğrenmek istiyorsanız, okurken kod yazma alışkanlığını da edinmelisiniz.</p>

<h2 id="rubyyi-yüklemek">Ruby’yi Yüklemek</h2>

<p>İşletim sisteminizde Ruby’nin zaten kurulu olması ihtimali yüksektir. Komut istemine <code class="language-plaintext highlighter-rouge">ruby --version</code> yazmayı deneyin; hoş bir sürpriz yaşayabilirsiniz. Ancak büyük olasılıkla Ruby sürümünün güncel olmadığını göreceksiniz. Örneğin, bu yazının yazıldığı sırada macOS Ruby 2.6.10 ile birlikte gelir.</p>

<p>Bu kitaptaki örnekler Ruby 3.2’de yazılmıştır. Kodun çoğu Ruby’nin eski sürümlerinde çalışsa da, performans ve güvenlik nedeniyle en güncel sürümü kullanmaya çalışmalısınız.</p>

<p>Ruby’yi çeşitli farklı şekillerde kurabilirsiniz, böylece genel kurulum talimatlarını vermek, biraz kendi maceranı seç hikayesine dönüşür. Bu kitaptaki örneklerin çoğu, Linux tarzı komutlara yanıt veren Linux veya Unix tarzı sistem kullandığınızı varsaymaktadır. Buna tüm Linux dağıtımları, macOS, Windows Subsystem for Linux (WSL) çalıştıran Windows sistemleri ve çoğu Docker container’ının yanı sıra Replit gibi bulut tabanlı geliştirme ortamları dahildir.</p>

<p>Bununla birlikte, Ruby Windows’ta çalışır. Windows’ta Ruby kurulumunu yönetme süreci farklıdır ve bunu bu bölümün ilerleyen kısımlarında tüm ayrıntılarıyla ele alacağız.</p>

<p>Lütfen Ruby kurulumuyla ilgili araçların sık sık değiştiğini ve bazı özel talimatların güncelliğini yitirmiş olabileceğini veya daha yeni araçlarla değiştirilebileceğini unutmayın.</p>

<p>Herhangi bir sebepten dolayı bilgisayarınıza herhangi bir şey yüklemek istemiyorsanız Replit veya GitHub Codespaces gibi bulut tabanlı geliştirme ortamlarından yararlanabilirsiniz. Bu ortamlar, kodunuzu bir tarayıcıda yazmanıza ve bulut tabanlı bir sanal makinede çalıştırmanıza olanak tanır.</p>

<h2 id="rubyyi-rbenv-version-manager-ile-yüklemek">Ruby’yi rbenv Version Manager ile Yüklemek</h2>

<p>Ruby kurulumumuzu kolaylaştırmak için, aynı makineye birden fazla Ruby sürümü kurmanıza ve bunlar arasında geçiş yapmanıza olanak tanıyan bir araç olan sürüm yöneticisi kullanacağız. Ruby kurulumunuzu gerçekleştirmek için sürüm yöneticisi kullanmanın birçok nedeni vardır. Ruby’nin birden çok sürümü arasında kolayca geçiş yapabilmek, size farklı zamanlarda yazılmış olabilecek birden çok projeyle çalışma esnekliği sağlar. Ayrıca sürüm yöneticileri kolay kurulum için oluşturulmuştur, bu nedenle birden fazla Ruby sürümünü bir sürüm yöneticisiyle kurmak, tek bir sürümü tek başına kurmaktan daha kolaydır. Ruby’nin yalnızca bir sürümünü indirmekle ilgileniyorsanız, sistem bazında talimatları <a href="https://www.ruby-lang.org/en/documentation/installation">https://www.ruby-lang.org/en/documentation/installation</a> adresinde bulabilirsiniz.</p>

<p>Bu kitapta kullanacağımız aracın adı rbenv’dir. Rbenv tek Ruby sürüm yöneticisi olmasa da muhtemelen bugünlerde en sık kullanılanıdır. Yaygın olarak kullanılan diğer sürüm yöneticileri RVM ve chruby’dir. Birden fazla dil için sürüm yönetimi kullanıyorsanız, farklı dillerin sürüm yöneticilerini birleştiren asdf adlı bir projeye bakmak isteyebilirsiniz ve Ruby’de hızla daha popüler hale geliyor.</p>

<p>Rbenv’i, rbenv-installer programı aracılığıyla kuracağız. Başka birinin shell script’ini çalıştırmak sizi tedirgin ediyorsa, script’i çalıştırmadan önce <a href="https://github.com/rbenv/rbenv-installer/blob/main/bin/rbenv-installer">https://github.com/rbenv/rbenv-installer/blob/main/bin/rbenv-installer</a> adresinden inceleyebilirsiniz.</p>

<p>Bir terminalden bu komutun tamamını tek bir satıra girin.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash
</code></pre></div></div>

<p>Curl, URL’lere erişmek ve dönüş değeriyle faydalı bir şeyler yapmak için kullanılan komut satırı aracıdır - bu durumda, rbenv GitHub reposundan shell script’ini alır ve bunu yürütecek olan bash kabuğuna iletir.</p>

<p>Bu script dosyası, sisteminiz için uygun paket yöneticisini kullanarak rbenv’i kuracak ve ayrıca farklı Ruby sürümlerinin indirilmesini ve kurulumunu yönetecek ruby-build adlı bir helper programı da kuracaktır.</p>

<p>Kurulum komutu çok fazla çıktı üretebilir; özellikle de Homebrew paket yöneticisini kullanan bir macOS sistemindeyseniz. Mac’te aşağıdaki şekilde bitmelidir; WSL altındaki bir Windows kullanıcısı farklı bir şey görebilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>All done! Note that this installer does NOT edit your shell configuration files: 1. Run `rbenv init' to view instructions on how to configure rbenv for your shell. 2. Launch a new terminal window after editing shell configuration files.
</code></pre></div></div>

<p>Talimatları takip ederek <code class="language-plaintext highlighter-rouge">rbenv init</code>‘i çalıştırın. Bu, zshell çalıştıran bir Mac’teki çıktıdır (talimatlarınız farklı olabilir):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rbenv init
# Load rbenv automatically by appending 
# the following to ~/.zshrc:
eval "$(rbenv init - zsh)"
</code></pre></div></div>

<p>Kurulumunuz ne olursa olsun, bu talimatta almanız gerekenler:</p>

<ul>
  <li>Güncellemeniz gereken shell yapılandırmasını içeren dosya</li>
  <li>Dosyanın sonuna koymanız gereken metin</li>
</ul>

<p>Önerilen metin satırını yapılandırma dosyanızın sonuna koymanız ve yeni bir terminal penceresi açmanız gerekir; değişiklik yalnızca bir pencere yüklendiğinde etkili olur, dolayısıyla rbenv’i başlatmanın en kolay yolu yeni bir terminal penceresi açmaktır.</p>

<p>Şimdi belirli bir Ruby sürümünü yükleyelim.</p>

<p><strong>Ruby’leri rbenv ile kurmak</strong></p>

<p>Rbenv, <code class="language-plaintext highlighter-rouge">rbenv install -l</code> komutuyla yüklemek isteyeceğiniz Ruby sürümlerinin bir listesini görmenizi sağlar. İşte bugün itibariyle liste:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rbenv install -l 
2.7.7
3.0.5
3.1.3
3.2.0
jruby-9.4.0.0
mruby-3.1.0
picoruby-3.0.0
rbx-5.0
truffleruby-22.3.0
truffleruby+graalvm-22.3.0
Only latest stable releases for each Ruby implementation are shown.
Use 'rbenv install --list-all / -L' to show all local versions.
</code></pre></div></div>

<p>Bu liste, çeşitli Ruby uygulamalarının en güncel yama sürümüdür. Ana Ruby uygulama sürümü 2.7, 3.0, 3.1 ve 3.2 için Ruby sürümlerini görebilirsiniz. (Farklı Ruby uygulamalarından bahsederken, ana versiyona bazen CRuby, bazen de “Matz’ın Ruby Yorumlayıcısı” için MRI denir.) Burada fazla bahsetmeyeceğimiz başka versiyonlar da var. JRuby, Java Sanal Makinesi üzerinde çalışan bir Ruby sürümüdür; mruby ise gömülü donanım üzerinde çalışmaya yönelik özel sınırlı bir Ruby yapısıdır. Rbx, Ruby’yi Ruby’de uygulamaya yönelik artık kullanılmayan bir proje olan Rubinius’tur ve TruffleRuby, yüksek performansa odaklanan dilin bir uygulamasıdır.</p>

<p>Şu an ilgi alanımız <code class="language-plaintext highlighter-rouge">rbenv install 3.2.0</code> komutuyla kurabileceğimiz Ruby 3.2.0. Listede Ruby’nin en güncel sürümünü göremiyorsanız ve rbenv’i daha önce yüklediyseniz, listenizde daha yeni Ruby sürümleri almak için ruby-build’i nasıl güncelleyeceğinize ilişkin talimatlar alabilirsiniz. Rbenv komutlarının hiçbirinin superuser erişimine sahip olmamızı veya sudo kullanmamızı gerektirmediğini unutmayın. Rbenv de dahil olmak üzere Ruby sürüm yöneticilerinin keyifleri yanlarından biri, her şeyi ana dizininizin içinde yapmalarıdır, yeni Ruby sürümlerini yüklemek veya kullanmak için herhangi bir özel sistem ayrıcalığına ihtiyacınız yoktur.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rbenv install 3.2.0
To follow progress, use
'tail -f &lt;REDACTED&gt;'
or pass --verbose
Installing openssl-3.0.7...
Installed openssl-3.0.7 to /Users/noel/.rbenv/versions/3.2.0
Installing ruby-3.2.0...
ruby-build: using readline from homebrew
ruby-build: using gmp from homebrew
Installed ruby-3.2.0 to /Users/noel/.rbenv/versions/3.2.0
</code></pre></div></div>

<p>Çıktınız, tam sürüm numarasına ve Ruby sürümünü yeniden kurup kurmadığınıza bağlı olarak biraz farklı olabilir.</p>

<p>Örneğin Ruby sürümünün rbenv sürümleriyle birlikte kurulduğunu doğrulayabiliriz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rbenv versions
* system
3.2.0
</code></pre></div></div>

<p>Buradaki system, eğer böyle bir şey varsa işletim sistemi için önceden tanımlanmış Ruby’dir ve yıldız işareti o anda hangi sürümün aktif olduğunu gösterir.</p>

<p>Önemli bir adım: rbenv aracılığıyla yeni bir Ruby kurduktan sonra <code class="language-plaintext highlighter-rouge">rbenv rehash</code> komutunu çalıştırmalısınız. Bu komut hiçbir çıktı üretmez ancak rbenv’in yeni yüklenen Ruby’yi kullanmasını sağlar.</p>

<p>Şu anda Ruby system hala aktif. Bunu değiştirelim.</p>

<p><strong>Ruby’leri rbenv ile değiştirmek</strong></p>

<p>İşte bu noktada getirisini görmeye başlıyoruz. Farklı Ruby sürümleri yüklendikten ve <code class="language-plaintext highlighter-rouge">rbenv rehash</code> çağrıldıktan sonra, Rbenv bize, kullandığımız Ruby sürümünü değiştirmemiz için birden fazla yol sağlar.</p>

<p><code class="language-plaintext highlighter-rouge">rbenv local &lt;version&gt;</code> komutu, bulunduğunuz dizinin Ruby sürümünü değiştirir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ruby --version
ruby 2.6.10p210 (2022-04-12 revision 67958) [universal.arm64e-darwin22]
$ rbenv local 3.2.0
$ ruby --version
ruby 3.2.0 (2022-12-25 revision a528908271) [arm64-darwin22]
</code></pre></div></div>

<p>Dizinin bu ayarı, dizinden çıkıp geri dönseniz bile korunur. (Değişikliğin geçerli oturumun ötesinde devam etmesini istemiyorsanız, <code class="language-plaintext highlighter-rouge">rbenv local</code> yerine <code class="language-plaintext highlighter-rouge">rbenv shell &lt;version&gt;</code> kullanabilirsiniz).</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ cd ..
$ cd test
$ ruby --version
ruby 3.2.0 (2022-12-25 revision a528908271) [arm64-darwin22]
</code></pre></div></div>

<p>Rbenv bunu dizine <code class="language-plaintext highlighter-rouge">.ruby-version</code> adlı bir dosya koyarak başarır, bu sadece o dizin için ayarladığınız Ruby’nin sürüm numarasını içerir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>% cat .ruby-version
3.2.0
</code></pre></div></div>

<p>Bu dosya aynı zamanda tersten de çalışır; eğer rbenv’i yüklediyseniz ve <code class="language-plaintext highlighter-rouge">.ruby-version</code> dosyasını içeren bir dizine geçerseniz, rbenv kuruluysa otomatik olarak o Ruby sürümüne geçecektir veya dizinin kaldırılmış bir Ruby beklediği konusunda sizi uyarabilir. Pek çok Ruby projesi, Ruby sürümünü belirtmek için <code class="language-plaintext highlighter-rouge">.ruby-version</code> dosyası kullanır ve bu dosya, tüm Ruby sürüm yöneticileri tarafından dikkate alınır.</p>

<p>Kendileri belirtmeyen dizinler için varsayılan bir Ruby sürümü ayarlamak istiyorsanız bunu <code class="language-plaintext highlighter-rouge">rbenv global &lt;version&gt;</code> ile yapabilirsiniz.</p>

<p>Bu, Ruby’yi kurmayı beklediğinizden daha fazla iş gerektirebilir. Eğer yapmak istediğiniz Ruby’nin tek bir versiyonunu kullanmak olsaydı, buna katılırdık. Ancak burada gerçekten yaptığınız şey, kendinize inanılmaz miktarda esneklik kazandırmaktır. Belki gelecekte <code class="language-plaintext highlighter-rouge">.ruby-version</code> dosyasına göre Ruby 2.7.5 kullanan bir proje ortaya çıkabilir. Bu sorun değil; yalnızca <code class="language-plaintext highlighter-rouge">rbenv install 2.7.5</code>‘i kullanın; <code class="language-plaintext highlighter-rouge">rbenv rehash</code> ve rbenv, sürümü <code class="language-plaintext highlighter-rouge">.ruby-version</code> dosyasından otomatik olarak alacaktır.</p>

<p><strong>rbenv aslında ne yapıyor?</strong></p>

<p>Rbenv, dinamik davranışını normal terminal ortamınızda mümkün olduğunca az değişiklik yaparak sağlamaya çalışır.</p>

<p>Unix terminali, bir komut yazdığınızda yürütülebilir programlar için hangi dizinlere bakacağını belirlemek amacıyla PATH adı verilen genel bir ortam değişkenini kullanır. Terminalinizin yapılandırma dosyasına bakarsanız, muhtemelen PATH değişkeninin değiştirildiğini göreceksiniz.</p>

<p>Terminal kurulumunuzun bir parçası olarak <code class="language-plaintext highlighter-rouge">rbenv init</code> komutu yürütüldüğünde, PATH’inizin önüne bir dizin ekler, böylece işletim sisteminiz başka bir yere bakmadan önce rbenv dizinine bakar. Bu dizinde rbenv’in shim olarak adlandırdığı bir dizi şey vardır; Tüm Ruby sürümlerinizdeki tüm yürütülebilir komutlarla eşleşen küçük programlar (Yeni bir Ruby kurduktan sonra <code class="language-plaintext highlighter-rouge">rbenv rehash</code>‘ı çalıştırmanızın nedeni bu dizini yenilemektir). Ruby veya (birazdan göreceğiniz gibi) <code class="language-plaintext highlighter-rouge">irb</code> gibi bir Ruby komutunu çağırdığınızda, ilk önce rbenv shim ile karşılaşılır ve genellikle bir <code class="language-plaintext highlighter-rouge">.ruby-version</code> dosyasının varlığına bağlı olarak hangi Ruby’nin aktif olduğunu dinamik olarak seçer. Daha sonra komut, o geçerli sürümdeki gerçek yürütülebilir programa aktarılır. Bu gerçek sürümleri görebilirsiniz, ~/.rbenv/versions adresindeki ana dizininizde bulunurlar.</p>

<h2 id="rubyyi-çalıştırmak">Ruby’yi Çalıştırmak</h2>

<p>Artık Ruby kurulduğuna göre muhtemelen bazı programları çalıştırmak isteyeceksiniz. Derlenmiş dillerden farklı olarak Ruby’yi çalıştırmanın iki yolu vardır; kodu etkileşimli olarak yazabilir veya program dosyaları oluşturup çalıştırabilirsiniz. Kodu etkileşimli olarak yazmak, dili denemenin harika bir yoludur, ancak daha karmaşık kodlar veya birden fazla çalıştırmak isteyeceğiniz kodlar için program dosyaları oluşturmanız ve bunları çalıştırmanız gerekir. Ancak daha ileri gitmeden önce Ruby’nin kurulu olup olmadığını test edelim. Yeni bir komut istemi açın ve şunu yazın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ruby --version
ruby 3.2.0 (2022-12-25 revision a528908271) [arm64-darwin22]
</code></pre></div></div>

<p>Teknik olarak, yalnızca shell komut istemine ruby yazarak Ruby’yi etkileşimli olarak çalıştırabilirsiniz. Yanıt olarak boş bir satır alacaksınız ve oraya Ruby kodunu yazabilirsiniz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ruby
puts "Hello, world!" 
^D
Hello, world!
</code></pre></div></div>

<p>Bu örnekte tek bir Ruby satırı yazdık. Bu satır iki bölümden oluşuyor. İlk bölüm, bir metodun adıdır. Metot önceden tanımlanmış bir kod yığınıdır. Bu durumda <code class="language-plaintext highlighter-rouge">puts</code> metodu Ruby tarafından bizim için tanımlanan çeşitli metotlardan biridir. İkinci bölüm olan “Hello, world!”, string (dize) adı verilen çift tırnakla çevrelenmiş metindir. İkisini birleştiren Ruby kodu, “Hello, world!” parametresiyle <code class="language-plaintext highlighter-rouge">puts</code> metodunu çağırır. <code class="language-plaintext highlighter-rouge">puts</code> metodu daha sonra bu parametreyi terminale geri gönderir; puts, “outPUT String”in kısaltmasıdır.</p>

<p>Bir sonraki satıra dosya sonu karakterini (sistemimizde Ctrl+D) yazarak programdan çıktık ve yazdıklarımızın değerlendirilmesine neden olduk. Ruby’yi bu şekilde kullanmak işe yarar, ancak yanıtları yalnızca açıkça yazdırırsanız gösterir. Ayrıca, bir yazım hatası yaparsanız can sıkıcı olabilir ve yazarken neler olduğunu göremezsiniz.</p>

<p>Neyse ki Ruby ile etkileşim kurmanın daha iyi bir yolu var.</p>

<p>Etkileşimli Ruby veya irb, Ruby’yi etkileşimli olarak çalıştırmak için tercih edilen araçtır. Irb, komut satırı geçmişi, satır düzenleme yetenekleri ve iş kontrolü ile eksiksiz bir Ruby kabuğudur. irb’yi komut satırından çalıştırabilirsiniz. Başladıktan sonra Ruby kodunu yazın. Her ifadeyi değerlendirirken size değerini gösterecektir. Exit yazarak veya Ctrl+D ile irb oturumundan çıkın.</p>

<p>İşte örnek bir oturum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ irb
irb(main):001:1* def sum(n1, n2)
irb(main):002:1* n1 + n2
irb(main):003:0&gt; end
=&gt; :sum
irb(main):004:0&gt; sum(3, 4)
=&gt; 7
irb(main):005:0&gt; sum("cat", "dog") =&gt; "catdog"
irb(main):006:0&gt; exit
</code></pre></div></div>

<p>Bu oturumun ilk üç satırında <code class="language-plaintext highlighter-rouge">sum</code> adında bir metot tanımlıyoruz. Bu metodu tanımlama eylemi, metodun adıyla eşleşen bir Ruby sembolü olan <code class="language-plaintext highlighter-rouge">:sum</code> değerini döndürür. Semboller ve metot adları hakkında daha sonra daha fazla konuşacağız. 4. satırda, önce 3 ve 4 parametreleriyle, ardından 5. satırda “cat” ve “dog” parametreleriyle metodu çağırıyoruz. Ruby’de dizeleri eklemek onları birleştirir, böylece “catdog” dizesi döndürülür. Daha sonra 6. satırda çıkıyoruz.</p>

<p>Bunu Ruby 3.1 veya üzeri sürümlerde denerseniz, irb’nin değişken adlarını, komutları ve renk kodlarını otomatik olarak tamamlamaya çalıştığını fark edeceksiniz; bunların hiçbirinin bir kitapta gösterilmesi kolay değildir.</p>

<p>irb’ye aşina olmanızı öneririz; Ruby kavramlarını keşfetmenin ve kodunuzda hata ayıklamanın harika bir yoludur ve Ruby deneyiminizi daha eğlenceli hale getirecektir.</p>

<p><strong>Docker’a ne dersiniz?</strong></p>

<p>Ruby’yi daha büyük bir projede veya daha büyük bir ekiple kullanıyorsanız Docker’ın geliştirme ortamınızın bir parçası olma ihtimali yüksektir. Docker, container tanımlamanıza ve çalıştırmanıza olanak tanıyan bir araçtır. Container, kod çalıştırmak için gereken tüm bağımlılıkları paketlemenin bir yoludur; etkili bir şekilde bilgisayarınızın içindeki sanal bir işletim sistemidir. Docker’ı kullanarak, hangi işletim sistemini çalıştırıyor olursanız olun bir Linux ortamını simüle edebilirsiniz.</p>

<p>Docker’ın tam bir açıklaması bu kitabın kapsamı dışındadır, ancak genel olarak Docker’a zaten aşina iseniz, Docker’ın önceden yüklenmiş farklı Ruby sürümleriyle image’leri tuttuğunu ve Ruby ile her zaman en son yayımlanan sürüme ulaşabileceğinizi belirtmekte fayda var. <code class="language-plaintext highlighter-rouge">ruby:latest</code> ile en son yayımlanan sürüme ulaşabilirsiniz ve <code class="language-plaintext highlighter-rouge">docker run -it ruby irb</code> ile doğrudan dockerize edilmiş irb komut istemine gidebilirsiniz. Harici Ruby dosyalarını Docker container’ında çalıştırmak da mümkündür ancak biraz daha Docker bilgisi gerektirir.</p>

<h2 id="ruby-programları-oluşturmak">Ruby Programları Oluşturmak</h2>

<p>Ruby programları yazmanın en yaygın yolu Ruby kodunu bir veya daha fazla metin dosyasına yerleştirmektir. Bu dosyaları oluşturmak için bir metin düzenleyici veya Entegre Geliştirme Ortamı (IDE) kullanacaksınız; Ruby desteğine sahip Visual Studio Code, Vim, Sublime Text ve RubyMine gibi birçok popüler editör vardır. Daha sonra dosyaları editörün içinden veya komut satırından çalıştıracaksınız. Her iki teknik de faydalıdır; tek dosyalı programları editörün içinden, daha karmaşık programları ise komut satırından çalıştırabilirsiniz.</p>

<p>Basit bir Ruby programı oluşturup çalıştıralım. Terminal penceresini açın ve bir yerde boş bir dizin oluşturun, belki buna pickaxe diyebilirsiniz.</p>

<p>Ardından, seçtiğiniz editörü kullanarak aşağıdaki metni içeren <em>myprog.rb</em> dosyasını oluşturun.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>puts "Hello, Ruby Programmer"
puts "It is now #{Time.now}"
</code></pre></div></div>

<p>İkinci dizenin parantezlerin değil süslü parantezlerin arasında Time.now metnini içerdiğini unutmayın.</p>

<p>Bir Ruby programını, Python gibi başka bir script dilindeki herhangi bir shell script’i gibi dosyadan çalıştırabilirsiniz. Basitçe Ruby yorumlayıcısını çalıştırın ve ona parametre olarak script dosyası adını verin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ruby myprog.rb
Hello, Ruby Programmer
It is now 2022-12-25 12:29:56 -0600
</code></pre></div></div>

<p>Unix sistemlerinde program dosyasının ilk satırı olarak “shebang” gösterimini kullanabilirsiniz. Sisteminiz destekliyorsa, <code class="language-plaintext highlighter-rouge">#!/usr/bin/env ruby</code> komutunu kullanarak Ruby yolunu sabit kodlamaktan kaçınabilirsiniz; bu, yolunuzu Ruby için arayacak ve daha sonra çalıştıracaktır.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#!/usr/bin/env ruby
puts "Hello, Ruby Programmer"
puts "It is now #{Time.now}"
</code></pre></div></div>

<p>Bu kaynak dosyayı yürütülebilir hale getirirseniz (örneğin, <code class="language-plaintext highlighter-rouge">chmod +x myprog.rb</code> kullanarak), Unix dosyayı bir program olarak çalıştırmanıza olanak tanır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ./myprog.rb
Hello, Ruby Programmer
It is now 2022-12-25 12:29:56 -0600
</code></pre></div></div>

<p>Dosya ilişkilendirmelerini kullanarak Microsoft Windows altında benzer bir şey yapabilirsiniz ve Ruby GUI uygulamalarını Windows Gezgini’nde adlarına çift tıklayarak çalıştırabilirsiniz.</p>

<h2 id="ruby-hakkında-daha-fazla-bilgi-almak">Ruby Hakkında Daha Fazla Bilgi Almak</h2>

<p>Ruby kütüphanelerinin hacmi büyüdükçe hepsini tek bir kitapta belgelemek imkansız hale geldi; Ruby ile birlikte gelen standart kütüphane artık 9.000’den fazla metot içeriyor.</p>

<p>Resmi Ruby belgeleri <a href="https://docs.ruby-lang.org">https://docs.ruby-lang.org</a> adresindedir. Ruby 3.2’de, siz yazdıkça irb size standart metot adlarının dokümantasyonunu verecektir.</p>

<p>Bu belgelerin çoğu, RubyDoc adlı bir araç kullanılarak kaynak kodundaki yorumlardan oluşturulmuştur. Buna “Ruby’yi Belgelemek” bölümünde bakacağız. <a href="https://www.rubydoc.info">https://www.rubydoc.info</a> adresindeki RubyDoc sitesi, RubyDoc kullanan Ruby projelerine yönelik dökümantasyonları içerir.</p>

<p>Ruby dünyasındaki third-party kütüphanelere gem denir ve Ruby Gems resmi listesi <a href="https://rubygems.org">https://rubygems.org</a> adresindedir. “Ruby Gems” bölümünde gemler hakkında daha fazla konuşacağız.</p>

<p>Ayrıca Ruby core dokümantasyonu için ri adı verilen bir komut satırı aracı da vardır. Bir sınıfın dokümantasyonunu bulmak için <code class="language-plaintext highlighter-rouge">ri &lt;sınıf adı&gt;</code> yazın. Örneğin String sınıfına ait özet bilgilerin başlangıcı aşağıdadır. Hiçbir parametre olmadan <code class="language-plaintext highlighter-rouge">ri</code> yazarsanız, sizden sınıf isteyen bir prompt alırsınız.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>= String &lt; Object
------------------------------------------------------------------------
= Includes:
Comparable (from ruby core)
(from ruby core)
------------------------------------------------------------------------
A String object has an arbitrary sequence of bytes, typically
representing text or binary data. A String object may be created using
String::new or as literals.
String objects differ from Symbol objects in that Symbol objects are
designed to be used as identifiers, instead of text or data.
</code></pre></div></div>

<p>String’in tüm metotlarını listelemeye devam ediyor.</p>

<p>Ayrıca bir metot adını da deneyebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ ri strip
= .strip

(from ruby core)
=== Implementation from String
------------------------------------------------------------------------
  strip -&gt; new_string

------------------------------------------------------------------------

Returns a copy of the receiver with leading and trailing whitespace
removed; see {Whitespace in
Strings}[rdoc-ref:String@Whitespace+in+Strings]:

  whitespace = "\x00\t\n\v\f\r "
  s = whitespace + 'abc' + whitespace
  s       # =&gt; "\u0000\t\n\v\f\r abc\u0000\t\n\v\f\r "
  s.strip # =&gt; "abc"

Related: String#lstrip, String#rstrip.
</code></pre></div></div>

<p>Daha sonra <code class="language-plaintext highlighter-rouge">q</code> yazarak listeden çıkabilirsiniz.</p>

<h2 id="sıradaki">Sıradaki</h2>

<p>Artık Ruby’yi kullanmaya başladığınıza göre Ruby’nin nasıl çalıştığını ve dilin temel özelliklerini öğrenmenin zamanı geldi. Ancak öncelikle dilin öne çıkan noktalarına hızlı bir genel bakış yapacağız.</p>

<p><strong>Kaynak:</strong> “Programming Ruby 3.2: The Pragmatic Programmers’ Guide”, Noel Rappin with Dave Thomas</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Bu kitapta Ruby dili hakkında konuşarak çok zaman harcayacağız. Bunu yapmadan önce Ruby’yi bilgisayarınıza kurup çalıştırabildiğinizden emin olmak istiyoruz. Bu şekilde örnek kodları deneyebilir ve okurken kendi başınıza denemeler yapabilirsiniz. Ruby’yi öğrenmek istiyorsanız, okurken kod yazma alışkanlığını da edinmelisiniz.]]></summary></entry><entry><title type="html">Gatsby’nin Temelleri</title><link href="http://localhost:4000/2024/03/28/gatsby-nin-temelleri.html" rel="alternate" type="text/html" title="Gatsby’nin Temelleri" /><published>2024-03-28T00:00:00+03:00</published><updated>2024-03-28T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/28/gatsby-nin-temelleri</id><content type="html" xml:base="http://localhost:4000/2024/03/28/gatsby-nin-temelleri.html"><![CDATA[<p>Gatsby, React’ı temel alan, statik siteler oluşturmaya yönelik açık kaynaklı bir framework’tür. Genellikle static site generator (SSG) olarak anılan Gatsby, Gridsome gibi diğer SSG’lerin ve statik site oluşturmayı kolaylaştıran Next.js gibi JavaScript framework’lerinin yanı sıra Jamstack teknoloji kategorisinin bir parçasıdır. Gatsby performansa, ölçeklenebilirliğe ve güvenliğe özellikle önem veriyor. Pluginler, temalar, starter’lar ve katkıda bulunulan diğer projelerden oluşan zengin ve büyüyen bir ekosisteme sahip.</p>

<p>Bu ilk bölümde Gatsby’yi tanıyacağız. Gatsby’nin yapı taşları sayfalar ve bileşenler (component) olmasına rağmen, GraphQL ve source pluginlerinden oluşan dahili veri katmanı, veri kaynakları ile statik site oluşturma sırasında derlenen sayfalar ve bileşenler arasında köprü görevi görür. Gatsby, React üzerine inşa edilmiştir, bu nedenle Gatsby geliştiricilerinin anlaması için gerekli olan bazı JavaScript ve React kavramlarına da göz atacağız. Son olarak, Gatsby’nin geliştirici araçlarına aşina olmanızı sağlayacak ilk Gatsby “Hello World” sitemizi oluşturacağız.</p>

<h2 id="gatsby-nedir">Gatsby Nedir?</h2>

<p>Kısacası Gatsby, web siteleri ve web uygulamaları oluşturmak için React’ı temel alan ücretsiz ve açık kaynaklı bir framework’tür. Ancak yaratıcısı Kyle Mathews, Gatsby’yi karmaşık ve sağlam bir “web compiler” olarak tanımlıyor; veri alma ve işleme, bileşenleri kullanarak sayfa oluşturma, sayfalar arasında bağlantı kurma ve son olarak bileşenlerin çalışan bir statik site halinde derlenmesiyle ilgilenen yapı taşlarından oluşan bir koleksiyon.</p>

<p>Bu bölümde, ana bileşenlerini anlamamıza yardımcı olması için Gatsby’nin mimarisine kuşbakışı bakacağız. Ayrıca Gatsby’nin en önemli üç bileşenini daha yakından inceleyeceğiz: temel yapı taşları (sayfalar ve bileşenler), Gatsby veri katmanı (GraphQL ve source pluginleri) ve Gatsby ekosistemi (plugin ve temalar).</p>

<h3 id="gatsby-sayfaları-ve-bileşenleri">Gatsby Sayfaları ve Bileşenleri</h3>

<p>Gatsby, web siteleri oluşturmak için React bileşenlerinden yararlanıyor. Başka bir deyişle Gatsby’deki her şey bileşenler kullanılarak oluşturulmuştur. Bunu daha derinlemesine anlamak için tipik Gatsby proje yapısına bir göz atalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/
|-- /.cache
|-- /plugins
|-- /public
|-- /src
    |-- /pages
    | |-- index.js
    |-- /templates
    | |-- article.js
    |-- html.js
|-- /static
|-- gatsby-config.js
|-- gatsby-node.js
|-- gatsby-ssr.js
|-- gatsby-browser.js
</code></pre></div></div>

<p>Gatsby’nin bileşen tabanlı mimarisini göstermek amacıyla, bu Gatsby projesinin, article template’ine (article.js) bağlı farklı sayfalara bağlantı veren bir ana sayfadan (index.js) oluştuğunu hayal edelim. Article template, makalelerin tam metnini tutarlı bir şablona dönüştürür. Bu örneği daha da basitleştirmek için, makalelerin yerel dosya sisteminden ziyade harici bir veritabanından geldiğini varsayalım.</p>

<p>Gatsby’nin dört ana bileşeni vardır; bunlardan ilk üçünü bu örnek proje yapısında kolayca tanımlayabiliriz:</p>

<p><strong>Sayfa Bileşenleri</strong></p>

<p>Sayfa bileşenleri tam teşekküllü Gatsby sayfalarına dönüşür. src/pages altına yerleştirilen herhangi bir bileşen, adının önerdiği yola otomatik olarak atanan bir sayfa haline gelir. Örneğin, React bileşeni içeren bir contact.js veya contact.jsx dosyasını src/pages’e yerleştirirseniz, bu dosya en sonunda <example.com>/contact/ adresinde mevcut olacaktır. src/pages içindeki bir dosyanın sayfa haline gelmesi için bir dizeye veya React bileşenine çözümlenmesi gerekir.</example.com></p>

<p><strong>Sayfa Template Bileşenleri</strong></p>

<p>Statik sayfalar için sayfa bileşenleri nispeten iyi çalışır. Ancak article template gibi template olan sayfalar için, her makaleyi buna göre oluşturacağımız yeniden kullanılabilir ortak bir metine ihtiyacımız var. Sayfa template’leri src/templates dizini altında bulunur.</p>

<p><strong>HTML bileşeni</strong></p>

<p>Yalnızca tek bir HTML bileşeni vardır: src/html.js. Bu dosya, Gatsby’nin kontrol ettiği sayfa alanı dışındaki her şeyden sorumludur. Örneğin, &lt;head&gt; öğesinde bulunan meta verileri değiştirebilir ve başka markup’lar ekleyebilirsiniz. Gatsby’nin ayrıca yedek olarak kullandığı varsayılan bir html.js dosyası olduğundan, bu dosyayı özelleştirmek isteğe bağlıdır.</p>

<p><strong>Sayfa Dışı Bileşenler</strong></p>

<p>Bunlar tipik sayfa içi React bileşenleridir. Bunlar, bir sayfanın header gibi daha büyük bir sayfa bileşenine gömülü olan ve hiyerarşi oluşturan bölümleridir. Bölümün ilerleyen kısımlarında göreceğimiz gibi, layout bileşeni gibi daha büyük öneme sahip bazı sayfa dışı bileşenler vardır. Örnek Gatsby kodumuza src/components dizinini dahil etmedim ancak sayfa dışı bileşenlerimiz burada görünecek.</p>

<p>Gatsby geliştiricilerinin en çok zaman harcayacağı üç bileşen türü, sayfa bileşenleri, sayfa template bileşenleri ve sayfa dışı bileşenlerdir. Peki Gatsby bu bileşenlerdeki property’leri veya prop’ları doldurmak için hangi verileri kullanacağını nasıl biliyor? GraphQL ve Gatsby’nin - sonraki bölümlerde çok daha derinlemesine inceleyeceğimiz - veri katmanı tam da bu noktada devreye giriyor.</p>

<h3 id="gatsby-veri-katmanı-graphql-ve-source-pluginleri">Gatsby Veri Katmanı: GraphQL ve Source Pluginleri</h3>

<p>GraphQL, yüksek düzeyde özelleştirilmiş sorgulara olanak tanıyan bir sorgu dilidir. REST modelinde, istemciler sunucudan hangi verileri alacaklarını tanımlayamazlar; bu sunucu tarafının sorumluluğundadır. GraphQL farklı bir yaklaşım benimseyerek istemcinin veri gereksinimlerini keyfi bir yapıya göre tanımlamasına olanak tanır. Sunucu daha sonra yalnızca gerekli verileri döndürmekle kalmaz, aynı zamanda istemcinin istekte gönderdiği yapıya göre de onu döndürür. Bu, GraphQL’in birincil avantajıdır.</p>

<p>GraphQL sıklıkla REST paradigmasını kullanan, genellikle RESTful API’ler veya REST API’ler olarak bilinen ve bunun yerine verileri istemci tarafından değil sunucu tarafından tanımlandığı şekilde döndüren API’lerin yerine geçmek veya geliştirmek için kullanılır. Örneğin birçok içerik ve ticaret mimarisinde, bir uygulamada kullanılmak üzere third-party servislerden ve veritabanları ve içerik yönetim sistemleri (CMS’ler) gibi veri sağlayıcılardan veri almak için kullanılır. Bu kullanım şeklinde sürekli çalışan ve gelen herhangi bir GraphQL sorgusuna yanıt vermeye hazır bir GraphQL sunucusu bulunur.</p>

<p>Ancak Gatsby, GraphQL’den ek bir amaç için yararlanıyor: dahili veri gereksinimlerinin bildirimi. Gatsby, GraphQL’i bir istemci olarak harici kaynaklardan tüketmek yerine, veri alımını ve işlemeyi çalışma zamanında değil derleme zamanında gerçekleştirmek için GraphQL’i ön plana yerleştirir. Başka bir deyişle, birçok framework GraphQL’i yalnızca verileri harici olarak tüketmek için kullanırken, Gatsby bunun yerine GraphQL’i dahili olarak kullanır, böylece birçok farklı veri kaynağı tek bir GraphQL API’sinde birleştirilebilir.</p>

<p>Tipik bir Gatsby sitesi birçok farklı veri kaynağından faydalanabilir. Farklı veri yapılarının daha sonra geliştiricilerin Gatsby’de kolayca kullanabileceği bir formatta uyumlu hale getirilmesi gerekiyor. Gatsby, doğrudan Gatsby sitesine yerleştirilmiş verileri dahili bir dosya sisteminden alabilir veya derleme süreci sırasında ayrı bir CMS veya ticaret platformuna API çağrıları gerçekleştirebilir. Gatsby, kendi dahili GraphQL API’sini doldurmak için bir GraphQL API’sinden bile faydalanabilir. Ve bu kaynakların tümünden verileri aynı anda tek bir birleşik GraphQL API’sine çekebilir, bu da verilere kolayca erişilmesini ve tüketilmesini kolaylaştırır.</p>

<p>Her Gatsby bileşeni, o bileşenin proplarını dolduran verileri belirleyen bir GraphQL sorgusu içerir. Verileri doğrudan bileşen içinde bildirebilseniz de, muhtemelen başka kaynaklardan çekmek istediğiniz verileriniz vardır. Gatsby bunu nasıl başarıyor?</p>

<p>Gatsby’de pluginler, mevcut bir Gatsby sitesinin üzerine yerleştirilebilecek ek işlevlerdir. Bazı pluginler Gatsby’nin temel işlevselliğini genişletebilir ancak harici kaynaklardan veri alınmasındaki anahtar rolü nedeniyle özellikle önemli olan bir eklenti türü vardır: source pluginleri.</p>

<p>Gatsby’de source pluginleri, kaynak ne olursa olsun, kaynağından veri getiren Node.js paketleridir. Gatsby, API’ler, veritabanları, CMS’ler, ticaret sistemleri ve hatta yerel dosya sistemleri dahil olmak üzere veri yayan çeşitli sistemler için birçok source plugin sunar. Örneğin, gatsby-source-filesystem, ister Markdown ister başka bir şey olsun, verileri doğrudan Gatsby’nin dahili dosya sisteminden çeker. Ayrıca, gatsby-source-drupal ve gatsby-source-contentful, sırasıyla Drupal ve Contentful CMS’lerden içerik çeker.</p>

<p>Source pluginlere kısa bir süre içinde geri döneceğiz, ancak önce Gatsby ekosistemine bir göz atalım.</p>

<h3 id="gatsby-ekosistemi">Gatsby Ekosistemi</h3>

<p>Gatsby ekosistemi, topluluğun katkıda bulunduğu tüm pluginleri ve Gatsby’ye yeni işlevler kazandıran diğer modülleri kapsar. Gatsby ekosisteminde dört tür proje vardır:</p>

<p><strong>Pluginler</strong></p>

<p>Önceki bölümde değindiğimiz gibi Gatsby’deki pluginler, Gatsby çekirdeğine önemli işlevler sağlayan Node.js paketleridir. Örneğin, gatsby-image eklentisi, güçlü görüntü işlemeye ihtiyaç duyan geliştiriciler tarafından Gatsby sitelerine eklenebilir. Gatsby pluginleri, site haritası ve RSS beslemesi işlevselliğinden arama motoru optimizasyonu (SEO) desteğine kadar geniş bir yelpazeyi çalıştırır.</p>

<p><strong>Temalar</strong></p>

<p>Gatsby teması, tıpkı normal bir Gatsby sitesi gibi, gatsby-config.js dosyasını içeren bir tür plugindir. Bu dosya, Gatsby sitelerine isteğe bağlı tanımlanmış yapılandırma sağlar. Bu, bir Gatsby sitesinin, başka bir Gatsby sitesine paket olarak yüklenen ve onu yeniden kullanılabilir işlevlerle genişleten bir Gatsby teması haline gelebileceği anlamına gelir.</p>

<p><strong>Starter’lar</strong></p>

<p>Gatsby ekosisteminde starter’lar, geliştiricilerin Gatsby geliştirmesi için bir başlangıç noktası olarak kendi makinelerine kolayca kopyalayabilecekleri standart şablonlardır. Bununla birlikte, bir geliştirici tarafından değiştirildikten sonra, starter’lar, temalardan farklı olarak orijinal durumlarıyla devam eden bir ilişkiye sahip olmazlar. Gatsby, tipik kullanım durumları için uzun bir official starter listesi sunar ve aynı zamanda kendi kullanım senaryonuzu oluşturmanız için de zengin bir sürece sahiptir.</p>

<p><strong>Tarifler (Recipes)</strong></p>

<p>Gatsby’de yeni olan ve hala deneysel olan tarifler, sayfa oluşturma, plugin yükleme, TypeScript’i destekleme ve daha fazlası gibi belirli ortak sorumlulukları otomatikleştirmek için Gatsby CLI’de çağrılan araçlardır. Bu yazının yazıldığı an itibariyle Gatsby, yaklaşık bir düzine örnek tarifin yanı sıra kendi tariflerinizi nasıl yazacağınıza dair bilgiler de yayınladı.</p>

<p>Bu tür Gatsby ekosistemi projelerinin her birini inceleyeceğiz, ancak artık Gatsby’nin ne olduğuna dair temel bir anlayışa sahip olduğunuza göre şunu merak ediyor olabilirsiniz: Neden Gatsby kullanmalıyım?</p>

<h2 id="neden-gatsby">Neden Gatsby?</h2>

<p>Bu bölümde, geliştiricileri bir sonraki web sitesi projeleri için Gatsby’yi seçmeye motive eden benzersiz avantajlardan ve gerekçelerden bahsedeceğiz. Gatsby, dört temel konuya odaklanmasıyla JavaScript ve Jamstack topluluklarında itibar kazandı: performans, erişilebilirlik, geliştirici deneyimi ve güvenlik.</p>

<h3 id="performans">Performans</h3>

<p>Günümüzde pek çok web sitesi esas olarak sunucu odaklı olmaya devam ediyor; bu, sunucuların tarayıcı tarafından gönderilen her bir isteğe yanıt verdiği anlamına geliyor. Ancak Gatsby, yük optimizasyonu (tarayıcıya gönderilen dosyanın boyutunun en aza indirilmesi) ve dağıtım optimizasyonu (oraya ulaşmak için gereken sürenin en aza indirilmesi) dahil olmak üzere her düzeyde performans iyileştirmelerini vurguluyor.</p>

<p>Gatsby ile performansı arttırmanın dört temel yolu şunlardır:</p>

<p><strong>Statik Site Teslimi</strong></p>

<p>Gatsby derlemesi gerçekleştirdiğinizde sonuç, Gatsby proje kökündeki public klasöründe bulunan herhangi bir içerik dağıtım ağına (CDN) yüklenebilecek statik varlıkların bir koleksiyonudur. Bu, Gatsby sitelerinin öncelikle tarayıcıda hızla yüklenen statik HTML’den oluştuğu anlamına gelir. Ancak o zaman React tarafından sağlananlar gibi istemci tarafı JavaScript başlatılır.</p>

<p><strong>Build-Time Derlemesi</strong></p>

<p>Gatsby, çok daha ayrıntılı olan ve yalnızca tek bir sayfayı oluşturabilen runtime derlemesi yerine, statik dosyalar üretmek için build-time derlemesini kullanır. Bunun yerine, build-time tam bir statik sayfa kümesi oluşturur. Bu, harici veri kaynaklarına yapılan isteklerin hiçbir zaman toplam teslim süresine ek gecikme getirmediği anlamına gelir; çünkü sonuç, istek üzerine verileri alması ve işlemesi gereken bir sayfa değil, verileri önceden doldurulmuş bir dizi statik dosyadır. Bu şekilde, genellikle sunucuların web uygulamaları için gerçekleştirmesi gereken iş miktarını sınırlar. Gatsby, performans açısından hangisinin daha uygun olduğuna bağlı olarak hem build-time hem de runtime derlemesinden yararlanabilir.</p>

<p><strong>Yük Optimizasyonu</strong></p>

<p>Gatsby, görsellerin ve JavaScript dosyalarının boyutlarını azaltarak, görsellerin ekranın alt kısmına yüklenmesini geciktirerek ve gerektiğinde CSS’i satır içi ekleyerek kullanıcılara iletilen nihai verinin boyutunu sınırlamak için çok çalışıyor. Gatsby ayrıca otomatik rota tabanlı kod bölme (böylece yalnızca gerekli rotaya ait kod yüklenir), arka planda önceden yükleme (böylece varlıklar arka planda yüklenebilir) ve hemen ihtiyaç duyulmayan varlıkların yavaş yüklenmesini de sağlar.</p>

<p><strong>Dağıtım Optimizasyonu</strong></p>

<p>Gatsby siteleri, dağıtılan kodun daha hızlı teslim edilmesini sağlayan bir CDN’den hizmet verecek şekilde optimize edilmiştir. Gereksinimlerinize bağlı olarak bu model, tarayıcıdan gelen her isteğin kaynak sunucuya ve veritabanına dönmesinin gerektiği tarihsel paradigmaya göre önemli bir gelişme olabilir.</p>

<p>Statik site teslimi ve build-time derlemesi sayesinde, Gatsby siteleri çok sayıda JavaScript dosyası yerine çoğunlukla HTML’den oluşur. Yük optimizasyonu ve dağıtım optimizasyonu sayesinde, Gatsby siteleri uçta hızla yüklenir ve küçük kalır.</p>

<h3 id="erişilebilirlik">Erişilebilirlik</h3>

<p>Bir zamanlar önemsiz bir konu olan erişilebilirlik, pazardaki işletmeler ve kuruluşlar için önemli bir husus haline geliyor. Tek sayfalı JavaScript uygulamaları yakın zamana kadar yardımcı teknolojilere olan zayıf destekleri ve farklı erişim ihtiyaçlarının dikkate alınmaması nedeniyle kötü bir şöhrete sahipti. Gatsby, yüksek düzeyde dinamik JavaScript yerine statik HTML sunmanın yanı sıra, erişilebilir markup ve erişilebilir yönlendirme sağlamaya odaklanarak erişilebilirliğe birinci sınıf bir vatandaş gibi davranır:</p>

<p><strong>Erişilebilir Markup</strong></p>

<p>Gatsby, varsayılan olarak kod için erişilebilirlik linting aracı olan eslint-plugin-jsx-a11y paketini sağlar. Bu paket, diğer olası sorunların yanı sıra resimler için eksik alternatif metin ve Accessible Rich Internet Applications (ARIA) property’lerinin doğru kullanımını arar.</p>

<p><strong>Erişilebilir Yönlendirme</strong></p>

<p>Çoğu JavaScript uygulaması, tarayıcının ek bir sayfa yüklemesine neden olmak yerine, kullanıcının bir bağlantıya tıklayıp sayfanın hemen yeniden oluşturulduğunu görmesine olanak sağlamak için dinamik sayfa navigasyonu kullanır. Ancak bu dinamik yönlendirmenin engelli kullanıcılar için normal linklerle aynı rahatlıkla kullanılması imkansız olabilir. Gatsby,  erişilebilir dinamik gezinmeyi destekleyen @reach/router kütüphanesi ile birlikte gelir ve ayrıca Gatsby linklerinin erişilebilir bir şekilde çalışmasını sağlar.</p>

<h3 id="geliştirici-deneyimi">Geliştirici Deneyimi</h3>

<p>Geliştirmeyi olabildiğince sorunsuz hale getirmek ve geliştiricilerin web siteleri oluştururken zamandan tasarruf etmelerini sağlamak için Gatsby, aşağıdakiler de dahil olmak üzere çeşitli geliştirici deneyimi özellikleri (özellikle tekrarlanan görevler için) sunar:</p>

<p><strong>Starter ve Minimum Konfigürasyon</strong></p>

<p>Gatsby starter’ları neredeyse sıfır yapılandırmaya izin verir ve tamamen hazır bir dizin yapısı sağlar. Tüm starter’lar tamamen işlevsel Gatsby siteleridir ve hemen devreye alınabilir. Sıfırdan bir Gatsby sitesi oluşturmak mümkün olsa da, mevcut official starter’lardan birinin kullanılması şiddetle tavsiye edilir ve bizimde bu kitapta benimsediğimiz yaklaşım budur.</p>

<p><strong>Hot Reloading ile Local Development Ortamı</strong></p>

<p>Gatsby, kod değişiklikleri kaydedildiğinde hot reloading özelliğine sahip local development ortamı sağlar. Bu, üzerinde çalıştığınız siteye değişikliklerin anında yansıdığını görebildiğiniz  hızlı bir geri bildirim döngüsü oluşturur.</p>

<p><strong>GraphiQL ve GraphQL Explorer</strong></p>

<p>Gatsby, GraphQL’in yanı sıra, GraphQL aracılığıyla veri almak isteyenlere yardımcı olacak geliştirici araçlarını da sağlar. Hata ayıklamayı sağlayan bir GUI olan GraphiQL ve GraphQL için bir schema explorer olan GraphQL Explorer, varsayılan olarak Gatsby ile birlikte gelir.</p>

<p><strong>Built-in Linting ve Test Etme</strong></p>

<p>Gatsby aynı zamanda temel paketinde, kodlama standartlarına ve erişilebilirlik kurallarına bağlılık, geliştirme süreci sırasında ortaya çıkan diğer kalite sorunları gibi endişeleri kontrol etmenize olanak tanıyan çeşitli yerleşik testler ve linting süreçlerini de içerir.</p>

<p><strong>Sürekli Entegrasyon ve Sürekli Dağıtım (CI/CD)</strong></p>

<p>Son olarak Gatsby, sürekli entegrasyon (Continuous Integration) ve sürekli dağıtım (Continuous Deployment) için gelişmiş yetenekler sunan çeşitli altyapı sağlayıcılarıyla zarif bir şekilde entegre olur. Gatsby için kullanılan en yaygın sunucu hizmetlerinden bazıları Netlify, Vercel ve Gatsby Cloud’dur.</p>

<h3 id="güvenlik">Güvenlik</h3>

<p>Günümüzde güvenlik, geleneksel web sitelerini rahatsız eden en önemli şeylerden biridir. Pek çok web sitesi, özellikle veritabanı ve kimlik doğrulama gibi katmanlar arasında koruma olmadığında, sistemleri saldırganlara karşı savunmasız olabilecek monolitik mimarilerden hâlâ yararlanıyor. Diğerleri aktif olarak çalışan veritabanı ve sunuculara ihtiyaç duyar,
dikkatli bir şekilde tasarlanmadığı takdirde güvenlik açıkları oluşabilir.</p>

<p>Gatsby, sitelerini API’ler veya tükettiği veri kaynakları hakkında bilgi içeren dinamik kod olarak değil, statik HTML olarak sunduğundan, harici veri kaynaklarını otomatik olarak gizleyerek onları istenmeyen gözlerden korur. İçerik ve ticaret sistemi yöneticileri için bu özellikle yararlı olabilir, çünkü bu veri kaynakları, devam eden bir Gatsby derlemesi veya verileri güncellemek isteyen bir editör tarafından ihtiyaç duyulmadığında kapatılabilir. Güvenlik, bu kitap boyunca geri döneceğimiz çok yönlü bir konudur, ancak Gatsby’nin statik site sunumu, markup ve veri kaynaklarınız arasında çok sağlam bir güvenlik duvarı sağlar.</p>

<p>Artık bu kadar çok geliştiricinin Gatsby’yi seçmesinin ardındaki bazı motivasyonları anladığınıza göre, bunu daha geniş mimari paradigmalar ve React ile JavaScript geliştirme çerçevesi içinde bağlamsallaştıralım.</p>

<p>Gatsby, yüksek performanslı web siteleri ve web uygulamaları oluşturmaya yönelik bağımsız bir framework olsa da, çalışma zamanı derlemesi yerine derleme süresini ödüllendiren, sunucuya bağımlı mimariler yerine sunucusuz olan web framework’lerine yönelik daha büyük bir eğilimin parçasıdır. Bir sonraki bölümde Gatsby’nin Jamstack mimarileri, JavaScript ve React’ın daha geniş bağlamlarına nasıl uyduğuna bakacağız.</p>

<h2 id="gatsby-ve-jamstack">Gatsby ve Jamstack</h2>

<p>Organizasyonların neden eskisinden daha az monolitik ve daha ayrık mimarilere geçiş yaptığını açıklayan mimari temelleri ele aldık. Ancak geliştiricilerin neden benzer hamleler yaptığını anlamak için, yüksek performanslı web siteleri geliştirmeyi kolaylaştıran JavaScript, API’ler ve statik markup ile denge kuran bir mimari paradigma olan Jamstack’ı (aynı zamanda JAMstack olarak da yazılır) keşfetmemiz gerekiyor.</p>

<p>Bu bölümde ilk olarak, çalışma zamanı yerine derleme zamanı deploy sağlayan SSG’leri ele alacağız. Ardından, içerik ve ticaret mimarilerinde meydana gelen eş zamanlı bir paradigma değişimi olan dağıtılmış içerik ve ticareti ele alacağız. Son olarak, Jamstack’in ne olduğunu ve Gatsby’nin Jamstack ekosisteminin neresinde olduğunu tanımlamak ve özetlemek için SSG’leri, içerik ve ticaret mimarilerini birleştireceğiz.</p>

<h3 id="statik-site-oluşturucular">Statik Site Oluşturucular</h3>

<p>En temel düzeyde Gatsby bir SSG’dir. SSG’ler, bir template ve harici bir veritabanı veya başka bir kaynak tarafından sağlanan verilere dayalı olarak statik HTML dosyaları oluşturan araçlardır. Bu veri girişi göz önüne alındığında, geliştirici tarafından yazılan mantığa dayalı olarak verilere Markdown, Liquid vb. template dillerinde yazılabilen stil ve template’ler uygulanır.</p>

<p>Genellikle aktif olarak çalışan bir sunucu gerektiren ve çalışma zamanında yürütülen tipik dinamik web uygulamalarından farklı olarak SSG’ler, web sunucusundaki dosya sisteminden daha fazlasını gerektirmeyen bir dizi statik dosya ve asset sağlar. Genellikle terminal komutu aracılığıyla manuel olarak çalıştırıldıkları için derleme zamanında yürütülürler; bu da yalnızca statik varlıkların durumunun güncellenmesi gerektiğinde çalıştıkları anlamına gelir.</p>

<p>SSG’lerin ilk dalgası, statik site oluşturmak ve verilerin template’ler halinde işlenmesini gerçekleştirmek için öncelikle sunucu tarafı teknolojilerine dayanıyordu. Örneğin, hala popüler olan Jekyll ve Hugo, Ruby ve Go gibi yalnızca sunucuya yönelik dillerde yazılsa da, Metalsmith gibi diğer eski SSG’ler Node.js ve JavaScript ile yazılmıştır. İlk nesil SSG’ler sınırlı içeriğe ve dinamizme sahip küçük web siteleri için ideal olsa da, JavaScript uygulamalarında bulunan etkileşimden tamamen yoksundular.</p>

<p>Son yıllarda, özellikle Javascript ekosisteminde ikinci nesil SSG’ler ortaya çıktı. Bunlar, Jekyll ve Hugo gibi eski araçlardan önemli ölçüde farklıdır. Sunucu tarafı diller ile ilişkilendirilmek yerine JavaScript framework’lerinin istemci tarafı yeteneklerine güvendikleri için bu SSG’ler kendilerini JavaScript framework’lerine bağlar. Örneğin Gatsby ve Next.js, React destekli SSG’ler kategorisine girerken Gridsome ve Nuxt.js, Vue ekosisteminin bir parçasıdır.</p>

<p>Bu bizi kitapta ele aldığımız framework’ün çeşitli tanımlarından ilkine getiriyor: Gatsby her şeyden önce React’ı temel alan bir SSG’dir. Ancak Gatsby’nin neden ortaya çıktığını ve başarısının ardındaki nedenleri incelemek için, Gatsby’nin içerik ve ticari web sitesi uygulamalarıyla ilgili birçok kalıcı sorunu nasıl çözdüğüne dair çok daha geniş bir perspektifi incelememiz gerekiyor.</p>

<h3 id="dağıtılmış-i̇çerik-ve-ticaret">Dağıtılmış İçerik ve Ticaret</h3>

<p>Son yıllarda Gatsby gibi framework’lerin ortaya çıkması sayesinde, dağıtılmış içerik ve ticaret biçiminde yeni bir mimari paradigma ortaya çıkmaya başladı (bunun içerik varyantına bazen “content mesh” te deniyor).</p>

<p>Herhangi bir içerik ve ticaret uygulamasının karşılaştığı en önemli sorunlardan biri, third-party entegrasyonlarını bir web uygulamasında kullanılabilir hale getiren plugin veya uzantıların mevcudiyetidir. Bu plugin’lerin çoğu, arama tool’u veya ödeme sistemi gibi içerik ve ticaret açısından önemli olan küçük sorunları ele alır. Monolitik içerik ve ticaret çağında çoğu plugin ekosistemi, aşağıda gösterildiği gibi sıkı bir şekilde kontrol altına alınmıştı. Örneğin WordPress plugin’leri hiçbir zaman Drupal modülleriyle değiştirilemezdi.</p>

<p><img src="/assets/images/2024/03/monolithic-content-and-commerce-systems.png" alt="Monolitik içerik ve ticaret mimarileri" title="Monolitik içerik ve ticaret mimarileri" class="img-fluid" /></p>

<p>Dağıtılmış içerik ve ticaret mimarilerinin ortaya çıkmasının bir diğer nedeni de bu plugin’lerin uzun vadeli bakımına ilişkin artan endişeydi. Bir ödeme sistemi geliştiricisi, açık kaynak projesini geliştirmeyi bırakmaya veya plugin’i kaldırmaya karar verirse, ortaya çıkan kesinti, söz konusu plugin kullanıcısının ödeme sistemini üzerinde büyük bir etkiye sahiptir. Çoğu zaman tek çözüm, plugin’i sıfırdan yeniden oluşturmak veya özel bir uygulama tasarlamaktır.</p>

<p>Son birkaç yılda, aşağıda görüldüğü gibi, geliştiriciler arasında, yeni servisler ortaya çıktıkça değiştirilebilir sayfa içi servislere olan ilgi arttı. Yeni bir startup sınıfı, içerik ve ticaret ekosistemlerinden bağımsız sayfa içi widget’lar ve third-party servisler sunuyor; bu da bunların herhangi bir web uygulamasında kullanılabileceği anlamına geliyor (yine de uzun vadeli bakım sorunları olabilir ve varlıkları sona erebilir). Bu şirketler arasında arama özellikleri sağlayan Algolia; alışveriş sepeti ve ödeme sürecini sağlayan Snipcart; ve kolayca yönetilen web formları sunan Typeform vardır.</p>

<p><img src="/assets/images/2024/03/distributed-cms-architecture.png" alt="Dağıtılmış içerik ve ticaret mimarileri" title="Dağıtılmış içerik ve ticaret mimarileri" class="img-fluid" /></p>

<p>Günümüzde bir içerik ve ticaret uygulamasının, web sitesini doldurmak için birden fazla kaynaktan veri çekmesi gerekebilir; backend veri sağlayıcıları ise arka planda eklenip kaldırılır. Dahası, bir içerik ve ticaret uygulaması, geliştiricilerin uygun gördüğü şekilde değiştirilebilen sayfa içi servislerin çok yönlü bir kombinasyonunu gerektirebilir. Tüm bunlar, içerik ve ticaret mimarileri için, geliştiricilerin çok çeşitli veri kaynaklarından ve bir o kadar çeşitli third-party plugin ve servislerinden yararlanmasına olanak tanıyan daha dağıtılmış bir geleceğe işaret ediyor.</p>

<h3 id="jamstack">Jamstack</h3>

<p>Jamstack mimarilerini diğer (yani monolitik ve ayrıştırılmış) mimarilerden bu kadar farklı kılan şeyin ne olduğuna geçmeden önce Jamstack’i tartışalım.</p>

<p>Jamstack framework’lerive metodolojileri, derleme zamanı render kavramını ortaya koymaktadır. Jamstack mimarilerinde, build başladıktan sonra render işlemini gerçekleştirmekten SSG sorumludur. Derleme sırasında SSG’nin JavaScript’i, API’lerden veri almaktan ve bu verileri statik markup’a dönüştürmekten sorumludur. Derleme sonucu, sürekli çalışan bir veritabanı, web sunucusu veya sunucu tarafı uygulaması yerine herhangi bir dosya sistemine yüklenebilen statik asset’lerin bir koleksiyonudur.</p>

<p><strong>Jamstack ve diğer mimariler arasındaki farklar</strong></p>

<p>Öte yandan, geleneksel mimariler (özellikle monolitik ve birçok ayrılmış içerik ve ticaret uygulaması), yalnızca bir kullanıcını web sitesi sayfasına istek yaptığında gerçekleşen çalışma zamanı derleme işlemini gerçekleştirir. Veritabanı bir istek alır ve gerekli verileri aktif olarak çalışan başka bir backend uygulamasına sağlar ve bu daha sonra sürekli çalışan bir web sunucusuna deploy edilir. Bu istenmeyen davranış, çoğu uygulamada agresif önbelleğe alma ve CDN yoluyla hafifletilir.</p>

<p>Jamstack mimarilerinin ortaya çıkmasından önce, sunucu tarafındaki tüm JavaScript uygulamaları, sürekli çalışan bir Node.js web sunucusundan yararlanıyordu. Ve tüm monolitik içerik ve ticaret mimarileri bu modeli takip ediyor. Ayrılmış içerik ve ticaret mimarilerine gelince, bu, derleme zamanı mı yoksa çalışma zamanı render’ın gerçekleştiğine bağlıdır. Dağıtılmış içerik ve ticaret mimarileri, derleme zamanı render’a en uygun şekilde katkıda bulunsa da, çalışma zamanı render uygulamak da mümkündür.</p>

<p>Çalışma zamanı oluşturmanın en önemli dezavantajı, yalnızca ayrı sayfaları üretmesidir. Bu sayfalar kesinlikle bir CDN’e yerleştirilebilir ve agresif bir şekilde önbelleğe alınabilir, ancak CDN’e veya önbelleğe alınmamış  yeni bir sayfa istendiğinde, çalışma zamanı render işleminin yeniden gerçekleşmesi gerekir.</p>

<p><strong>Sunucusuz altyapı</strong></p>

<p>Günümüzde Jamstack uygulamaları için derleme zamanı render ve CDN desteği sağlayan, özellikle de Gatsby’dekiler olmak üzere çok çeşitli altyapı sağlayıcıları bulunmaktadır. Toplu olarak bu altyapı sağlayıcıları serverless (sunucusuz) olarak bilinir çünkü Jamstack uygulamalarında meydana gelmeyen çalışma zamanı render gibi işlevselliklere güç sağlamak için sürekli çalışan sunuculara güvenmezler. Sunucusuz host’lar, Jamstack deploy’unun başarılı olması için genellikle üç öğeye ihtiyaç duyar:</p>

<ul>
  <li>Statik site kod tabanı, genellikle GitHub gibi bir repository.</li>
  <li>Sunucusuz host tarafından yürütülen derleme ve render sürecini tetikleyen bir build komutu.</li>
  <li>Statik olarak oluşturulan asset’lerin yerleştirileceği URL’i temsil eden bir deploy hedefi.</li>
</ul>

<p>En yaygın kullanılan sunucusuz altyapı sağlayıcılarından biri Netlify’dır. Diğerleri arasında Next.js framework’ün yaratıcısı Vercel (eski adıyla Zeit); Amazon’un AWS Amplify’ı; ve Gatsby uygulamalarında uzmanlaşmış Gatsby Cloud.</p>

<p>Artık Jamstack’ın temellerini anladığınıza göre, Gatsby’nin JavaScript dünyasına nasıl uyduğuna ve kullanıcı arayüzleri oluşturmak için JavaScript ve React’tan nasıl yararlandığına göz atalım.</p>

<h2 id="gatsbyde-javascript">Gatsby’de JavaScript</h2>

<p>JavaScript’in temel kavramlarına ve geçmişine kapsamlı bir genel bakış bu kitabın kapsamı dışındadır, ancak bir geliştirici olarak Gatsby ile çalışmak için gerekli olan en dikkat çekici kavramlardan birkaçını vurgulayalım.</p>

<p>JavaScript, bir web tarayıcısında çalıştırılabilen iki programlama dilinden biridir (diğeri bu yazının yazıldığı an itibariyle yaygın bir desteği bulunmayan WebAssembly’dir). Imperative orientation, weak typing, prototip tabanlı kalıtım ve fonksiyonel doğası ile karakterize edilir.</p>

<p>JavaScript tarihinin ilk yıllarında, öncelikle sınırlı etkileşim ve asenkron render ile statik web sayfalarını süslemek için kullanıldı. Ancak günümüzde JavaScript, web geliştirmede en önemli diller arasındadır ve geçtiğimiz yıllarda JavaScript ile çalışmış olanlar - CLI’ler ve modern JavaScript’in modülerliği nasıl kullandığı da dahil olmak üzere - JavaScript geliştirici deneyiminin birçok yönünün önemli ölçüde değiştiğini göreceklerdir.</p>

<h3 id="komut-satırı-arayüzleri-cli">Komut Satırı Arayüzleri (CLI)</h3>

<p>CLI’ler, geliştiricilerin local makinelerindeki dosya sistemleriyle etkileşimde bulunmak ve uygulama geliştirme, bağımlılık yönetimi ve deploy için araçlar sağlamak için kullandıkları metin tabanlı arayüzlerdir. En eski CLI’ler Shell veya Bash gibi düşük seviyeli dillerde yazılmış olsa da, bugün bu arayüzlerin çoğu Node.js (veya Rust ve Go gibi diğer diller) aracılığıyla JavaScript’te yazılmıştır.</p>

<p>Server side rendering (SSR) ve evrensel JavaScript yaklaşımları için kullanılan Node.js, yalnızca tarayıcı ortamı dışında yürütülmesi gereken JavaScript için kullanışlı değildir. Dosyaları işlemek ve uygulama geliştirme açısından önemli olan diğer eylemleri gerçekleştirmek için bilgisayarlarda yürütülmesi gereken JavaScript için de faydalıdır. Örneğin, birçok JavaScript uygulaması, çoğu zaman JavaScript alanındaki diğer geliştiriciler tarafından sağlanan kütüphaneler olmak üzere birçok bağımlılığın indirilmesini gerektirir.</p>

<p>Bugün, JavaScript bağımlılıkları, kütüphanelerin alınmasını ve bunlara ihtiyaç duyan uygulamaya enterpolasyonunu kolaylaştıran paket yöneticileri tarafından indirilmekte ve yönetilmektedir. En popüler paket yöneticileri, her biri geniş bir paket koleksiyonu sunan Node Package Manager (NPM) ve Yarn’dır. Bu paketlerin birçoğu kodun küçük bölümleri haline gelen bağımlılıklar olabilse de, diğer paketler genel olarak kurulabilen ve komutları yerel dosya sisteminin herhangi bir yerinde çağrılabilen CLI’ler içerir.</p>

<p>“Gatsby ile Çalışmaya Başlamak” bölümünde ele aldığımız Gatsby CLI, genellikle şu komut çağrılarak global olarak yüklenen JavaScript odaklı CLI’ın bir örneğidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm install -g gatsby-cli
</code></pre></div></div>

<p>NPM ve Yarn, JavaScript dünyasında hem bağımlılık yönetimi hem de CLI kurulumuna yönelik en popüler yaklaşımlardır.</p>

<h3 id="modüler-javascript">Modüler JavaScript</h3>

<p>Bugün JavaScript kayda değer bir modülerlik sergiliyor; dilin ilk kez 1990’larda tanıtıldığı zaman durum böyle değildi. O zamanlar, çoğu JavaScript kodu ya devasa boyutlarda tek bir gömülü dosyada ya da yürütme sırasını belirten art arda gömülü birden fazla JavaScript dosyasında tutuluyordu. 2000’lerin sonlarında ve 2010’ların başlarında JavaScript uygulamaları daha karmaşık hale geldikçe, birçok geliştirici modülerliğe daha iyi bir yaklaşımın gerekli olduğunu fark etti.</p>

<p>ES6 (ECMAScript 2015), modül kavramını tanıtan ilk JavaScript sürümüdür. Kısacası modüller, uygulamadaki diğer JavaScript dosyalarıyla dışa ve içe aktarma yoluyla ilişkileri olan izole edilmiş JavaScript dosyalarıdır. Biri modül sağlayan ve diğeri bu modülü kullanan iki JavaScript dosyasını düşünün (bu örnek, Axel Rauschmayer’in Exploring ES6 kitabından uyarlanmıştır). İşte ilki:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// lib.js
export const sqrt = Math.sqrt
export function sq(x) {
    return x * x
}
export function diag(x, y) {
    return sqrt(sq(x) + sq(y))
}
</code></pre></div></div>

<p>Bu, modül olarak başka bir uygulamaya aktarmak istediğimiz bir kütüphane ise, yalnızca ihtiyaç duyduğumuz dışa aktarılan fonksiyon adlarıyla başvurarak içe aktarabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// app.js
import { sq, diag } from 'lib'
console.log(sq(5))
// 25
console.log(diag(4, 3))
// 5
</code></pre></div></div>

<p>Veya modülün tamamını içe aktarabilir ve ona atadığımız namespace altında başvurabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// app.js
import * as lib from 'lib'
console.log(lib.sq(5))
// 25
console.log(lib.diag(4, 3))
// 5
</code></pre></div></div>

<p>Birçok JavaScript framework’ü ve JavaScript uygulaması, değerleri, fonksiyonları ve sınıfları birbirleri arasında iletmek ve böylece bunların uygulamanın diğer alanlarında çağrılabilmesi ve kullanılabilmesi için modüller kullanır. Gatsby bir istisna değildir ve JavaScript’te bileşen odaklı kullanıcı arayüzleri yazmaya yönelik bir kütüphane olan React da bir istisna değildir. Bir sonraki bölümde, Gatsby’yi daha geniş React ekosistemi içinde bağlamsallaştırmak için bilmeniz gereken temel React kavramlarına hızlı bir giriş yapacağız.</p>

<h2 id="gatsbyde-react">Gatsby’de React</h2>

<p>React kendisini kullanıcı arayüzleri oluşturmaya yönelik bir JavaScript kütüphanesi olarak tanımlıyor ancak etkileşimli kullanıcı arayüzleri yazmaya yönelik bildirimsel, bileşen tabanlı bir yaklaşım olarak tanımlanması belki daha iyi olur. Başlangıçta Facebook tarafından oluşturulan React, artık JavaScript uygulamaları oluşturmak için kullanılan en popüler kütüphanelerden biridir. Gatsby, React’ın üzerine inşa edilmiştir ve onun birçok konseptini miras almıştır.</p>

<p>Burada React’a kapsamlı bir genel bakış sunmak imkansız olabilir, ancak kitap boyunca birçok React konseptini ele alacağız. React söz konusu olduğunda keşfedilecek çok şey olsa da, şimdilik anlaşılması gereken en önemli fikirler bildirimsel render, render ve logic’in React bileşenlerinde yerleşimi ve React prop’larını kullanarak React bileşenleri arasında değerlerin aktarılmasıdır.</p>

<h3 id="jsx-ile-bildirime-dayalı-render">JSX ile Bildirime Dayalı Render</h3>

<p>JSX, çoğu React geliştiricisinin React’te rendering gerçekleştirmek için kullandığı sözdizimi uzantısına verilen addır. Örneğin şu “Hello World” örneğini düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = &lt;h1&gt;Hello, world!&lt;/h1&gt;
</code></pre></div></div>

<p>JSX görünüm olarak HTML’e veya templating diline benzese de aslında React tarafından JavaScript sözdizimi olarak yorumlanır. Bu kod yürütüldüğünde bir HTML öğesi oluşturacak ve onu Document Object Model’a (DOM) eklemeye hazırlayacaktır.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = &lt;h1&gt;Hello, world!&lt;/h1&gt;

ReactDOM.render(
    el,
    document.getElementById('root')
)
</code></pre></div></div>

<p>Çoğu React uygulaması, herhangi bir render gerçekleşmeden önce, yalnızca &lt;body&gt; öğesi içindeki bir &lt;div id="root"&gt;&lt;/div&gt;‘den oluşur. React, JSX’te belirtilen tüm öğeleri o HTML öğesi içinde oluşturacaktır.</p>

<p>JSX ayrıca HTML’den farklı olarak öğe bildirimleri içinde ifadelere de izin verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = &lt;h1&gt;Hello, world! The current time is {Date().toLocaleString()}.&lt;/h1&gt;

ReactDOM.render(
    el,
    document.getElementById('root')
)
</code></pre></div></div>

<p>JavaScript’teki mevcut anahtar kelimeler ile oluşabilecek çakışmalar veya React’ın camelCase’e yönelik stilistik tercihi nedeniyle HTML niteliklerinin JSX’te farklı şekilde adlandırılması gerektiğini unutmayın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = &lt;div tabIndex="0"&gt;&lt;/div&gt;
const el = &lt;div className="hero-image"&gt;&lt;/div&gt;
</code></pre></div></div>

<p>JSX’te çift tırnak kullanamazsınız (JavaScript tarafından farklı şekilde yorumlandıkları için), bu nedenle JSX niteliklerine yapılan enterpolasyon küme parantezleri içinde gerçekleşmelidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = &lt;img src={article.heroImageUrl} /&gt;
</code></pre></div></div>

<p>JSX ayrıca iç içe geçmiş öğelere de izin verebilir. Çok satırlı öğe bildirimleri için, bunları parantez içine almak en iyisidir, böylece JavaScript bunları ifade olarak yorumlayabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = (
    &lt;div className="hero-image"&gt;
        &lt;img src={article.heroImageUrl} /&gt;
    &lt;/div&gt;
)
</code></pre></div></div>

<p>JSX’i ham JavaScript’e derleyebilen bir JavaScript derleyicisi olan Babel’in, JSX’i JavaScript’e nasıl derlediğine dair bir örnek görelim. Aşağıdaki kod önceki örnekteki koda eşdeğerdir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const el = React.createElement(
    "div",
    {
        className: "hero-image"
    },
    React.createElement(
        "img",
        {
            src: article.heroImageUrl
        }
    )
);
</code></pre></div></div>

<p>Artık React’ın bildirimsel render gerçekleştirmek için JSX’i nasıl kullandığını anladığınıza göre, dikkatimizi React’ın kullanıcı arayüzü geliştirmeye yönelik bileşen odaklı yaklaşımına çevirebiliriz.</p>

<h3 id="react-bileşenleri">React Bileşenleri</h3>

<p>React, uygulama geliştirmede önemli olan iki temel şeyin birlikte konumlandırılması konusunda açık bir tercih sergiliyor: markup ve mantık (logic). Markup ve mantığı isteğe bağlı olarak farklı dosyalara ayıran birçok tarihsel yaklaşımın aksine, React, her iki sorumluluğun tek bir dosyada birlikte konumlandırılmasını teşvik etmek için hem render kodunu hem de veri yönetimi mantığını içeren bileşenler üzerinde çalışır. Bu nedenle, önceki bölümde gördüğümüz bildirimsel render, genellikle her dosyada, işlenecek verileri yöneten mantıkla birlikte sunulur.</p>

<p>Bir bileşeni fonksiyon veya sınıf olarak tanımlayabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// Function component
function HeroImage(props) {
    return (
        &lt;div className="hero-image"&gt;
            &lt;img src={props.heroImageUrl} /&gt;
        &lt;/div&gt;
    )
}

// Class component
class HeroImage extends React.Component {
    render() {
        return (
            &lt;div className="hero-image"&gt;
                &lt;img src={this.props.heroImageUrl} /&gt;
            &lt;/div&gt;
        )
    }
}
</code></pre></div></div>

<p>Şimdiye kadar, bileşen içinde meydana gelen dahili bildirime dayalı render tanımlamak için yalnızca standart HTML öğelerini kullandık. Ancak React, bileşenlerin JSX öğeleri olarak tanımlandıktan sonra yeniden kullanılmasına izin verir. React, mevcut bir HTML öğesi yerine bileşene eşdeğer bir JSX öğesiyle karşılaştığında, bu öğe bildiriminin içerdiği nitelikleri, React dilinde props olarak bilinen bir nesne olarak bileşene iletir (“properties”in kısaltması).</p>

<h3 id="react-propları">React Prop’ları</h3>

<p>Öğeyi bildirirken, makalenin “hero” görselinin URL’sini, props içinde yer alan yeni bir değerle değiştirdiğimiz aşağıdaki örneği düşünün. Burada ortaya çıkan HTML şu şekildedir: <code class="language-plaintext highlighter-rouge">&lt;img src="https://img.example.com/hero.jpg" /&gt;</code>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class HeroImage extends React.Component {
    render() {
        return &lt;img src={this.props.src} /&gt;
    }
}

const el = &lt;HeroImage src="https://img.example.com/hero.jpg" /&gt;

ReactDOM.render(
    el,
    document.getElementById('root')
)
</code></pre></div></div>

<p>Yeni React uygulamalarının çoğu, uygulamanın tamamını ve içerdiği bileşenleri temsil eden tek bir root-level App bileşeni içerir. Bu örnekte, az önce oluşturduğumuz diğer bileşeni içeren yeni bir App bileşeni oluşturuyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class HeroImage extends React.Component {
    render() {
        return &lt;img src={this.props.src} /&gt;
    }
}

class App extends React.Component {
    render() {
        return &lt;HeroImage src="https://img.example.com/hero.jpg" /&gt;
    }
)

ReactDOM.render(
    &lt;App /&gt;,
    document.getElementById('root')
)
</code></pre></div></div>

<p>React’ta prop’ların, fonksiyon veya sınıf olarak tanımlanmalarına bakılmaksızın bileşenler içinde salt okunur olduğunu unutmayın. React’ın tüm prop’lar için geçerli olan katı bir kuralı vardır: “Tüm React bileşenleri, kendi prop’larına göre saf fonksiyonlar gibi davranmalıdır.” Başka bir deyişle, bileşen kendi prop’larını değiştirmemelidir; bunları yalnızca öngörülebilir bir sonuç döndürmek için kullanmalıdır. Saf fonksiyonlar asla girişlerini değiştirmez.</p>

<p>Gatsby konseptlerini derinlemesine incelerken yeni React konseptleri ortaya çıktıkça bunları tek başına incelemek yerine Gatsby bağlamında inceleyeceğiz. Gatsby, bileşen ve prop kullanımı açısından temellerini güçlendirmek için React’ı yoğun bir şekilde kullanıyor. Şimdi ilk Gatsby sitemizi oluşturalım!</p>

<h2 id="gatsbyyle-başlarken">Gatsby’yle Başlarken</h2>

<p>Artık Gatsby’nin Java-Script ve React dünyalarına nasıl uyduğunu anladığınıza göre, ilk Gatsby sitemize başlamanın zamanı geldi. Bu bölümde Gatsby için local development ortamı oluşturmaya odaklanacağız.</p>

<h3 id="komut-satırı">Komut Satırı</h3>

<p>Diğer birçok web geliştirme aracı gibi, Gatsby de geliştiricilerin grafiksel araçlar yerine bilgisayardaki dosya ve klasörleri değiştirmesine olanak tanıyan metin tabanlı arayüz olan komut satırını kapsamlı bir şekilde kullanır. Terminal olarak da bilinen komut satırı, Gatsby’nin uygulama için temel olarak sağladığı local development ortamıyla etkileşim kurmanın birincil yoludur.</p>

<p>Local makinenizde komut satırını bulmak için:</p>

<ul>
  <li>macOS’ta Launchpad’i açın ve “Terminal” yazarak uygulamayı başlatın.</li>
  <li>Windows 10’da Başlat düğmesine tıklayın ve cmd yazın. Ardından, döndürülen arama sonuçlarından Komut İstemi’ni seçin.</li>
  <li>Linux’te masaüstündeki application menüsünü açın, uygulamaları aramak için “Terminal” yazın ve Terminal’i seçin.</li>
</ul>

<p>Her üç komut satırında da, komutu yazıp Enter tuşuna basarak bir komutu çalıştırabilirsiniz (örneğin, mevcut çalışma dizininin yolunu terminale yazdıran pwd veya bir dizine girmek için dizini değiştiren cd).</p>

<h3 id="gatsby-cliyi-yükleme">Gatsby CLI’yi Yükleme</h3>

<p>Gatsby CLI, geliştiricilerin Gatsby siteleriyle etkileşimde bulunmak için kullanabileceği birincil araçtır. Gatsby sitelerini çok hızlı bir şekilde oluşturabilen ve çalıştırabilen bir araçtır ve Gatsby sitelerini geliştirmek için yararlı olan çeşitli komutlar içerir. Bir NPM paketi olarak yayınlandığından, Gatsby CLI, Node.js’nin yüklü olduğu herhangi bir bilgisayara kurulabilir.</p>

<p>Gatsby CLI’yi kurmak için aşağıdaki NPM komutunu çalıştırın. Local makinenizin dosya sisteminin herhangi bir yerinde yeni Gatsby sitelerini oluşturabilmeniz için Gatsby CLI’yi global olarak -g flag’ini kullanarak kurmanız önemle tavsiye edilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm install -g gatsby-cli
</code></pre></div></div>

<p><strong>Not</strong>: Gatsby CLI’yi ilk yüklediğinizde, framework’ün gelecekteki gelişimine rehberlik etmek üzere Gatsby komutlarının anonim kullanım verilerini toplayan Gatsby telemetrisi hakkında sizi bilgilendiren bir mesaj görünecektir. Telemetri verilerinin toplanmasını önlemek için aşağıdaki komutu kullanabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby telemetry --disable
</code></pre></div></div>

<p>Gatsby CLI’de kullanabileceğiniz tüm komutları görmek için help komutunu çalıştırın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby --help
</code></pre></div></div>

<p>Artık ilk Gatsby komutunuzu başarıyla çalıştırdığınıza göre, Gatsby CLI’yi kullanarak ilk Gatsby sitenizi oluşturmaya hazırsınız. Siteyi sıfırdan oluşturmak yerine Gatsby’nin “Merhaba Dünya” starter’ı ile başlayacağız.</p>

<h2 id="i̇lk-gatsby-sitenizi-oluşturun">İlk Gatsby Sitenizi Oluşturun</h2>

<p>Gatsby starter’ları, bazı başlangıç yapılandırmalarına sahip, kısmen tamamlanmış Gatsby sitelerini içeren standart template’lerdir. Starter’lar, belirli bir kullanım durumu için yeni bir site oluşturmayı mümkün olduğunca sorunsuz ve kolay hale getirecek şekilde tasarlanmıştır. “Hello World” starter, tipik bir Gatsby sitesi için temel düzeydeki gereksinimleri içerir.</p>

<p>“Hello World” starter’ını temel alan yeni bir site oluşturmak için, terminalinizi açın ve ardından belirttiğiniz dizinde yeni bir proje oluşturan gatsby new komutunu çalıştırın (komutu herhangi bir argüman olmadan çalıştırırsanız, Gatsby CLI belirli parametreleri sağlayabileceğiniz etkileşimli bir shell açacaktır):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby new hello-world \ https://github.com/gatsbyjs/gatsby-starter-hello-world
</code></pre></div></div>

<p>İsteğe bağlı bir dizin adı girebilirsiniz; eğer dizin mevcut değilse, Gatsby CLI devam edip dizini sizin için oluşturacaktır; örneğin:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby new my-new-gatsby-site \ https://github.com/gatsbyjs/gatsby-starter-hello-world
</code></pre></div></div>

<p>Komutun https:// ile başlayan son kısmı, starter kodunun bulunduğu URL’dir. Gatsby, starter kod repolarını yönetmek için GitHub’u kullanıyor.</p>

<p>Artık dizine geçebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd hello-world
</code></pre></div></div>

<p>Dizine farklı bir ad verdiyseniz bunun yerine şunu kullanın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd my-new-gatsby-site
</code></pre></div></div>

<p>“Hello World” starter’ını kapsayan dosya ve dizinleri görmek için list komutunu (ls) verebilirsiniz. Yeni değiştirdiğiniz dizinde aşağıdaki komutu çalıştırarak, tipik bir Gatsby sitesinin kök klasörünün nasıl göründüğünü görebileceksiniz. -la flag’i, terminale, adları nokta (.) ile başlayan dosyalar da dahil olmak üzere tüm dosyaları ve bunlarla ilgili belirli ayrıntıları göstermesi talimatını verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ls -la
</code></pre></div></div>

<p>Bu komutun çıktısı projedeki dosya ve dizinlerin bir listesidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>total 2240
drwxr-xr-x   13 fox  staff      416 27 Mar 16:47 .
drwxr-xr-x@   4 fox  staff      128 27 Mar 16:39 ..
-rw-r--r--@   1 fox  staff     6148 27 Mar 16:47 .DS_Store
drwxr-xr-x   12 fox  staff      384 27 Mar 16:39 .git
-rw-r--r--    1 fox  staff      974 27 Mar 16:38 .gitignore
-rw-r--r--    1 fox  staff      675 27 Mar 16:38 LICENSE
-rw-r--r--    1 fox  staff     5657 27 Mar 16:38 README.md
-rw-r--r--    1 fox  staff      219 27 Mar 16:38 gatsby-config.js
drwxr-xr-x  863 fox  staff    27616 27 Mar 16:39 node_modules
-rw-r--r--    1 fox  staff  1074190 27 Mar 16:39 package-lock.json
-rw-r--r--    1 fox  staff      703 27 Mar 16:38 package.json
drwxr-xr-x    4 fox  staff      128 27 Mar 16:47 src
drwxr-xr-x    3 fox  staff       96 27 Mar 16:38 static
</code></pre></div></div>

<p>Ama işimiz daha bitmedi! Bir Gatsby sitesinin dosya ve dizinlerinin listesini görmek yeterli değildir; onu bir tarayıcıda görmek isteriz. Ancak Gatsby sitemizi hâlâ çevrimdışıyken tarayıcıda görüntülemek için local development sunucusunu çalıştırmamız gerekecek. Bunu daha sonra yapacağız.</p>

<h3 id="development-sunucusunu-başlatma">Development Sunucusunu Başlatma</h3>

<p>Henüz çevrimiçi olmayan bir Gatsby sitesini web tarayıcısında görüntülemek için, tarayıcının bu Gatsby sitesine erişebilmesini sağlayacak bir yol bulmamız gerekir. Bu, çevrimiçi web sitesini taklit edebilecek ve Gatsby sitemizi tarayıcıda ziyaret etme deneyimine mümkün olduğunca yakın olabilecek bir local development sunucusu kurmamız gerektiği anlamına gelir.</p>

<p>Gatsby CLI, sizin için local development sunucusu başlatan bir development modu içerir. Development moduna girmek ve local development sunucusunu başlatmak için Gatsby sitenizin kök dizininde aşağıdaki komutu çalıştırın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby develop
</code></pre></div></div>

<p>Gatsby CLI’den görüntülenecek mesajların bir parçası olarak http://localhost:8000 gibi bir URL göreceksiniz. Burada localhost, Gatsby’nin “Hello World” sitesine, herhangi bir yerde çevrimiçi olarak harici bir URL’den getirmek yerine, local makinemizde oluşturduğunuz “Hello World” sitesine eriştiğimiz anlamına geliyor. Bu, localhost URL’lerine erişmek için internet bağlantısına ihtiyacımız olmadığı anlamına gelir.</p>

<p>İstediğiniz tarayıcıyı açın ve http://localhost:8000 adresine veya terminalinizde belirtilen URL’ye gidin. Orada “Hello world!” mesajını göreceksiniz. İyi iş çıkardınız; Aşağıda görebileceğiniz gibi, ilk Gatsby sitenizi oluşturdunuz!</p>

<p><img src="/assets/images/2024/03/gatsby-develop.png" alt="Gatsby develop command" title="Gatsby develop command" class="img-fluid" /></p>

<p>Terminale geri döndüğünüzde, gatsby develop’un hala çalışmakta olduğuna dikkat edin; Aşağıda gösterildiği gibi komut sonlandırılmadı. Bu, local development sunucusunu ihtiyaç duyduğunuz süre boyunca kullanmaya devam edebilmenizi sağlamak içindir. Gatsby development çalıştığı sürece, local development sunucusu localhost URL’sinde mevcut olacak ve kodda yaptığınız değişikliklerin büyük çoğunluğu tarayıcıya anında yansıtılacaktır.</p>

<p><img src="/assets/images/2024/03/gatsby-localhost.png" alt="Gatsby localhost" title="Gatsby localhost" class="img-fluid" /></p>

<p>Hot reloading’i çalışırken görmek için src/pages/index.js dosyasını açın ve “Hello world!” yazısını değiştirin. Dosyanızı gatsby development hala çalışırken kaydederseniz, tarayıcının yeniden yüklenmesine gerek kalmadan değişikliğin anında etkili olduğunu göreceksiniz!</p>

<p>Development sunucusunu çalıştırmayı durdurmak için terminal penceresinde Ctrl-C tuşlayın. Bu, komutu sonlandıracak, local development sunucusunu kapatacak ve siteye tarayıcı aracılığıyla erişilemez hale gelecektir. Local development sunucusunu yeniden başlatmak için <code class="language-plaintext highlighter-rouge">gatsby develop</code> komutunu yeniden çalıştırın.</p>

<h3 id="production-build-oluşturma">Production Build Oluşturma</h3>

<p>İlk Gatsby sitemizi kurmuş olsak da işimiz henüz bitmedi. Şu ana kadar bir web sitesinin development sunucusunda local olarak çalışmasını sağladık ancak bu, web sitemizin çevrimiçi olduğu anlamına gelmiyor. Bunun için Gatsby sitemizi, herkesin siteye erişebildiği, bir web sunucusuna sahip olan production ortamına deploy etmemiz gerekiyor. Ek olarak, bir Gatsby sitesini production’a hazır hale getirecek ve mümkün olduğunca küçültecek bir Gatsby build’i oluşturmamız gerekiyor, böylece herhangi bir performans sorunu yaşanmaz.</p>

<p>Production build oluşturmak, geliştirmeye hazır bir Gatsby sitesini, HTML, CSS, JavaScript ve resimler gibi statik varlıklardan oluşan, production’a hazır statik bir Gatsby sitesine dönüştürmek anlamına gelir. Production build oluşturmak için, Gatsby site dizininizin kökünde aşağıdaki komutu çalıştırın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby build
</code></pre></div></div>

<p>Bu komut, local ve işlenmiş statik dosyaları, production ortamına deploy etmeye hazır şekilde public dizinine depolayacaktır.</p>

<h3 id="production-buildi-local-olarak-sunmak">Production Build’i Local Olarak Sunmak</h3>

<p>Artık bu statik siteyi kendi seçeceğimiz bir altyapı sağlayıcısına deploy edebiliriz veya yerel olarak deploy edip tarayıcıda görüntüleyebiliriz. Production sitenizi local olarak görüntülemek için local development sunucusunu başlatan aşağıdaki komutu çalıştırın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gatsby serve
</code></pre></div></div>

<p>Bu portu başka bir şey için kullanmıyorsanız, site https://localhost:9000 adresinde görüntülenebilir (local development sunucusundan farkına dikkat edin). İşte karşınızda: ilk Gatsby siteniz, local development veya production dağıtımına hazır!</p>

<p><strong>Kaynak:</strong> “Gatsby: The Definitive Guide”, Preston So</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Gatsby, React’ı temel alan, statik siteler oluşturmaya yönelik açık kaynaklı bir framework’tür. Genellikle static site generator (SSG) olarak anılan Gatsby, Gridsome gibi diğer SSG’lerin ve statik site oluşturmayı kolaylaştıran Next.js gibi JavaScript framework’lerinin yanı sıra Jamstack teknoloji kategorisinin bir parçasıdır. Gatsby performansa, ölçeklenebilirliğe ve güvenliğe özellikle önem veriyor. Pluginler, temalar, starter’lar ve katkıda bulunulan diğer projelerden oluşan zengin ve büyüyen bir ekosisteme sahip.]]></summary></entry><entry><title type="html">Windows’ta PHP CLI versiyonu nasıl değiştirilir?</title><link href="http://localhost:4000/2024/03/28/windows-ta-php-cli-versiyonu-nasil-degistirilir.html" rel="alternate" type="text/html" title="Windows’ta PHP CLI versiyonu nasıl değiştirilir?" /><published>2024-03-28T00:00:00+03:00</published><updated>2024-03-28T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/28/windows-ta-php-cli-versiyonu-nasil-degistirilir</id><content type="html" xml:base="http://localhost:4000/2024/03/28/windows-ta-php-cli-versiyonu-nasil-degistirilir.html"><![CDATA[<p>Son zamanlarda Laravel 10 ile bir uygulama geliştiriyorum. Laravel 10 ile çalışabilmek için PHP CLI’mı yükseltmem gerekiyor. Şu anda bilgisayarımda PHP 7.0.33 (cli) yüklü. Terminalde kendi PHP CLI’nizi aşağıdaki gibi kontrol edebilirsiniz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>php -v
PHP 7.0.33 (cli) (built: Dec  5 2018 21:22:29) ( ZTS )
Copyright (c) 1997-2017 The PHP Group
Zend Engine v3.0.0, Copyright (c) 1998-2017 Zend Technologies
</code></pre></div></div>

<p>Şimdi Ortam Değişkenleri altında Sistem değişkenleri bölümündeki PHP CLI yolunu değiştireceğim. Bu şekilde PHP CLI, composer, nodejs’e ve daha fazlasına bilgisayarımın her yerinden erişebilirim.</p>

<h2 id="ortam-değişkenlerine-erişim">Ortam Değişkenlerine Erişim</h2>

<p>Ortam değişkenlerine erişmek için Başlat menüsüne tıklayıp “ortam değişkenleri” yazmanız yeterlidir. Ortam değişkenlerine şuradan erişebilirsiniz:</p>

<p><img src="/assets/images/2024/03/ortam-degiskenleri.png" alt="Ortam Değişkenleri" title="Ortam Değişkenleri" class="img-fluid" /></p>

<p>“Ortam Değişkenleri” butonuna tıkladıktan sonra aşağıdaki popup pencereyi göreceksiniz.</p>

<p><img src="/assets/images/2024/03/sistem-degiskenleri.png" alt="Sistem Değişkenleri" title="Sistem Değişkenleri" class="img-fluid" /></p>

<p>Ortam değişkenlerini düzenleyin.</p>

<h2 id="php-cli-sürümünü-değiştirin">PHP CLI Sürümünü Değiştirin</h2>

<p>Path değişkenini düzenleyin. İstediğiniz PHP CLI sürümünü Path değişkenine eklemeniz yeterlidir.</p>

<p><img src="/assets/images/2024/03/ortam-degiskenini-duzenle.png" alt="Ortam Değişkenini Düzenle" title="Ortam Değişkenini Düzenle" class="img-fluid" /></p>

<p>WampServer’da birden fazla PHP sürümüne sahibim. PHP CLI sürümünü değiştirmek için 7.0.33 olan PHP verisyonunu 8.1.8 olarak düzenledim.</p>

<p>Path değişkenimi, “C:\wamp64\bin\php\php7.0.33” yerine “C:\wamp64\bin\php\php8.1.8” olarak değiştiriyorum.</p>

<p>Açık olan tüm terminalleri ve VSCode’u kapatın. Daha sonra aşağıdaki gibi PHP CLI verisyonunu kontrol edin. Şimdi değiştirdiğiniz PHP CLI sürümünü görmelisiniz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PS C:\Users\melih&gt; php -v
PHP 8.1.8 (cli) (built: Jul  5 2022 23:04:29) (ZTS Visual C++ 2019 x64)
Copyright (c) The PHP Group
Zend Engine v4.1.8, Copyright (c) Zend Technologies
</code></pre></div></div>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Son zamanlarda Laravel 10 ile bir uygulama geliştiriyorum. Laravel 10 ile çalışabilmek için PHP CLI’mı yükseltmem gerekiyor. Şu anda bilgisayarımda PHP 7.0.33 (cli) yüklü. Terminalde kendi PHP CLI’nizi aşağıdaki gibi kontrol edebilirsiniz.]]></summary></entry><entry><title type="html">PHP - Nesne Araçları</title><link href="http://localhost:4000/2024/03/23/php-nesne-araclari.html" rel="alternate" type="text/html" title="PHP - Nesne Araçları" /><published>2024-03-23T00:00:00+03:00</published><updated>2024-03-23T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/23/php-nesne-araclari</id><content type="html" xml:base="http://localhost:4000/2024/03/23/php-nesne-araclari.html"><![CDATA[<p>Gördüğümüz gibi PHP, sınıflar ve metotlar gibi dil yapıları aracılığıyla nesne yönelimli programlamayı destekler. Dil ayrıca nesnelerle çalışmanıza yardımcı olmak için tasarlanmış fonksiyon ve sınıflar aracılığıyla daha geniş destek sağlar.</p>

<p>Bu bölümde, nesneleri ve sınıfları düzenlemek, test etmek ve değiştirmek için kullanabileceğiniz bazı araçlara ve tekniklere bakacağız.</p>

<p>Bu bölüm aşağıdaki araçları ve teknikleri kapsayacaktır:</p>

<ul>
  <li><strong>Namespace</strong>: Kodunuzu ayrı paket benzeri bölümler halinde düzenleyin</li>
  <li><strong>Include path</strong>: Kod kütüphaneleriniz için merkezi erişilebilir konumları ayarlama</li>
  <li><strong>Sınıf ve nesne fonksiyonları</strong>: Nesneleri, sınıfları, property ve metotları test etmeye yönelik fonksiyonlar</li>
  <li><strong>Reflection API</strong>: Çalışma zamanında sınıf bilgilerine benzeri görülmemiş erişim sağlayan yerleşik sınıflardan oluşan güçlü bir paket</li>
  <li><strong>Nitelikler (Attributes)</strong>: PHP’nin annotation uygulaması - sınıfların, metotların, property ve parametrelerin kodda etiketler kullanılarak zengin bilgilerle geliştirilebildiği bir mekanizma</li>
</ul>

<h2 id="php-ve-paketler">PHP ve Paketler</h2>

<p>Bir paket, genellikle bir şekilde birlikte gruplandırılmış, ilgili sınıflar ve fonksiyonlar kümesidir. Paketler, bir sistemin parçalarını birbirinden ayırmak için kullanılabilir. Bazı programlama dilleri, paketleri resmi olarak tanır ve onlara farklı namespace’ler (ad alanları) sağlar. PHP’nin native bir paket kavramı yoktur, ancak PHP 5.3’ten itibaren namespace’leri tanıttı. Bu özelliği bir sonraki bölümde inceleyeceğiz. Ayrıca, sınıfları paket benzeri yapılar halinde organize etmenin eski yoluna da bir göz atacağım.</p>

<h3 id="php-paketleri-ve-namespaceler">PHP Paketleri ve Namespace’ler</h3>

<p>PHP paket kavramını özünde desteklemese de, geliştiriciler geleneksel olarak kodlarını paket benzeri yapılar halinde düzenlemek için hem adlandırma şemalarını hem de dosya sistemini kullandılar.</p>

<p>PHP 5.3’e kadar, geliştiriciler dosyalarını paylaşılan bir bağlamda adlandırmak zorundaydılar. Başka bir deyişle, bir sınıfa ShoppingBasket adını verirseniz, sisteminizde anında kullanılabilir hale gelir. Bu iki büyük soruna neden oldu. Birincisi ve en zarar verici olanı, çakışmaları adlandırma olasılığıydı. Sonuçta, tek yapmanız gereken tüm sınıflarınıza benzersiz isimler vermeyi unutmamak, değil mi? Sorun şu ki, hepimiz kod kütüphanelerine giderek daha fazla güveniyoruz. Bu elbette iyi bir şey çünkü kodun yeniden kullanımını teşvik ediyor. Ancak projenizin şunu yaptığını varsayalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once __DIR__ . "/../useful/Outputter.php";

class Outputter
{
    // output data
}
</code></pre></div></div>

<p>Şimdi, dahil edilen dosyayı useful/Outputter.php adresinde birleştirdiğinizi varsayalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Outputter
{
    //
}
</code></pre></div></div>

<p>Peki, ne olacağını tahmin edebiliyorsun, değil mi? Şu olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PHP Fatal error: Cannot declare class Outputter because the name is
already in use in /var/popp/src/ch05/batch01/useful/Outputter.php on
line 4
</code></pre></div></div>

<p>Namespace’lerin tanıtılmasından önce, bu soruna geleneksel bir geçici çözüm vardı. Cevap, paket adlarını sınıf adlarının başına eklemekti, böylece sınıf adlarının benzersiz olması garanti altına alındı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// my/Outputer.php
require_once __DIR __ . "/../useful/Outputter.php";

class my_Outputter
{
    // output data
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// useful/Outputter.php
class useful_Outputter
{
    //
}
</code></pre></div></div>

<p>Buradaki sorun, daha fazla proje dahil oldukça, sınıf isimlerinin daha da uzamasıydı. Çok büyük bir sorun değildi, ancak kodun okunabilirliğiyle ilgili sorunlara yol açtı ve çalışırken sınıf isimlerini kafanızda tutmayı zorlaştırdı. Kodlama için harcanan zamanın bir kısmı yazım hataları nedeniyle kaybedildi.</p>

<p>Eski kodunuzu koruyorsanız, bu kuralı izleyen kodlar görmeye devam edebilirsiniz. Bu nedenle, bu bölümün ilerleyen kısımlarında paketleri işlemenin eski yöntemine kısaca geri döneceğim.</p>

<p><strong>Hayat Kurtaran Namespace</strong></p>

<p>PHP 5.3, namespace’leri tanıttı. Özünde namespace, sınıflarınızı, fonksiyonlarınızı ve değişkenlerinizi yerleştirebileceğiniz bir kovadır. Namespace içinde, bu öğelere nitelik olmadan erişebilirsiniz. Dışarıdan, içerdiği öğelere erişmek için namespace’i içe aktarmalı veya ona başvurmalısınız.</p>

<p>Kafanız mı karıştı? Bir örnek yardımcı olabilir. Burada, namespace kullanarak önceki örneği yeniden yazıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace my;
require_once __DIR__ . "/../useful/Outputter.php";

class Outputter
{
    // output data
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace useful;

class Outputter
{
    //
}
</code></pre></div></div>

<p>namespace anahtar sözcüğüne dikkat edin. Tahmin edebileceğiniz gibi, bu anahtar sözcük bir namespace oluşturur. Bu özelliği kullanıyorsanız, namespace bildirimi, dosyadaki ilk ifade olmalıdır. İki namespace’lerimi oluşturdum: my ve useful. Bununla birlikte, tipik olarak, daha derin namespace’lere sahip olmak isteyeceksiniz. Bir organizasyon veya proje tanımlayıcısı ile başlayacaksınız. PHP, iç içe namespace bildirmenize izin verir. Bunu yapmak için, her seviyeyi bölerken ters eğik çizgi karakteri kullanmanız yeterlidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04\util;

class Debug
{
    public static function helloWorld(): void
    {
        print "hello from Debug\n";
    }
}
</code></pre></div></div>

<p>Bir repository tanımlamak için genellikle bir ürün veya organizasyonla ilgili bir ad kullanırsınız. Domain’lerimden birini kullanabilirim: örneğin getinstance.com. Bir domain, sahibine özel olduğundan, bu, Java geliştiricilerinin genellikle paket adları için kullandıkları bir numaradır. Domain adlarını en genelden en özele doğru çalışacak şekilde ters çevirirler. Alternatif olarak, bu kitaptaki kod örnekleri için seçtiğim namespace’leri kullanabilirim: kitap adı için popp. Repomu belirledikten sonra, paketleri tanımlamaya devam edebilirim. Bu durumda, bölümü ve ardından numaralandırılmış bir grubu kullanıyorum. Bu, örnek gruplarını ayrı kovalar halinde düzenlememe izin veriyor. Yani bölümün bu noktasında, popp\ch05\batch04’teyim. Son olarak, kodu kategoriye göre de düzenleyebilirim.</p>

<p>Peki metodu nasıl çağırırım? Aslında, çağrıyı nereden yaptığınıza bağlıdır. Metodu namespace içinden çağırıyorsanız, devam edip doğrudan çağırabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Debug::helloWorld();
</code></pre></div></div>

<p>Bu, niteliksiz (unqualified) ad olarak bilinir. Zaten popp\ch05\batch04\util namespace’inde olduğum için, sınıf adının başına herhangi bir yol eklemem gerekmiyor. Sınıfa namespace dışından erişiyor olsaydım, şunu yapabilirdim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\popp\ch05\batch04\Debug::helloworld();
</code></pre></div></div>

<p>Aşağıdaki koddan nasıl bir çıktı alırdım?</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace main;

popp\ch05\batch04\util\Debug::helloworld();
</code></pre></div></div>

<p>Bu hileli bir soru. Aslında, şu benim çıktım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PHP Fatal error: Class 'popp\ch05\batch04\Debug' not found in...
</code></pre></div></div>

<p>Çünkü burada göreceli (relative) bir namespace kullanıyorum. PHP, popp\ch05\batch04\util\Debug için main namespace’inin altına bakıyor ve bulamıyor. Tıpkı bir separatörle başlayarak mutlak (absolute) URL’ler ve dosya yolları oluşturabileceğiniz gibi, namespace’lerde tanımlayabilirsiniz. Örneğin bu sürüm, önceki hatayı düzeltir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace main;

\popp\ch05\batch04\util\Debug::helloworld();
</code></pre></div></div>

<p>Bu baştaki ters eğik çizgi, PHP’ye aramaya geçerli namespace’ten değil, kökten başlamasını söyler.</p>

<p>Ancak namespace’lerin kod yazmayı azaltmanıza yardımcı olması gerekmiyor mu? Debug sınıfı bildirimi kesinlikle daha kısadır, ancak bu çağrılar, eski adlandırma kuralında olduğu kadar endişe vericidir. Bunu use anahtar sözcüğü ile çözebilirsiniz. Bu, geçerli namespace içindeki diğer namespace’lere takma ad vermenize olanak tanır. İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace main;

use popp\ch05\batch04\util;

util\Debug::helloWorld();
</code></pre></div></div>

<p>popp\ch05\batch04\util içe aktarılır ve dolaylı olarak util’e takma ad verilir. Başta bir ters eğik çizgi karakteriyle başlamadığıma dikkat edin. Kullanılacak argüman, geçerli namespace’te değil, kök alanda aranır. Bir namespace’e ihtiyacım yoksa, Debug sınıfının kendisini içe aktarabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace main;

use popp\ch05\batch04\util\Debug;

Debug::helloWorld();
</code></pre></div></div>

<p>Bu, en sık kullanılan şeklidir. Ancak, çağıran namespace’te zaten bir Debug sınıfım olsaydı ne olurdu?</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04;

class Debug {
    public static function helloWorld(): void
    {
        print "hello from popp\\ch05\\batch04\\Debug\n";
    }
}
</code></pre></div></div>

<p>Ve burada popp\ch05\batch04 namespace’inden gelen ve her iki Debug sınıfına başvuran kodlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04;

use popp\ch05\batch04\util\Debug;
use popp\ch05\batch04\Debug;

Debug::helloWorld();
</code></pre></div></div>

<p>Tahmin edebileceğiniz gibi, bu önemli bir hataya neden olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PHP Fatal error: Cannot use popp\ch05\batch04\Debug as Debug because the
name is already in use in...
</code></pre></div></div>

<p>Bu yüzden, namespace çakışmalarına geri dönerek tam bir daire çizmiş gibiyim. Neyse ki, bu sorunun bir cevabı var. Takma adımı açık hale getirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04;

use popp\ch05\batch04\util\Debug;
use popp\ch05\batch04\Debug as CoreDebug;

CoreDebug::helloWorld();
</code></pre></div></div>

<p>as ifadesi ile, Debug takma adını coreDebug olarak değiştirebiliyorum.</p>

<p>Bir namespace’e kod yazıyorsanız ve kök (namespace olmayan) alanda bulunan bir sınıf, trait veya arayüze (örneğin, Exception, Error, Closure gibi PHP’nin çekirdek sınıfları) erişmek istiyorsanız, ters eğik çizgi ile adını kullanın. İşte kök uzayda bildirilen bir sınıf:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class TreeLister
{
    public static function helloWorld(): void
    {
        print "hello from root namespace\n";
    }
}
</code></pre></div></div>

<p>İşte bazı namespace’li kodlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04\util;

class TreeLister
{
    public static function helloWorld(): void
    {
        print "hello from " . __NAMESPACE__ . "\n";
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch04;

use popp\ch05\batch04\util\TreeLister;

TreeLister::helloWorld(); // access local
\TreeLister::helloWorld(); // access from root
</code></pre></div></div>

<p>Namespace’li kod, kendi TreeLister sınıfını bildirir. Yukarıdaki kod, use ifadesiyle tam yolu belirterek local versiyonu kullanır. Ters eğik çizgi ile nitelendirilen bir ad, kök namespace’te benzer şekilde adlandırılmış bir sınıfa erişir.</p>

<p>İşte önceki kodun çıktısı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hello from popp\ch05\batch04\util
hello from root namespace
</code></pre></div></div>

<p>Bu çıktı, <code class="language-plaintext highlighter-rouge">__NAMESPACE__</code> sabitinin işleyişini gösterdiği için gösterilmeye değer. Bu, mevcut namespace’i verir ve hata ayıklamada kullanışlıdır.</p>

<p>Daha önce gördüğünüz sözdizimini kullanarak aynı dosyada birden fazla namespace bildirebilirsiniz. Ayrıca, namespace anahtar sözcüğüyle parantez kullanan alternatif bir sözdizimi de kullanabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace com\getinstance\util {
    class Debug
    {
        public static function helloWorld(): void
        {
            print "hello from Debug\n";
        }
    }
}

namespace other {
    \com\getinstance\util\Debug::helloWorld();
}
</code></pre></div></div>

<p>Birden çok namespace’i aynı dosyada birleştirmeniz gerekiyorsa, önerilen uygulama budur. Ancak genellikle, namespace’leri dosya bazında tanımlamak en iyi uygulama olarak kabul edilir.</p>

<p><strong>Not</strong>: Parantez ve satır namespace sözdizimlerini aynı dosyada kullanamazsınız. Birini seçmeli ve ona bağlı kalmalısınız.</p>

<p><strong>Paketleri Simüle Etmek İçin Dosya Sistemini Kullanmak</strong></p>

<p>Hangi PHP sürümünü kullanırsanız kullanın, sınıfları bir tür paket yapısı sağlayan dosya sistemini kullanarak düzenlemelisiniz. Örneğin, util ve business dizinleri oluşturabilir ve require_once() ifadesiyle sınıf dosyalarını şu şekilde dahil edebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once('business/Customer.php');
require_once('util/WebTools.php');
</code></pre></div></div>

<p>Aynı şekilde include_once() fonksiyonunu da kullanabilirsiniz. include() ve require() ifadeleri arasındaki tek fark, hataları ele alma biçimleridir. require() kullanılarak çağrılan bir dosya, bir hatayla karşılaştığınızda tüm sürecinizi çökertir. include() çağrısı sırasında karşılaşılan aynı hata, yalnızca bir uyarı oluşturacak, dahil edilen dosyanın yürütülmesini sonlandıracak ve çağıran kodu devam edecek şekilde bırakacaktır. require() ve require_once() kütüphane dosyalarını dahil etmek için güvenlidir ve include() ve include_once() template oluşturma gibi işlemler için kullanışlıdır.</p>

<p><strong>Not</strong>: require() ve require_once() aslında ifadedir, fonksiyon değildir. Bu, parantezleri kullanırken atlayabileceğiniz anlamına gelir. Şahsen, ben yine de parantez kullanmayı tercih ederim, ama aynı şeyi yaparsanız, hatanızı açıklamaya hevesli ukalalar tarafından sıkılmaya hazırlıklı olun.</p>

<p><img src="/assets/images/2024/03/php-packages-organized-using-the-file-system.png" alt="Dosya sistemi kullanılarak düzenlenen PHP paketleri" title="Dosya sistemi kullanılarak düzenlenen PHP paketleri" class="img-fluid" /></p>

<p>PHP söz konusu olduğunda, bu yapı hakkında özel bir şey yoktur. Kütüphanelerinizi farklı dizinlere yerleştirmeniz yeterlidir. Temiz organizasyona katkıda bulunur ve namespace veya bir adlandırma kuralı ile paralel olarak kullanılabilir.</p>

<p><strong>PEAR Yolunun Adlandırılması</strong></p>

<p>Namespace’ler tanıtılmadan önce, geliştiriciler sınıf adı çakışmalarını önlemek için kurallara başvurmak zorunda kalıyorlardı. Gördüğümüz gibi bunlardan en yaygın olanı, PEAR geliştiricileri tarafından sağlanan sahte namespace’ti.</p>

<p>Not: PEAR, PHP uzantısını ve uygulama reposunu ifade eder. PHP’nin işlevselliğine katkıda bulunan paketlerin ve araçların resmi olarak tutulan arşividir. Çekirdek PEAR paketleri PHP dağıtımına dahildir ve diğerleri basit bir komut satırı aracı kullanılarak eklenebilir. PEAR paketlerine <a href="http://pear.php.net">http://pear.php.net</a> adresinden göz atabilirsiniz.</p>

<p>PEAR, tarif ettiğim gibi paketlerini tanımlamak için dosya sistemini kullanır. Namespace’ler tanıtılmadan önce, her sınıf, paket yoluna göre, her dizin adı bir alt çizgi karakteriyle ayrılmış olarak adlandırılıyordu.</p>

<p>Örneğin, PEAR, bir RPC alt paketine sahip olan XML adlı bir paket içerir. RPC paketi, Server.php adlı bir dosya içerir. Server.php içinde tanımlanan sınıf, tahmin edebileceğiniz gibi Server olarak adlandırılmaz. Namespace olmadan, bu er ya da geç PEAR projesinin herhangi bir yerindeki veya bir kullanıcının kodundaki başka bir Server sınıfıyla çakışır. Bunun yerine sınıf, XML_RPC_Server olarak adlandırılır. Bu yaklaşım, çekici olmayan sınıf adları için yapılmıştır. Bununla birlikte, sınıf adı her zaman kendi bağlamını tanımladığı için kodun okunmasını kolaylaştırdı.</p>

<p><strong>Include Path</strong></p>

<p>Bileşenlerinizi düzenlerken aklınızda bulundurmanız gereken iki bakış açısı vardır. Dosyaların ve dizinlerin dosya sistemine yerleştirildiği ilk konuyu ele aldım. Ancak bileşenlerin birbirine erişme şeklini de göz önünde bulundurmalısınız. Bu bölümde şu ana kadar include path konusuna değindim.</p>

<p>Bir dosya eklediğinizde, geçerli çalışma dizininden göreli bir yol veya dosya sistemindeki mutlak bir yol kullanarak ona başvurabilirsiniz.</p>

<p><strong>Not</strong>: Include path’in nasıl çalıştığını ve gerekli dosyalarla ilgili sorunları anlamak önemli olsa da, birçok modern sistemin artık sınıf düzeyinde require deyimlerine dayanmadığını akılda tutmak da önemlidir. Bunun yerine, autoload (otomatik yükleme) ve namespace’in bir kombinasyonunu kullanırlar. Autoload’ı daha sonra ele alacağım ve ardından 15. ve 16. bölümlerde pratik autoload önerilerine ve araçlarına daha ayrıntılı olarak bakacağım.</p>

<p>Şimdiye kadar gördüğünüz örnekler, zaman zaman zorunlu ve gerekli dosyalar arasında sabit bir ilişki belirledi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once __DIR__ . "/../useful/Outputter.php";
</code></pre></div></div>

<p>Bu, dosyalar arasındaki ilişkinin hardcoding olması dışında oldukça iyi çalışır. Çağıran sınıfın bulunduğu dizinin yanında her zaman bir useful dizini olmalıdır.</p>

<p>Belki de en kötü yaklaşım, dolambaçlı göreceli yoldur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once('../../projectlib/business/User.php');
</code></pre></div></div>

<p>Bu problemlidir çünkü burada belirtilen yol, require_once ifadesini içeren dosyaya göre değil, yapılandırılmış bir çağrı bağlamına (çoğu zaman, ancak her zaman değil, geçerli çalışma dizini) bağlıdır. Bunun gibi dosya yolları kafa karışıklığına yol açar (ve deneyimlerime göre neredeyse her zaman bir sistemin diğer alanlarda da önemli ölçüde iyileştirmeye ihtiyaç duyacağının bir işaretidir).</p>

<p>Elbette mutlak bir yol kullanabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once('/home/john/projectlib/business/User.php');
</code></pre></div></div>

<p>Bu, tek bir örnek için işe yarayacaktır - ancak kırılgandır. Yolları bu kadar ayrıntılı belirterek, kütüphane dosyasını belirli bir bağlamda dondurursunuz. Projeyi yeni bir sunucuya her yüklediğinizde, tüm gerekli ifadelerin yeni dosya yolunu hesaba katmak için değiştirilmesi gerekecektir. Bu, kütüphanelerin yerinin değiştirilmesini zorlaştırabilir ve kopyalamadan projeler arasında paylaşılması pratik olmayabilir. Her iki durumda da, tüm ek dizinlerde paket fikrini kaybedersiniz. business paketi mi, yoksa projectlib/business paketi mi?</p>

<p>Dosyaları kodunuza manuel olarak dahil etmeniz gerekiyorsa, en temiz yaklaşım, çağıran kodu kütüphaneden ayırmaktır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require_once('business/User.php');
</code></pre></div></div>

<p>Bunun gibi bir yol kullanan önceki örneklerde, dolaylı olarak göreli yol kullandık. Başka bir deyişle, business/User.php işlevsel olarak ./business/User.php ile aynıdır. Ancak, önceki require ifadesi sistemdeki herhangi bir dizinden çalıştırılabilir? Bunu include path ile yapabilirsiniz. Bu, PHP’nin bir dosya bulmaya çalışırken aradığı dizinlerin listesidir. include_path yönergesini değiştirerek bu listeye ekleyebilirsiniz. include_path genellikle PHP’nin merkezi yapılandırma dosyası php.ini’de ayarlanır. Unix benzeri sistemlerde iki nokta üst üste ve Windows sistemlerinde noktalı virgülle ayrılmış bir dizin listesi tanımlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>include_path = ".:/usr/local/lib/php-libraries"
</code></pre></div></div>

<p>Apache kullanıyorsanız, yapılandırma dosyasında (genellikle httpd.conf olarak adlandırılır) veya dizindeki bir Apache yapılandırma dosyasında (genellikle .htaccess olarak adlandırılır) include_path’i şu sözdizimiyle de ayarlayabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>php_value include_path value .:/usr/local/lib/php-libraries
</code></pre></div></div>

<p><strong>Not</strong>: .htaccess dosyaları, sunucu ortamına çok sınırlı erişim sağlayan bazı hosting şirketlerinin sağladığı web alanında özellikle kullanışlıdır.</p>

<p>Geçerli çalışma dizinine göre var olmayan mutlak olmayan bir yolla fopen() veya require() gibi bir dosya sistemi fonksiyonu kullandığınızda, include path’teki dizinler listedeki ilk dizinden başlayarak otomatik olarak aranır (fopen() durumunda, bu özelliği etkinleştirmek için parametre listesine flag eklemelisiniz). Hedef dosya ile karşılaşıldığında arama sona erer ve fonksiyon görevini tamamlar.</p>

<p>require() ifadelerinizde yalnızca paketlere ve dosyalara başvurmanız gerekir.</p>

<p>Kendi kütüphanenizi kullanabilmeniz için include_path’e bir dizin eklemeniz gerekebilir. Bunu yapmak için php.ini dosyasını düzenleyebilirsiniz (Düzenledikten sonra sunucunuzu yeniden başlatmanız gerektiğini unutmayın).</p>

<p>php.ini dosyasıyla çalışmak için gerekli ayrıcalıklara sahip değilseniz, set_include_path() fonksiyonunu kullanarak kodunuzda include path’i ayarlayabilirsiniz. set_include_path() include path’i (php.ini’de göründüğü gibi) kabul eder ve yalnızca geçerli proses için include_path ayarını değiştirir. php.ini dosyası muhtemelen include_path için kullanışlı bir değer tanımlıyor, yani üzerine yazmak yerine get_include_path() fonksiyonunu kullanarak ona erişebilir ve kendi dizininizi ekleyebilirsiniz. Geçerli include path’e nasıl dizin ekleyebileceğiniz aşağıda açıklanmıştır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>set_include_path(get_include_path() . PATH_SEPARATOR . "/home/john/phplib/");
</code></pre></div></div>

<p>PATH_SEPARATOR sabiti, Unix sisteminde iki nokta üst üste ve Windows platformunda noktalı virgül olarak çözümlenecektir. Taşınabilirlik nedeniyle, kullanımı en iyi uygulama olarak kabul edilir.</p>

<h3 id="autoload-otomatik-yükleme">Autoload (Otomatik Yükleme)</h3>

<p>require_once’ı include path ile birlikte kullanmak zarif olsa da, birçok geliştirici require ifadelerini ortadan kaldırıyor ve bunun yerine autoload’a güveniyor.</p>

<p><strong>Not</strong>: Bu kitabın önceki baskılarında, bu bölümde tartışılan işlevselliğin basit bir versiyonunu sağlayan __autoload() adlı yerleşik bir fonksiyon ele alınmıştır. Bu işlev php 7.2.0’den itibaren kullanımdan kaldırılmıştır ve php 8’de tamamen kaldırılmıştır.</p>

<p>Bunu yapmak için, sınıflarınızı her biri kendi dosyasında yer alacak şekilde düzenlemelisiniz. Her sınıf dosyası, içerdiği sınıfın adıyla ilişki olmalıdır, bu nedenle ShopProduct.php adlı bir dosyada, sınıfın namespace’teki öğelere karşılık gelen dizinlerle bir ShopProduct sınıfı tanımlayabilirsiniz.</p>

<p>PHP 5, sınıf dosyalarının dahil edilmesini otomatikleştirmeye yardımcı olmak için autoload işlevini tanıttı. Varsayılan destek oldukça basit ama yine de kullanışlı. Parametre içermeyen spl_autoload_register() fonksiyonu çağrılarak etkinleştirilebilir. Ardından, autoload işlevi bu şekilde etkinleştirildiyse, bilinmeyen bir sınıfı başlatmaya çalıştığınızda,
PHP, spl_autoload() adlı yerleşik fonksiyonu çağırır. Bu, <code class="language-plaintext highlighter-rouge">&lt;classname&gt;.php</code> veya <code class="language-plaintext highlighter-rouge">&lt;classname&gt;.inc</code> adlı dosyalar için (burada <code class="language-plaintext highlighter-rouge">&lt;classname&gt;</code> bilinmeyen sınıfın adıdır) include path’i ararken sağlanan sınıf adını (küçük harfe dönüştürülür) kullanır.</p>

<p>İşte basit bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>spl_autoload_register();
$writer = new Writer();
</code></pre></div></div>

<p>Henüz Writer nesnesi içeren bir dosya eklemediğimi varsayarsak, bu örnekleme başarısız olmaya mahkûm görünüyor. Bununla birlikte PHP, autoload’ı ayarladığım için, write.php veya write.inc adlı bir dosyayı eklemeye çalışacak ve ardından örneklemeyi ikinci kez deneyecek. Bu dosyalardan biri varsa ve Writer adlı bir sınıf içeriyorsa, o zaman her şey yolunda olacaktır.</p>

<p>Bu varsayılan davranış, namespace’leri destekler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>spl_autoload_register();
$writer = new util\Writer();
</code></pre></div></div>

<p>Önceki kod, util adlı dizinde write.php (küçük harfli isme dikkat edin) adlı bir dosya bulacaktır.</p>

<p>Sınıf dosyalarımı büyük/küçük harfe bağlı olarak adlandırırsam ne olur? Yani, onları büyük harfle adlandırırsam ne olur? Writer sınıfını Writer.php adlı bir dosyaya yerleştirmiş olsaydım, varsayılan uygulama onu bulamazdı.</p>

<p>Neyse ki, farklı kuralları işlemek için kendi özel fonksiyonumu kaydedebilirim. Bunu yapabilmek için spl_autoload_register()’a özel bir fonksiyon başvurusu iletmeliyim. Autoload fonksiyonum tek bir parametre gerektirmelidir. Daha sonra, PHP bilinmeyen bir sınıfı başlatma girişimiyle karşılaşırsa, bu fonksiyonu çağırır ve ona bilinmeyen sınıf adını bir dize olarak iletir. Kayıp sınıf dosyasını bulmak ve ardından dahil etmek için bir strateji tanımlamak autoload fonksiyonuna bağlıdır. Autoload çağrıldığında, PHP sınıfı bir kez daha başlatmaya çalışır.</p>

<p>İşte yüklenecek bir sınıfla birlikte basit bir autoload fonksiyonu:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Blah
{
    public function wave(): void
    {
        print "saying hi from root";
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$basic = function (string $classname) {
    $file = __DIR__ . "/" . "{$classname}.php";
    if (file_exists($file)) {
        require_once($file);
    }
};

\spl_autoload_register($basic);
$blah = new Blah();
$blah-&gt;wave();
</code></pre></div></div>

<p>Başlangıçta Blah’ı başlatmayı başaramayan PHP, spl_autoload_register() fonksiyonuyla autoload fonksiyonu kaydettiğimi görecek ve ona “Blah” dizesini iletecek. Benim uygulamam basitçe Blah.php dosyasını dahil etmeye çalışır. Bu, elbette, yalnızca dosya, autoload fonksiyonunun bildirildiği dosyayla aynı dizindeyse çalışır. Gerçek dünyada, include path yapılandırmasını autoload mantığımla birleştirmem gerekir (bu tam olarak Composer’ın autoload uygulamasının yaptığı şeydir).</p>

<p>Old school desteği sağlamak istersem, PEAR paketi şunları içerir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class util_Blah
{
    public function wave(): void
    {
        print "saying hi from underscore file";
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$underscores = function (string $classname) {
    $path = str_replace('_', DIRECTORY_SEPARATOR, $classname);
    $path = __DIR__ . "/$path";
    if (file_exists("{$path}.php")) {
        require_once("{$path}.php");
    }
};

\spl_autoload_register($underscores);
$blah = new util_Blah();
$blah-&gt;wave();
</code></pre></div></div>

<p>Gördüğünüz gibi, autoload fonksiyonu verilen $classname içindeki alt çizgileri eşleştirir ve her birini DIRECTORY_SEPARATOR karakteriyle (Unix sistemlerinde /) değiştirir. Sınıf dosyasını (util/Blah.php) dahil etmeye çalışıyorum. Sınıf dosyası varsa ve içerdiği sınıf doğru bir şekilde adlandırılmışsa, nesne hatasız olarak başlatılmalıdır. Elbette bu, yazılımcının  paketleri böldüğü durumlar dışında, bir sınıf adında alt çizgi karakterini yasaklayan bir adlandırma kuralına uymasını gerektirir.</p>

<p>Peki ya namespace’ler? Varsayılan autoload fonksiyonunun namespace’leri desteklediğini gördük. Ancak bu varsayılanı geçersiz kılarsak, namespace desteği sağlamak bize düşer. Bu sadece ters eğik çizgi karakterlerini eşleştirme ve değiştirme meselesidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace util;

class LocalPath
{
    public function wave(): void
    {
        print "hello from " . get_class();
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$namespaces = function (string $path) {
    if (preg_match('/\\\\/', $path)) {
        $path = str_replace('\\', DIRECTORY_SEPARATOR, $path);
    }
    if (file_exists("{$path}.php")) {
        require_once("{$path}.php");
    }
};

\spl_autoload_register($namespaces);
$obj = new util\LocalPath();
$obj-&gt;wave();
</code></pre></div></div>

<p>Autoload fonksiyonuna iletilen değer, başında ters eğik çizgi olmadan her zaman tam nitelikli bir ada normalleştirilir, bu nedenle örnekleme noktasında takma ad veya göreli ad alanları hakkında endişelenmenize gerek yoktur.</p>

<p>Bu çözümün mükemmel olmadığını unutmayın. file_exists() fonksiyonu, include path’i hesaba katmaz, bu nedenle, require_once’ın mükemmel çalışacağı tüm koşulları doğru bir şekilde yansıtmaz. Bunun çeşitli çözümleri var. Kendi path’e duyarlı file_exists() sürümünüzü kullanabilir veya dosyayı try ifadesi ile kullanabilirsiniz (bu durumda Error ‘u yakalayabilirsiniz, Exception’ı değil). Neyse ki PHP, stream_resolve_include_path() fonksiyonunu sağlar. Bu, verilen path’in mutlak dosya adını temsil eden bir dize döndürür veya bizim amaçlarımız için çok önemli bir şekilde, dosya include path’te bulunamazsa false döndürür.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$namespaces = function (string $path) {
    if (preg_match('/\\\\/', $path)) {
        $path = str_replace('\\', DIRECTORY_SEPARATOR, $path);
    }
    if (\stream_resolve_include_path("{$path}.php") !== false) {
        require_once("{$path}.php");
    }
};

\spl_autoload_register($namespaces);
$obj = new util\LocalPath();
$obj-&gt;wave();
</code></pre></div></div>

<p>Hem PEAR tarzı sınıf adlarını hem de namespace’leri desteklemek istersem ne olur? Autoload uygulamalarımı tek bir fonksiyonda birleştirebilirim. Veya spl_autoload_register()‘ın autoload fonksiyonlarını istiflediği gerçeğini kullanabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$underscores = function (string $classname) {
    $path = str_replace('_', DIRECTORY_SEPARATOR, $classname);
    $path = __DIR__ . "/$path";

    if (\stream_resolve_include_path("{$path}.php") !== false) {
        require_once("{$path}.php");
    }
};

$namespaces = function (string $path) {
    if (preg_match('/\\\\/', $path)) {
        $path = str_replace('\\', DIRECTORY_SEPARATOR, $path);
    }
    if (\stream_resolve_include_path("{$path}.php") !== false) {
        require_once("{$path}.php");
    }
};

\spl_autoload_register($namespaces);
\spl_autoload_register($underscores);
$blah = new util_Blah();
$blah-&gt;wave();
$obj = new util\LocalPath();
$obj-&gt;wave();
</code></pre></div></div>

<p>Bilinmeyen bir sınıfla karşılaştığında, PHP motoru sırayla (kaydedilme sırasına göre) autoload fonksiyonlarını çağırır, başlatma mümkün olduğunda veya tüm seçenekler tükendiğinde durur.</p>

<p>Açıkça bu şekilde istiflemenin bir yükü vardır, peki PHP bunu neden destekliyor? Gerçek dünyadaki bir projede, muhtemelen namespace ve alt çizgi stratejilerini tek bir fonksiyonda birleştirirsiniz. Ancak, büyük sistemlerdeki ve kütüphanelerdeki bileşenlerin kendi autoload mekanizmalarını kaydetmesi gerekebilir. Yığınlama (stacking), bir sistemin birden çok bölümünün autoload stratejilerini birbirinin üzerine yazmadan bağımsız olarak kaydetmesine olanak tanır. Aslında, autoload mekanizmasına yalnızca kısa bir süre ihtiyaç duyan bir kütüphane, autoload fonksiyonunun adını spl_autoload_unregister()’a ileterek kendi arkasını temizleyebilir!</p>

<h2 id="sınıf-ve-nesne-fonksiyonları">Sınıf ve Nesne Fonksiyonları</h2>

<p>PHP, sınıfları ve nesneleri test etmek için güçlü bir dizi fonksiyon sağlar. Peki bu neden yararlıdır? Ne de olsa, scriptinizde kullandığınız sınıfların çoğunu muhtemelen siz yazdınız.</p>

<p>Aslında, çalışma zamanında kullandığınız sınıflar hakkında her zaman bilgi sahibi olmazsınız. Örneğin, third-party sınıflarla şeffaf bir şekilde çalışacak bir sistem tasarlamış olabilirsiniz. Bu durumda, tipik olarak yalnızca sınıf adı verilen bir nesneyi başlatırsınız. PHP, sınıflara dinamik olarak başvurmak için dizeleri kullanmanıza izin verir, bunun gibi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace tasks;

class Task
{
    public function doSpeak()
    {
        print "hello\n";
    }
}

$classname = "Task";
require_once("tasks/{$classname}.php");
$classname = "tasks\\$classname";
$myObj = new $classname();
$myObj-&gt;doSpeak();
</code></pre></div></div>

<p>Bu script, $classname öğesine atadığım dizeyi yapılandırma dosyasından veya bir web isteğini bir dizinin içeriğiyle karşılaştırarak alabilir. Daha sonra dizeyi bir sınıf dosyası yüklemek ve bir nesneyi başlatmak için kullanabilirsiniz. Burada bir namespace niteliği oluşturduğuma dikkat edin.</p>

<p>Tipik olarak, sisteminizin kullanıcı tarafından oluşturulan eklentileri çalıştırabilmesini istediğinizde buna benzer bir şey yaparsınız. Gerçek bir projede bu kadar riskli bir şey yapmadan önce, sınıfın var olup olmadığını, beklediğiniz metotlara sahip olup olmadığını vb. kontrol etmeniz gerekir.</p>

<p><strong>Not</strong>: Güvenlik önlemi almış olsanız bile, third-party eklenti kodunu dinamik olarak yükleme konusunda son derece dikkatli olmalısınız. Kullanıcılar tarafından yüklenen kodu asla otomatik olarak çalıştırmamalısınız. Bu şekilde yüklenen herhangi bir eklenti, genellikle çekirdek kodunuzla aynı ayrıcalıklarla yürütülür, bu nedenle kötü niyetli bir eklenti sisteminize zarar verebilir.</p>

<p>Bu, eklentilerin iyi bir fikir olmadığı anlamına gelmez. Third-party geliştiricilerin çekirdek sistemi geliştirmesine izin vermek, büyük bir esneklik sunabilir. Daha fazla güvenlik sağlamak için eklentilere özel bir dizini destekleyebilirsiniz, ancak kod dosyalarının sistem yöneticisi tarafından doğrudan veya parola korumalı bir yönetim ortamından yüklenmesini isteyebilirsiniz. Yönetici, kurulumdan önce eklenti kodunu kontrol eder veya eklentileri güvenilir bir repodan alır.</p>

<p>Bazı sınıf fonksiyonlarının yerini, bu bölümde daha sonra inceleyeceğim daha güçlü olan Reflection API aldı. Bununla birlikte, basitlikleri ve kullanım kolaylıkları, onları bazı durumlarda ilk bağlantı noktası haline getirir.</p>

<h3 id="sınıfları-aramak">Sınıfları Aramak</h3>

<p>class_exists() fonksiyonu, denetlenecek sınıfı temsil eden bir dizeyi kabul eder ve sınıf mevcut ise true, aksi takdirde false döndürür.</p>

<p>Bu fonksiyonu kullanarak önceki kod parçasını biraz daha güvenli hale getirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$base = __DIR__;
$classname = "Task";
$path = "{$base}/tasks/{$classname}.php";

if (!file_exists($path)) {
    throw new \Exception("No such file as {$path}");
}

require_once($path);
$qclassname = "tasks\\$classname";

if (!class_exists($qclassname)) {
    throw new Exception("No such class as $qclassname");
}

$myObj = new $qclassname();
$myObj-&gt;doSpeak();
</code></pre></div></div>

<p>Tabii ki, söz konusu sınıfın kurucu parametrelere ihtiyaç duymadığından emin olamazsınız. Bu güvenlik düzeyi için, bölümün ilerleyen kısımlarında ele alınan Reflection API’ye başvurmanız gerekir. Bununla birlikte, class_exists(), onunla çalışmadan önce sınıfın var olup olmadığını kontrol etmenize izin verir.</p>

<p>Ayrıca get_declared_classes() fonksiyonunu kullanarak kodunuzda tanımlanan tüm sınıfların bir dizisini elde edebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print_r(get_declared_classes());
</code></pre></div></div>

<p>Bu, kullanıcı tanımlı ve yerleşik sınıfları listeler. Yalnızca fonksiyon çağrısı sırasında bildirilen sınıfları döndürdüğünü unutmayın. Daha sonra require() veya require_once() komutunu çalıştırabilir ve böylece kodunuzdaki sınıf sayısını artırabilirsiniz.</p>

<h3 id="nesne-veya-sınıf-hakkında-bilgi-almak">Nesne veya Sınıf Hakkında Bilgi Almak</h3>

<p>Bildiğiniz gibi, sınıf tipi ipuçlarını kullanarak metot parametrelerinin nesne türlerini sınırlayabilirsiniz. Bu araçla bile, bir nesnenin türünden her zaman emin olamazsınız.</p>

<p>Bir nesnenin türünü kontrol etmek için kullanılabilecek bir dizi temel araç vardır. Öncelikle get_class() fonksiyonu ile bir nesnenin sınıfını kontrol edebilirsiniz. Bu, herhangi bir nesneyi parametre olarak kabul eder ve sınıf adını bir dize olarak döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getProduct();

if (get_class($product) === 'popp\ch05\batch05\CdProduct') {
    print "\$product is a CdProduct object\n";
}
</code></pre></div></div>

<p>Bu kod parçasında, getProduct() fonksiyonundan bir şey alıyorum. Bunun bir CdProduct nesnesi olduğundan kesinlikle emin olmak için get_class() metodunu kullanıyorum.</p>

<p>İşte getProduct() fonksiyonu:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function getProduct()
{
    return new CdProduct(
        "Exile on Coldharbour Lane",
        "The",
        "Alabama 3",
        10.99,
        60.33
    );
}
</code></pre></div></div>

<p>getProduct() basitçe bir CdProduct nesnesi başlatır ve döndürür. Bu bölümde bu fonksiyonu iyi bir şekilde kullanacağım.</p>

<p>get_class() fonksiyonu çok özel bir araçtır. Genellikle bir sınıfın türü hakkında daha genel bir onay istersiniz. Bir nesnenin ShopProduct ailesine ait olduğunu bilmek isteyebilirsiniz, ancak asıl sınıfının BookProduct veya CdProduct olup olmadığı umurunuzda değildir. Bu amaçla PHP, instanceof operatörünü sağlar.</p>

<p><strong>Not</strong>: PHP 4, instanceof’u desteklemiyordu. Bunun yerine, PHP 5.0’da kullanımdan kaldırılan ancak PHP 5.3 ile tekrar yüklenen is_a() fonksiyonunu sağladı.</p>

<p>instanceof operatörü, anahtar kelimenin solunda test edilecek nesne ve sağında sınıf veya arayüz adı olmak üzere iki işlenenle çalışır. Nesne verilen türün bir örneğiyse, true olarak çözümlenir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getProduct();

if ($product instanceof \popp\ch05\batch05\CdProduct) {
    print "\$product is an instance of CdProduct\n";
}
</code></pre></div></div>

<h3 id="sınıfın-tam-nitelikli-dize-referansını-almak">Sınıfın Tam Nitelikli Dize Referansını Almak</h3>

<p>Namespace’ler, nesne yönelimli PHP ile ilgili çirkin olan birçok şeyi temizledi. Artık gülünç derecede uzun sınıf adlarına veya isim çakışmalarına (eski kod bir yana) katlanmamız gerekmiyor. Öte yandan, takma adla ve göreli ad alanı referanslarıyla, bazı sınıf yollarını tam olarak nitelenmiş olacak şekilde çözümlemek angarya olabilir.</p>

<p>Çözülmesi zor sınıf adlarına bazı örnekler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace mypackage;

use util as u;
use util\db\Querier as q;

class Local
{
}

// Resolve these:
// Aliased namespace
// u\Writer;
// Aliased class
// q;
// Class referenced in local context
// Local
</code></pre></div></div>

<p>Bu sınıf referanslarının nasıl çözüldüğünü anlamak çok zor değil, ancak her olasılığı yakalamak için kod yazmak acı verici olurdu. Örneğin, u\Writer verildiğinde, otomatik bir çözümleyicinin util’in takma adı olduğunu ve kendi başına bir namespace olmadığını bilmesi gerekir. PHP 5.5, ClassName::class sözdizimini tanıttı. Başka bir deyişle, bir sınıf referansı verildiğinde, tam nitelikli sınıf adını elde etmek için kapsam çözümleme operatörü ve class anahtar sözcüğünü ekleyebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print u\Writer::class . "\n";
print q::class . "\n";
print Local::class . "\n";
</code></pre></div></div>

<p>Önceki kod parçası şunu verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>util\Writer
util\db\Querier
mypackage\Local
</code></pre></div></div>

<p>PHP 8’den itibaren, bir nesne üzerinde ::class’ı çağırabilirsiniz. Örneğin, bir ShopProduct örneği verildiğinde, tam sınıf adını şu şekilde alabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$bookp = new BookProduct(
    "Catch 22",
    "Joseph",
    "Heller",
    11.99,
    300
);

print $bookp::class;
// popp\ch04\batch02\BookProduct
</code></pre></div></div>

<p>Bu kullanışlı sözdiziminin yeni bir işlevsellik sağlamadığını unutmayın; aynı sonucu veren get_class() fonksiyonuyla zaten karşılaştınız.</p>

<h3 id="metodlar-hakkında-bilgi-almak">Metodlar Hakkında Bilgi Almak</h3>

<p>get_class_methods() fonksiyonunu kullanarak bir sınıftaki tüm metotların listesini elde edebilirsiniz. Bu, bir sınıf adı gerektirir ve sınıftaki tüm metotların adlarını içeren bir dizi döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print_r(get_class_methods('\\popp\\ch04\\batch02\\BookProduct'));
</code></pre></div></div>

<p>BookProduct sınıfının var olduğunu varsayarsak, şöyle bir şey görebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Array
(
    [0] =&gt; __construct
    [1] =&gt; getNumberOfPages
    [2] =&gt; getSummaryLine
    [3] =&gt; getPrice
    [4] =&gt; setID
    [5] =&gt; getProducerFirstName
    [6] =&gt; getProducerMainName
    [7] =&gt; setDiscount
    [8] =&gt; getDiscount
    [9] =&gt; getTitle
    [10] =&gt; getProducer
    [11] =&gt; getInstance
)
</code></pre></div></div>

<p>Örnekte, sınıf adını içeren bir dizeyi get_class_methods() fonksiyonuna iletiyorum ve döndürülen diziyi print_r() fonksiyonu ile yazdırıyorum. Alternatif olarak, get_class_methods()’a bir nesne iletebilirdim. Döndürülen listeye yalnızca public metotların adları dahil edilecektir.</p>

<p>Gördüğünüz gibi, bir metot adını değişkende saklayabilir ve onu bir nesneyle birlikte dinamik olarak çağırabilirsiniz, bunun gibi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getProduct();
$method = "getTitle"; // define a method name
print $product-&gt;$method(); // invoke the method
</code></pre></div></div>

<p>Tabii ki, bu tehlikeli olabilir. Metot yoksa ne olur? Tahmin edebileceğiniz gibi, kodunuz hata vererek başarısız olacaktır. Bir metodun var olduğunu test etmenin yolu ile zaten karşılaştınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (in_array($method, get_class_methods($product))) {
    print $product-&gt;$method(); // invoke the method
}
</code></pre></div></div>

<p>Çağırmadan önce get_class_methods() tarafından döndürülen dizide metot adının var olup olmadığını kontrol ediyorum.</p>

<p>PHP bu amaç için daha özel araçlar sağlar. Metot adlarını iki fonksiyon ile kontrol edebilirsiniz: is_callable() ve method_exists(). is_callable(), iki fonksiyondan daha gelişmiş olanıdır. Fonksiyon adını temsil eden bir dize değişkenini ilk parametre olarak kabul eder ve fonksiyon varsa ve çağrılabiliyorsa true değerini döndürür. Aynı testi bir metoda uygulamak için, fonksiyon adı yerine bir dizi iletmelisiniz. Dizi, ilk öğesi olarak bir nesne veya sınıf adı ve ikinci öğesi olarak kontrol edilecek metot adını içermelidir. Metot sınıfta mevcutsa, fonksiyon true değerini döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (is_callable([$product, $method])) {
    print $product-&gt;$method(); // invoke the method
}
</code></pre></div></div>

<p>method_exists() fonksiyonu, bir nesne (veya bir sınıf adı) ve bir metot adı gerektirir ve verilen metot, nesnenin sınıfında varsa true değerini döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (method_exists($product, $method)) {
    print $product-&gt;$method(); // invoke the method
}
</code></pre></div></div>

<p><strong>Dikkat</strong>: Bir metodun var olmasının, onun çağrılabilir olacağı anlamına gelmediğini unutmayın. method_exists(), private ve protected metotların yanı sıra public metotlar için de true değerini döndürür.</p>

<h3 id="propertyler-hakkında-bilgi-almak">Property’ler Hakkında Bilgi Almak</h3>

<p>Bir sınıfın metotlarını sorgulayabildiğiniz gibi property’lerini de sorgulayabilirsiniz. get_class_vars() fonksiyonu, sınıf adı gerektirir ve bir ilişkisel dizi döndürür. Döndürülen dizi, anahtarları olarak property adlarını ve değerleri olarak property değerlerini içerir. Bu testi CdProduct nesnesine uygulayalım. Açıklama amacıyla, CdProduct::$coverUrl sınıfına bir public property ekliyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print_r(get_class_vars('\\popp\\ch05\\batch05\\CdProduct'));

Array (
    [coverUrl] =&gt; cover url
)
</code></pre></div></div>

<h3 id="kalıtım-hakkında-bilgi-almak">Kalıtım Hakkında Bilgi Almak</h3>

<p>Sınıf fonksiyonları aynı zamanda kalıtım ilişkilerinin grafiğini çıkarmamıza da izin verir. Bir sınıfın ebeveynini örneğin get_parent_class() ile bulabiliriz. Bu fonksiyon, bir nesne veya sınıf adı gerektirir ve varsa, üst sınıfın adını döndürür. Böyle bir sınıf yoksa, yani test ettiğimiz sınıfın bir üst öğesi yoksa, fonksiyon false değerini döndürür.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print get_parent_class('\\popp\\ch04\\batch02\\BookProduct');
</code></pre></div></div>

<p>Tahmin edebileceğiniz gibi bu, üst sınıfı verir: ShopProduct.</p>

<p>is_subclass_of() fonksiyonunu kullanarak bir sınıfın diğerinin alt sınıfı olup olmadığını da test edebiliriz.  Bu, bir alt nesne (veya bir sınıfın adı) ve üst sınıfın adını gerektirir. İkinci parametre birinci parametrenin üst sınıfıysa, fonksiyon true değerini döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getBookProduct(); // acquire an object

if (is_subclass_of($product, '\\popp\\ch04\\batch02\\ShopProduct')) {
    print "BookProduct is a subclass of ShopProduct\n";
}
</code></pre></div></div>

<p>is_subclass_of() size yalnızca sınıf kalıtım ilişkileri hakkında bilgi verecektir. Size sınıfın bir arayüz uyguladığını söylemez. Bunun için instanceof operatörünü kullanmalısınız. Veya SPL’nin (Standard PHP Library) parçası olan bir fonksiyonu kullanabilirsiniz. class_implements(), sınıf adını veya nesne referansını kabul eder ve arayüz adları dizisi döndürür:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (in_array('someInterface', class_implements($product))) {
    print "BookProduct is an interface of someInterface\n";
}
</code></pre></div></div>

<h3 id="metot-çağırmak">Metot Çağırmak</h3>

<p>Bir metodu dinamik olarak çağırmak için dize kullandığım bir örnekle zaten karşılaştınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getProduct();
$method = "getTitle"; // define a method name
print $product-&gt;$method(); // invoke the method
</code></pre></div></div>

<p>PHP aynı sonuca ulaşmak için call_user_func() metodunu da sağlar. call_user_func() her türlü callable çağırabilir (fonksiyon adı veya anonim fonksiyon gibi). Burada, fonksiyon adını bir dizede ileterek fonksiyon çağırıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$returnVal = call_user_func("myFunction");
</code></pre></div></div>

<p>Bir metodu çağırmak için bir diziyi iletebilirim. Bunun ilk öğesi bir nesne olmalı ve ikincisi çağrılacak metodun adı olmalıdır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$returnVal = call_user_func([$myObj, "methodName"]);
</code></pre></div></div>

<p>call_user_func() fonksiyonuna iletilen diğer tüm parametreler, hedef fonksiyonun veya metodun parametreleri olarak ele alınacak ve şu şekilde aynı sırada iletilecektir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = self::getBookProduct(); // Acquire a BookProduct object
call_user_func([$product, 'setDiscount'], 20);
</code></pre></div></div>

<p>Bu dinamik çağrı elbette şuna eşdeğerdir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product-&gt;setDiscount(20);
</code></pre></div></div>

<p>call_user_func() fonksiyonu hayatınızı büyük ölçüde değiştirmeyecek çünkü metot adı yerine doğrudan bir dize kullanabilirsiniz, bunun gibi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$method = "setDiscount";
$product-&gt;$method(20);
</code></pre></div></div>

<p>Bununla birlikte, ilgili call_user_func_array() fonksiyonu çok daha etkileyicidir. Bu, hedef metot veya fonksiyonun seçilmesi söz konusu olduğunda call_user_func() ile aynı şekilde çalışır. En önemlisi, hedef metodun gerektirdiği tüm parametreleri bir dizi olarak kabul eder.</p>

<p><strong>Dikkat</strong>: call_user_func() kullanılarak bir fonksiyon veya metoda iletilen parametreler referans olarak iletilmez.</p>

<p>Peki bu neden yararlıdır? Nadiren, size dizi biçiminde argümanlar verilir. Karşılaştığınız argümanların sayısını önceden bilmiyorsanız, onları başkalarına aktarmak zor olabilir. Bölüm 4’te, delegator sınıfları oluşturmak için kullanılabilecek interceptor metotları inceledik. İşte __call() metodunun basit bir örneği:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __call(string $method, array $args): mixed
{
    if (method_exists($this-&gt;thirdpartyShop, $method)) {
        return $this-&gt;thirdpartyShop-&gt;$method();
    }
}
</code></pre></div></div>

<p>Gördüğünüz gibi, yazılımcı tarafından tanımsız bir metot çağrıldığında __call() metodu çağrılır. Bu örnekte, $thirdpartyShop adlı property’de bir nesne tutuyorum. Saklanan nesnede $method parametresi ile eşleşen bir metot bulursam, onu çağırırım. Hedef metodun herhangi bir argüman gerektirmediğini varsayıyorum, bu da benim sorunlarımın başladığı yer. __call() metodunu yazarken, çağrıdan çağrıya $args dizisinin ne kadar büyük olabileceğini söylememin hiçbir yolu yok. $args’ı doğrudan delegate metoda iletirsem, beklediği argümanları değil, tek bir dizi argümanı iletirim. call_user_func_array() sorunu mükemmel şekilde çözer:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __call(string $method, array $args): mixed
{
    if (method_exists($this-&gt;thirdpartyShop, $method)) {
        return call_user_func_array(
            [
                $this-&gt;thirdpartyShop,
                $method
            ],
            $args
        );
    }
}
</code></pre></div></div>

<h2 id="reflection-api">Reflection API</h2>

<p>Java için java.lang.reflect paketi ne ise, PHP’nin Reflection API’si de PHP için odur. Property, metot ve sınıfları analiz etmek için yerleşik sınıflardan oluşur. Bazı açılardan get_class_vars() gibi mevcut nesne fonksiyonlarına benzer, ancak daha esnektir ve çok daha fazla ayrıntı sağlar. Ayrıca PHP’nin erişim denetimi, arayüzler ve soyut sınıflar gibi nesne yönelimli özellikleriyle, daha eski, daha sınırlı sınıf fonksiyonlarının olmadığı bir şekilde çalışmak üzere tasarlanmıştır.</p>

<h3 id="giriş">Giriş</h3>

<p>Reflection API, sınıflardan daha fazlasını incelemek için kullanılabilir. Örneğin, ReflectionFunction sınıfı, belirli bir fonksiyon hakkında bilgi sağlar ve ReflectionExtension, dilde derlenen bir uzantı hakkında fikir verir. Tablo 5-1, API’deki bazı sınıfları listeler.</p>

<p><strong>Tablo 5-1</strong>. Reflection API’deki anahtar sınıflar</p>

<table>
  <thead>
    <tr>
      <th>Sınıf</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Reflection</td>
      <td>Sınıf bilgilerini özetlemek için static export() metodunu sağlar</td>
    </tr>
    <tr>
      <td>ReflectionAttribute</td>
      <td>Sınıflar, property’ler, sabitler veya parametreler hakkında bağlamsal bilgiler</td>
    </tr>
    <tr>
      <td>ReflectionClass</td>
      <td>Sınıf bilgileri ve araçları</td>
    </tr>
    <tr>
      <td>ReflectionClassConstant</td>
      <td>Sabit hakkında bilgi</td>
    </tr>
    <tr>
      <td>ReflectionException</td>
      <td>Hata sınıfı</td>
    </tr>
    <tr>
      <td>ReflectionExtension</td>
      <td>PHP uzantı bilgisi</td>
    </tr>
    <tr>
      <td>ReflectionFunction</td>
      <td>Fonksiyon bilgileri ve araçları</td>
    </tr>
    <tr>
      <td>ReflectionGenerator</td>
      <td>Generator hakkında bilgi</td>
    </tr>
    <tr>
      <td>ReflectionMethod</td>
      <td>Sınıf metot bilgileri ve araçları</td>
    </tr>
    <tr>
      <td>ReflectionNamedType</td>
      <td>Fonksiyon veya metodun dönüş tipi hakkında bilgi</td>
    </tr>
    <tr>
      <td>ReflectionObject</td>
      <td>Nesne bilgileri ve araçları (ReflectionClass’tan miras alınır)</td>
    </tr>
    <tr>
      <td>ReflectionParameter</td>
      <td>Metot parametre bilgileri</td>
    </tr>
    <tr>
      <td>ReflectionProperty</td>
      <td>Sınıf property bilgileri</td>
    </tr>
    <tr>
      <td>ReflectionType</td>
      <td>Fonksiyon veya metodun dönüş tipi hakkında bilgi</td>
    </tr>
    <tr>
      <td>ReflectionUnionType</td>
      <td>Union tip bildirimi için ReflectionType nesnelerinin koleksiyonu</td>
    </tr>
    <tr>
      <td>ReflectionZendExtension</td>
      <td>PHP Zend uzantısı bilgileri</td>
    </tr>
  </tbody>
</table>

<p>Bunların arasında, Reflection API’deki sınıflar, komut dosyalarınız daki nesneler, fonksiyonlar ve uzantılar hakkındaki bilgilere benzeri görülmemiş bir çalışma zamanı erişimi sağlar.</p>

<p>Reflection API’nin gücü ve erişimi, onu genellikle sınıf ve nesne fonksiyonlarına tercih ederek kullanmanız gerektiği anlamına gelir. Yakında sınıfları test etmek için vazgeçilmez bir araç olduğunu göreceksiniz. Örneğin class diyagramları veya dökümantasyon oluşturmak isteyebilirsiniz veya field adlarını çıkarmak için nesnenin accessor (getter ve setter) metotlarını inceleyerek nesne bilgilerini veritabanına kaydetmek isteyebilirsiniz. Şemaya göre modül sınıflarındaki metotları çağıran bir framework oluşturmak, Reflection’ın başka bir kullanımıdır.</p>

<h3 id="kolları-sıvamanın-zamanı">Kolları Sıvamanın Zamanı</h3>

<p>Sınıfların niteliklerini incelemek için bazı fonksiyonlarla zaten karşılaştınız. Bunlar faydalıdır ancak çoğu zaman sınırlıdır. İşte işe uygun bir araç. ReflectionClass, ister kullanıcı tanımlı ister dahili bir sınıf olsun, sınıf ile ilgili tüm bilgileri ortaya çıkaran metotlar sağlar. ReflectionClass’ın kurucusu, bir sınıf veya arayüz adını (veya bir nesne örneğini) tek parametre olarak kabul eder:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$prodclass = new \ReflectionClass(CdProduct::class);
print $prodclass;
</code></pre></div></div>

<p>ReflectionClass nesnesi oluşturduğunuzda, sınıfa ilişkin her türlü bilgiyi dize olarak aktarabilirsiniz. ShopProduct için ReflectionClass örneğimi yazdırdığımda oluşturulan çıktının kısaltılmış özeti:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Class [ &lt;user&gt; class popp\ch04\batch02\CdProduct extends popp\ch04\batch02\ShopProduct ] {
    @@ /var/popp/src/ch04/batch02/CdProduct.php 6-37
    - Constants [2] {
        Constant [ public int AVAILABLE ] { 0 }
        Constant [ public int OUT_OF_STOCK ] { 1 }
}
    - Static properties [0] {
}
- Static methods [1] {
    Method [ &lt;user, inherits popp\ch04\batch02\ShopProduct&gt; static public method getInstance ] {
    @@ /var/popp/src/ch04/batch02/ShopProduct.php 93 - 130
    - Parameters [2] {
        Parameter #0 [ &lt;required&gt; int $id ]
        Parameter #1 [ &lt;required&gt; PDO $pdo ]
    }
    - Return [ popp\ch04\batch02\ShopProduct ]
    }
}
- Properties [3] {
    Property [ private $playLength = 0 ]
    Property [ public $status = NULL ]
    Property [ protected int|float $price ]
}
...
</code></pre></div></div>

<p><strong>Not</strong>: Yardımcı metot olan Reflection::export(), bir zamanlar ReflectionClass bilgilerini aktarmanın standart yoluydu. Bu, PHP 7.4’te kullanımdan kaldırıldı ve PHP 8.0’da tamamen kaldırıldı.</p>

<p>Gördüğünüz gibi ReflectionClass, bir sınıfla ilgili bilgilere olağanüstü erişim sağlar. Dize çıktısı, property ve metotların erişim kontrolü durumu, her metodun gerektirdiği parametreler ve her metodun script dosyasındaki konumu dahil olmak üzere CdProduct’ın hemen hemen her yönü hakkında özet bilgi sağlar. Bunu daha yerleşik bir hata ayıklama fonksiyonuyla karşılaştırın. var_dump() fonksiyonu, verileri özetlemeye yönelik genel amaçlı bir araçtır. Özet bilgi alabilmeniz için önce nesneyi başlatmanız gerekir ve o zaman bile ReflectionClass tarafından sağlanan kadar ayrıntı vermez:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$cd = new CdProduct("cd1", "bob", "bobbleson", 4, 50);
var_dump($cd);

Here's the output:
object(popp\ch04\batch02\CdProduct)#15 (8) {
["playLength":"popp\ch04\batch02\CdProduct":private]=&gt;int(50)
["status"]=&gt;NULL
["title":"popp\ch04\batch02\ShopProduct":private]=&gt;string(3) "cd1"
["producerMainName":"popp\ch04\batch02\ShopProduct":private]=&gt;string(9) "bobbleson"
["producerFirstName":"popp\ch04\batch02\ShopProduct":private]=&gt;string(3) "bob"
["price":protected]=&gt;float(4)
["discount":"popp\ch04\batch02\ShopProduct":private]=&gt;int(0)
["id":"popp\ch04\batch02\ShopProduct":private]=&gt;int(0)
}
</code></pre></div></div>

<p>var_dump() ve onun kuzeni print_r(), verileri açığa çıkarmak için son derece kullanışlı araçlardır. Ancak sınıflar ve fonksiyonlar için Reflection API, işleri tamamen yeni bir düzeye taşıyor.</p>

<h3 id="sınıfları-i̇ncelemek">Sınıfları İncelemek</h3>

<p>ReflectionClass örneğinin ham dökümü, hata ayıklama için çok sayıda yararlı bilgi sağlayabilir, ancak API’yi daha özel yollarla kullanabiliriz. Doğrudan Reflection sınıflarıyla çalışalım.</p>

<p>ReflectionClass nesnesinin nasıl başlatılacağını zaten gördünüz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$prodclass = new \ReflectionClass(CdProduct::class);
</code></pre></div></div>

<p>Daha sonra, CdProduct’ı araştırmak için ReflectionClass nesnesini kullanacağım. Bu ne tür bir sınıf? Bir örnek oluşturulabilir mi? İşte bu soruları cevaplayacak bir fonksiyon:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ClassInfo
{
    public static function getData(\ReflectionClass $class): string
    {
        $details = "";
        $name = $class-&gt;getName();
        $details .= ($class-&gt;isUserDefined()) ? "$name is user defined\n" : "";
        $details .= ($class-&gt;isInternal()) ? "$name is built-in\n" : "";
        $details .= ($class-&gt;isInterface()) ? "$name is interface\n" : "";
        $details .= ($class-&gt;isAbstract()) ? "$name is an abstract class\n" : "";
        $details .= ($class-&gt;isFinal()) ? "$name is a final class\n" : "";
        $details .= ($class-&gt;isInstantiable()) ? "$name can be instantiated\n" : "$name can not be instantiated\n";
        $details .= ($class-&gt;isCloneable()) ? "$name can be cloned\n" : "$name can not be cloned\n";
        return $details;
    }
}

$prodclass = new \ReflectionClass(CdProduct::class);
print ClassInfo::getData($prodclass);
</code></pre></div></div>

<p>CdProduct sınıfının adını ReflectionClass kurucusuna ileterek ReflectionClass nesnesini oluşturuyorum. $prodclass değişkenini daha sonra bir sınıfı sorgulamak için kullanılabilecek metotlardan bazılarını gösteren ClassInfo::classData() adlı bir metoda iletilir.</p>

<p>Metotlar kendi kendini açıklayıcı niteliktedir, ancak burada bazılarının kısa bir açıklaması verilmiştir:</p>

<ul>
  <li>ReflectionClass::getName() incelenen sınıfın adını döndürür.</li>
  <li>ReflectionClass::isUserDefined() metodu, sınıf PHP kodunda bildirilmişse true değerini döndürür ve sınıf yerleşikse ReflectionClass::isInternal() metodu true değerini döndürür.</li>
  <li>ReflectionClass::isAbstract() ile sınıfın soyut olup olmadığını ve ReflectionClass::isInterface() ile arayüz olup olmadığını test edebilirsiniz.</li>
  <li>Sınıfın bir örneğini almak istiyorsanız bunun uygulanabilirliğini ReflectionClass::isInstantiable() ile test edebilirsiniz.</li>
  <li>ReflectionClass::isCloneable() metoduyla bir sınıfın kopyalanabilir olup olmadığını kontrol edebilirsiniz.</li>
  <li>Kullanıcı tanımlı bir sınıfın kaynak kodunu bile inceleyebilirsiniz. ReflectionClass nesnesi, kendi sınıfının dosya adına ve dosyadaki sınıfın başlangıç ve bitiş satırlarına erişim sağlar.</li>
</ul>

<p>İşte sınıfın kaynağına erişmek için ReflectionClass’ı kullanan bir metot:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ReflectionUtil
{
    public static function getClassSource(\ReflectionClass $class): string
    {
        $path = $class-&gt;getFileName();
        $lines = @file($path);
        $from = $class-&gt;getStartLine();
        $to = $class-&gt;getEndLine();
        $len = $to - $from + 1;
        return implode(array_slice($lines, $from - 1, $len));
    }
}

print ReflectionUtil::getClassSource(
    new \ReflectionClass(CdProduct::class)
);
</code></pre></div></div>

<p>ReflectionUtil, tek bir static metot olan ReflectionUtil::getClassSource()’a sahip basit bir sınıftır. Bu metot, tek parametre olarak ReflectionClass nesnesini alır ve başvurulan sınıfın kaynak kodunu döndürür. ReflectionClass::getFileName(), sınıf dosyasının yolunu verir. file() dosyadaki tüm satırların bir dizisini elde eder. ReflectionClass::getStartLine() sınıfın başlangıç satırını, ReflectionClass::getEndLine() sınıfın son satırı bulur. Buradan itibaren istenilen satırları almak için array_slice() fonksiyonunu kullanmak yeterlidir.</p>

<p>İşleri kısa tutmak için, bu kod hata işlemeyi atlar ( @ karakterini file() önüne yerleştirerek). Gerçek dünyadaki bir uygulamada parametreleri ve sonucu kontrol etmek istersiniz.</p>

<h3 id="metotları-i̇ncelemek">Metotları İncelemek</h3>

<p>ReflectionClass sınıfları incelemek için kullanıldığı gibi, ReflectionMethod nesnesi de metotları inceler.</p>

<p>ReflectionClass::getMethods() öğesinden ReflectionMethod nesnelerinin dizisini alabilirsiniz. Alternatif olarak, belirli bir metotla çalışmanız gerekiyorsa, ReflectionClass::getMethod() metot adını kabul eder ve ilgili ReflectionMethod nesnesini döndürür.</p>

<p>Ayrıca ReflectionMethod’u doğrudan sınıf/metot dizesi, sınıf adı ve metot adı veya nesne/metot adı ileterek de başlatabilirsiniz. İşte bu varyasyonlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$cd = new CdProduct("cd1", "bob", "bobbleson", 4, 50);
$classname = CdProduct::class;
$rmethod1 = new \ReflectionMethod("{$classname}::construct");
// class/method string
$rmethod2 = new \ReflectionMethod($classname, "construct");
// class name and method name
$rmethod3 = new \ReflectionMethod($cd, "construct");
// object and method name
</code></pre></div></div>

<p>Burada, ReflectionMethod sınıfını adım adım ilerletmek için ReflectionClass::getMethods()’u kullanıyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$prodclass = new \ReflectionClass(CdProduct::class);
$methods = $prodclass-&gt;getMethods();
foreach ($methods as $method) {
    print ClassInfo::methodData($method);
    print "\n----\n";
}

public static function methodData(\ReflectionMethod $method): string
{
    $details = "";
    $name = $method-&gt;getName();
    $details .= ($method-&gt;isUserDefined()) ? "$name is user defined\n" : "" ;
    $details .= ($method-&gt;isInternal()) ? "$name is built-in\n" : "" ;
    $details .= ($method-&gt;isAbstract()) ? "$name is an abstract class\n" : "" ;
    $details .= ($method-&gt;isPublic()) ? "$name is public\n" : "" ;
    $details .= ($method-&gt;isProtected()) ? "$name is protected\n" : "" ;
    $details .= ($method-&gt;isPrivate()) ? "$name is private\n" : "" ;
    $details .= ($method-&gt;isStatic()) ? "$name is static\n" : "" ;
    $details .= ($method-&gt;isFinal()) ? "$name is final\n" : "" ;
    $details .= ($method-&gt;isConstructor()) ? "$name is the constructor\n" : "" ;
    $details .= ($method-&gt;returnsReference()) ? "$name returns a reference (as opposed to a value)\n" : "" ;
    return $details;
}
</code></pre></div></div>

<p>Kod, ReflectionMethod nesnelerinin dizisini elde etmek için ReflectionClass::getMethods()’u kullanır ve ardından diziyi döngüye sokarak nesneleri methodData()’ya iletir.</p>

<p>methodData()’da kullanılan metotların adları amaçlarını yansıtır: kod, metodun kullanıcı tanımlı, abstract, public, protected, static veya final olup olmadığını kontrol eder. Ayrıca metodun kendi sınıfının kurucusu olup olmadığını ve bir referans döndürüp döndürmediğini de kontrol edebilirsiniz.</p>

<p>Tahmin edebileceğiniz gibi, daha önce ReflectionClass’ta kullanılana benzer bir teknik kullanarak bir metodun kaynak koduna erişebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function getMethodSource(\ReflectionMethod $method): string
{
    $path = $method-&gt;getFileName();
    $lines = @file($path);
    $from = $method-&gt;getStartLine();
    $to = $method-&gt;getEndLine();
    $len = $to - $from + 1;
    return implode(array_slice($lines, $from - 1, $len));
}

$class = new \ReflectionClass(CdProduct::class);
$method = $class-&gt;getMethod('getSummaryLine');
print ReflectionUtil::getMethodSource($method);
</code></pre></div></div>

<h3 id="metot-parametrelerini-i̇ncelemek">Metot Parametrelerini İncelemek</h3>

<p>Artık metot imzaları nesne parametre türlerini sınırlayabildiğinden, metot imzasında bildirilen parametreleri inceleme yeteneği son derece yararlı hale gelir. Reflection API, ReflectionParameter sınıfını yalnızca bu amaç için sağlar. ReflectionParameter nesnesi elde etmek için ReflectionMethod nesnesinin yardımına ihtiyacınız vardır. ReflectionMethod::getParameters() metodu, ReflectionParameter nesnelerinin dizisini döndürür.</p>

<p>Ayrıca ReflectionParameter nesnesini her zamanki gibi doğrudan başlatabilirsiniz. ReflectionParameter kurucusu, çağrılabilir bir parametre ve parametre numarasını temsil eden bir tamsayı (sıfır ile indekslenmiş) veya parametre adını temsil eden bir dize gerektirir.</p>

<p>Yani bu örneklerin dördü de eşdeğerdir. Her biri, CdProduct sınıfının kurucusuna yönelik ikinci parametre için bir ReflectionParameter nesnesi oluşturur.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$classname = CdProduct::class;
$rparam1 = new \ReflectionParameter([$classname, "__construct"], 1);
$rparam2 = new \ReflectionParameter([$classname, "__construct"], "firstName");
$cd = new CdProduct("cd1", "bob", "bobbleson", 4, 50);
$rparam3 = new \ReflectionParameter([$cd, "__construct"], 1);
$rparam4 = new \ReflectionParameter([$cd, "__construct"], "firstName");
</code></pre></div></div>

<p>ReflectionParameter size bir parametrenin adını ve değişkenin referans yoluyla (yani, metot bildiriminde öncesinde &amp; işaretiyle) iletilip iletilmediğini söyleyebilir. Ayrıca parametre tipinin gerektirdiği sınıf ve metodun null değer kabul edip etmeyeceğini de söyleyebilir.</p>

<p>ReflectionParameter’ın metot uygulamalarından bazıları şunlardır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$class = new \ReflectionClass(CdProduct::class);
$method = $class-&gt;getMethod("__construct");
$params = $method-&gt;getParameters();

foreach ($params as $param) {
    print ClassInfo::argData($param) . "\n";
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ClassInfo
{
    public static function argData(\ReflectionParameter $arg): string
    {
        $details = "";
        $declaringclass = $arg-&gt;getDeclaringClass();
        $name = $arg-&gt;getName();
        $position = $arg-&gt;getPosition();
        $details .= "\$$name has position $position\n";
        if ($arg-&gt;hasType()) {
            $type = $arg-&gt;getType();
            $typenames = [];
            if ($type instanceof \ReflectionUnionType) {
                $types = $type-&gt;getTypes();
                foreach ($types as $utype) {
                    $typenames[] = $utype-&gt;getName();
                }
            } else {
                $typenames[] = $type-&gt;getName();
            }
            $typename = implode("|", $typenames);
            $details .= "\$$name should be type {$typename}\n";
        }
        if ($arg-&gt;isPassedByReference()) {
            $details .= "\${$name} is passed by reference\n";
        }
        if ($arg-&gt;isDefaultValueAvailable()) {
            $def = $arg-&gt;getDefaultValue();
            $details .= "\${$name} has default: $def\n";
        }
        if ($arg-&gt;allowsNull()) {
            $details .= "\${$name} can be null\n";
        }
        return $details;
    }
}
</code></pre></div></div>

<p>Kod, ReflectionClass::getMethod() metodunu kullanarak, bir ReflectionMethod nesnesi alır. Daha sonra ReflectionParameter nesnelerinin dizisini elde etmek için ReflectionMethod::getParameters() metodunu kullanır. argData() fonksiyonu, parametre hakkında bilgi edinmek için iletildiği ReflectionParameter nesnesini kullanır.</p>

<p>İlk olarak, ReflectionParameter::getName() ile parametrenin değişken adını alır. ReflectionParameter::getType() metodu, bir tür belirtildiyse ReflectionType nesnesini veya belirtilen tür union ise ReflectionUnionType sınıfını döndürür. Bunlardan hangisi döndürülürse, gerekli türü temsil eden bir dize oluşturulur. Kod daha sonra parametrenin isPassedByReference(); ile referans olup olmadığını kontrol eder. ve son olarak, varsayılan değerin kullanılabilirliğini arar ve bunu daha sonra return dizesine ekler.</p>

<h3 id="reflection-apiyi-kullanmak">Reflection API’yi Kullanmak</h3>

<p>Reflection API’nin temelleri elinizin altında olduğuna göre artık API’yi çalıştırabilirsiniz.</p>

<p>Module nesnelerini dinamik olarak çağıran bir sınıf oluşturduğunuzu hayal edin. Yani third-party tarafından yazılan ve herhangi bir kod değişikliğine ihtiyaç duymadan uygulamaya yerleştirilebilen eklentileri kabul edebilir. Bunu başarmak için, Module arayüzünde veya soyut üst sınıfta executive() metodunu tanımlayabilir ve tüm alt sınıfları uygulamaya zorlayabilirsiniz. Sisteminizin kullanıcılarının Module sınıflarını harici bir XML yapılandırma dosyasında listelemesine izin verebilirsiniz. Sisteminiz bu bilgiyi, her biri için executive() metodunu çağırmadan önce bir dizi Module nesnesini bir araya getirmek için kullanabilir.</p>

<p>Ancak her Module işini yapmak için farklı bilgilere ihtiyaç duyarsa ne olur? Bu durumda, XML dosyası her Module için property anahtar ve değerleri sağlayabilir ve her Module’ün yaratıcısı, her property için setter metorlar oluşturabilir. Bu temel göz önüne alındığında, doğru property adı için doğru setter metodun çağrıldığından emin olmak kodunuza kalmıştır.</p>

<p>Module arayüzü için birkaç uygulama sınıfını burada bulabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    public $name;

    public function __construct(string $name)
    {
        $this-&gt;name = $name;
    }
}

interface Module
{
    public function execute(): void;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class FtpModule implements Module
{
    public function setHost(string $host): void
    {
        print "FtpModule::setHost(): $host\n";
    }

    public function setUser(string|int $user): void
    {
        print "FtpModule::setUser(): $user\n";
    }

    public function execute(): void
    {
        // do things
    }
}

class PersonModule implements Module
{
    public function setPerson(Person $person): void
    {
        print "PersonModule::setPerson(): {$person-&gt;name}\n";
    }
    
    public function execute(): void
    {
        // do things
    }
}
</code></pre></div></div>

<p>Burada PersonModule ve FtpModule’ün her ikisi de executive() metodunun boş uygulamalarını sağlar. Her sınıf ayrıca, çağrıldıklarını bildirmekten başka hiçbir şey yapmayan setter metotları da uygular. Sistem, tüm setter metotların tek bir parametre beklemesi gerektiği kuralını ortaya koyar: ya bir dize ya da tek bir dize parametresiyle örneklenebilecek bir nesne. PersonModule::setPerson() metodu Person nesnesi beklediğinden, örneğime Person sınıfını dahil ediyorum.</p>

<p>PersonModule ve FtpModule ile çalışmak için bir sonraki adım ModuleRunner sınıfı oluşturmaktır. XML dosyasında sağlanan yapılandırma bilgilerini temsil etmek için Module adına göre indekslenen çok boyutlu bir dizi kullanacaktır. İşte o kod:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ModuleRunner
{
    private array $configData = [
        PersonModule::class =&gt; ['person' =&gt; 'bob'],
        FtpModule::class =&gt; [
            'host' =&gt; 'example.com',
            'user' =&gt; 'anon'
        ]
    ];
    
    private array $modules = [];
    // ...
}
</code></pre></div></div>

<p>ModuleRunner::$configData property’si, iki Module sınıfına referanslar içerir. Her Module öğesi için, bir dizi property içeren alt diziyi tutar. ModuleRunner’ın init() metodu, burada gösterildiği gibi doğru Module nesnelerinin oluşturulmasından sorumludur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ModuleRunner
{
    public function init(): void
    {
        $interface = new \ReflectionClass(Module::class);
        
        foreach ($this-&gt;configData as $modulename =&gt; $params) {
            $module_class = new \ReflectionClass($modulename);
            if (!$module_class-&gt;isSubclassOf($interface)) {
                throw new Exception("unknown module type: $modulename");
            }
            $module = $module_class-&gt;newInstance();
            foreach ($module_class-&gt;getMethods() as $method) {
                $this-&gt;handleMethod($module, $method, $params);
                // we cover handleMethod() in a future listing!
            }
            array_push($this-&gt;modules, $module);
        }
    }
}

$test = new ModuleRunner();
$test-&gt;init();
</code></pre></div></div>

<p>init() metodu, ModuleRunner::$configData dizisi boyunca döngü yapar ve her Module öğesi için bir ReflectionClass nesnesi oluşturmaya çalışır. ReflectionClass’ın kurucusu var olmayan bir sınıfın adıyla çağrıldığında exception fırlatılır, bu nedenle buraya daha fazla hata işleme dahil edeceğim. Module sınıfının Module türüne ait olduğundan emin olmak için ReflectionClass::isSubclassOf() metodunu kullanıyorum.</p>

<p>Her Module’ün executive() metodunu çağırmadan önce bir örneğin oluşturulması gerekir. ReflectionClass::newInstance()’ın amacı budur. Bu metot herhangi bir sayıda parametreyi kabul eder ve bunları ilgili sınıfın kurucu metoduna iletir. Her şey yolunda giderse sınıfın örneğini döndürür.</p>

<p>ReflectionClass::getMethods(), sınıf için mevcut olan tüm ReflectionMethod nesnelerinin dizisini döndürür. Dizideki her öğe için kod, ModuleRunner::handleMethod() metodunu çağırır. Daha sonra ona Module örneğini, ReflectionMethod nesnesini ve Module ile ilişkilendirilecek bir dizi parametre iletir. handleMethod(), Module nesnesinin kurucu metotlarını doğrular ve çağırır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ModuleRunner
{
    public function handleMethod(
        Module $module,
        \ReflectionMethod $method,
        array $params
    ): bool {
        $name = $method-&gt;getName();
        $args = $method-&gt;getParameters();
        if (count($args) != 1 || substr($name, 0, 3) != "set") {
            return false;
        }
        $property = strtolower(substr($name, 3));
        if (!isset($params[$property])) {
            return false;
        }
        if (!$args[0]-&gt;hasType()) {
            $method-&gt;invoke($module, $params[$property]);
            return true;
        }
        $arg_type = $args[0]-&gt;getType();
        if (!($arg_type instanceof \ReflectionUnionType) &amp;&amp; class_exists(
            $arg_type-&gt;getName()
        )) {
            $method-&gt;invoke(
                $module,
                (new \ReflectionClass($arg_type-&gt;getName()))-&gt;newInstance(
                    $params[$property]
                )
            );
        } else {
            $method-&gt;invoke($module, $params[$property]);
        }
        return true;
    }
}
</code></pre></div></div>

<p>handleMethod() ilk önce metotta geçerli bir setter olup olmadığını kontrol eder. Kodda geçerli bir setter metodun setXXXX() olarak adlandırılması ve yalnızca bir parametre bildirmesi gerekir.</p>

<p>Kod, parametrenin kontrol edildiğini varsayarak, metot adının başlangıcından set öğesini kaldırır ve elde edilen dizeyi küçük harf karakterlere dönüştürerek metot adından property adını çıkarır. Bu dize $params dizi parametresini test etmek için kullanılır. Bu dizi, Module nesnesiyle ilişkilendirilecek, kullanıcı tarafından sağlanan property’leri içerir. $params dizisi bu property’i içermiyorsa, false döndürür.</p>

<p>Module metodundan çıkarılan property adı $params dizisindeki bir öğeyle eşleşiyorsa, devam edip doğru setter metodunu çağırabilirim. Bunu yapmak için, setter metodun ilk (ve tek) gerekli parametresinin türünü kontrol etmesi gerekir. Parametrenin tür bildirimi varsa (ReflectionParameter::hasType()) ve belirtilen tür bir sınıfa çözümleniyorsa, o zaman metodun nesne beklediğini biliyoruz. Aksi halde primitive bir tür beklediğini varsayıyoruz.</p>

<p>Setter metotları çağırmak için yeni bir Reflection API metoduna ihtiyacım var. ReflectionMethod::invoke(), temsil ettiği metoda iletmek için bir nesneye (veya static metot için null) ve herhangi bir sayıda metot parametresine ihtiyaç duyar. ReflectionMethod::invoke(), sağlanan nesnenin metoduyla eşleşmemesi durumunda exception fırlatır. Bu metodu iki yoldan biriyle adlandırıyorum. Setter metot nesne parametresi gerektirmiyorsa, kullanıcının sağladığı property dizesiyle ReflectionMethod::invoke()’u çağırırım. Metot nesne gerektiriyorsa (ki bunu class_exists ile test edebilirim), doğru türde bir nesneyi başlatmak için property dizesini kullanırım. Bu daha sonra setter’a iletilir.</p>

<p>Örnek, gerekli nesnenin, kurucusuna yönelik tek bir dize parametresi ile başlatılabileceğini varsayar. Elbette ReflectionClass::newInstance()’ı çağırmadan önce bunu kontrol etmek en iyisidir.</p>

<p>ModuleRunner::init() metodu yürütüldüğünde, nesne, tamamı verilerle hazırlanmış olan Module nesneleri deposuna sahip olur. Artık sınıfa, Module nesneleri arasında döngü yapması ve her birinde execute()çağırması için bir metot verilebilir.</p>

<h3 id="nitelikler-attributes">Nitelikler (Attributes)</h3>

<p>Birçok dil, kaynak dosyalardaki özel etiketlerin kodun kullanımına sunulmasını sağlayan bir mekanizma sağlar. Bunlar genellikle annotation olarak bilinir. PHP paketlerinde (özellikle Doctrine veritabanı kütüphanesi ve Symfony routing bileşeni gibi) PHP 8’e kadar bazı uygulamalar olmasına rağmen, bu özellik için dil düzeyinde destek yoktu. Niteliklerin eklenmesiyle bu durum değişti.</p>

<p>Temel olarak nitelik, bir sınıfa, metoda, property’e, parametreye veya sabite ek bilgi eklemenizi sağlayan özel bir etikettir. Bu bilgi reflection yoluyla sistem tarafından kullanılabilir hale gelir.</p>

<p>Peki annotation’ı ne için kullanabilirsiniz? Tipik olarak bir metot, kullanılmasını beklediği yol hakkında daha fazla bilgi sağlayabilir. Örneğin otomatik olarak çalıştırılması gereken metotları keşfetmek için sınıfı tarayabilirsiniz. İlerledikçe diğer kullanım alanlarından da bahsedeceğim.</p>

<p>Bir annotation bildirelim ve ona erişelim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#[info]
class Person
{
}
</code></pre></div></div>

<p>Yani bir annotation #[ ve ] arasına alınmış bir dize belirteci ile bildirilir. Bu durumda #[info] kullandım. Birçok kod örneğinde, namespace bildirimini hariç tutuyorum çünkü kod, bildirilen namespace veya main içinde eşit derecede iyi çalışacaktır. Ancak bu durumda namespace’e dikkat etmek önemlidir. Bu noktaya geri döneceğim.</p>

<p>Şimdi annotation’a erişmek için:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$attrs = $rpers-&gt;getAttributes();

foreach ($attrs as $attr) {
    print $attr-&gt;getName() . "\n";
}
</code></pre></div></div>

<p>Person’ı inceleyebilmek için ReflectionClass nesnesini başlatıyorum. Sonra getAttributes() metodunu çağırıyorum. Bu, ReflectionAttribute nesnelerinin dizisini döndürür. ReflectionAttribute::getName(), bildirdiğim property’nin adını döndürür.</p>

<p>İşte çıktı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>popp\ch05\batch09\info
</code></pre></div></div>

<p>Çıktıda annotation namespace’e sahip. Adın popp\ch05\batch09 kısmı örtülüdür. Sınıfa referans vermek için kullandığımız kurallara ve alias’lara göre bir annotation’a referans verebilirim. Yani popp\ch05\batch09 namespace’inde [#info] bildirmek, başka bir yerde [#\popp\ch05\batch09\info] bildirmeye eşdeğerdir. Aslında göreceğiniz gibi, referans verdiğiniz herhangi bir nitelik için somutlaştırılabilecek bir sınıf bile tanımlayabilirsiniz.</p>

<p>Annotation PHP’nin çeşitli yönlerine uygulanabilir. Tablo 5-2, ilgili Reflection sınıflarıyla birlikte annotation eklenebilecek özellikleri listelemektedir.</p>

<p><strong>Tablo 5-2</strong>. Annotation’a uygun PHP özellikleri</p>

<table>
  <thead>
    <tr>
      <th>Özellik</th>
      <th>Metot</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Sınıf</td>
      <td>ReflectionClass::getAttributes()</td>
    </tr>
    <tr>
      <td>Property</td>
      <td>ReflectionProperty::getAttributes()</td>
    </tr>
    <tr>
      <td>Fonksiyon/Metot</td>
      <td>ReflectionFunction::getAttributes()</td>
    </tr>
    <tr>
      <td>Sabit</td>
      <td>ReflectionConstant::getAttributes()</td>
    </tr>
  </tbody>
</table>

<p>Aşağıda bir metoda uygulanan niteliğin örneği verilmiştir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#[moreinfo]
public function setName(string $name): void
{
    $this-&gt;name = $name;
}
</code></pre></div></div>

<p>Şimdi ona erişelim. Süreci oldukça tanıdık bulacaksınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$rmeth = $rpers-&gt;getMethod("setName");
$attrs = $rmeth-&gt;getAttributes();

foreach ($attrs as $attr) {
    print $attr-&gt;getName() . "\n";
}

// popp\ch05\batch09\moreinfo
</code></pre></div></div>

<p>Şu ana kadar gördüklerinizin zaten bir faydası var. Bir niteliği bir tür flag olarak kullanabiliriz. Örneğin, bir Hata Ayıklama niteliği yalnızca geliştirme sırasında çağrılması gereken metotlarla ilişkilendirilebilir. Bununla birlikte, niteliklerin hala daha fazlası var. Bir tür tanımlayabilir ve argümanlar aracılığıyla daha fazla bilgi sağlayabiliriz. Bu yeni olasılıkların önünü açıyor. Routing kütüphanesinde, metodun eşlenmesi gereken URL endpoint’ini belirtebilirim. Event sisteminde bir nitelik, bir sınıfın veya metodun belirli bir event ile ilişkilendirilmesi gerektiğinin sinyalini verebilir.</p>

<p>Bu örnekte iki argüman içeren bir nitelik tanımlıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#[ApiInfo("The 3 digit company identifier", "A five character department tag")]
public function setInfo(int $companyid, string $department): void
{
    $this-&gt;companyid = $companyid;
    $this-&gt;department = $department;
}
</code></pre></div></div>

<p>ReflectionAttribute nesnesini edindikten sonra getArguments() metodunu kullanarak argümanlara erişebilirim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$rmeth = $rpers-&gt;getMethod("setInfo");
$attrs = $rmeth-&gt;getAttributes();

foreach ($attrs as $attr) {
    print $attr-&gt;getName() . "\n";
    foreach ($attr-&gt;getArguments() as $arg) {
        print " - $arg\n";
    }
}
</code></pre></div></div>

<p>İşte çıktısı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>popp\ch05\batch09\ApiInfo
 - The 3 digit company identifier
 - A five character department tag
</code></pre></div></div>

<p>Bahsettiğim gibi, bir niteliği açıkça bir sınıfa eşleyebilirsiniz. İşte basit bir ApiInfo sınıfı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace popp\ch05\batch09;

use Attribute;

#[Attribute]
class ApiInfo
{
    public function __construct(public string $compinfo, public string $depinfo)
    {
    }
}
</code></pre></div></div>

<p>Nitelik ile sınıfım arasında düzgün bir ilişki kurabilmek için, Attribute’u kullanmayı ve ayrıca yerleşik [#Attribute]’u sınıfa uygulamayı hatırlamalıyım.</p>

<p>Örnekleme sırasında, niteliğe ilişkin tüm argümanlar otomatik olarak karşılık gelen sınıfın kurucusuna iletilir. Bu durumda, verileri yalnızca ilgili property’lere atadım. Gerçek dünyadaki bir uygulamada, muhtemelen sınıf bildirimini doğrulamak için bazı ek işlemler gerçekleştirir veya ilgili işlevleri sağlardım.</p>

<p>Nitelik sınıfının otomatik olarak çağrılmadığını anlamak önemlidir. Bunu ReflectionAttribute::newInstance() aracılığıyla yapmalıyız. Kodumu yeni sınıfla çalışacak şekilde uyarlıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$rmeth = $rpers-&gt;getMethod("setInfo");
$attrs = $rmeth-&gt;getAttributes();

foreach ($attrs as $attr)  {
    print $attr-&gt;getName() . "\n";
    $attrobj = $attr-&gt;newInstance();
    print " - " . $attrobj-&gt;compinfo . "\n";
    print " - " . $attrobj-&gt;depinfo . "\n";
}
</code></pre></div></div>

<p>Her ne kadar nitelik verilerine ApiInfo nesnesi aracılığıyla erişiyor olsam da buradaki etki aynıdır. ReflectionAttribute::newInstance()’ı çağırıyorum ve ardından doldurulmuş property’lere erişiyorum.</p>

<p>Bu son örneğin potansiyel olarak ölümcül bir kusuru var. Bir metoda birden fazla nitelik eklenebilir. Bu nedenle setInfo() metofuna atanan her niteliğin ApiInfo’nun bir örneği olduğundan emin olamayız. ApiInfo::$compinfo ve ApiInfo::$depinfo’ya bu property erişimleri, ApiInfo türünde olmayan herhangi bir nitelik için başarısız olmaya mahkumdur.</p>

<p>Neyse ki getAttributes()’a bir filtre uygulayabiliyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$rmeth = $rpers-&gt;getMethod("setInfo");
$attrs = $rmeth-&gt;getAttributes(ApiInfo::class);
</code></pre></div></div>

<p>Artık yalnızca ApiInfo::class için kesin olan eşleşmeler döndürülecek ve böylece kodun geri kalanı güvenli hale getirilecek. İşleri biraz daha gevşetebiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$rpers = new \ReflectionClass(Person::class);
$rmeth = $rpers-&gt;getMethod("setInfo");
$attrs = $rmeth-&gt;getAttributes(ApiInfo::class, \ReflectionAttribute::IS_INSTANCEOF);
</code></pre></div></div>

<p>İkinci bir parametre olan ReflectionAttribute::IS_INSTANCEOF’u ReflectionAttribute::getAttributes()’a ileterek, filtreyi belirtilen sınıfla, miras alan alt sınıflarla veya arayüzlerle eşleşecek şekilde düzenliyorum.</p>

<p>Tablo 5-3 karşılaştığımız ReflectionAttribute metotlarını listelemektedir.</p>

<p><strong>Tablo 5-3</strong>. Bazı ReflectionAttribute metotları</p>

<table>
  <thead>
    <tr>
      <th>Metot</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>getName()</td>
      <td>Nitelik için namespace’li bir tür döndürür</td>
    </tr>
    <tr>
      <td>getArguments()</td>
      <td>Başvurulan nitelikle ilişkili tüm parametrelerin dizisini döndürür</td>
    </tr>
    <tr>
      <td>newInstance()</td>
      <td>Herhangi bir argümanı kurucuya ileterek nitelik sınıfının örneğini başlatır ve döndürür</td>
    </tr>
  </tbody>
</table>

<p><strong>Özet</strong></p>

<p>Bu bölümde kütüphane ve sınıflarınızı yönetmek için kullanabileceğiniz bazı teknik ve araçları ele aldım. PHP’nin namespace özelliğini araştırdım. Sınıflar için esnek bir organizasyon sağlamak amacıyla namespace, autoloading ve dosya sistemini birleştirebileceğimizi gördünüz.</p>

<p>Ayrıca Reflection API ile işleri bir sonraki aşamaya taşımadan önce PHP’nin nesne ve sınıf fonksiyonlarını da inceledik. Reflection’ın sunduğu potansiyel kullanımlardan birini gösteren basit bir örnek oluşturmak için Reflection sınıflarını kullandık. Son olarak Reflection sınıflarını niteliklerle birleştirdik: PHP 8’de yeni ve önemli bir özellik.</p>

<p><strong>Kaynak:</strong> “PHP 8 Objects, Patterns, and Practice”, Matt Zandstra</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Gördüğümüz gibi PHP, sınıflar ve metotlar gibi dil yapıları aracılığıyla nesne yönelimli programlamayı destekler. Dil ayrıca nesnelerle çalışmanıza yardımcı olmak için tasarlanmış fonksiyon ve sınıflar aracılığıyla daha geniş destek sağlar.]]></summary></entry><entry><title type="html">PHP - Gelişmiş Özellikler</title><link href="http://localhost:4000/2024/03/22/php-gelismis-ozellikler.html" rel="alternate" type="text/html" title="PHP - Gelişmiş Özellikler" /><published>2024-03-22T00:00:00+03:00</published><updated>2024-03-22T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/22/php-gelismis-ozellikler</id><content type="html" xml:base="http://localhost:4000/2024/03/22/php-gelismis-ozellikler.html"><![CDATA[<p>Sınıf tipi ipuçlarının ve erişim kontrolünün size bir sınıfın arayüzü üzerinde nasıl daha fazla kontrol sağladığını zaten gördünüz. Bu bölümde, PHP’nin nesne yönelimli özelliklerini daha derinlemesine inceleyeceğim.</p>

<p>Bu bölüm birkaç konuyu kapsayacaktır:</p>

<ul>
  <li><em>Static metotlar ve property’ler</em>: Verilere ve işlevselliğe nesneler yerine sınıflar aracılığıyla erişme</li>
  <li><em>Soyut (Abstract) sınıflar ve interface</em>: Tasarımı uygulamadan ayırma</li>
  <li><em>Trait</em>: Uygulamayı sınıf hiyerarşileri arasında paylaşma</li>
  <li><em>Hata işleme</em>: İstisnaların (Exception) tanıtılması</li>
  <li><em>Final sınıf ve metotlar</em>: Kalıtımın sınırlandırılması</li>
  <li><em>Interceptor metotlar</em>: Delegasyonu otomatikleştirme</li>
  <li><em>Yıkıcı (Destructor) metotlar</em>: Nesnelerinizin arkasını temizlemek</li>
  <li><em>Nesneleri klonlamak</em>: Nesne kopyaları oluşturma</li>
  <li><em>Nesneleri dizelere dönüştürmek</em>: Özet metodu oluşturma</li>
  <li><em>Callbacks</em>: Anonim işlev ve sınıflara sahip bileşenlere işlevsellik ekleme</li>
</ul>

<h2 id="static-metot-ve-propertyler">Static Metot ve Property’ler</h2>

<p>Önceki bölümdeki örneklerin tümü nesnelerle çalıştı. Sınıfları, nesnelerin üretildiği şablonlar olarak ve nesneleri, metotlarını çağırdığınız ve property’lerine eriştiğiniz sınıfların aktif örnekleri olarak tanımladım. Nesne yönelimli programlamada gerçek işin sınıf örnekleri tarafından yapıldığını ima ettim. Ne de olsa sınıflar, yalnızca nesneler için şablonlardır.</p>

<p>Aslında, o kadar basit değil. Hem metotlara hem de property’lere nesne yerine sınıf bağlamında erişebilirsiniz. Bu tür metot ve property’ler “statik” tir ve static anahtar kelimesi kullanılarak bildirilmelidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class StaticExample
{
    public static int $aNum = 0;

    public static function sayHello(): void
    {
        print "hello";
    }
}
</code></pre></div></div>

<p>Static metotlar, sınıf kapsamına sahip fonksiyonlardır. Sınıftaki herhangi bir normal property’e kendileri erişemezler çünkü bunlar bir nesneye ait olacaktır; ancak static property’lere erişebilirler. Static bir property’i değiştirirseniz, o sınıfın tüm örnekleri yeni değere erişebilir.</p>

<p>Static öğeye örnek aracılığıyla değil, sınıf aracılığıyla eriştiğiniz için, nesneye başvuran bir değişkene ihtiyacınız yoktur. Bunun yerine, bu örnekte olduğu gibi sınıf adını :: ile birlikte kullanırsınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print StaticExample::$aNum;
StaticExample::sayHello();
</code></pre></div></div>

<p>Bu sözdizimi önceki bölümden tanıdık olmalıdır. Geçersiz kılınan bir metoda erişmek için parent ile birlikte :: kullandım. Şimdi, o zaman olduğu gibi, nesne yerine sınıfa erişiyorum. Sınıf kodu, sınıf adını kullanmadan bir üst sınıfa erişmek için parent anahtar sözcüğünü kullanabilir. Static bir metod veya property’e aynı sınıf içinden erişmek için self anahtar sözcüğünü kullanıyorum. Nesneler için $this sözde değişkeni ne ise sınıflar için “self” odur. StaticExample sınıfının dışından $aNum property’sine sınıf adını kullanarak erişiyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>StaticExample::$aNum;
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class StaticExample2
{
    public static int $aNum = 0;

    public static function sayHello(): void
    {
        self::$aNum++;
        print "hello (" . self::$aNum . ")\n";
    }
}
</code></pre></div></div>

<p><strong>Not</strong>: parent kullanarak metot çağrısı yapmak, static olmayan bir metoda static başvuru kullanmanız gereken tek durumdur.</p>

<p>Geçersiz kılınan bir metoda erişemiyorsanız, açıkça static olarak bildirilen bir metoda veya  property’e erişmek için yalnızca :: kullanmalısınız.</p>

<p>Bununla birlikte, belgelerde, genellikle bir metot veya property’e atıfta bulunmak için static sözdizimi kullanıldığını görürsünüz. Bu, söz konusu öğenin mutlaka static olduğu anlamına gelmez, yalnızca belirli bir sınıfa aittir. Örneğin ShopProductWriter sınıfının write() metodu static olmasa bile ShopProductWriter::write() olarak anılabilir. Uygun durumlarda bu sözdizimini burada göreceksiniz.</p>

<p>Tanım gereği, static metot ve property’ler nesnelerde değil, sınıflarda çağrılır. Bu nedenle, genellikle sınıf değişkenleri ve property’leri olarak adlandırılırlar. Bu sınıf yöneliminin sonucu olarak, static bir metot içinde $this sözde değişkenini kullanamazsınız.</p>

<p>Öyleyse neden static bir metot veya property kullanasınız? Static elemanlar, yararlı olabilecek bir dizi özelliğe sahiptir. İlk olarak, kodunuzun herhangi bir yerinden - sınıfa erişiminiz olduğu varsayılarak - erişilebilirler. Bu, sınıfın örneğini nesneden nesneye aktarmaya veya daha da kötüsü örneği global değişkende depolamaya gerek kalmadan işlevselliğe erişebileceğiniz anlamına gelir. İkinci olarak, sınıfın her örneği için static property mevcuttur, böylece bir türün tüm üyeleri tarafından kullanılabilir olmasını istediğiniz değerleri ayarlayabilirsiniz. Son olarak, static property veya metoda erişmek için bir örneğe ihtiyacınızın olmaması, sizi yalnızca basit bir işleve ulaşmak için nesneyi başlatmaktan kurtarabilir.</p>

<p>Bunu göstermek için, ShopProduct nesnelerinin örneklenmesini otomatikleştirmek için static bir metot oluşturacağım. SQLite kullanarak şöyle bir products tablosu tanımlayabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT,
    firstname TEXT,
    mainname TEXT,
    title TEXT,
    price float,
    numpages int,
    playlength int,
    discount int )
</code></pre></div></div>

<p>Şimdi row ID ve PDO nesnesi kabul eden, bunları bir veritabanı satırı elde etmek için kullanan ve ardından ShopProduct nesnesi döndüren getInstance() metodu oluşturmak istiyorum. Bir önceki bölümde oluşturduğum ShopProduct sınıfına bu metotları ekleyebilirim. Muhtemelen bildiğiniz gibi, PDO, PHP Data Object anlamına gelir. PDO sınıfı, farklı veritabanı uygulamaları için ortak bir arabirim sağlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// ShopProduct class...
private int $id = 0;
// ...
public function setID(int $id): void
{
    $this-&gt;id = $id;
}
// ...
public static function getInstance(int $id, \PDO $pdo): ShopProduct
{
    $stmt = $pdo-&gt;prepare("select * from products where id=?");
    $result = $stmt-&gt;execute([$id]);
    $row = $stmt-&gt;fetch();
    if (empty($row)) {
        return null;
    }
    if ($row['type'] == "book") {
        $product = new BookProduct(
            $row['title'],
            $row['firstname'],
            $row['mainname'],
            (float) $row['price'],
            (int) $row['numpages']
        );
    } elseif ($row['type'] == "cd") {
        $product = new CdProduct(
            $row['title'],
            $row['firstname'],
            $row['mainname'],
            (float) $row['price'],
            (int) $row['playlength']
        );
    } else {
        $firstname = (is_null($row['firstname'])) ? "" :
            $row['firstname'];
        $product = new ShopProduct(
            $row['title'],
            $firstname,
            $row['mainname'],
            (float) $row['price']
        );
    }
    $product-&gt;setId((int) $row['id']);
    $product-&gt;setDiscount((int) $row['discount']);
    return $product;
}
</code></pre></div></div>

<p>Gördüğünüz gibi, getInstance() metodu ShopProduct nesnesi döndürür ve türe bağlı  olarak, başlatması gereken nesneyi belirleyecek kadar akıllıdır. Örneği kompakt tutmak için herhangi bir hata işlemeyi atladım. Örneğin, bunun gerçek dünyadaki versiyonunda, sağlanan PDO nesnesinin doğru veritabanıyla konuşmak için başlatıldığını varsayacak kadar güvenmezdim. Aslında, muhtemelen PDO’yu bu davranışı garanti edecek bir sınıfla sarardım. Bölüm 13’te nesne yönelimli programlama ve veritabanları hakkında daha fazla bilgi edinebilirsiniz.</p>

<p>Bu metot, sınıf bağlamında nesne bağlamından daha kullanışlıdır. Başlamak için ShopProduct nesnesine sahip olmanızı gerektirmeden, veritabanındaki ham verileri kolayca bir nesneye dönüştürmenize olanak tanır. Metot, herhangi bir örnek property veya metodu kullanmaz, bu nedenle static olarak bildirilmemesi için hiçbir neden yoktur. Geçerli bir PDO nesnesi verildiğinde, metodu uygulamanın herhangi bir yerinden çağırabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$dsn = "sqlite:/tmp/products.sqlite3";
$pdo = new \PDO($dsn, null, null);
$pdo-&gt;setAttribute(\PDO::ATTR_ERRMODE, \PDO::ERRMODE_EXCEPTION);
$obj = ShopProduct::getInstance(1, $pdo);
</code></pre></div></div>

<p>Bunun gibi metotlar, hammaddeleri (satır verileri veya yapılandırma bilgileri gibi) alıp bunlarla nesneler ürettikleri için “fabrika” (factory) görevi görür. Factory terimi, nesne örnekleri oluşturmak için tasarlanmış koda uygulanır. İlerleyen bölümlerde yine factory örnekleri ile karşılaşacaksınız.</p>

<p>Elbette bazı açılardan bu örnek çözdüğü kadar çok sorunu da beraberinde getiriyor. ShopProduct::getInstance() metodu ShopProduct örneğine ihtiyaç duymadan sistemin herhangi bir yerinden erişilebilir kılsam da, yazılımcının PDO nesnesi sağlamasını da talep ediyorum. Bu nerede bulunur? Ve bir üst sınıfın alt sınıflar hakkında bu kadar bilgiye sahip olması gerçekten iyi bir uygulama mı? (İpucu: Hayır, değil.) Bu tür problemler - anahtar nesnelerin ve değerlerin nereden elde edileceği ve sınıfların birbiri hakkında ne kadar bilgi sahibi olması gerektiği - nesne yönelimli programlamada çok yaygındır. Bölüm 9’da nesne üretimine yönelik çeşitli yaklaşımları inceliyorum.</p>

<h2 id="sabit-constant-propertyler">Sabit (Constant) Property’ler</h2>

<p>Bazı property’ler değiştirilmemelidir. Hata ve status flag’leri genellikle sınıflarınıza sabit olarak kodlanır. Herkese açık ve static olarak erişilebilir olmaları gerekmesine rağmen, yazılımcı bunları değiştirememelidir.</p>

<p>PHP, sınıf içinde sabit property’ler tanımlamanıza izin verir. Global sabitler gibi, sınıf sabitleri de ayarlandıktan sonra değiştirilemezler. Sabit property, const anahtar sözcüğüyle bildirilir. Sabitler, normal property’ler gibi dolar işareti ile ön eklenmez. Geleneksel olarak, genellikle yalnızca büyük harfler kullanılarak adlandırılırlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public const AVAILABLE = 0;
    public const OUT_OF_STOCK = 1;
</code></pre></div></div>

<p>Sabit property’ler yalnızca primitive değerler içerebilir. Bir sabite nesne atayamazsınız. Static property’ler gibi, sabit property’lere de örnek üzerinden değil, sınıf aracılığıyla erişilir. Dolar işareti olmadan sabit tanımlarken olduğu gibi, sabite atıfta bulunduğunuzda baştaki simgeye gerek yoktur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print ShopProduct::AVAILABLE;
</code></pre></div></div>

<p><strong>Not</strong>: Sabit görünürlük bildirimi (visibility modifiers) için destek PHP 7.1’de sunulmuştur. Property’ler de olduğu gibi çalışırlar.</p>

<p>Bildirildikten sonra sabite değer atamaya çalışmak ayrıştırma hatasına neden olur.</p>

<p>Property’nizin sınıfın tüm örneklerinde kullanılabilir olması ve property değerinin sabitlenmesi ve değişmemesi gerektiğinde sabitleri kullanmalısınız.</p>

<h2 id="soyut-abstract-sınıflar">Soyut (Abstract) Sınıflar</h2>

<p>Soyut bir sınıf somutlaştırılamaz. Bunun yerine, onu genişletebilecek herhangi bir sınıf için arayüz tanımlar (ve isteğe bağlı olarak kısmen uygular).</p>

<p>Soyut sınıf abstract anahtar sözcüğü ile tanımlanır. Bir önceki bölümde oluşturduğum ShopProductWriter sınıfını bu kez soyut sınıf olarak yeniden tanımlıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class ShopProductWriter
{
    protected array $products = [];
    
    public function addProduct(ShopProduct $shopProduct): void
    {
        $this-&gt;products[] = $shopProduct;
    }
}
</code></pre></div></div>

<p>Metot ve property’leri normal şekilde oluşturabilirsiniz, ancak soyut bir nesneyi bu şekilde başlatma girişimleri hataya neden olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$writer = new ShopProductWriter();
</code></pre></div></div>

<p>Bu çıktıda hatayı görebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: Cannot instantiate abstract class
popp\ch04\batch03\ShopProductWriter
</code></pre></div></div>

<p>Çoğu durumda, soyut sınıf en az bir soyut metot içerecektir. Bunlar yine abstract anahtar sözcüğü ile bildirilir. Soyut metodun uygulaması olamaz. Bunu normal şekilde bildirirsiniz, ancak bildirimi metot gövdesi yerine noktalı virgülle biter. Burada, ShopProductWriter sınıfına soyut bir write() metodu ekliyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class ShopProductWriter
{
    protected array $products = [];

    public function addProduct(ShopProduct $shopProduct): void
    {
        $this-&gt;products[] = $shopProduct;
    }
    
    abstract public function write(): void;
}
</code></pre></div></div>

<p>Soyut bir metot oluştururken, uygulamanın tüm somut alt sınıflarda mevcut olmasını sağlarsınız, ancak bu uygulamanın ayrıntılarını tanımsız bırakırsınız.</p>

<p>Bu örnekte olduğu gibi, write() metodunu uygulamayan ShopProductWriter’dan türetilen bir sınıf oluşturacağımı varsayalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ErroredWriter extends ShopProductWriter
{
}
</code></pre></div></div>

<p>Aşağıdaki hatayla karşılaşıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fatal error: Class popp\ch04\batch03\ErroredWriter contains 1 abstract
method and must therefore be declared abstract or implement the remaining methods (popp\ch04\batch03\ShopProductWriter::write) in...
</code></pre></div></div>

<p>Bu nedenle, soyut sınıfı genişleten herhangi bir sınıf, tüm soyut metotları uygulamalı veya kendisi soyut olarak bildirilmelidir. Genişleyen sınıf, soyut metodu uygulamaktan daha fazlasından sorumludur. Bunu yaparken, metot imzasını yeniden üretmelidir. Bu, uygulama metodunun erişim kontrolünün soyut metottan daha katı olamayacağı anlamına gelir. Uygulama metodu ayrıca, soyut metotla aynı sayıda parametre gerektirmelidir.</p>

<p>İşte ShopProductWriter’ın iki uygulaması: Birincisi, XmlProductWriter:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class XmlProductWriter extends ShopProductWriter
{
    public function write(): void
    {
        $writer = new \XMLWriter();
        $writer-&gt;openMemory();
        $writer-&gt;startDocument('1.0', 'UTF-8');
        $writer-&gt;startElement("products");

        foreach ($this-&gt;products as $shopProduct) {
            $writer-&gt;startElement("product");
            $writer-&gt;writeAttribute("title", $shopProduct-&gt;getTitle());
            $writer-&gt;startElement("summary");
            $writer-&gt;text($shopProduct-&gt;getSummaryLine());
            $writer-&gt;endElement(); // summary
            $writer-&gt;endElement(); // product
        }
        
        $writer-&gt;endElement(); // products
        $writer-&gt;endDocument();
        print $writer-&gt;flush();
    }
}
</code></pre></div></div>

<p>Bu, daha basit TextProductWriter’dır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class TextProductWriter extends ShopProductWriter
{
    public function write(): void
    {
        $str = "PRODUCTS:\n";
        foreach ($this-&gt;products as $shopProduct) {
            $str .= $shopProduct-&gt;getSummaryLine() . "\n";
        }
        print $str;
    }
}
</code></pre></div></div>

<p>Böylece, her biri kendi write() metot uygulamasına sahip iki sınıf oluşturdum. Birincisi XML çıktısı, ikincisi ise metin çıktısı verir. ShopProductWriter nesnesi gerektiren bir metot, bu iki sınıftan hangisini aldığını bilemez, ancak write() metodunun uygulandığından kesinlikle emin olabilir. Dizi olarak ele almadan önce $products’ın türünü test etmediğimi unutmayın. Bunun nedeni, bu property’nin hem bir dizi olarak bildirilmesi hem de ShopProductWriter sınıfında başlatılmasıdır.</p>

<h2 id="arayüzler-interfaces">Arayüzler (Interfaces)</h2>

<p>Soyut sınıflar, bir miktar uygulama sağlamanıza izin verse de, arayüzler saf şablonlardır. Arayüz yalnızca işlevselliği tanımlayabilir; asla uygulayamaz. interface anahtar kelimesiyle arayüz tanımlanır. Property ve metot bildirimlerini içerebilir, ancak metot gövdelerini içeremez.</p>

<p>İşte bir arayüz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>interface Chargeable
{
    public function getPrice(): float;
}
</code></pre></div></div>

<p>Gördüğünüz gibi, arayüz bir sınıfa çok benziyor. Bu arayüzü içeren herhangi bir sınıf, tanımladığı tüm metotları uygulamayı taahhüt eder veya soyut olarak bildirilmelidir.</p>

<p>Bir sınıf, bildiriminde implements anahtar sözcüğünü kullanarak arayüzü uygulayabilir. Bunu yaptıktan sonra, arayüzü uygulama süreci, soyut bir sınıfı genişletmekle aynıdır. Şimdi ShopProduct sınıfının Chargeable uygulamasını yapacağım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct implements Chargeable
{
    // ...
    protected float $price;
    // ...
    public function getPrice(): float
    {
        return $this-&gt;price;
    }
    // ...
}
</code></pre></div></div>

<p>ShopProduct zaten getPrice() metoduna sahipti, öyleyse Chargeable arayüzünü uygulamak neden yararlı olabilir? Bir kez daha, cevabın türlerle ilgisi var. Uygulama sınıfı, genişlettiği sınıfın türünü ve uyguladığı arayüzü alır.</p>

<p>Bu, CdProduct sınıfının aşağıdakilere ait olduğu anlamına gelir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CdProduct
ShopProduct
Chargeable
</code></pre></div></div>

<p>Bir nesnenin türünü bilmek, onun yeteneklerini bilmektir. Bu metodu göz önünde bulundurun:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function cdInfo(CdProduct $prod): int
{
    // we know we can call getPlayLength()
    $length = $prod-&gt;getPlayLength();
    // ...
}
</code></pre></div></div>

<p>Metot, $prod nesnesinin, ShopProduct sınıfında ve Chargeable arabiriminde tanımlanan tüm metotlara ek olarak getPlayLength() metoduna sahip olduğunu bilir.</p>

<p>Daha genel tür gereksinimi olan bir metot - CdProduct yerine ShopProduct - sağlanan nesnenin yalnızca ShopProduct metotlarını içerdiğini bilebilir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function addProduct(ShopProduct $prod)
{
    // even if $prod is a CdProduct object
    // we don't *know* this -- so we can't
    // presume to use getPlayLength()
    // ...
}
</code></pre></div></div>

<p>Daha fazla test yapılmadan metot, getPlayLength() metodu hakkında hiçbir şey bilmeyecektir.</p>

<p>Chargeable nesnesi gerektiren bir metot ShopProduct veya CdProduct türleriyle ilgili  hiçbir şey bilmiyor:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function addChargeableItem(Chargeable $item)
{
    // all we know about $item is that it
    // is a Chargeable object -- the fact that it
    // is also a CdProduct object is irrelevant.
    // We can only be sure of getPrice()
    //
    //...
}
</code></pre></div></div>

<p>Bu metot yalnızca $item parametresinin getPrice() metodu içerip içermediğiyle ilgilenir.</p>

<p>Herhangi bir sınıf bir arayüz uygulayabildiğinden (aslında, bir sınıf herhangi bir sayıda arayüz uygulayabilir), arayüzler alakasız olan türleri etkili bir şekilde birleştirir. Chargeable’ı uygulayan tamamen yeni bir sınıf tanımlayabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Shipping implements Chargeable
{
    public function __construct(private float $price)
    {
    }
    
    public function getPrice(): float
    {
        return $this-&gt;price;
    }
}
</code></pre></div></div>

<p>ShopProduct nesnesinde olduğu gibi, Shipping nesnesini de addChargeableItem() metoduna iletebilirim.</p>

<p>Chargeable nesnesiyle çalışan bir istemci için önemli olan, getPrice() metodunu çağırabilmesidir. Kullanılabilir diğer tüm metotlar, nesnenin kendi sınıfı, bir üst sınıf veya başka bir arayüz aracılığıyla diğer türlerle ilişkilendirilir.</p>

<p>Bir sınıf, hem üst sınıfı genişletebilir hem de herhangi bir sayıda arayüz uygulayabilir. extends ifadesi, implements ifadesinden önce gelmelidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Consultancy extends TimedService implements Bookable, Chargeable
{
    // ...
}
</code></pre></div></div>

<p>Consultancy sınıfının birden fazla arayüz uyguladığına dikkat edin. Birden çok arayüz, virgülle ayrılmış bir listede implements anahtar sözcüğünü takip eder. PHP yalnızca tek bir ebeveynden kalıtımı destekler, bu nedenle extends anahtar sözcüğü yalnızca tek bir sınıf adından önce gelebilir.</p>

<h2 id="traitler">Trait’ler</h2>

<p>Gördüğümüz gibi, arayüzler Java gibi PHP’nin çoklu kalıtımı desteklemediği gerçeğini yönetmenize yardımcı olur. Başka bir deyişle, PHP’deki bir sınıf yalnızca tek bir ebeveyni genişletebilir. Ancak, bir sınıfa istediğiniz kadar arayüz uygulayabilirsiniz; sınıf, uyguladığı her arayüz için karşılık gelen türü alır.</p>

<p>Böylece arayüzler, uygulama olmadan türler sağlar. Peki ya bir uygulamayı kalıtım hiyerarşileri arasında paylaşmak isterseniz? PHP 5.4 trait’leri tanıttı ve bunlar tam olarak bunu yapmanıza izin veriyor.</p>

<p>Trait, kendisi somutlaştırılamayan ancak sınıflara dahil edilebilen sınıf benzeri bir yapıdır. Trait’te tanımlanan tüm metotlar, onu kullanan herhangi bir sınıfın parçası olarak kullanılabilir hale gelir. Trait, bir sınıfın yapısını değiştirir, ancak türünü değiştirmez.</p>

<p>Trait’in neden yararlı olabileceğine bakalım.</p>

<h3 id="traitlerin-çözmesi-gereken-bir-problem">Trait’lerin Çözmesi Gereken Bir Problem</h3>

<p>İşte ShopProduct sınıfının calculateTax() metotlu versiyonu:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    private int $taxrate = 20;
    // ...
    public function calculateTax(float $price): float
    {
        return (($this-&gt;taxrate / 100) * $price);
    }
}
</code></pre></div></div>

<p>calculateTax() metodu, $price parametresini alır ve private $taxrate property’sine göre satış vergisi tutarını hesaplar.</p>

<p>Tabii ki, bir alt sınıf calculateTax()’a erişebilir. Peki ya tamamen farklı sınıf hiyerarşileri? Service sınıfından miras alan UtilityService adlı bir sınıf düşünün. UtilityService’in aynı yordamı kullanması gerekiyorsa, kendimi calculateTax()’ı kopyalarken bulabilirim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class Service
{
    // service oriented stuff
}

// And here is UtilityService:
class UtilityService extends Service
{
    private int $taxrate = 20;

    public function calculateTax(float $price): float
    {
        return (($this-&gt;taxrate / 100) * $price);
    }
}
</code></pre></div></div>

<p>UtilityService ve ShopProduct herhangi bir ortak temel sınıfa sahip olmadığından, calculateTax() uygulamasını kolayca paylaşamazlar. Bu nedenle, uygulamamızı bir sınıftan diğerine kopyalayıp yapıştırmak zorunda kalıyoruz.</p>

<h3 id="trait-tanımlamak-ve-kullanmak">Trait Tanımlamak ve Kullanmak</h3>

<p>Bu kitapta ele alacağım nesne yönelimli tasarım hedeflerinden biri, kod tekrarından kaçınmaktır. Bölüm 11’de göreceğiniz gibi, bu tür bir yinelemenin çözümü, onu yeniden kullanılabilir bir strateji sınıfına ayırmaktır. Trait’ler başka bir yaklaşım sağlar - çok şık olmasa da kesinlikle etkilidir.</p>

<p>Burada, calculateTax() metodunu tanımlayan tek bir trait bildiriyorum ve ardından bunu hem ShopProduct hem de UtilityService’e dahil ediyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trait PriceUtilities
{
    private $taxrate = 20;
    
    public function calculateTax(float $price): float
    {
        return (($this-&gt;taxrate / 100) * $price);
    }
    // other utilities
}
</code></pre></div></div>

<p>trait anahtar sözcüğü ile PriceUtilities’i deklare ediyorum. Trait gövdesi, sınıfın gövdesine çok benziyor. Basitçe parantez içinde toplanan bir dizi metot ve property’dir. Bunu tanımladıktan sonra, sınıflarımdan PriceUtilities trait’ine erişebilirim. Bunu, use anahtar kelimesini ve ardından dahil etmek istediğim trait adını kullanarak yapıyorum. calculateTax() metodunu tek bir yerde tanımlayıp uyguladıktan sonra, devam edip ShopProduct sınıfına dahil ediyorum.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>use popp\ch04\batch06_1\PriceUtilities;

class ShopProduct
{
    use PriceUtilities;
}
</code></pre></div></div>

<p>Ayrıca, tabii ki onu UtilityService sınıfına ekliyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities;
}
</code></pre></div></div>

<p>Şimdi, bu sınıfları çağırdığımda, kod tekrarı olmadan PriceUtilities uygulamasını paylaştıklarını biliyorum. PriceUtilities’de bir hata bulsaydım, onu tek bir yerde düzeltebilirdim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$p = new ShopProduct();
print $p-&gt;calculateTax(100) . "\n";

$u = new UtilityService();
print $u-&gt;calculateTax(100) . "\n";
</code></pre></div></div>

<h3 id="birden-fazla-trait-kullanmak">Birden Fazla Trait Kullanmak</h3>

<p>Her birini use anahtar sözcüğünden sonra virgülle ayırıp listeleyerek bir sınıfa birden fazla trait ekleyebilirsiniz. Bu örnekte, orijinal PriceUtilities trait’imi koruyarak yeni bir trait olan IdentityTrait‘i tanımlayıp uyguluyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trait IdentityTrait
{
    public function generateId(): string
    {
        return uniqid();
    }
}
</code></pre></div></div>

<p>Hem PriceUtilities hem de IdentityTrait’i use anahtar sözcüğüyle uygulayarak, calculateTax() ve generateId() metodlarını ShopProduct sınıfı için kullanılabilir hale getiriyorum.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    use PriceUtilities;
    use IdentityTrait;
}
</code></pre></div></div>

<p><strong>Not</strong>: IdentityTrait, generateId() metodunu sağlar. Aslında, veritabanı genellikle nesneler için tanımlayıcılar üretir, ancak yerel uygulamalarda test amacıyla kullanabilirsiniz. Identity Map pattern’ini kapsayan Bölüm 13’te nesneler, veritabanları ve benzersiz tanımlayıcılar (unique identifiers) hakkında daha fazla bilgi edinebilirsiniz. Bölüm 18’de test etme hakkında daha fazla bilgi edinebilirsiniz.</p>

<p>Artık ShopProduct sınıfında hem generateId() hem de calculateTax() metodlarını çağırabilirim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$p = new ShopProduct();
print $p-&gt;calculateTax(100) . "\n";
print $p-&gt;generateId() . "\n";
</code></pre></div></div>

<h3 id="trait-ve-arayüzleri-birleştirmek">Trait ve Arayüzleri Birleştirmek</h3>

<p>Trait’ler yararlı olsalar da uygulandıkları sınıfın türünü değiştirmezler. Dolayısıyla, IdentityTrait‘i birden çok sınıfa uyguladığınızda, metot imzasında ima edilebilecek bir türü paylaşmazlar.</p>

<p>Neyse ki, trait’ler arayüzlerle iyi çalışıyor. generateId() metodu gerektiren bir arayüz tanımlayabilir ve ardından ShopProduct’ın bunu uyguladığını bildirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>interface IdentityObject
{
    public function generateId(): string;
}
</code></pre></div></div>

<p>ShopProduct’ın IdentityObject trait’ini yerine getirmesini istiyorsam, şimdi onun IdentityObject arayüzünü uygulamasını sağlamalıyım.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct implements IdentityObject
{
    use PriceUtilities;
    use IdentityTrait;
}
</code></pre></div></div>

<p>Daha önce olduğu gibi ShopProduct, IdentityTrait‘i kullanır. Ancak, bu içe aktarılan metot, artık IdentityObject arayüzü taahhüdünü de yerine getiriyor. Bu, ShopProduct nesnelerini, IdentityObject örneklerini talep etmek için tip ipuçlarını (type hinting) kullanan metotlara ve fonksiyonlara aktarabileceğimiz anlamına gelir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function storeIdentityObject(IdentityObject $idobj)
{
    // do something with the IdentityObject
}
</code></pre></div></div>

<h3 id="metot-adı-çakışmalarını-yönetmek">Metot Adı Çakışmalarını Yönetmek</h3>

<p>Trait’leri birleştirme yeteneği güzel bir özelliktir, ancak er ya da geç çakışmalar kaçınılmazdır. Örneğin, calculateTax() metodu sağlayan iki trait kullanırsam ne olacağını düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trait TaxTools
{
    public function calculateTax(float $price): float
    {
        return 222;
    }
}
</code></pre></div></div>

<p>calculateTax() metotlarını içeren iki trait eklediğim için, PHP hangisinin diğerini geçersiz kılacağını çözemiyor. Sonuç hatadır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fatal error: Trait method popp\ch04\batch06_3\TaxTools::calculateTax has
not been applied as
popp\ch04\batch06_3\UtilityService::calculateTax, because of collision with
popp\ch04\batch06_3\PriceUtilities::calculateTax in...
</code></pre></div></div>

<p>Bu sorunu çözmek için, insteadof anahtar sözcüğünü kullanabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities;
    use TaxTools {
        TaxTools::calculateTax insteadof PriceUtilities;
    }
}
</code></pre></div></div>

<p>use ifadesine başka yönergeler uygulamak için önce gövde eklemeliyim. Bunu parantezler ile yapıyorum. Bu blok içinde insteadof operatörünü kullanıyorum. Bu, sol tarafta tam metot referansı (yani, hem trait hem de metot adlarını kapsam çözümleme operatörüyle ayrılmış olarak tanımlayan) gerektirir.</p>

<p>Sağ tarafta insteadof, eşdeğer metodu geçersiz kılması gereken trait’in adını gerektirir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TaxTools::calculateTax insteadof PriceUtilities;
</code></pre></div></div>

<p>Önceki kod parçasının anlamı “PriceUtilities’de aynı adlı metot yerine TaxTools’un calculateTax() metodunu kullanın.”</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$u = new UtilityService();
print $u-&gt;calculateTax(100) . "\n";
</code></pre></div></div>

<p>TaxTools::calculateTax() içine yerleştirdiğim dummy çıktıyı alıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>222
</code></pre></div></div>

<h3 id="geçersiz-kılınan-trait-metotlarına-takma-ad-vermek">Geçersiz Kılınan Trait Metotlarına Takma Ad Vermek</h3>

<p>Metotlar arasındaki belirsizliği gidermek için insteadof kullanabileceğinizi gördük. Yine de, geçersiz kılınan metoda erişmek isterseniz ne yaparsınız? as operatörü, trait metotlarına takma ad vermenizi sağlar. Bir kez daha, as operatörü, sol tarafında bir metoda tam referans gerektirir. Operatörün sağ tarafına takma adı yazmalısınız. Bu nedenle burada, örneğin, basicTax() takma adını kullanarak PriceUtilities trait’inin calculateTax() metodunu eski haline getiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities;
    
    use TaxTools {
        TaxTools::calculateTax insteadof PriceUtilities;
        PriceUtilities::calculateTax as basicTax;
    }
}
</code></pre></div></div>

<p>Artık UtilityService sınıfı iki metot edinmiştir: calculateTax()’ın TaxTools sürümü ve basicTax() takma adıyla PriceUtilities sürümü. Bu metotları çalıştıralım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$u = new UtilityService();
print $u-&gt;calculateTax(100) . "\n";
print $u-&gt;basicTax(100) . "\n";
</code></pre></div></div>

<p>Bu, aşağıdaki çıktıyı verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>222
20
</code></pre></div></div>

<p>Böylece PriceUtilities::calculateTax(), basicTax() adı altında UtilityService sınıfının bir parçası olarak yeniden canlandırıldı.</p>

<p><strong>Not</strong>: Metot adının trait’ler arasında çakışması durumunda, use bloğundaki metot adlarından birini takma adla adlandırmak yeterli değildir. Öncelikle insteadof operatörünü kullanarak hangi metodun diğerinin yerine geçtiğini belirlemelisiniz. Ardından, atılan metoda as operatörüyle yeni bir ad atayabilirsiniz.</p>

<p>Bu arada, ad çakışmasının olmadığı durumlarda da takma ad kullanabilirsiniz. Örneğin, üst sınıfta veya arayüzde bildirilen soyut metot imzasını uygulamak için trait metodu kullanmak isteyebilirsiniz.</p>

<h3 id="traitlerde-static-metot-kullanmak">Trait’lerde Static Metot Kullanmak</h3>

<p>Şimdiye kadar gördüğünüz örneklerin çoğu, örnek verilerini depolamadıkları için static metotlar kullanabilir. Trait’e static metot yerleştirmenin karmaşık bir tarafı yoktur. Burada, PriceUtilities::$taxrate property’sini ve PriceUtilities::calculateTax() metodunu static olacak şekilde değiştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trait PriceUtilities
{
    private static int $taxrate = 20;

    public static function calculateTax(float $price): float
    {
        return ((self::$taxrate / 100) * $price);
    }
    // other utilities
}
</code></pre></div></div>

<p>İşte UtilityService minimal biçimine geri döndü:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities;
}
</code></pre></div></div>

<p>Tek yaptığı, PriceUtilities trait’ini kullanmak. Bununla birlikte, calculateTax() metodunun çağrılması söz konusu olduğunda önemli bir fark vardır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print UtilityService::calculateTax(100) . "\n";
</code></pre></div></div>

<p>Şimdi metodu nesne yerine sınıfta çağırmalıyım. Tahmin edebileceğiniz gibi, bu kod satırı aşağıdakini verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>20
</code></pre></div></div>

<p>Böylece, static metotlar trait’lerde bildirilir ve üst sınıf aracılığıyla normal şekilde erişilir.</p>

<h3 id="traitlerde-soyut-metot-tanımlamak">Trait’lerde Soyut Metot Tanımlamak</h3>

<p>Trait’te soyut metot ları tıpkı sınıfta yaptığınız gibi tanımlayabilirsiniz. Trait bir sınıf tarafından kullanıldığında, bildirdiği herhangi bir soyut metodu uygulama taahhüdünü üstlenir.</p>

<p><strong>Not</strong>: PHP 8’den önce, trait’lerde tanımlanan soyut metotlar için metot imzaları her zaman tam olarak uygulanmıyordu. Bu, bazı durumlarda parametre ve dönüş tiplerinin, uygulama sınıfında belirtilenlerden farklı olabileceği anlamına geliyordu. Bu boşluk artık kapatılmıştır.</p>

<p>Bu bilgiyle donanmış olarak, önceki örneğimi yeniden uygulayabilirim, böylece trait onu kullanan herhangi bir sınıfı tax rate bilgisi vermeye zorlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trait PriceUtilities
{
    public function calculateTax(float $price): float
    {
        // better design.. we know getTaxRate() is implemented
        return (($this-&gt;getTaxRate() / 100) * $price);
    }
    
    abstract public function getTaxRate(): float;
    // other utilities
}
</code></pre></div></div>

<p>PriceUtilities trait’inde soyut getTaxRate() metodu bildirerek, UtilityService sınıfını uygulama sağlamaya zorluyorum.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities;
    
    public function getTaxRate(): float
    {
        return 20;
    }
}
</code></pre></div></div>

<p>Trait’teki soyut bildirim sayesinde, getTaxRate() metodu sağlamamış olsaydım, fatal error ile ödüllendirilirdim.</p>

<h3 id="trait-metotlarına-erişim-haklarını-değiştirmek">Trait Metotlarına Erişim Haklarını Değiştirmek</h3>

<p>Elbette trait metodunu public, private veya protected olarak tanımlayabilirsiniz. Ancak, bu erişimi trait’i kullanan sınıfın içinden de değiştirebilirsiniz. as operatörünün metot adına takma ad vermek için kullanılabileceğini zaten gördünüz. Bu operatörün sağ tarafında erişim değiştirici (access modifier) kullanırsanız, metodun adı yerine erişim düzeyini değiştirir.</p>

<p>Örneğin, UtilityService içinden calculateTax()’ı kullanmak istediğinizi, ancak kod uygulama için uygun hale getirmediğinizi hayal edin. use deyimini şu şekilde değiştirebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class UtilityService extends Service
{
    use PriceUtilities {
        PriceUtilities::calculateTax as private;
    }

    public function __construct(private float $price)
    {
    }

    public function getTaxRate(): float
    {
        return 20;
    }
    
    public function getFinalPrice(): float
    {
        return ($this-&gt;price + $this-&gt;calculateTax($this-&gt;price));
    }
}
</code></pre></div></div>

<p>calculateTax() metoduna private erişim ayarlamak için as operatörünü private anahtar sözcüğüyle birlikte kullanıyorum. Bu, getFinalPrice() metoduna erişebileceğim anlamına gelir. calculateTax() metoduna dışarıdan bir erişim girişimi aşağıda verilmiştir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$u = new UtilityService(100);
print $u-&gt;calculateTax() . "\n";
</code></pre></div></div>

<p>Ne yazık ki, bu kod hata üretecek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: Call to private method popp\ch04\batch06_9\
UtilityService::calculateTax() from context ...
</code></pre></div></div>

<h2 id="late-static-binding-static-anahtar-kelimesi">Late Static Binding: Static Anahtar Kelimesi</h2>

<p>Artık soyut sınıfları, trait’leri ve arayüzleri gördüğünüze göre, kısaca static metotlara dönme zamanı. Sınıf örneklerini oluşturmak için factory kullanılabileceğini gördünüz. Benim kadar tembel bir kodlayıcıysanız, bunun gibi bir örnek sizi rahatsız edebilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class DomainObject
{
}

class User extends DomainObject
{
    public static function create(): User
    {
        return new User();
    }
}

class Document extends DomainObject
{
    public static function create(): Document
    {
        return new Document();
    }
}
</code></pre></div></div>

<p>DomainObject adında bir üst sınıf oluşturuyorum. Gerçek dünyadaki bir projede, elbette, bu, genişleyen sınıfların ortak işlevlerini içerecektir. Sonra iki alt sınıf oluşturuyorum, User and Document. Somut sınıflarımın static create() metotlarına sahip olmasını istiyorum.</p>

<p>Bu kod iyi çalışıyor, ancak can sıkıcı yinelemeler var. Her DomainObject alt sınıfı için bunun gibi bir kod oluşturmak istemiyorum. Bunun yerine, create() metodunu üst sınıfa taşımayı deneyeceğim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class DomainObject
{
    public static function create(): DomainObject
    {
        return new self();
    }
}
</code></pre></div></div>

<p>Güzel görünüyor. Artık tek bir yerde ortak kodum var ve sınıfa referans olarak self’i kullandım. Ancak self anahtar kelimesi hakkında bir varsayımda bulundum. Aslında, $this gibi davranmaz. self, arama bağlamına atıfta bulunmaz; çözüm bağlamını ifade eder. Önceki örneği çalıştırırsam şunu elde ederim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: Cannot instantiate abstract class
popp\ch04\batch06\DomainObject
</code></pre></div></div>

<p>Böylece self, çağrıldığı sınıf olan Document’a değil, create()’in tanımlandığı yer olan DomainObject’i çözümler. PHP 5.3’e kadar bu, pek çok beceriksiz geçici çözümün ortaya çıkmasına neden olan ciddi bir sınırlamaydı. PHP 5.3, late static binding adı verilen bir kavram getirmiştir. Bu özelliğin en belirgin tezahürü static anahtar kelimesidir. static, kapsayıcı sınıftan ziyade çağrılan sınıfa atıfta bulunması dışında self’e benzer. Bu durum, Document::create() çağrısının, yeni bir Document nesnesiyle sonuçlandığı anlamına gelir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class DomainObject
{
    public static function create(): DomainObject
    {
        return new static();
    }
}

class User extends DomainObject
{
}

class Document extends DomainObject
{
}
</code></pre></div></div>

<p>Şimdi, alt sınıflardan birinde create()’i çağırırsak, artık bir hataya neden olmaz ve create()’i barındıran sınıfla değil, çağırdığımız sınıfla ilgili nesneyi geri alırız.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print_r(Document::create());
</code></pre></div></div>

<p>İşte çıktı.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>popp\ch04\batch07\Document Object
(
)
</code></pre></div></div>

<p>Static anahtar kelimesi, örneklemeden daha fazlası için kullanılabilir. self ve parent gibi static olmayan bir bağlamdan bile static metot çağrıları için bir tanımlayıcı olarak kullanılabilir. Diyelim ki DomainObject sınıflarıma grup kavramını dahil etmek istiyorum.</p>

<p>Varsayılan olarak, yeni sınıflandırmamda, tüm sınıflar “default” kategorisine giriyor, ancak bunu miras hiyerarşimin bazı dalları için geçersiz kılmak istiyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>abstract class DomainObject
{
    private string $group;

    public function __construct()
    {
        $this-&gt;group = static::getGroup();
    }

    public static function create(): DomainObject
    {
        return new static();
    }

    public static function getGroup(): string
    {
        return "default";
    }
}

class User extends DomainObject
{
}

class Document extends DomainObject
{
    public static function getGroup(): string
    {
        return "document";
    }
}

class SpreadSheet extends Document
{
}

print_r(User::create());
print_r(SpreadSheet::create());
</code></pre></div></div>

<p>DomainObject sınıfına bir kurucu tanıttım. Static bir metodu çağırmak için static anahtar sözcüğünü kullanır. DomainObject varsayılan uygulamayı sağlar, ancak Document bunu geçersiz kılar. Ayrıca Document’ı genişleten yeni bir SpreadSheet sınıfı oluşturdum. İşte çıktı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>popp\ch04\batch07\User Object (
    [group:popp\ch04\batch07\DomainObject:private] =&gt; default
)

popp\ch04\batch07\SpreadSheet Object (
    [group:popp\ch04\batch07\DomainObject:private] =&gt; document
)
</code></pre></div></div>

<p>User sınıfı için çok zekice bir şey olması gerekmez. DomainObject kurucusu getGroup()’u çağırır ve onu yerel olarak bulur. SpreadSheet söz konusu olduğunda, arama, SpreadSheet’in kendisinde çağrılan sınıfta başlar. Uygulama sağlamaz, bu nedenle Document sınıfındaki getGroup() metodu çağrılır. PHP 5.3’ten önce, burada sadece DomainObject sınıfında getGroup()’u arayan self anahtar sözcüğüyle takılıp kalırdım.</p>

<h2 id="hataları-i̇şlemek">Hataları İşlemek</h2>

<p>İşler ters gidiyor. Dosyalar yanlış yerleştirilmiş, veritabanı sunucuları başlatılmamış durumda, URL’ler değiştirilmiş, XML dosyaları karıştırılmış, izinler doğru şekilde ayarlanmamış ve disk kotaları aşılmış. Liste uzayıp gidiyor. Sorunu tahmin etme mücadelesinde, basit bir yöntem bazen kendi hata işleme kodunun ağırlığı altında kalabilir.</p>

<p>Bir XML yapılandırma dosyasında verileri depolayan, alan ve ayarlayan basit bir Conf sınıfı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Conf
{
    private \SimpleXMLElement $xml;
    private \SimpleXMLElement $lastmatch;

    public function __construct(private string $file)
    {
        $this-&gt;xml = simplexml_load_file($file);
    }

    public function write(): void
    {
        file_put_contents($this-&gt;file, $this-&gt;xml-&gt;asXML());
    }

    public function get(string $str): ?string
    {
        $matches = $this-&gt;xml-&gt;xpath("/conf/item[@name=\"$str\"]");
        if (count($matches)) {
            $this-&gt;lastmatch = $matches[0];
            return (string)$matches[0];
        }
        return null;
    }
    
    public function set(string $key, string $value): void
    {
        if (!is_null($this-&gt;get($key))) {
            $this-&gt;lastmatch[0] = $value;
            return;
        }
        $conf = $this-&gt;xml-&gt;conf;
        $this-&gt;xml-&gt;addChild('item', $value)-&gt;addAttribute('name', $key);
    }
}
</code></pre></div></div>

<p>Conf sınıfı, ad değer çiftlerine erişmek için SimpleXml uzantısını kullanır. İşte çalışmak üzere tasarlandığı format:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;?xml version="1.0" ?&gt;
&lt;conf&gt;
    &lt;item name="user"&gt;bob&lt;/item&gt;
    &lt;item name="pass"&gt;newpass&lt;/item&gt;
    &lt;item name="host"&gt;localhost&lt;/item&gt;
&lt;/conf&gt;
</code></pre></div></div>

<p>Conf sınıfının kurucusu, simplexml_load_file()‘a ilettiği bir dosya yolunu kabul eder. Ortaya çıkan SimpleXmlElement nesnesini $xml adlı property’de saklar. get() metodu, verilen ada sahip öğeyi bulmak için XPath’i kullanır ve değerini döndürür. set() mevcut bir öğenin değerini değiştirir veya yeni bir öğe oluşturur. Son olarak, write() metodu yeni yapılandırma verilerini dosyaya kaydeder.</p>

<p>Çoğu örnek kod gibi, Conf sınıfı da oldukça basitleştirilmiştir. Özellikle var olmayan veya yazılamayan dosyaları işlemek için bir stratejisi yoktur. Görünüm açısından da iyimser. XML belgesinin iyi biçimlendirileceğini ve beklenen öğeleri içereceğini varsayar.</p>

<p>Bu hata koşullarını test etmek nispeten önemsizdir, ancak yine de ortaya çıkmaları durumunda bunlara nasıl yanıt vereceğime karar vermeliyim. Genelde iki seçenek vardır.</p>

<p>Önce uygulamayı durdurabilirim. Bu basit ama sert. Mütevazi sınıfım daha sonra çöken uygulamanın sorumluluğunu üstlenirdi. __construct() ve write() gibi metotlar, hataları saptamak için iyi yerleştirilmiş olsalar da, bunları nasıl ele alacaklarına karar verecek bilgiye sahip değiller.</p>

<p>Sınıfımdaki hatayı işlemek yerine, bir tür hata bayrağı döndürebilirim. Bu değer boole veya 0, -1 gibi bir tamsayılar olabilir. Bazı sınıflar, yazılımcının bir hatadan sonra daha fazla bilgi alabilmesi için hata dizesi veya bayrağı da ayarlar.</p>

<p>Pek çok PEAR paketi, hata oluştuğuna dair bildirim işlevi gören ve içinde hata mesajını içeren bir hata nesnesi (PEAR_Error örneği) döndürerek bu iki yaklaşımı birleştirir. Bu yaklaşım artık kullanımdan kaldırılmıştır, ancak pek çok sınıf güncellenmemiştir, çünkü kodlar genellikle eski davranışa bağlıdır.</p>

<p>Buradaki sorun, dönüş değerinin bozulmasıdır. Hataya açık metot her çağrıldığında dönüş türünü test etmek için yazılımcıya güvenmeniz gerekir. Bu riskli olabilir. Kimseye güvenmeyin!</p>

<p>Çağıran koda hata değeri döndürdüğünüzde, hatanın doğru şekilde ele alacağının garantisi yoktur. Eğer durum buysa, sorun baştan başlar. Yazılımcı, hata durumuna nasıl yanıt verileceğini belirlemeli, hatta belki farklı bir hata raporlama stratejisi uygulamalıdır.</p>

<h3 id="i̇stisnalar-exceptions">İstisnalar (Exceptions)</h3>

<p>PHP 5, hataları ele almanın kökten farklı bir yolu olan istisnaları getirdi. PHP için farklı fakat Java veya C++ deneyiminiz varsa, onları akıl almaz derecede tanıdık bulacaksınız. İstisnalar, bu bölümde şimdiye kadar dile getirdiğim tüm sorunları ele alır.</p>

<p>İstisna, yerleşik Exception sınıfından (veya türetilmiş bir sınıftan) örneklenen özel bir nesnedir.</p>

<p>Exception türündeki nesneler, hata bilgilerini tutmak ve raporlamak için tasarlanmıştır.</p>

<p>Exception sınıfı kurucusu, ileti dizesi ve hata kodu olmak üzere isteğe bağlı iki parametre kabul eder. Sınıf, hata koşullarını analiz etmek için bazı yararlı metotlar sağlar. Bunlar Tablo 4-1’de açıklanmıştır.</p>

<p>Exception sınıfı, hata bildirimi ve hata ayıklama bilgileri sağlamak için oldukça yararlıdır (getTrace() ve getTraceAsString() metotları bu açıdan özellikle yararlıdır). Aslında, daha önce tartışılan PEAR_Error sınıfıyla neredeyse aynıdır. Yine de, bir istisna için sahip olduğu bilgilerden çok daha fazlası vardır.</p>

<p><strong>Tablo 4-1</strong>. İstisna Sınıfının Genel Yöntemleri</p>

<table>
  <thead>
    <tr>
      <th>Metot</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>getMessage()</td>
      <td>Kurucuya iletilen mesaj dizesini alın</td>
    </tr>
    <tr>
      <td>getCode()</td>
      <td>Kurucuya iletilen kod tamsayısını alın</td>
    </tr>
    <tr>
      <td>getFile()</td>
      <td>İstisnanın oluşturulduğu dosyayı alın</td>
    </tr>
    <tr>
      <td>getLine()</td>
      <td>İstisnanın oluşturulduğu satır numarasını alın</td>
    </tr>
    <tr>
      <td>getPrevious()</td>
      <td>İç içe geçmiş istisna nesnesi alın</td>
    </tr>
    <tr>
      <td>getTrace()</td>
      <td>Metot, sınıf, dosya ve parametre verileri dahil olmak üzere istisnaya yol açan metot çağrılarını izleyen çok boyutlu bir dizi elde edin</td>
    </tr>
    <tr>
      <td>getTraceAsString()</td>
      <td>getTrace() tarafından döndürülen verilerin dize sürümünü alın</td>
    </tr>
    <tr>
      <td>__toString()</td>
      <td>İstisna nesnesi dize bağlamında kullanıldığında otomatik olarak çağrılır. İstisna ayrıntılarını açıklayan bir dize döndürür.</td>
    </tr>
  </tbody>
</table>

<p><strong>Exception Fırlatmak</strong></p>

<p>throw anahtar sözcüğü, bir Exception nesnesiyle bağlantılı olarak kullanılır. Geçerli metodun yürütülmesini durdurur ve hatayı işleme sorumluluğunu çağıran koda geri verir. Burada, throw deyimini kullanmak için __construct() metodunu değiştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __construct(private string $file)
{
    if (! file_exists($file)) {
        throw new \Exception("file '{$file}' does not exist");
    }
    $this-&gt;xml = simplexml_load_file($file);
}
</code></pre></div></div>

<p>write() metodu benzer bir yapı kullanabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function write(): void
{
    if (! is_writeable($this-&gt;file)) {
        throw new \Exception("file '{$this-&gt;file}' is not writeable");
    }
    print "{$this-&gt;file} is apparently writeable\n";
    file_put_contents($this-&gt;file, $this-&gt;xml-&gt;asXML());
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>try {
    $conf = new Conf("/tmp/conf01.xml");
    //$conf = new Conf( "/root/unwriteable.xml" );
    //$conf = new Conf( "nonexistent/not_there.xml" );
    print "user: " . $conf-&gt;get('user') . "\n";
    print "host: " . $conf-&gt;get('host') . "\n";
    $conf-&gt;set("pass", "newpass");
    $conf-&gt;write();
} catch (\Exception $e) {
    // handle error in some way
}
</code></pre></div></div>

<p>Gördüğünüz gibi, catch bloğu yüzeysel olarak bir metot bildirimine benziyor. Exception fırlatıldığında, catch bloğu çağrılır. Exception nesnesi, parametre olarak otomatik iletilir.</p>

<p>Exception fırlatıldığında kontrol doğrudan catch bloğuna geçer. Orada, herhangi bir hata kurtarma görevini gerçekleştirebilirsiniz. Bir hatadan kurtulamazsanız, her zaman yeni bir exception fırlatabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>} catch (\Exception $e) {
    // handle error in some way
    // or
    throw new \Exception("Conf error: " . $e-&gt;getMessage());
}
</code></pre></div></div>

<p>Alternatif olarak, size verilen istisnayı yeniden yazabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>try {
    $conf = new Conf("nonexistent/not_there.xml");
} catch (\Exception $e) {
    // handle error...
    // or rethrow
    throw $e;
}
</code></pre></div></div>

<p>Hata işlerken Exception nesnesinin kendisine ihtiyacınız yoksa, PHP 8’den itibaren exception argümanını tamamen atlayabilir ve sadece türü belirtebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>try {
    $conf = new Conf("nonexistent/not_there.xml");
} catch (\Exception) {
    // handle error without using the Exception object
}
</code></pre></div></div>

<p><strong>İstisnaları Genişletmek</strong></p>

<p>Herhangi bir sınıfta yaptığınız gibi, Exception sınıfını genişleten sınıflar oluşturabilirsiniz. Bunu yapmak istemenizin iki nedeni olabilir. İlk olarak, sınıfın işlevselliğini genişletebilirsiniz. İkinci olarak, türetilmiş sınıfın yeni bir sınıf türü tanımlaması, kendi başına hata işlemeye yardımcı olabilir.</p>

<p>Aslında try deyimi için ihtiyaç duyduğunuz kadar çok catch bloğu tanımlayabilirsiniz. Çağrılan catch bloğu, fırlatılan exception’ın türüne ve parametre listesindeki sınıf türüne bağlı olacaktır. İstisna’yı genişleten bazı basit sınıflar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class XmlException extends \Exception
{
    public function construct(private \LibXmlError $error)
    {
        $shortfile = basename($error-&gt;file);
        $msg = "[{$shortfile}, line {$error-&gt;line}, col {$error-&gt;column}]
        {$error-&gt;message}";
        $this-&gt;error = $error;
        parent:: __construct($msg, $error-&gt;code);
    }
    
    public function getLibXmlError(): \LibXmlError
    {
        return $this-&gt;error;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class FileException extends \Exception
{
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ConfException extends \Exception
{
}
</code></pre></div></div>

<p>LibXmlError sınıfı, SimpleXml bozuk bir XML dosyasıyla karşılaştığında arka planda oluşturulur. $message ve $code property’lerine sahiptir ve Exception sınıfına benzer. Bu benzerlikten yararlanarak XmlException sınıfında LibXmlError nesnesini kullanıyorum. FileException ve ConfException sınıfları, Exception alt sınıfından farklı bir şey yapmaz. Artık bu sınıfları kodumda kullanabilir ve hem construct() hem de write() öğelerini değiştirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __construct(private string $file)
{
    if (! file_exists($file)) {
        throw new FileException("file '$file' does not exist");
    }
    $this-&gt;xml = simplexml_load_file($file, null, LIBXML_NOERROR);
    if (! is_object($this-&gt;xml)) {
        throw new XmlException(libxml_get_last_error());
    }
    $matches = $this-&gt;xml-&gt;xpath("/conf");
    if (! count($matches)) {
        throw new ConfException("could not find root element: conf");
    }
}

public function write(): void
{
    if (! is_writeable($this-&gt;file)) {
        throw new FileException("file '{$this-&gt;file}' is not writeable");
    }
    file_put_contents($this-&gt;file, $this-&gt;xml-&gt;asXML());
}
</code></pre></div></div>

<p>__construct(), karşılaştığı hatanın türüne bağlı olarak XmlException, FileException veya ConfException fırlatır. LIBXML_NOERROR flag’ini simplexml_load_file() fonksiyonuna parametre olarak verdiğime dikkat edin. Bu, uyarıları bastırır ve XmlException sınıfımla bunları işlemek için beni özgür bırakır. Hatalı biçimlendirilmiş bir XML dosyasıyla karşılaşırsam, simplexml_load_file() nesne döndürmeyeceğinden hata oluştuğunu biliyorum. Daha sonra libxml_get_last_error() kullanarak hataya erişebilirim.</p>

<p>$file property’si yazılamaz bir varlığa işaret ediyorsa write() metdu FileException fırlatır.</p>

<p>Böylece, __construct()’ın olası üç istisnadan birini oluşturabileceğini belirledim. Bundan nasıl yararlanabilirim? İşte bir Conf nesnesini başlatan bazı kodlar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Runner
{
    public static function init()
    {
        try {
            $conf = new Conf(__DIR__ . "/conf.broken.xml");
            print "user: " . $conf-&gt;get('user') . "\n";
            print "host: " . $conf-&gt;get('host') . "\n";
            $conf-&gt;set("pass", "newpass");
            $conf-&gt;write();
        } catch (FileException $e) {
            // permissions issue or non-existent file throw $e;
        } catch (XmlException $e) {
            // broken xml
        } catch (ConfException $e) {
            // wrong kind of XML file
        } catch (\Exception $e) {
            // backstop: should not be called
        }
    }
}
</code></pre></div></div>

<p>Her sınıf türü için bir catch bloğu sağlıyorum. Çağrılan blok, fırlatılan exception tipine bağlıdır. İlk eşleşen çalıştırılacaktır, bu nedenle en genel türü sona ve en özel türü başlangıca yerleştirmeyi unutmayın. Örneğin, Exception için catch bloğunu XmlException ve ConfException bloğunun önüne yerleştirecek olsaydınız, bunların hiçbiri çağrılmazdı. Bunun nedeni, bu sınıfların her ikisinin de Exception türüne ait olması ve dolayısıyla ilk testle eşleşecek olmasıdır.</p>

<p>Yapılandırma dosyasında bir sorun varsa (dosya mevcut değilse veya yazılamıyorsa) ilk catch bloğu (FileException) çağrılır. İkinci blok (XmlException), XML dosyasını ayrıştırırken bir hata oluşursa (örneğin, bir öğe kapalı değilse) çağrılır. Üçüncü blok (ConfException), geçerli bir XML dosyası beklenen kök conf öğesini içermiyorsa çağrılır. Son bloğa (Exception) ulaşılmamalı çünkü benim metotlarım yalnızca açıkça ele alınan üç exception oluşturuyor. Geliştirme sırasında koda yeni istisnalar eklemeniz durumunda, bunun gibi bir “backstop” bloğuna sahip olmak genellikle iyi bir fikirdir.</p>

<p><strong>Not</strong>: Bir “backstop” yakalama bloğu kullanırsanız, çoğu durumda istisna hakkında gerçekten bir şeyler yaptığınızdan emin olmalısınız; sessizce fail etmek, teşhis edilmesi zor hatalara neden olabilir.</p>

<p>Bu ayrıntılı catch bloklarının yararı, farklı hatalara farklı kurtarma veya fail mekanizmaları uygulamanıza izin vermesidir. Örneğin, yürütmeyi sonlandırmaya, hatayı loglara kaydedip devam etmeye veya açıkça bir exception fırlatmaya karar verebilirsiniz.</p>

<p>Burada oynayabileceğiniz başka bir numara, mevcut istisnayı saran yeni bir istisna oluşturmaktır. Bu, yakaladığınız istisna tarafından özetlenen verileri korurken kendi bağlamsal bilgilerinizi eklemenize olanak tanır. Bölüm 15’te bu teknik hakkında daha fazla bilgi edinebilirsiniz.</p>

<p>Peki yazılımcı tarafından istisna yakalanmazsa ne olur? Örtük olarak yeniden fırlatılır ve yazılımcının kendi kodunda onu yakalama fırsatı verilir. Bu süreç ya istisna yakalanana kadar ya da artık fırlatılamayana kadar devam eder. Bu noktada fatal error oluşur. Örneğimdeki istisnalardan birini yakalamazsam ne olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PHP Fatal error: Uncaught exception 'FileException' with message
'file 'nonexistent/not_there.xml' does not exist' in ...
</code></pre></div></div>

<p>Bu nedenle, bir istisna oluşturduğunuzda, yazılımcıyı bununla ilgilenme sorumluluğunu almaya zorlarsınız. Bu sorumluluktan kaçmak değildir. Metod bir hata algıladığında, ancak bunu akıllıca işleyebilmek için bağlamsal bilgiye sahip olmadığında exception fırlatılmalıdır. Örneğimdeki write() metodu, yazma girişiminin ne zaman başarısız olacağını ve nedenini biliyor ama bu konuda ne yapacağını bilmiyor. Bu olması gerektiği gibi. Conf sınıfını şu anda olduğundan daha bilgili hale getirirsem, odak noktasını kaybeder ve daha az tekrar kullanılabilir hale gelir.</p>

<p><strong>try/catch Bloklarını finally ile Temizlemek</strong></p>

<p>Kod akışının istisnalardan etkilenme şekli beklenmeyen sorunlara neden olabilir. Örneğin, try bloğu içinde istisna oluşturulduktan sonra temizlik işlemleri gerçekleştirilemeyebilir. Gördüğünüz gibi try bloğu içinde bir istisna üretilirse akış doğrudan ilgili catch bloğuna gider. Veritabanı bağlantılarını veya dosya tanıtıcılarını kapatan kod çağrılamayabilir ve durum bilgileri güncellenmeyebilir.</p>

<p>Örneğin, Runner::init()’in eylemlerinin log’unu tuttuğunu hayal edin. Başlatma işlemini, karşılaşılan hataları log’a kaydeder ve ardından sonucu günlüğe kaydeder. Burada, bu tür log kaydının tipik olarak basitleştirilmiş bir örneğini sunuyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function init(): void
{
    try {
        $fh = fopen("/tmp/log.txt", "a"); fputs($fh, "start\n");
        $conf = new Conf(dirname( FILE ) . "/conf.broken.xml");
        print "user: " . $conf-&gt;get('user') . "\n";
        print "host: " . $conf-&gt;get('host') . "\n";
        $conf-&gt;set("pass", "newpass");
        $conf-&gt;write();
        fputs($fh, "end\n");
        fclose($fh);
    } catch (FileException $e) {
        // permissions issue or non-existent file
        fputs($fh, "file exception\n");
        throw $e;
    } catch (XmlException $e) {
        fputs($fh, "xml exception\n");
        // broken xml
    } catch (ConfException $e) {
        fputs($fh, "conf exception\n");
        // wrong kind of XML file
    } catch (\Exception $e) {
        fputs($fh, "general exception\n");
        // backstop: should not be called
    }
}
</code></pre></div></div>

<p>Bir dosya açıyorum, log.txt; ona yazıyorum; ve sonra yapılandırma kodumu çağırıyorum. Bu süreçte bir istisna ile karşılaşılırsa, bunu ilgili catch bloğuna kaydederim. Log’a yazıp dosyayı kapatarak try bloğunu sonlandırıyorum.</p>

<p>Elbette, bir istisna ile karşılaşılırsa bu son adıma asla ulaşılamaz. Akış doğrudan ilgili catch bloğuna geçer ve try bloğunun geri kalanı asla çalıştırılmaz. İşte bir XML istisnası oluşturulduğunda log çıktısı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>start
xml exception
</code></pre></div></div>

<p>Gördüğünüz gibi, log’a kaydetme başladı ve file exception not edildi, ancak log’un sonunu kaydeden kod bölümüne hiçbir zaman ulaşılamadı ve bu nedenle log bununla güncellenmedi.</p>

<p>Çözümün, son kayıt adımını try/catch bloğunun tamamen dışına yerleştirmek olduğunu düşünebilirsiniz. Bu güvenilir bir şekilde çalışmaz. Oluşturulan bir istisna yakalanırsa ve try bloğu yürütmenin devam etmesine izin verirse, akış try/catch yapısının ötesine geçer. Ancak, bir catch bloğu exception’ı yeniden fırlatabilir veya komut dosyasının yürütülmesini tamamen sonlandırabilir.</p>

<p>Programcıların bu gibi sorunlarla başa çıkmalarına yardımcı olmak için PHP 5.5 yeni bir anahtar sözcük ekledi: finally.  Java’ya aşina iseniz, muhtemelen bunu daha önce görmüşsünüzdür. catch blokları yalnızca eşleşen exception’lar fırlatıldığında koşullu olarak çalıştırılsa da, try bloğu içinde bir istisna oluşturulsun veya oluşturulmasın, her zaman finally bloğu çalıştırılır.</p>

<p>Log kodumu finally bloğuna yaklaştırarak bu sorunu çözebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function init2(): void
{
    $fh = fopen("/tmp/log.txt", "a");
    try {
        fputs($fh, "start\n");
        $conf = new Conf(dirname( FILE ) . "/conf.not-there.xml");
        print "user: " . $conf-&gt;get('user') . "\n";
        print "host: " . $conf-&gt;get('host') . "\n";
        $conf-&gt;set("pass", "newpass");
        $conf-&gt;write();
    } catch (FileException $e) {
        // permissions issue or non-existent file
        fputs($fh, "file exception\n");
        //throw $e;
    } catch (XmlException $e) {
        fputs($fh, "xml exception\n");
        // broken xml
    } catch (ConfException $e) {
        fputs($fh, "conf exception\n");
        // wrong kind of XML file
    } catch (Exception $e) {
        fputs($fh, "general exception\n");
        // backstop: should not be called
    } finally {
        fputs($fh, "end\n");
        fclose($fh);
    }
}
</code></pre></div></div>

<p>Log yazma ve fclose() çağrısı finally bloğu ile sarıldığı için, bu ifadeler, FileException yakalandığında olduğu gibi, exception yeniden fırlatılsa bile çalıştırılacaktır.</p>

<p>Şimdi, FileException oluşturulduğunda log metni şöyledir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>start
file exception
end
</code></pre></div></div>

<p><strong>Not</strong>: Çağrılan catch bloğu exception’ı yeniden fırlatırsa veya bir değer döndürürse, finally bloğu çalıştırılır. Ancak, try veya catch bloğunda die() veya exit() öğesinin çağrılması komut dosyasının yürütülmesini sonlandırır ve finally bloğu çalıştırılmaz.</p>

<h2 id="final-sınıf-ve-metotlar">Final Sınıf ve Metotlar</h2>

<p>Kalıtım, sınıf hiyerarşisi içinde muazzam bir esneklik sağlar. Bir sınıfı veya metodu geçersiz kılabilirsiniz, böylece yazılımcının yapacağı çağrı, sınıf örneğine göre kökten farklı etkiler elde eder. Ancak bazen sınıf veya metot sabit ve değişmeden kalmalıdır. Sınıfınız veya metodunuz için kesin işlevselliğe ulaştıysanız ve bunu geçersiz kılmanın yalnızca çalışmanızın nihai mükemmelliğine zarar vereceğini düşünüyorsanız, final anahtar kelimesine ihtiyacınız olabilir.</p>

<p>final kalıtıma son verir. final sınıf alt sınıflara ayrılamaz ve final metot geçersiz kılınamaz.</p>

<p>İşte final sınıf:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>final class Checkout
{
    // ...
}
</code></pre></div></div>

<p>Checkout sınıfından alt sınıf oluşturma girişimi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class IllegalCheckout extends Checkout
{
    // ...
}
</code></pre></div></div>

<p>Bu bir hata üretir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fatal error: Class popp\ch04\batch13\IllegalCheckout may not inherit
from final class (popp\ch04\batch13\Checkout) in ...
</code></pre></div></div>

<p>Tüm sınıf yerine final metot tanımlayarak işleri biraz rahatlatabilirim. final anahtar sözcüğü, aşağıdaki gibi, protected ve static gibi diğer erişim belirteçlerinin önüne yerleştirilmelidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Checkout
{
    final public function totalize(): void
    {
        // calculate bill
    }
}
</code></pre></div></div>

<p>Artık Checkout’un alt sınıflarını oluşturabilirim, ancak totalize() metodunu geçersiz kılmaya yönelik herhangi bir girişim, hataya neden olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class IllegalCheckout extends Checkout
{
    final public function totalize(): void
    {
        // change bill calculation
    }
}

Fatal error: Cannot override final method popp\ch04\batch14\
Checkout::totalize() in /var/popp/src/ch04/batch14/IllegalCheckout.php on line 9
</code></pre></div></div>

<p>İyi nesne yönelimli kod, iyi tanımlanmış arayüzü vurgulama eğilimindedir. Bununla birlikte, arayüzün arkasında, uygulamalar genellikle değişiklik gösterecektir. Farklı sınıflar veya sınıf kombinasyonları, ortak arayüzlere uygundur ancak farklı koşullarda farklı davranır. Bir sınıf veya metodu final olarak tanımlarsanız bu esnekliği sınırlandırırsınız. Bunun istendiği zamanlar olacaktır ve bunlardan bazılarını kitabın ilerleyen kısımlarında göreceksiniz.</p>

<p>Ancak, bir şeyi final olarak tanımlamadan önce dikkatlice düşünmelisiniz. Geçersiz kılmanın yararlı olacağı hiçbir koşul yok mu? Elbette daha sonra her zaman fikrinizi değiştirebilirsiniz, ancak başkalarının kullanması için bir kütüphane dağıtıyorsanız bu o kadar kolay olmayabilir. final’ı dikkatli kullanın.</p>

<h2 id="dahili-hata-sınıfı">Dahili Hata Sınıfı</h2>

<p>İstisnalar ilk ortaya çıktığında, try ve catch dünyası, çekirdek motora değil, öncelikle PHP’de yazılmış koda uygulanıyordu. Dahili olarak üretilen hatalar kendi mantığını korudu. Temel hataları, kod tarafından oluşturulan istisnalarla aynı şekilde yönetmek istiyorsanız, bu durum karmaşık bir hal alabilir. PHP 7, Error sınıfıyla bu sorunu çözmeye başladı. Bu, Exception sınıfının uyguladığı yerleşik arayüzün aynısı olan Throwable’ı uygular ve bu nedenle aynı şekilde ele alınabilir. Bu aynı zamanda Tablo 4-2’de açıklanan metotların da dikkate alındığı anlamına gelir. Error, özel hata türleri için alt sınıflara ayrılır. eval ifadesi tarafından oluşturulan ayrıştırma hatasını şu şekilde yakalayabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>try {
    eval("illegal code");
} catch (\Error $e) {
    print get_class($e) . "\n";
    print $e-&gt;getMessage();
} catch (\Exception $e) {
    // do something with an Exception
}
</code></pre></div></div>

<p>İşte çıktı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ParseError
syntax error, unexpected identifier "code"
</code></pre></div></div>

<p>Böylece, bazı dahili hata türlerini catch bloklarında, Error üst sınıfını belirterek veya daha spesifik bir alt sınıf belirleyerek eşleştirebilirsiniz. Tablo 4-2 mevcut Error alt sınıflarını gösterir.</p>

<p><strong>Tablo 4-2</strong>. PHP 7 Tarafından Sunulan Yerleşik Hata Sınıfları</p>

<table>
  <thead>
    <tr>
      <th>Fonksiyon</th>
      <th>Tip</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ArgumentCountError</td>
      <td>Kullanıcı tanımlı bir metot veya fonksiyona çok az argüman iletildiğinde fırlatılır</td>
    </tr>
    <tr>
      <td>ArithmeticError</td>
      <td>Matematikle ilgili hatalar için fırlatılır - özellikle bitsel aritmetik ile ilgili olanlar</td>
    </tr>
    <tr>
      <td>AssertionError</td>
      <td>assert() dil yapısı başarısız olduğunda (hata ayıklama) fırlatılır</td>
    </tr>
    <tr>
      <td>CompileError</td>
      <td>PHP kodu hatalı olduğunda ve çalıştırmak için derlenemediğinde fırlatılır</td>
    </tr>
    <tr>
      <td>DivisionByZeroError</td>
      <td>Bir sayı sıfıra bölünmek istendiğinde fırlatılır</td>
    </tr>
    <tr>
      <td>ParseError</td>
      <td>PHP’yi ayrıştırmak için bir çalışma zamanı girişimi (örneğin, eval() kullanmak) başarısız olduğunda fırlatılır</td>
    </tr>
    <tr>
      <td>TypeError</td>
      <td>Metoda yanlış türde bir parametre iletildiğinde, metot yanlış türde bir değer döndürdüğünde veya metoda yanlış sayıda parametre iletildiğinde fırlatılır.</td>
    </tr>
  </tbody>
</table>

<h2 id="interceptorlar-ile-çalışmak">Interceptor’lar ile Çalışmak</h2>

<p>PHP, tanımsız metot ve property’lere gönderilen mesajları yakalayabilen yerleşik interceptor metodlarını sağlar. Bu aynı zamanda aşırı yükleme (overloading) olarak da bilinir, ancak bu terim Java ve C++’da oldukça farklı bir anlama geldiği için, interception açısından konuşmanın daha iyi olacağını düşünüyorum.</p>

<p>PHP, çeşitli yerleşik interceptor veya “sihirli” metodları destekler. __construct() gibi, bunlar da doğru koşullar oluştuğunda sizin için çağrılır. Tablo 4-3’te bu metotlardan bazıları açıklanmaktadır.</p>

<p><strong>Tablo 4-3</strong>. Interceptor Metotları</p>

<table>
  <thead>
    <tr>
      <th>Metot</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>__get($property)</td>
      <td>Tanımlanmamış property’e erişildiğinde çağrılır</td>
    </tr>
    <tr>
      <td>__set($property, $value)</td>
      <td>Tanımlanmamış property’e değer atandığında çağrılır</td>
    </tr>
    <tr>
      <td>__isset($property)</td>
      <td>Tanımlanmamış property üzerinde isset() çağrıldığında çağrılır</td>
    </tr>
    <tr>
      <td>__unset($property)</td>
      <td>Tanımlanmamış property üzerinde unset() çağrıldığında çağrılır</td>
    </tr>
    <tr>
      <td>__call($method, $arg_array)</td>
      <td>Tanımsız, static olmayan bir metot çağrıldığında çağrılır</td>
    </tr>
    <tr>
      <td>__callStatic($method, $arg_array)</td>
      <td>Tanımsız, static bir metot çağrıldığında çağrılır</td>
    </tr>
  </tbody>
</table>

<p><strong>Not</strong>: Interceptor veya sihirli metotlar hakkında daha fazla bilgiyi PHP kılavuzunda bulabilirsiniz: www.php.net/manual/en/language.oop5.magic.php.</p>

<p>__get() ve __set() metotları, sınıfta (veya onun ebeveynlerinde) bildirilmemiş property’lerle çalışmak için tasarlanmıştır.</p>

<p>Yazılımcı bildirilmemiş bir property’i okumaya çalıştığında __get() çağrılır. Yazılımcının erişmeye çalıştığı property’nin adını içeren tek bir string parametre ile otomatik olarak çağrılır. __get() metodundan döndürdüğünüz her şey, hedef property bu değere sahipmiş gibi geri gönderilir. İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    public function __get(string $property): mixed
    {
        $method = "get{$property}";
        if (method_exists($this, $method)) {
            return $this-&gt;$method();
        }
    }

    public function getName(): string
    {
        return "Bob";
    }
    
    public function getAge(): int
    {
        return 44;
    }
}
</code></pre></div></div>

<p>Tanımsız bir property’e erişilmeye çalışıldığında, __get() metodu çağrılır. Property adını almak ve başına “get” sözcüğünü koyarak yeni bir dize oluşturmak için __get()’i uyguladım. Bu dizeyi, nesne ve metot adını kabul eden ve metodun varlığını test eden method_exists() adlı fonksiyona iletiyorum. Metot varsa, onu çağırırım ve dönüş değerini iletirim. $name property’sinin istendiğini varsayalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$p = new Person();
print $p-&gt;name;
</code></pre></div></div>

<p>Bu durumda, getName() metodu arka planda çağrılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Bob
</code></pre></div></div>

<p>Metod yoksa, kullanıcının erişmeye çalıştığı property null olarak çözümlenecektir.</p>

<p>__isset() metodu, __get() metoduna benzer şekilde çalışır. Yazılımcı, tanımsız bir property üzerinde isset() öğesini çağırdıktan sonra çağrılır. Person’ı şu şekilde genişletebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __isset(string $property): bool
{
    $method = "get{$property}";
    return (method_exists($this, $method));
}
</code></pre></div></div>

<p>Artık dikkatli bir kullanıcı, property’i üzerinde çalışmadan önce test edebilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$p = new Person();
if (isset($p-&gt;name)) {
    print $p-&gt;name;
}
</code></pre></div></div>

<p>Tanımsız bir property’e atama yapmaya çalıştığınızda __set() metodu çağrılır. İki parametre iletilir: property adı ve ayarlamaya çalışılan değer. Daha sonra bu değişkenlerle nasıl çalışacağınıza karar verebilirsiniz. Burada, Person sınıfını değiştirmeye devam ediyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    private ?string $myname;
    private ?int $myage;

    public function __set(string $property, mixed $value): void
    {
        $method = "set{$property}";
        if (method_exists($this, $method)) {
            $this-&gt;$method($value);
        }
    }

    public function setName(?string $name): void
    {
        $this-&gt;myname = $name;
        if (!is_null($name)) {
            $this-&gt;myname = strtoupper($this-&gt;myname);
        }
    }
    
    public function setAge(?int $age): void
    {
        $this-&gt;myage = $age;
    }
}
</code></pre></div></div>

<p>Bu örnekte, “getters” yerine “setters” metodlarla çalışıyorum. Yazılımcı tanımsız bir property’e değer atamaya çalışırsa, property adı ve atanan değerle __set() metodu çağrılır. Uygun metodun varlığını test ederim ve varsa onu çağırırım. Bu şekilde atanan değeri filtreleyebilirim.</p>

<p><strong>Not</strong>: PHP belgelerindeki metot ve özelliklerden, sınıflarıyla özdeşleştirilmeleri için sıklıkla static terimlerle bahsedildiğini unutmayın. Dolayısıyla, property static olarak bildirilmese ve aslında bir nesne aracılığıyla erişilecek olsa bile, Person::$name property’si hakkında konuşabilirsiniz.</p>

<p>Bu nedenle, bir Person nesnesi oluşturur ve ardından Person::$name adlı bir property’e değer atamaya çalışırsam, bu sınıf $name property’sini tanımlamadığından __set() metodu çağrılır. Metot, “name” dizesini ve yazılımcının atadığı değeri iletir. Değerin daha sonra nasıl kullanılacağı __set()’in uygulanmasına bağlıdır. Bu örnekte, property adını “set” dizesiyle birleştirip bir metot adı oluşturuyorum. setName() metodu bulunur ve gereken şekilde çağrılır. Bu, gelen değeri dönüştürür ve bir property’de saklar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$p = new Person();
$p-&gt;name = "bob";
// the $myname property becomes 'BOB'
</code></pre></div></div>

<p>Tahmin edebileceğiniz gibi, __unset() __set()’in tersidir. Tanımlanmamış bir property üzerinde unset() çağrıldığında, __unset() property’nin adıyla birlikte çağrılır. Daha sonra bu bilgilerle istediğinizi yapabilirsiniz. Aşağıdaki örnek, __set() tarafından kullanıldığını gördüğünüz teknik ile çözümlenen bir metoda null değerini iletir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __unset(string $property): void
{
    $method = "set{$property}";
    if (method_exists($this, $method)) {
        $this-&gt;$method(null);
    }
}
</code></pre></div></div>

<p>__call() metodu, muhtemelen tüm interceptor metotları arasında en yararlı olanıdır. Yazılımcı tarafından tanımsız bir metot çağrıldığında çağrılır. __call(), metot adı ve iletilen tüm parametreleri tutan bir dizi ile çağrılır. __call() metodundan döndürdüğünüz herhangi bir değer, çağrılan metot tarafından döndürülmüş gibi istemciye döndürülür.</p>

<p><strong>Not</strong>: __get(), __set() ve __call() erişilemeyen bir metot veya property’e (yani private veya protected olarak ayarlanan metot veya property’ler) erişilmeye çalışıldığında da otomatik olarak çağrılır.</p>

<h2 id="yıkıcı-destructor-metot-tanımlamak">Yıkıcı (Destructor) Metot Tanımlamak</h2>

<p>Bir nesne başlatıldığında __construct() metodunun otomatik olarak çağrıldığını gördünüz. PHP 5 ayrıca __destruct() metodunu da tanıttı. Bu, bir nesne çöp olarak toplanmadan hemen önce - yani bellekten silinmeden önce - çağrılır. Gerekli olabilecek herhangi bir son temizliği gerçekleştirmek için bu yöntemi kullanabilirsiniz.</p>

<p>Örneğin, kendisini veritabanına kaydeden bir sınıf düşünün. Bir örneğin silindiğinde verilerini kaydetmesini sağlamak için __destruct() metodunu kullanabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    private int $id;
    
    public function __construct(protected string $name, private int $age)
    {
        $this-&gt;name = $name;
        $this-&gt;age = $age;
    }

    public function setId(int $id): void
    {
        $this-&gt;id = $id;
    }

    public function __destruct()
    {
        if (!empty($this-&gt;id)) {
            // save Person data
            print "saving person\n";
        }
    }
}
</code></pre></div></div>

<p>__destruct() metodu, nesne üzerinde unset() işlevini her çağırdığınızda veya proseste nesneye başka referans olmadığında çağrılır. Dolayısıyla, Person nesnesi oluşturup yok edersem, __destruct() metodunun devreye girdiğini görebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person("bob", 44);
$person-&gt;setId(343);
unset($person);
</code></pre></div></div>

<p>İşte çıktı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>saving person
</code></pre></div></div>

<p>Bunun gibi numaralar eğlenceli olsa da, bir uyarıda bulunmaya değer. __call(), __destruct() ve arkadaşlarına bazen sihirli metotlar denir. Daha önce fantastik roman okuduysanız bileceğiniz gibi, sihir her zaman iyi bir şey değildir. Sihir keyfi ve beklenmediktir. Sihir kuralları esnetir. Sihir gizli maliyetler doğurur.</p>

<p>Örneğin, __destruct() durumunda, kullanıcıları istenmeyen sürprizlerle karşı karşıya bırakabilirsiniz. Person sınıfını düşünün, kendi __destruct() metodunda veritabanına yazma işlemi gerçekleştirir. Şimdi, acemi bir geliştiricinin, Person sınıfını adım adım ilerlettiğini hayal edin. __destruct() matodunu fark etmez ve bir dizi Person nesnesinin örneğini oluşturmaya başlar. Değerleri kurucuya ileterek CEO’nun gizli ve biraz müstehcen takma adını $name property’sine atar ve ardından $age’i 150 olarak ayarlar. Test script’ini birkaç kez çalıştırarak renkli ad ve yaş kombinasyonlarını dener.</p>

<p>Ertesi sabah, yöneticisi onu toplantı odasına çağırarak veritabanının neden aşağılayıcı Person verileri içerdiğini açıklamasını ister. Sihire güvenmeyin.</p>

<h2 id="__clone-ile-nesneleri-kopyalamak">__clone() ile Nesneleri Kopyalamak</h2>

<p>PHP 4’te, nesne kopyalamak, bir değişkenden diğerine atama yapmak kadar basitti:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class CopyMe
{
}

$first = new CopyMe();
$second = $first;
// PHP 4: $second and $first are 2 distinct objects
// PHP 5 plus: $second and $first refer to one object
</code></pre></div></div>

<p>Değişkenler atandığında, metotlar çağrıldığında ve nesneler döndürüldüğünde yanlışlıkla nesne kopyaları oluşturulduğundan, bu “basit mesele” birçok hatanın kaynağıydı. Bu, aynı nesneye atıfta bulunup bulunmadığını görmek için iki değişkeni test etmenin bir yolu olmadığı gerçeğiyle daha da kötüleşti. Eşdeğerlik testleri size tüm alanların aynı olup olmadığını (==) veya her iki değişkenin de nesne olup olmadığını (===) söyler, ancak aynı nesneyi işaret edip etmediklerini söylemez.</p>

<p>PHP’de, nesne içeriyormuş gibi görünen bir değişken aslında temeldeki veri yapısına başvuran bir tanımlayıcı içerir. Böyle bir değişken metoda atandığında veya iletildiğinde, içerdiği tanımlayıcı kopyalanır. Ancak, her kopya aynı nesneyi işaret etmeye devam eder. Bu, önceki örneğimde, $first ve $second nesnenin iki kopyası yerine aynı nesneyi işaret eden tanımlayıcılar içerdiği anlamına gelir. Genellikle nesnelerle çalışırken istediğiniz şey bu olsa da, nesnenin kopyasını almanız gereken durumlar olacaktır.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$first = new CopyMe();
$second = clone $first;
// PHP 5 plus: $second and $first are 2 distinct objects
</code></pre></div></div>

<p>Nesne kopyalamayı çevreleyen sorunlar burada başlıyor. Önceki bölümde uyguladığım Person sınıfını ele alalım. Person nesnesinin kopyası, veritabanında doğru satırı bulmak için kullanacağım tanımlayıcıyı ($id property’si) içerir. Bu property’nin kopyalanmasına izin verirsem, aynı veri varlığını (veritabanı satırı) temsil eden iki ayrı nesne oluşabilir.</p>

<p>Neyse ki, nesne üzerinde clone çağrıldığında neyin kopyalanacağını kontrol edebilirsiniz. Bunu, __clone() adlı özel bir metodu uygulayarak yababilirsiniz (sihirli mettoların özelliği olan baştaki iki alt çizgiye dikkat edin). __clone(), nesne üzerinde clone anahtar sözcüğü çağrıldığında otomatik olarak çağrılır.</p>

<p>__clone() uyguladığınızda, metodun çalıştığı bağlamı anlamak önemlidir. __clone() kopyalanan nesne üzerinde çalıştırılır, orijinal üzerinde çalıştırılmaz. Burada, Person sınıfının başka bir versiyonuna __clone() ekliyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    private int $id = 0;

    public function __construct(private string $name, private $age)
    {
    }

    public function setId(int $id): void
    {
        $this-&gt;id = $id;
    }
    
    public function __clone(): void
    {
        $this-&gt;id = 0;
    }
}
</code></pre></div></div>

<p>Person nesnesinde clone çağrıldığında, yeni bir yüzeysel kopya yapılır ve onun __clone() metodu çağrılır. Bu, __clone() içinde yaptığım herhangi bir şeyin, kopyanın üzerine yazacağı anlamına gelir. Bu durumda, kopyalanan nesnenin $id property’sinin sıfır olmasını sağlarım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person("bob", 44);
$person-&gt;setId(343);
$person2 = clone $person;
</code></pre></div></div>

<p>Yüzeysel kopya, basit özelliklerin eski nesneden yeni nesneye kopyalanmasını sağlar. Nesne property’lerinin tanımlayıcıları kopyalanır ancak temel verileri kopyalanmaz; bu, bir nesneyi klonlarken istediğiniz veya beklediğiniz şey olmayabilir. Person nesnesine bir Account nesnesi property’si verdiğimi varsayalım. Bu nesne, klonlanan nesneye kopyalanmasını istediğim bir bakiyeye sahiptir. Yine de istemediğim şey, her iki Person nesnesinin de aynı hesaba referanslar içermesidir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Account
{
    public function __construct(public float $balance)
    {
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    private int $id;

    public function __construct(
        private string $name,
        private int $age,
        public Account $account
    ) {
    }

    public function setId(int $id): void
    {
        $this-&gt;id = $id;
    }
    
    public function __clone(): void
    {
        $this-&gt;id = 0;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person("bob", 44, new Account(200));
$person-&gt;setId(343);
$person2 = clone $person;
// give $person some money
$person-&gt;account-&gt;balance += 10;
// $person2 sees the credit too
print $person2-&gt;account-&gt;balance;
</code></pre></div></div>

<p>Bu, aşağıdaki çıktıyı verir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>210
</code></pre></div></div>

<p>$person, herkesin erişimine açık tuttuğum bir Account nesnesine referans tutuyor (bildiğiniz gibi, genellikle property’e erişimi kısıtlar, gerekirse accessor metot sağlardım). Klon oluşturulduğunda, aynı Account nesnesine ait bir referansı da içerir. Bunu, $person nesnesinin Account’una ekleyerek ve artan bakiyeyi $person2 aracılığıyla onaylayarak gösteriyorum.</p>

<p>Klonlama işleminden sonra nesne property’sinin paylaşılmasını istemiyorsam, bunu __clone() metodunda açıkça klonlamak bana kalmıştır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function __clone(): void
{
    $this-&gt;id = 0;
    $this-&gt;account = clone $this-&gt;account;
}
</code></pre></div></div>

<h2 id="nesneleriniz-i̇çin-string-değerleri-tanımlamak">Nesneleriniz İçin String Değerleri Tanımlamak</h2>

<p>PHP 5 tarafından tanıtılan Java’dan ilham alan başka bir özellik de __toString() metoduydu. PHP 5.2’den önce, bir nesneyi yazdırdığınızda, şuna benzer bir dizeye çözümlenirdi:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class StringThing
{
}

$st = new StringThing();
print $st;
</code></pre></div></div>

<p>PHP 5.2’den bu yana, bu kod şöyle bir hata üretecektir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Object of class popp\ch04\batch22\StringThing could not be converted to
string ...
</code></pre></div></div>

<p>__toString() metodunu uygulayarak, dize bağlamında erişildiğinde (veya açıkça bir dizeye dönüştürüldüğünde) nesnelerinizin kendilerini nasıl temsil ettiğini kontrol edebilirsiniz. Bir dizi değeri döndürmek için __toString() yazılmalıdır. Metod, nesneniz print veya echo’ya iletildiğinde otomatik olarak çağrılır ve dönüş değeri değiştirilir. Burada, Person sınıfına __toString() ekliyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    public function getName(): string
    {
        return "Bob";
    }

    public function getAge(): int
    {
        return 44;
    }
    
    public function __toString(): string
    {
        $desc = $this-&gt;getName() . " (age ";
        $desc .= $this-&gt;getAge() . ")";
        return $desc;
    }
}
</code></pre></div></div>

<p>Şimdi Person nesnesini yazdırdığımda, nesne şu şekilde çözümlenecek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person();
print $person;
Bob (age 44)
</code></pre></div></div>

<p>__toString() metodu, ana görevi bilgi iletmek olan sınıfların yanı sıra log kaydı ve hata raporlama için özellikle yararlıdır. Örneğin, Exception sınıfı, istisna verilerini __toString() metodunda özetler.</p>

<p>PHP 8’den itibaren, __toString() metodunu uygulayan herhangi bir sınıfın, yerleşik Stringable arayüzünü uyguladığı dolaylı olarak bildirilir. Bu, parametre ve property’leri kısıtlamak için bileşik tür bildirimi kullanabileceğiniz anlamına gelir. İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static function printThing(string|\Stringable $str): void
{
    print $str;
}
</code></pre></div></div>

<p>Bir dizeyi veya Person nesnesini printThing() metoduna iletebiliriz ve seçtiğimiz herhangi bir dize benzeri şeyle çalışabileceğinden emin olabiliriz.</p>

<h2 id="callbackler-anonim-fonksiyonlar-ve-closurelar">Callback’ler, Anonim Fonksiyonlar ve Closure’lar</h2>

<p>Kesin olarak nesne yönelimli bir özellik olmasa da, anonim fonksiyonlar, callback kullanan nesne yönelimli uygulamalarda karşılaşabileceğiniz için burada bahsedeceğiz.</p>

<p><strong>Not</strong>: Callback, bir değişkende saklanabilen veya daha sonra çağırmak üzere metot ve fonksiyonlara aktarılabilen yürütülebilir kod bloğudur.</p>

<p>İşte birkaç sınıf:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Product
{
    public function __construct(public string $name, public float $price)
    {
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ProcessSale
{
    private array $callbacks;

    public function registerCallback(callable $callback): void
    {
        $this-&gt;callbacks[] = $callback;
    }
    
    public function sale(Product $product): void
    {
        print "{$product-&gt;name}: processing \n";
        foreach ($this-&gt;callbacks as $callback) {
            call_user_func($callback, $product);
        }
    }
}
</code></pre></div></div>

<p>Bu kod, çeşitli callback’leri çalıştırmak için tasarlanmıştır. Product ve ProcessSale olmak üzere iki sınıftan oluşur. Product, yalnızca $name ve $price property’lerini saklar.</p>

<p>ProcessSale iki metottan oluşur. İlki olan registerCallback(), callable türü kabul eder ve onu $callbacks dizi property’sine ekler. İkinci metot olan sale(), Product nesnesini kabul eder, bununla ilgili bir mesaj verir ve ardından $callbacks dizi property’sini döngüye sokar.</p>

<p>Her öğeyi, kodu çağıran call_user_func()’a $product parametresiyle beraber olarak iletir. Aşağıdaki örneklerin tümü framework ile çalışacaktır.</p>

<p>Callback’ler neden yararlıdır? Çalışma zamanında o bileşenin asıl görevi ile doğrudan ilgili olmayan bir işlevsellik eklemenize izin verirler. Bir bileşeni callback’ten haberdar hale getirerek, başkalarına kodunuzu henüz bilmediğiniz bağlamlarda genişletme gücü verirsiniz.</p>

<p>Örneğin, gelecekteki bir ProcessSale kullanıcısının satış log’u oluşturmak istediğini hayal edin. Kullanıcının sınıfa erişimi varsa, log kodunu doğrudan sale() metoduna ekleyebilir. Yine de bu her zaman iyi bir fikir değildir. ProcessSale paketinin bir sonraki güncellemesinde değişikliklerinin üzerine yazılacaktır. Paketi yazan siz olsanız bile, sale() metoduna pek çok arızi görev eklemek, temel sorumluluğunu aşmaya başlayacak ve potansiyel olarak onu projeler arasında daha az kullanılabilir hale getirecektir. Buna bir sonraki bölümde döneceğim.</p>

<p>ProcessSale’i callback’in farkındadır. Burada, log’a kaydetmeyi simüle eden bir callback oluşturuyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$logger = function ($product) {
    print " logging ({$product-&gt;name})\n";
};

$processor = new ProcessSale();
$processor-&gt;registerCallback($logger);
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
$processor-&gt;sale(new Product("coffee", 6));
</code></pre></div></div>

<p>Burada anonim fonksiyon oluşturuyorum. Yani, function anahtar kelimesini fonksiyon adı olmadan kullanıyorum. Bu bir satır içi ifade olduğundan, kod bloğunun sonunda noktalı virgül gerektiğini unutmayın. Anonim fonksiyon değişkende saklanabilir, metot ve fonksiyonlara parametre olarak iletilebilir. $logger değişkenine fonksiyon atayarak ve bunu ProcessSale::registerCallback() metoduna ileterek yaptığım tam da bu. Son olarak, birkaç ürün oluşturup bunları sale() metoduna aktarıyorum. Satış daha sonra işlenir (gerçekte ürün hakkında basit bir mesaj yazdırılır) ve tüm callback’ler yürütülür.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shoes: processing
    logging (shoes)
coffee: processing
    logging (coffee)
</code></pre></div></div>

<p>PHP 7.4, anonim fonksiyonları bildirmenin yeni bir yolunu tanıttı. Arrow fonksiyonları, daha önce karşılaştığınız anonim fonksiyonlara işlevsel olarak çok benzer. Bununla birlikte, sözdizimi çok daha derli topludur. function anahtar sözcüğü yerine, fn ile tanımlanırlar. Ardından parametreler için parantez ve son olarak süslü parantez yerine arrow işleci (=&gt;) ve ardından tek bir ifade gelir. Bu derli toplu form, arrow fonksiyonlarını özel sıralamalar ve benzerleri için - küçük callback’ler oluşturarak - çok kullanışlı hale getirir. Burada, $logger anonim fonksiyonunu arrow kullanarak eşdeğeriyle değiştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$logger = fn($product) =&gt; print " logging ({$product-&gt;name})\n";
</code></pre></div></div>

<p>Arrow fonksiyon çok daha derli topludur, ancak yalnızca tek bir ifade tanımladığınız için nispeten basit görevlerde kullanılır.</p>

<p>Elbette, callback’lerin isimsiz olması gerekmez. Bir fonksiyonun adını, hatta bir nesne referansını ve bir metodu callback olarak kullanabilirsiniz. İşte tam da bunu yapıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Mailer
{
    public function doMail(Product $product): void
    {
        print " mailing ({$product-&gt;name})\n";
    }
}

$processor = new ProcessSale();
$processor-&gt;registerCallback([new Mailer(), "doMail"]);
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
$processor-&gt;sale(new Product("coffee", 6));
</code></pre></div></div>

<p>Bir sınıf oluşturuyorum: Mailer. Tek metodu olan doMail(), Product nesnesini kabul eder ve bununla ilgili bir mesaj verir. registerCallback()’i çağırdığımda, ona bir dizi iletiyorum. İlk öğe Mailer nesnesi ve ikincisi, çağrılmasını istediğim metodun adıyla eşleşen bir dizedir.</p>

<p>registerCallback()’in callable parametreyi zorlamak için bir tür bildirimi kullandığını unutmayın. PHP, bu tür bir diziyi callable olarak tanıyacak kadar akıllıdır. Dizi biçimindeki geçerli bir callback, ilk öğesi olarak bir nesneye ve ikinci öğesi olarak bir metot adına sahip olmalıdır. Testi burada geçiyorum ve işte çıktı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shoes: processing
    mailing (shoes)
coffee: processing
    mailing (coffee)
</code></pre></div></div>

<p>Bir metodun anonim fonksiyon döndürmesini sağlayabilirsiniz;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Totalizer
{
    public static function warnAmount(): callable
    {
        return function (Product $product) {
            if ($product-&gt;price &gt; 5) {
                print " reached high price: {$product-&gt;price}\n";
            }
        };
    }
}

$processor = new ProcessSale();
$processor-&gt;registerCallback(Totalizer::warnAmount());
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
</code></pre></div></div>

<p>Anonim fonksiyon için factory olarak warnAmount() metodunu kullanmanın dışında, pek ilgi çekici bir şey eklemedim. Ancak bu yapı, anonim fonksiyon oluşturmaktan çok daha fazlasını yapmama izin veriyor. Closure’lardan yararlanmamı sağlıyor. Anonim fonksiyonlar, anonim fonksiyonların üst kapsamında bildirilen değişkenlere başvurabilir. Bu bazen anlaşılması zor bir kavramdır. Anonim fonksiyon, oluşturulduğu bağlamı hatırlamaya devam ediyor gibidir. Totalizer::warnAmount() fonksiyonunun iki şey yapmasını istediğimi hayal edin. Her şeyden önce, isteğe bağlı bir hedef tutarı kabul etmesini istiyorum. İkincisi, ürünler satıldıkça fiyatların çetelesini tutmasını istiyorum. Toplam, hedeflenen tutarı aştığında, fonksiyon bir eylem gerçekleştirecektir (bu durumda, tahmin edebileceğiniz gibi, sadece bir mesaj yazacaktır).</p>

<p>use kullanarak anonim fonksiyonumun değişkenleri daha geniş bir kapsamdan izlemesini sağlayabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Totalizer2
{
    public static function warnAmount($amt): callable
    {
        $count = 0;
        return function ($product) use ($amt, &amp;$count) {
            $count += $product-&gt;price;
            print " count: $count\n";
            if ($count &gt; $amt) {
                print " high price reached: {$count}\n";
            }
        };
    }
}

$processor = new ProcessSale();
$processor-&gt;registerCallback(Totalizer2::warnAmount(8));
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
$processor-&gt;sale(new Product("coffee", 6));
</code></pre></div></div>

<p>Totalizer2::warnAmount() tarafından döndürülen anonim fonksiyon, use ifadesinde iki değişken belirtir. İlki $amt. Bu, warnAmount() tarafından kabul edilen parametredir. İkinci closure değişkeni $count’tur. $count, warnAmount() gövdesinde bildirilir ve başlangıçta sıfıra ayarlanır. Use ifadesindeki $count değişkeninin başına &amp; işareti koyduğuma dikkat edin. Bu, değişkene anonim fonksiyondaki değer yerine referans yoluyla erişileceği anlamına gelir. Anonim fonksiyonun gövdesinde, ürünün değerine göre $count’u artırıyorum ve ardından yeni toplamı $amt’a karşı test ediyorum. Hedef değere ulaşıldıysa bildirim çıktısı alıyorum.</p>

<p>İşte çalışan kod:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shoes: processing
    count: 6
coffee: processing
    count: 12
    high price reached: 12
</code></pre></div></div>

<p>Bu, callback’in çağrılar arasında $count’u takip ettiğini gösterir. Hem $count hem de $amt fonksiyonla ilişkili kalır, çünkü fonksiyon bildiriminin bağlamında bulunurlar ve use ifadesinde belirtilirler.</p>

<p>Arrow fonksiyonları da closure oluşturur (anonim fonksiyonlar gibi, yerleşik Closure sınıfının bir örneğine çözümlenirler). Closure değişkenleriyle açık bir ilişki gerektiren anonim fonksiyonların aksine, kapsamdaki tüm değişkenlerin yan-değer kopyasını otomatik olarak alırlar. İşte bir örnek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$markup = 3;
$counter = fn (Product $product) =&gt; print "($product-&gt;name) marked up
price: " .
    ($product-&gt;price + $markup) . "\n";
$processor = new ProcessSale();
$processor-&gt;registerCallback($counter);
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
$processor-&gt;sale(new Product("coffee", 6));
</code></pre></div></div>

<p>ProcessSale::sale()’e ilettiğim anonim fonksiyon içinde $markup’a erişebiliyorum. Ancak, fonksiyon yalnızca değere göre erişime sahip olduğundan, fonksiyon içinde gerçekleştireceğim herhangi bir değişiklik kaynak değişkeni etkilemeyecektir.</p>

<p>PHP 7.1, nesne bağlamındaki closure’ları yönetmenin yeni bir yolunu tanıttı. Closure::fromCallable() metodu, bir nesnenin sınıflarına ve property’lerine çağıran kod erişimi sağlayan bir closure oluşturmanıza olanak tanır. Son örnekle aynı sonucu elde etmek için nesne özelliklerini kullanan Totalizer serisinin bir versiyonu:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Totalizer3
{
    private float $count = 0;
    private float $amt = 0;
    
    public function warnAmount(int $amt): callable
    {
        $this-&gt;amt = $amt;
        return \Closure::fromCallable([$this, "processPrice"]);
    }
    
    private function processPrice(Product $product): void
    {
        $this-&gt;count += $product-&gt;price;
        print " count: {$this-&gt;count}\n";
        if ($this-&gt;count &gt; $this-&gt;amt) {
            print " high price reached: {$this-&gt;count}\n";
        }
    }
}
</code></pre></div></div>

<p>warnAmount() metodu bu örnekte static değildir. Bunun nedeni, Closure::fromCallable() sayesinde, daha geniş nesneye erişimi olan processPrice() metoduna bir callback döndürmemdir. $amt property’sini ayarlıyorum ve callable metot referansı döndürüyorum. processPrice() çağrıldığında, $count property’sini artırır ve $amt değerine ulaşıldığında uyarı verir. processPrice() public bir metot olsaydı, basitçe [$this, “processPrice”] döndürebilirdim. Gördüğümüz gibi, PHP böyle iki elemanlı bir dizinin callable olarak çözülmesi gerektiğini anlayacak kadar zekidir. Bununla birlikte, Closure::fromCallable()’ı kullanmak istememin iki iyi nedeni var. İlk olarak, private veya protected metotlara, onları tüm dünyaya açmak zorunda kalmadan kontrollü erişim verebilirim; bu, erişimi kontrol ederken gelişmiş işlevsellik sunar. İkincisi, performans artışı elde ediyorum çünkü dönüş değerinin gerçekten callable olup olmadığını anlamaya yönelik bir ek yükten kurtuluyorum.</p>

<p>Burada Totalizer3’ü değişmemiş ProcessSale sınıfıyla kullanıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$totalizer3 = new Totalizer3();
$processor = new ProcessSale();
$processor-&gt;registerCallback($totalizer3-&gt;warnAmount(8));
$processor-&gt;sale(new Product("shoes", 6));
print "\n";
$processor-&gt;sale(new Product("coffee", 6));
</code></pre></div></div>

<h2 id="anonim-sınıflar">Anonim Sınıflar</h2>

<p>PHP 7 anonim sınıfları tanıttı. Bunlar, küçük bir sınıftan örnek oluşturmanız ve türetmeniz gerektiğinde, söz konusu üst sınıf basit ve yerel bağlama özgü olduğunda kullanışlıdır.</p>

<p>PersonWriter örneğimize dönelim. Bu sefer bir arayüz oluşturarak başlayacağım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>interface PersonWriter
{
    public function write(Person $person): void;
}
</code></pre></div></div>

<p>PersonWriter nesnesini kullanabilen bir Person sınıfı sürümü:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Person
{
    public function output(PersonWriter $writer): void
    {
        $writer-&gt;write($this);
    }

    public function getName(): string
    {
        return "Bob";
    }
    
    public function getAge(): int
    {
        return 44;
    }
}
</code></pre></div></div>

<p>output() metodu, PersonWriter örneğini kabul eder ve ardından geçerli sınıfın örneğini write() metoduna iletir. Bu şekilde, Person sınıfı, write uygulamasından güzel bir şekilde yalıtılmıştır.</p>

<p>Person nesnesi için name ve age değerlerini yazdıracak bir writer’a ihtiyacımız olursa, devam edip her zamanki gibi bir sınıf oluşturabiliriz. Ancak bu o kadar önemsiz bir uygulama ki, aynı anda bir sınıf oluşturup bunu Person’a iletebiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person();

$person-&gt;output(
    new class implements PersonWriter
    {
        public function write(Person $person): void
        {
            print $person-&gt;getName() . " " . $person-&gt;getAge() . "\n";
        }
    }
);
</code></pre></div></div>

<p>Gördüğünüz gibi, new class anahtar kelimeleri ile anonim bir sınıf tanımlayabilirsiniz. Daha sonra, sınıf bloğunu oluşturmadan önce gerekli olan herhangi bir extensions ve implements ifadesini ekleyebilirsiniz.</p>

<p>Anonim sınıflar closure’ları desteklemez. Yani daha geniş kapsamda bildirilen değişkenlere sınıf içerisinden erişilemez. Ancak, değerleri anonim sınıfın kurucusuna iletebilirsiniz. Biraz daha karmaşık bir PersonWriter oluşturalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$person = new Person();

$person-&gt;output(
    new class("/tmp/persondump") implements PersonWriter
    {
        private $path;

        public function __construct(string $path)
        {
            $this-&gt;path = $path;
        }
        
        public function write(Person $person): void
        {
            file_put_contents($this-&gt;path, $person-&gt;getName() . " " .
                $person-&gt;getAge() . "\n");
        }
    }
);
</code></pre></div></div>

<p>Kurucuya bir path parametresi ilettim. Bu değer, $path property’sinde saklandı ve sonunda write() metodu tarafından kullanıldı.</p>

<p>Elbette, anonim sınıfınızın boyutu ve karmaşıklığı büyümeye başlarsa, bir sınıf dosyasında adlandırılmış bir sınıf oluşturmak daha mantıklı hale gelir. Bu, özellikle anonim sınıfınızı birden fazla yere kopyalamanız gerekirse geçerlidir.</p>

<p><strong>Özet</strong></p>

<p>Bu bölümde, PHP’nin gelişmiş nesne yönelimli özelliklerini ele aldık. Kitapta çalıştıkça bunlardan bazıları size tanıdık gelecek. Özellikle soyut sınıflara, istisnalara ve static metotlara sık sık döneceğim.</p>

<p>Bir sonraki bölümde, yerleşik nesne özelliklerinden bir adım geri çekiliyorum ve nesnelerle çalışmanıza yardımcı olmak için tasarlanmış sınıflara ve fonksiyonlara bakıyorum.</p>

<p><strong>Kaynak:</strong> “PHP 8 Objects, Patterns, and Practice”, Matt Zandstra</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Sınıf tipi ipuçlarının ve erişim kontrolünün size bir sınıfın arayüzü üzerinde nasıl daha fazla kontrol sağladığını zaten gördünüz. Bu bölümde, PHP’nin nesne yönelimli özelliklerini daha derinlemesine inceleyeceğim.]]></summary></entry><entry><title type="html">PHP - Nesnelerin Temelleri</title><link href="http://localhost:4000/2024/03/20/php-nesnelerin-temelleri.html" rel="alternate" type="text/html" title="PHP - Nesnelerin Temelleri" /><published>2024-03-20T00:00:00+03:00</published><updated>2024-03-20T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/20/php-nesnelerin-temelleri</id><content type="html" xml:base="http://localhost:4000/2024/03/20/php-nesnelerin-temelleri.html"><![CDATA[<p>Nesneler ve sınıflar bu kitabın merkezindedir ve on yıl önce PHP 5’in piyasaya sürülmesinden bu yana PHP’nin de kalbinde yer alıyor. Bu bölümde, PHP’nin nesne yönelimli özelliklerini inceleyerek nesneleri ve tasarımı daha derinlemesine ele almak için temel oluşturuyorum. Nesne yönelimli programlamada yeniyseniz, bu bölümü dikkatlice okumalısınız.</p>

<p>Bu bölüm aşağıdaki konuları kapsayacaktır:</p>

<ul>
  <li>Sınıflar ve nesneler: Sınıfları tanımlamak ve nesneleri başlatmak</li>
  <li>Kurucu metotlar: Nesnelerin kurulumunu otomatikleştirmek</li>
  <li>Primitif ve sınıf tipleri: Tipler neden önemlidir?</li>
  <li>Kalıtım: Neden kalıtıma ihtiyacımız var ve onu nasıl kullanacağız?</li>
  <li>Görünürlük: Nesne arayüzünü düzene sokmak</li>
</ul>

<h2 id="sınıflar-ve-nesneler">Sınıflar ve Nesneler</h2>

<p>Nesne yönelimli programlamayı anlamanın önündeki ilk engel, sınıf ve nesne arasındaki garip ve harika ilişkidir. Pek çok insan için, aydınlanmayı temsil eden bu ilişkidir. O halde temel bilgileri gözden kaçırmayalım.</p>

<p><strong>İlk Sınıf</strong></p>

<p>Sınıflar genellikle nesneler açısından tanımlanır. Bu ilginç çünkü nesneler genellikle sınıflar olarak tanımlanır. Bu döngüsellik, nesne yönelimli programlamanın ilk adımlarını zorlaştırabilir. Nesneleri şekillendiren sınıflar olduğu için, bir sınıf tanımlayarak başlamalıyız.</p>

<p>Kısacası, bir sınıf, bir veya daha fazla nesne oluşturmak için kullanılan bir kod şablonudur. class anahtar sözcüğü ve sınıf adıyla bir sınıf bildirirsiniz. Sınıf adları bir sayı ile başlayamaz, fakat sayı ve harflerin herhangi bir bileşimi olabilir. Alt çizgi karakteri de içerebilirler. Sınıfla ilişkilendirilen kod parantez içine alınmalıdır. Bir sınıf oluşturmak için bu öğeleri birleştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    // class body
}
</code></pre></div></div>

<p>Örnekteki ShopProduct sınıfı, henüz çok kullanışlı olmasa da kurala uygun bir sınıftır. Yine de oldukça önemli bir şey yaptım. Bir tür tanımladım; yani kullanabileceğim bir veri kategorisi oluşturdum. Bölüm boyunca bunun gücü daha net hale gelecektir.</p>

<p><strong>İlk Nesne (veya İki)</strong></p>

<p>Sınıf, nesneleri oluşturmak için bir şablonsa, nesnenin, sınıfta tanımlanan şablona göre yapılandırılmış veriler olduğu sonucu çıkar. Nesne, sınıfının örneğidir. Sınıf tarafından tanımlanan tiptedir.</p>

<p>ShopProduct nesnelerini oluşturmak için ShopProduct sınıfını kalıp olarak kullanıyorum. Bunu yapmak için new operatörüne ihtiyacım var. new operatörü, sınıfın adıyla birlikte kullanılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct();
$product2 = new ShopProduct();
</code></pre></div></div>

<p>new operatörü, tek operant olarak sınıf adıyla çağrılır ve o sınıfın örneğini döndürür; yukarıda ShopProduct nesnesini oluşturur.</p>

<p>ShopProduct sınıfını şablon olarak kullanarak, iki ShopProduct nesnesi oluşturdum. $product1 ve $product2 işlevsel olarak aynı olmalarına rağmen, tek bir sınıftan türetilmiş, aynı türden farklı nesnelerdir.</p>

<p>Hala kafanız karışıksa, şu benzetmeyi deneyin. Sınıfı, plastik ördek yapan bir makinede kalıp olarak düşünün. Nesnelerimiz, bu makinenin ürettiği ördekler. Üretilen şeyin türü, preslendiği kalıba göre belirlenir. Ördekler her yönden aynı görünürler, ancak farklı varlıklardır. Başka bir deyişle, aynı türün farklı örnekleridir. Ördeklerin kimliklerini kanıtlamak için kendi seri numaraları bile olabilir. PHP kodunda oluşturulan her nesneye ayrıca kendi benzersiz kimliği verilir. Bunu, $product1 ve $product2 nesnelerini yazdırarak gösterebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>var_dump($product1);
var_dump($product2);
</code></pre></div></div>

<p>Bu fonksiyonları çalıştırmak aşağıdaki çıktıyı üretir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>object(popp\ch03\batch01\ShopProduct)#235 (0) {
}
object(popp\ch03\batch01\ShopProduct)#234 (0) {
}
</code></pre></div></div>

<p><strong>Not</strong>: PHP’nin eski sürümlerinde (5.1 sürümüne kadar), bir nesneyi doğrudan yazdırabilirsiniz. Bu, nesneyi, nesnenin kimliğini içeren bir metne dönüştürür. PHP 5.2’den itibaren artık bu desteklenmiyor ve bir nesneyi metin olarak yazdırma girişimleri, nesnenin sınıfında __toString() adlı bir metot tanımlanmadıkça hataya neden oluyor. Metotlara daha sonra bakacağım ve Bölüm 4’te __toString()’i ele alacağım.</p>

<p>Nesneleri var_dump()’a ileterek, # işaretinden sonra nesnelerin kimliği de dahil olmak üzere yararlı bilgileri alıyorum.</p>

<p>Bu nesneleri daha ilginç hale getirmek için ShopProduct sınıfını, properties adı verilen özel veri alanlarını destekleyecek şekilde değiştirebilirim.</p>

<h2 id="sınıf-özellikleri-properties">Sınıf Özellikleri (Properties)</h2>

<p>Sınıflar, property adı verilen özel değişkenleri tanımlayabilir. Üye değişken olarak da bilinen property, nesneden nesneye değişebilen verileri tutar. Dolayısıyla, ShopProduct nesneleri söz konusu olduğunda, örneğin başlık ve fiyat alanlarını değiştirmek isteyebilirsiniz.</p>

<p>Property, standart bir değişkene benzer, ancak bir property bildirirken, değişkenden önce görünürlük (visibility) anahtar sözcüğü kullanmalısınız. Bu, public, protected ve private olabilir ve erişilebilirliği belirler. Örneğin, public property’lere sınıf dışından erişilebilir ve private property’lere yalnızca sınıf içindeki koddan erişilebilir.</p>

<p>Anahtar kelimelere ve erişilebilirlik konusuna daha sonra döneceğim. Şimdilik, public anahtar sözcüğünü kullanarak bazı property’ler tanımlayacağım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $title = "default product";
    public $producerMainName = "main name";
    public $producerFirstName = "first name";
    public $price = 0;
}
</code></pre></div></div>

<p>Gördüğünüz gibi, her birine varsayılan değer atayarak dört property oluşturdum. ShopProduct sınıfından örneklendirdiğim tüm nesneler artık varsayılan verilerle önceden doldurulacak. Her property bildirimindeki public anahtar sözcüğü, property’e nesne bağlamı dışından erişebilmemi sağlar.</p>

<p>“-&gt;” karakterini (nesne operatörü) kullanarak, property değişkenlerine nesne bazında erişebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct();
print $product1-&gt;title;
// default product
</code></pre></div></div>

<p>Property’ler public olarak tanımlandığından, sınıftaki herhangi bir varsayılan değeri değiştirerek - okuyabildiğiniz gibi - değer atayabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct();
$product2 = new ShopProduct();
$product1-&gt;title = "My Antonia";
$product2-&gt;title = "Catch 22";
</code></pre></div></div>

<p>ShopProduct sınıfında $title property’sini bildirerek ve değer atayarak, tüm ShopProduct nesnelerinin ilk oluşturulduğunda bu özelliğe sahip olmasını sağlayabilirsiniz. Bu sınıfı kullanan kodun, bu varsayıma dayalı olarak ShopProduct nesneleriyle çalışabileceği anlamına gelir. Yine de sıfırlayabildiğim için, $title’ın değeri nesneden nesneye değişebilir.</p>

<p>Aslında, PHP bizi sınıfın tüm property’lerini bildirmeye zorlamaz. Property’leri bir nesneye dinamik olarak ekleyebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1-&gt;arbitraryAddition = "treehouse";
</code></pre></div></div>

<p>Ancak bu yöntem, nesne yönelimli programlamada iyi bir uygulama olarak kabul edilmez.</p>

<p>Property’leri dinamik olarak oluşturmak neden kötü bir uygulamadır? Bir sınıf oluşturduğunuzda, tür tanımlarsınız. Dünyaya, sınıfınızın (ve ondan örneklenen herhangi bir nesnenin) belirli bir alan ve metot kümesinden oluştuğunu bildirirsiniz. ShopProduct sınıfınızda $title property’si tanımlanmışsa ShopProduct nesneleriyle çalışan herhangi bir kod, $title property’sinin kullanılabilir olacağı varsayımıyla ilerleyebilir. Yine de, dinamik olarak ayarlanmış property’ler hakkında hiçbir garanti verilemez.</p>

<p>Nesnelerim bu aşamada hala hantal. Nesnenin özellikleriyle çalışmam gerektiğinde, bunu şu anda nesnenin dışında yapmalıyım. Birden çok nesnede birden çok property ile çalışmak, kısa sürede angarya haline gelecektir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct();
$product1-&gt;title = "My Antonia";
$product1-&gt;producerMainName = "Cather";
$product1-&gt;producerFirstName = "Willa";
$product1-&gt;price = 5.99;
</code></pre></div></div>

<p>Tüm ürün ayrıntılarını set edene kadar varsayılan property değerlerini birer birer geçersiz kılıyorum. Artık bazı verileri set ettiğime göre, bunlara da erişebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print "author: {$product1-&gt;producerFirstName} "
    . "{$product1-&gt;producerMainName}\n";
// author: Willa Cather
</code></pre></div></div>

<p>Bu yaklaşımla ilgili bir dizi sorun vardır. PHP, property’leri dinamik olarak set etmenize izin verdiği için, property adını yanlış yazarsanız veya unutursanız uyarı almazsınız. Örneğin, şu satırı yazmak istediğimi varsayalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>product1-&gt;producerMainName = "Cather";
</code></pre></div></div>

<p>Maalesef yanlışlıkla şöyle yazıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1-&gt;producerSecondName = "Cather";
</code></pre></div></div>

<p>PHP motoru söz konusu olduğunda, bu kod tamamen kurala uygundur ve sizi bu konuda uyarmaz. Yazarın adını yazdırmak istediğimde ise beklenmedik sonuçlarla karşılaşacağım.</p>

<p>Başka bir sorun da, sınıfımın fazla rahat olması. Başlık, fiyat veya yazar isimleri belirlemek zorunda değilim. Yazılımcı, bu property’lerin var olduğundan emin olabilir, ancak çoğu zaman varsayılan değerlerle karşılaşma olasılığı yüksektir. İdeal olarak, ShopProduct nesnesini başlatan herkesi anlamlı property değerleri belirlemeye teşvik etmek istiyorum.</p>

<p>Son olarak, muhtemelen oldukça sık yapmak isteyeceğim bir şeyi yapmak için çemberlerin içinden atlamam gerekiyor. Gördüğünüz gibi, yazar adının basılması yorucu bir süreçtir.</p>

<p>Nesnenin bu tür angarya işleri benim adıma halletmesi güzel olurdu.</p>

<p>Bu sorunlar, ShopProduct nesnesine, property verilerini nesne bağlamı içinde işlemek için kullanılabilecek kendi fonksiyon kümesi verilerek çözülebilir.</p>

<h2 id="metotlarla-çalışmak">Metotlarla Çalışmak</h2>

<p>Property’ler, nesnelerinizin veri depolamasına izin verdiği gibi, metotlar da nesnelerinizin görevleri gerçekleştirmesine izin verir. Metotlar, bir sınıf içinde bildirilen özel fonksiyonlardır. Tahmin edebileceğiniz gibi, metot bildirimi fonksiyon bildirimine benzer. function anahtar sözcüğü, metot adından önce gelir, ardından parantez içinde isteğe bağlı parametre listesi gelir. Metot gövdesi parantez içine alınır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function myMethod($argument, $another)
{
    // ... 
}
</code></pre></div></div>

<p>Fonksiyonlardan farklı olarak, metotlar bir sınıfın gövdesinde bildirilmelidir. Ayrıca, görünürlük anahtar kelimesi de dahil olmak üzere bir dizi niteleyiciyi kabul edebilirler. Property gibi, metotlar da public, protected veya private olarak bildirilebilir. Metodu public olarak tanımlayarak, geçerli nesnenin dışından çağrılabilmesini sağlarsınız. Metot bildiriminde görünürlük anahtar sözcüğünü atlarsanız, dolaylı olarak public olarak tanımlanır. Bununla birlikte, tüm metotlar için açıkça görünürlük tanımlamak iyi bir uygulama olarak kabul edilir.</p>

<p><strong>Not</strong>: Kodlama stili standardı PSR-12, görünürlüğün tüm yöntemler için bildirilmesini gerektirir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $title = "default product";
    public $producerMainName = "main name";
    public $producerFirstName = "first name";
    public $price = 0;

    public function getProducer()
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<p>Çoğu durumda, nesne işleci -&gt; ve metot adıyla bağlantılı bir nesne değişkeni kullanarak metotları çağırırsınız. Fonksiyon çağırıyormuşsunuz gibi (metoda herhangi bir parametre iletmiyor olsanız bile) metot çağrınızda parantez kullanmalısınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct();
$product1-&gt;title = "My Antonia";
$product1-&gt;producerMainName = "Cather";
$product1-&gt;producerFirstName = "Willa";
$product1-&gt;price = 5.99;

print "author: {$product1-&gt;getProducer()}\n";
// author: Willa Cather
</code></pre></div></div>

<p>GetProducer() metodunu ShopProduct sınıfına ekliyorum. getProducer() metodunu public olarak tanımladığıma dikkat edin, bu onun sınıf dışından çağrılabileceği anlamına gelir.</p>

<p>Metot gövdesinde bir özellik tanıtıyorum. $this sözde değişkeni, sınıfın nesne örneğine atıfta bulunabileceği mekanizmadır. Bu kavramı anlamakta zorlanıyorsanız, $this ifadesini “geçerli örnek” ifadesiyle değiştirmeyi deneyin. Aşağıdaki ifadeyi göz önünde bulundurun:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$this-&gt;producerFirstName
</code></pre></div></div>

<p>Bu, şu anlama gelir:</p>

<p><em>geçerli örneğin</em> $producerFirstName <em>property’si</em></p>

<p>Böylece getProducer() metodu, $producerFirstName ve $producerMainName property’lerini birleştirir ve döndürür, bu da beni, tam yapımcı adını her alıntılamam gerektiğinde bu işi yapma zahmetinden kurtarır.</p>

<p>Sınıfı biraz geliştirdim. ShopProduct nesnesinin property’lerinin varsayılan değerlerinin değiştirilmesi için yazılımcıya güveniyorum. Bu iki yönden sorunludur. İlk olarak, ShopProduct nesnesini düzgün bir şekilde başlatmak beş satır alır ve hiçbir yazılımcı bunun için size teşekkür etmez. İkincisi, ShopProduct nesnesi başlatıldığında herhangi bir property’nin set edildiğinden emin olmanın hiçbir yolu yoktur.</p>

<p>İhtiyacım olan, nesne oluşturulduğunda otomatik olarak çağrılan bir metot.</p>

<h2 id="kurucu-constructor-metot-oluşturmak">Kurucu (Constructor) Metot Oluşturmak</h2>

<p>Bir nesne oluşturulduğunda kurucu metot çağrılır. Gerekli property’lere değer atamak, bir şeyleri ayarlamak ve gerekli ön işleri tamamlamak için kullanabilirsiniz.</p>

<p><strong>Not</strong>: PHP 5’ten önceki sürümlerde, sınıfın adını alan bir kurucu metot vardı. Böylece ShopProduct sınıfı, kurucu olarak ShopProduct() metodunu kullanır. Bu, PHP 7’den itibaren kullanımdan kaldırılmıştır ve PHP 8’den itibaren artık çalışmamaktadır. Kurucu metodunuzu  __construct() olarak adlandırın.</p>

<p>Metot adının iki alt çizgi karakteriyle başladığını unutmayın. PHP sınıflarındaki diğer birçok özel metot için bu adlandırma kuralının kullanıldığını göreceksiniz. ShopProduct sınıfı için bir kurucu tanımlıyorum:</p>

<p><strong>Not</strong>: Bu şekilde başlayan metotlar, belirli durumlarda otomatik olarak çağrıldıklarından sihirli metotlar olarak ta bilinirler. Bunlar hakkında daha fazla bilgiyi www.php.net/manual/en/language.oop5.magic.php adresinde bulabilirsiniz. Çift alt çizgi belirli bir çağrışıma sahip olduğu için, bunları kendi özel metotlarınızda kullanmamak iyi bir fikirdir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price = 0;

    public function __construct(
        $title,
        $firstName,
        $mainName,
        $price
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
    }

    public function getProducer()
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<p>Bir kez daha sınıfımı geliştiriyorum, koddan ve tekrardan tasarruf ediyorum. __construct() metodu, new operatörü kullanılarak nesne oluşturulduğunda çağrılır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct(
    "My Antonia",
    "Willa",
    "Cather",
    5.99
);

print "author: {$product1-&gt;getProducer()}\n";
// author: Willa Cather
</code></pre></div></div>

<p>Verilen parametreler kurucuya iletilir. Kurucu metot, nesnenin property’lerine değer atamak için $this sözde değişkenini kullanır.</p>

<p><strong>Not</strong>: ShopProduct nesnesinin örneğini oluşturmak artık daha kolay ve kullanımı daha güvenli. Örneklendirme ve kurulum tek bir ifadede tamamlanıyor. ShopProduct nesnesini kullanan herhangi bir kod, tüm property’lere değer atandığından makul ölçüde emin olabilir.</p>

<p><strong>Yeni Kurucu Metot Sözdizimi</strong></p>

<p>ShopProduct sınıfını daha güvenli ve daha pratik hale getirdik. Sınıfa bir göz atın. Dört property’e sahip bir nesneyi örneklendirmek için, property’leri tanımlıyoruz, verileri tutmak için kurucu metot parametrelerini tanımlıyoruz ve ardından, property’lere metot parametrelerini atayarak hepsini bir araya getiriyoruz. PHP 8, yeni bir kurucu metot kısayolu sağlar. Kurucu metot parametrelerinize görünürlük anahtar sözcüğü ekleyerek, bunları property bildirimleriyle birleştirebilir ve aynı anda atayabilirsiniz. İşte ShopProduct’ın yeni bir versiyonu:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public function __construct(
        public $title,
        public $producerFirstName,
        public $producerMainName,
        public $price
    ) {
    }

    public function getProducer()
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<p>Kurucu metottaki property’lerin hem bildirimi hem de ataması dolaylı olarak işlenir. Bu tekrarı ve hata yapma olasılığını da azaltır. Sınıfı daha kompakt hale getirerek, kaynak kodu okuyanların mantığa odaklanmasını kolaylaştırır.</p>

<p>Öngörülebilirlik, nesne yönelimli programlamanın önemli bir yönüdür. Sınıflarınızı, nesneleri  kullananların özelliklerinden emin olabileceği şekilde tasarlamalısınız. Nesneyi güvenli hale getirmenin bir yolu, property’lerinde tuttuğu veri türlerini tahmin edilebilir kılmaktır. Örneğin, $name property’sinin her zaman karakterlerden oluşması sağlanabilir. Ancak property verileri sınıfın dışından alınıyorsa bunu nasıl başarabilirsiniz? Bir sonraki bölümde, metot bildirimlerinde nesne türlerini zorlamak için kullanabileceğiniz bir mekanizmayı inceliyorum.</p>

<h2 id="varsayılan-parametreler-ve-adlandırılmış-parametreler">Varsayılan Parametreler ve Adlandırılmış Parametreler</h2>

<p>Zamanla, metot parametre listesi uzun ve hantal hale gelebilir. Bu, metotların gerektirdiği parametreleri takip etmeyi zorlaştırır. Metot tanımlarında varsayılan değerler kullanarak yazılımcı için işleri kolaylaştırabiliriz. Örneğin, ShopProduct nesnemiz için başlığa ihtiyacımız olduğunu, ancak üretici adları için boş string değerlerini ve fiyat için sıfır değerini kabul ettiğimizi varsayalım. Çağıran kodun tüm bu verileri sağlaması gerekir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct("Shop Catalogue", "", "", 0);
</code></pre></div></div>

<p>Parametrelerimiz için varsayılan değerler atayarak örneklemeyi kolaylaştırabiliriz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public function __construct(
        public $title,
        public $producerFirstName = "",
        public $producerMainName = "",
        public $price = 0
    ) {
    }

    // ...
}
</code></pre></div></div>

<p>Şimdi, kurucuya yapılan isteğin yalnızca bir değer belirtmesi gerekir: başlık.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct("Shop Catalogue");
</code></pre></div></div>

<p>Varsayılan parametre değerleri, metotlarla çalışmayı daha kolay hale getirebilir, ancak çoğu zaman, istenmeyen komplikasyonlara da neden olabilir. Bir fiyat vermek istesem, ancak yine de isimlerin varsayılan değerlerinde kalmasını istersem, kurucu metot çağrıma ne olur? PHP 8’den önce takılıp kalırdım. Fiyatı belirtmek için boş isimler vermem gerekecek. Bu bizi başladığımız yere getiriyor. Ayrıca, kurucunun boş isimler için ne tür değerler beklediğini de bulmam gerekecek. Boş bir metin mi atamalıyım? Yoksa null mu olmalı? İşi kurtarmak şöyle dursun, varsayılan değerler kafa karışıklığına yol açmış olabilir.</p>

<p>Şanslıyız, PHP 8 adlandırılmış parametreleri sağlar. Metot çağrısında, artık iletmek istediğim değerin önünde parametre adını belirtebilirim. PHP değeri, - daha sonra çağıran koddaki sıra ne olursa olsun -  metot imzasındaki doğru parametre ile ilişkilendirir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct(
    price: 0.7,
    title: "Shop Catalogue"
);
</code></pre></div></div>

<p>Buradaki sözdizimine dikkat edin: PHP’ye $price parametresini 0.7’ye set etmek istediğimi, önce parametre adını, sonra iki nokta üst üste ve ardından değeri belirterek söylüyorum. Adlandırılmış parametreler kullandığım için artık sıraları önemli değil ve artık boş değerler vermem gerekmiyor.</p>

<h2 id="parametreler-ve-tipler">Parametreler ve Tipler</h2>

<p>Tipler, verilerin yönetilme şeklini belirler. Örneğin, karakter verilerini görüntülemek için string türünü kullanırsınız ve bu tür verileri string fonksiyonlarıyla manipule edersiniz. Integer matematiksel ifadelerde, boolean test ifadelerinde vb. kullanılır. Bunlar ilkel (primitive) tipler olarak bilinir. Bu nedenle ShopProduct nesnesi, ilkel nesne tipine  aittir, ancak aynı zamanda sınıf tipine de aittir. Bu bölümde, sınıf metotlarıyla ilgili olarak her iki türe de bakacağım.</p>

<p>Metot ve fonksiyon tanımları, parametrenin belirli bir türde olmasını zorunlu olarak gerektirmez. Bu hem bir lanet hem de bir nimettir. Parametrenin herhangi bir türden olabileceği gerçeği size esneklik sunar. İşlevselliği değişen koşullara uyarlayarak farklı veri türlerine akıllıca yanıt veren metotlar oluşturabilirsiniz. Bu esneklik, belirsizliğe neden olabilir.</p>

<h3 id="i̇lkel-tipler">İlkel Tipler</h3>

<p>PHP gevşek (loosely typed) yazılmış bir dildir. Bu, bir değişkenin belirli bir veri tipini tutması için tanımlanmasına gerek olmadığı anlamına gelir. $number değişkeni 2 ve “two” değerini aynı kapsamda tutabilir. C veya Java gibi sıkı tip denetimi olan (strongly typed) dillerde, bir değer atamadan önce değişkenin türünü bildirmelisiniz ve tabii ki değer belirtilen türde olmalıdır.</p>

<p>Bu, PHP’nin tip kavramı olmadığı anlamına gelmez. Değişkene atanabilen her değerin bir tipi vardır. PHP’nin tip kontrol işlevlerinden birini kullanarak değişkenin değerinin tipini belirleyebilirsiniz. Aşağıdaki tablo, PHP’de tanınan ilkel türleri ve bunlara karşılık gelen test fonksiyonlarını listeler. Her fonksiyon bir değişkeni veya değeri kabul eder ve bu parametre  ilgili türdeyse true döndürür.</p>

<p><strong>Tablo 3-1</strong>. PHP’de İlkel Tipler ve Kontrol Fonksiyonları</p>

<table>
  <thead>
    <tr>
      <th>Fonksiyon</th>
      <th>Tip</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>is_bool()</td>
      <td>Boolean</td>
      <td>true veya false</td>
    </tr>
    <tr>
      <td>is_integer()</td>
      <td>Integer</td>
      <td>Tam sayı. is_int() ve is_long()’un takma adı</td>
    </tr>
    <tr>
      <td>is_float()</td>
      <td>Float</td>
      <td>Kayan noktalı sayı. is_double()’ın takma adı</td>
    </tr>
    <tr>
      <td>is_string()</td>
      <td>String</td>
      <td>Karakter verileri</td>
    </tr>
    <tr>
      <td>is_object()</td>
      <td>Object</td>
      <td>Nesne</td>
    </tr>
    <tr>
      <td>is_resource()</td>
      <td>Resource</td>
      <td>Veritabanları veya dosyalar gibi dış kaynakları tanımlamak ve bunlarla çalışmak için kullanılan veri tipi</td>
    </tr>
    <tr>
      <td>is_array()</td>
      <td>Array</td>
      <td>Dizi</td>
    </tr>
    <tr>
      <td>is_null()</td>
      <td>Null</td>
      <td>Atanmamış değer</td>
    </tr>
  </tbody>
</table>

<p>Özellikle metot ve fonksiyon parametreleriyle çalışırken değişkenin türünü kontrol etmek önemli olabilir.</p>

<p><strong>İlkel Tipler: Bir Örnek</strong></p>

<p>Kodunuzu yazarken dikkatli olmalısınız. İşte karşılaşabileceğiniz tiple ilgili birçok sorundan birine örnek.</p>

<p>Yapılandırma ayarlarının bir XML dosyasında tutulduğunu hayal edin. <code class="language-plaintext highlighter-rouge">&lt;resolveddomains&gt;&lt;/resolveddomains&gt;</code> XML öğesi, uygulamanıza IP adreslerini çözümlemeye çalışıp çalışmayacağını söyler; bu yararlı ancak maliyetli bir işlemdir.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;settings&gt;
    &lt;resolvedomains&gt;false&lt;/resolvedomains&gt;
&lt;/settings&gt;
</code></pre></div></div>

<p>“false” dizesi, uygulamanız tarafından alınır ve IP adresi verilerini görüntüleyen outputAddresses() adlı bir metoda iletilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class AddressManager
{
    private $addresses = ["209.131.36.159", "216.58.213.174"];

    public function outputAddresses($resolve)
    {
        foreach ($this-&gt;addresses as $address) {
            print $address;
            if ($resolve) {
                print " (" . gethostbyaddr($address) . ")";
            }
            print "\n";
        }
    }
}
</code></pre></div></div>

<p>Elbette, AddressManager sınıfı biraz geliştirilebilir. Örneğin, IP adreslerini bir sınıfın içine kodlamak pek kullanışlı değildir. Yine de outputAddresses() metodu, döngü oluşturarak  her öğeyi yazdırır. $resolve parametresi true olarak çözümlenirse, metot alan adının yanı sıra IP adresini de verir.</p>

<p>İşte, AddressManager sınıfıyla birlikte XML yapılandırma öğesini kullanan bir yaklaşım. Kusurlu olduğunu tespit edip edemeyeceğinize bakın:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$settings = simplexml_load_file(__DIR__ . "/resolve.xml");
$manager = new AddressManager();
$manager-&gt;outputAddresses((string)$settings-&gt;resolvedomains);
</code></pre></div></div>

<p>Kod parçası, SimpleXML API’sini kullanır. Bu örnekte, değerin “false” olduğunu biliyorum ve SimpleXML belgelerinin yapmamı önerdiği gibi onu string’e dönüştürüyorum.</p>

<p>Bu kod beklediğiniz gibi davranmayacaktır. “false” dizesini outputAddresses() metoduna iletirken, metodun parametre hakkında yaptığı örtük varsayımı yanlış anlıyorum. Metot bir boolean değer (yani, true veya false) bekliyor. “false” dizesi aslında true olarak yorumlanır. Bunun nedeni, PHP’nin sizin için boş olmayan bir string değerini boolean tipine çevirecek olmasıdır. Bu kod üzerinde düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if ("false") {
    // ...
}
</code></pre></div></div>

<p>Aslında şuna eşdeğerdir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (true) {
    // ...
}
</code></pre></div></div>

<p>Bunu düzeltmek için uygulayabileceğiniz birkaç yaklaşım var.</p>

<p>outputAddresses() matodunu daha töleranslı hale getirebilirsiniz, böylece dizeyi tanır ve onu boolean eşdeğerine dönüştürmek için bazı temel kurallar uygular:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function outputAddresses($resolve)
{
    if (is_string($resolve)) {
        $resolve = (preg_match("/^(false|no|off)$/i", $resolve)) ? false : true;
    }
    // ...
}
</code></pre></div></div>

<p>Bununla birlikte, böyle bir yaklaşımdan kaçınmak gerekir. Genel olarak konuşursak, metot veya fonksiyon için net ve katı bir arayüz sağlamak, belirsiz bir arayüz sunmaktan daha iyidir. Belirsiz metot ve fonksiyonlar kafa karışıklığına neden olabilir ve bu nedenle bugları besleyebilir.</p>

<p>Başka bir yaklaşım benimseyebilirsiniz: outputAddresses() metodunu olduğu gibi bırakın ve $resolve parametresinin boolean değer içermesi gerektiğine dair açık talimatlar içeren bir yorum ekleyin. Bu yaklaşım, temel olarak yazılımcıya küçük yazıları okumasını söyler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/**
* Outputs the list of addresses.
* If $resolve is true then each address will be resolved
* @param $resolve boolean Resolve the address?
*/
public function outputAddresses($resolve)
{
    // ...
}
</code></pre></div></div>

<p>Bu, yazılımcının belgeleri özenle okuduğu (veya bu tür ek açıklamaları tanıyan akıllı editörler kullandıkları) varsayıldığında makul bir yaklaşımdır.</p>

<p>Son olarak, outputAddresses() metodunu veri tipi konusunda katı hale getirebilirsiniz. Boolean gibi ilkel türler için, PHP 7’nin yayınlanmasından önce bunu yapmanın gerçekten tek bir yolu vardı. Gelen verileri incelemek için kod yazmanız ve gerekli tiple eşleşmiyorsa bir şeyler yapmanız gerekir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function outputAddresses($resolve)
{
    if (! is_bool($resolve)) {
        // do something drastic
    }
}
</code></pre></div></div>

<p>Bu yaklaşım, doğru veri türünü sağlamaya zorlamak veya bir uyarı vermek için kullanılabilir.</p>

<p><strong>Not</strong>: Sonraki bölüm olan “Tip Bildirimleri: Nesne Tipleri”nde, metotlara ve fonksiyonlara iletilen parametre tiplerini sınırlamanın çok daha iyi bir yolunu açıklayacağım.</p>

<p>String parametreyi dönüştürmek kolay olabilir, ancak muhtemelen başka sorunlara da yol açabilir. Bir dönüştürme mekanizması sağlarken, yazılımcının bağlamını ve amacını ikinci kez tahmin edersiniz. Öte yandan, boolean veri türünü zorlayarak, dizelerin boolean değerler ile eşleşip eşlemeyeceğine ve hangi sözcüğün true veya false olarak yorumlanacağına karar verme işini yazılımcıya bırakmış olursunuz. outputAddresses() metodu ise gerçekleştirmek üzere tasarlandığı göreve odaklanır. Belirli bir görevi daha geniş bağlamı göz ardı ederek gerçekleştirmeye yapılan bu vurgu, nesne yönelimli programlamada önemli bir ilkedir ve kitap boyunca buna sık sık döneceğim.</p>

<p>Aslında, parametre tipleri ile başa çıkma stratejileri bir yandan olası hataların ciddiyetine, diğer yandan esnekliğin faydalarına bağlı olacaktır. PHP, sizin için - içeriğe bağlı olarak - ilkel değerleri atar. Dizelerdeki sayılar, örneğin bir matematiksel ifadede kullanıldığında integer veya float eşdeğerlerine dönüştürülür. Dolayısıyla kodunuz doğal olarak tip hatalarına karşı töleranslı olabilir.</p>

<p>Bununla birlikte, genel olarak, hem nesne hem de ilkel tipler söz konusu olduğunda katı olmak en iyisidir. Şanslıyız ki, PHP 8, tip güvenliğini sağlamak için her zamankinden daha fazla araç sağlar.</p>

<h3 id="diğer-bazı-tip-kontrol-fonksiyonları">Diğer Bazı Tip Kontrol Fonksiyonları</h3>

<p>İlkel tipleri kontrol eden fonksiyonları gördük. Değişkenlerimizin içeriğini kontrol ederken -  değişkende tutulan verilerin nasıl kullanılabileceği hakkında daha genel bilgi sağlamak için -  ilkel türleri kontrol etmenin ötesine geçen birkaç fonksiyondan bahsetmekte fayda var. Bunları Tablo 3-2’de listeliyorum.</p>

<p><strong>Tablo 3-2</strong>. Sözde Tip Denetimi İşlevleri</p>

<table>
  <thead>
    <tr>
      <th>Fonksiyon</th>
      <th>Açıklama</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>is_countable()</td>
      <td>count() fonksiyonuna aktarılabilen dizi veya nesne</td>
    </tr>
    <tr>
      <td>is_iterable()</td>
      <td>foreach kullanılarak döngüye alınabilen bir veri yapısı</td>
    </tr>
    <tr>
      <td>is_callable()</td>
      <td>Çağrılabilen kod - genellikle anonim bir fonksiyon veya fonksiyon adı</td>
    </tr>
    <tr>
      <td>is_numeric()</td>
      <td>integer, long veya sayı olarak çözümlenebilen string</td>
    </tr>
  </tbody>
</table>

<p>Tablo 3-2’de açıklanan fonksiyonlar, belirli tipleri kontrol etmekten çok, test ettiğiniz değerleri nasıl ele alacağınızı kontrol eder. Örneğin, is_callable() değişken için true değerini döndürürse, ona bir fonksiyon veya metot gibi davranabileceğinizi ve onu çağırabileceğinizi bilirsiniz. Benzer şekilde, is_iterable() testinden geçen bir değeri - dizi yerine özel bir nesne türü olsa bile - döngüye sokabilirsiniz.</p>

<h3 id="tip-bildirimleri-nesne-tipleri">Tip Bildirimleri: Nesne Tipleri</h3>

<p>Tıpkı bir parametre değişkeninin herhangi bir ilkel türü içerebileceği gibi, varsayılan olarak herhangi bir türden nesneyi de içerebilir. Bu esnekliğin kullanım alanları vardır, ancak bir metot tanımı bağlamında sorunlar ortaya çıkarabilir.</p>

<p>ShopProduct nesnesiyle çalışmak üzere tasarlanmış bir metot düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProductWriter
{
    public function write($shopProduct)
    {
        $str = $shopProduct-&gt;title . ": "
            . $shopProduct-&gt;getProducer()
            . " (" . $shopProduct-&gt;price . ")\n";
        print $str;
    }
}
</code></pre></div></div>

<p>Bu sınıfı şu şekilde test edebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct("My Antonia", "Willa", "Cather", 5.99);
$writer = new ShopProductWriter();
$writer-&gt;write($product1);
// My Antonia: Willa Cather (5.99)
</code></pre></div></div>

<p>ShopProductWriter sınıfı tek bir metot içerir, write(). Metot, ShopProduct nesnesini kabul eder ve nesnenin özetini oluşturmak ve yazdırmak için property ve metotlarını kullanır. $shopProduct parametresinin adını - metodun beklediği parametreye uygun olarak - ShopProduct olarak kullandım, ancak bunu zorlamadım. Bu, beklenmeyen bir nesne veya ilkel bir tür iletilebileceğim anlamına gelir. O zamana kadar kod, kendisine gerçek bir ShopProduct nesnesinin iletildiği varsayımına göre hareket etmiş olabilir.</p>

<p><strong>Not</strong>: write() metodunu neden doğrudan ShopProduct’a eklemediğimi merak edebilirsiniz. Nedeni sorumluluk alanlarında yatmaktadır. ShopProduct sınıfı, ürün verilerinin yönetiminden sorumludur; ShopProductWriter bunu yazdırmaktan sorumludur. Bu bölümü okurken, işbölümünün yararlarını görmeye başlayacaksınız.</p>

<p>Bu sorunu çözmek için PHP 5, sınıf tipi bildirimlerini tanıttı. Metot parametresine sınıf tipi bildirimi eklemek için, sınırlandırmanız gereken parametrenin önüne sınıf adını koymanız yeterlidir. write() metodunu şu şekilde değiştirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function write(ShopProduct $shopProduct)
{
    // ...
}
</code></pre></div></div>

<p>Artık write() metodu, $shopProduct parametresini yalnızca ShopProduct türünde bir nesne içeriyorsa kabul edecektir.</p>

<p>İşte basit bir sınıf:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Wrong
{
}
</code></pre></div></div>

<p>Ve burada write() metodunu Wrong nesnesiyle çağırmaya çalışıyoruz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$writer = new ShopProductWriter();
$writer-&gt;write(new Wrong());
</code></pre></div></div>

<p>write() metoduna Wrong nesnesini iletmek önemli bir hataya neden olur.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TypeError: popp\ch03\batch08\ShopProductWriter::write(): Argument #1
($shopProduct) must be of type
popp\ch03\batch04\ShopProduct, popp\ch03\batch08\Wrong given, called in
/var/popp/src/ch03/batch08/Runner.php on ...
</code></pre></div></div>

<p><strong>Not</strong>: TypeError çıktısının çok fazla ek bilgi içerdiğini fark etmiş olabilirsiniz. Örneğin, Wrong sınıfı popp\ch03\batch08\Wrong şeklinde belirtilir. Bunlar namespace (isim alanı) örnekleridir ve bunları Bölüm 4’te ayrıntılı olarak göreceksiniz.</p>

<p>Bu, beni parametrenin türünü test etme zorunluluğundan kurtarıyor. Ayrıca yazılımcı için metot imzasını çok daha net hale getirir. Bir bakışta write() metodunun gereksinimlerini görebilir. Deklarasyon katı bir şekilde uygulandığından, tip hatasından kaynaklanan bazı belirsiz bug’lar hakkında endişelenmeye gerek yoktur.</p>

<p>Otomatikleştirilmiş tip denetimi, hataları önlemenin harika bir yolu olsa da, tip bildirimlerinin çalışma zamanında denetlendiğini anlamak önemlidir. Bu, yalnızca metoda istenmeyen bir nesne gönderildiği anda hata oluştuğu anlamına gelir. write() çağrısı, yalnızca Noel sabahı çalışan koşullu bir ifadeye gömülürse ve kodunuzu dikkatli bir şekilde kontrol etmediyseniz, kendinizi tatilde çalışırken bulabilirsiniz.</p>

<h3 id="tip-bildirimleri-i̇lkel-tipler">Tip Bildirimleri: İlkel Tipler</h3>

<p>PHP 7’nin piyasaya sürülmesine kadar, yalnızca nesneleri ve diğer birkaç türü (callable ve array) kısıtlamak mümkündü. PHP 7 sonunda skaler tip bildirimlerini tanıttı. Bu, parametre listenizde boolean, string, integer ve float türlerini kullanmanıza olanak tanır.</p>

<p>Skaler tip bildirimleriyle donanmış olarak, ShopProduct sınıfına bazı kısıtlamalar ekleyebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price = 0;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
    }
    // ...
}
</code></pre></div></div>

<p>Kurucu metot bu şekilde desteklendiğinde, $title, $firstName ve $mainName parametrelerinin her zaman string ve $price öğesinin float veri içerdiğinden emin olabilirim. Bunu, ShopProduct’ı yanlış bilgilerle başlatarak gösterebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = new ShopProduct("title", "first", "main", []);
</code></pre></div></div>

<p>ShopProduct nesnesini başlatmaya çalışıyorum. Kurucuya üç dize iletiyorum, ancak sonuncu da gerekli değişken yerine boş bir dizi ileterek başarısız oluyorum. Tip bildirimleri sayesinde, PHP bundan kurtulmama izin vermiyor:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TypeError: popp\ch03\batch09\ShopProduct:: construct(): Argument #4
($price) must be of type float, array given, called in...
</code></pre></div></div>

<p>Varsayılan olarak PHP, mümkün olduğunda, parametreleri gerekli tipe dolaylı olarak dönüştürür. Bu, daha önce karşılaştığımız güvenlik ve esneklik arasındaki ikileme bir örnektir. Örneğin, ShopProduct sınıfının yeni uygulaması, bizim için sessizce string’i float’a çevirecek. Dolayısıyla, bu örnekleme başarısız olmaz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product = new ShopProduct("title", "first", "main", "4.22");
</code></pre></div></div>

<p>Perde arkasında, string “4.22” float 4.22 olur. Şimdiye kadar, çok faydalı görünüyor. Ancak, AddressManager sınıfında karşılaştığımız sorunu tekrar düşünün. “false” dizesi sessizce true boolean’a çözümleniyordu. Varsayılan olarak, AddressManager::outputAddresses() metodunda şuna benzer bir bool tip bildirimi kullanırsam aynı şey olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function outputAddresses(bool $resolve)
{
    // ...
}
</code></pre></div></div>

<p>Şimdi şöyle bir metot çağrısını düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$manager-&gt;outputAddresses("false");
</code></pre></div></div>

<p>Örtülü atama nedeniyle, işlevsel olarak aynıdır. Yalnızca dosya bazında olmasına rağmen, skaler tip bildirimlerini katı hale geirebilirsiniz. Burada katı tip bildirimlerini açıyorum ve outputAddresses()’i bir kez daha string ile çağırıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>declare(strict_types=1);

$manager-&gt;outputAddresses("false");
</code></pre></div></div>

<p>Bu çağrı TypeError hatasına neden olur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TypeError: popp\ch03\batch09\AddressManager::outputAddresses(): Argument #1
($resolve) must be of type bool, string given, called in...
</code></pre></div></div>

<p><strong>Not</strong>: strict_types bildirimi, fonksiyonun veya metodun uygulandığı dosyaya değil, çağrının yapıldığı dosyaya uygulanır. Bu nedenle, katılığı uygulamak yazılımcıya bağlıdır.</p>

<p>İsteğe bağlı bir parametre yapmanız gerekebilir, ancak yine de onun türünü kısıtlayın. Bunu varsayılan değer atayarak yapabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ConfReader
{
    public function getValues(array $default = [])
    {
        $values = [];
        // do something to get values
        // merge the provided defaults (it will always be an array)
        $values = array_merge($default, $values);
        return $values;
    }
}
</code></pre></div></div>

<h3 id="mixed-tipler">Mixed Tipler</h3>

<p>PHP 8.0 ile tanıtılan mixed tip bildirimi, sentetik şeker olarak görülebilir, yani kendi başına pek bir şey yapmaz. Şunlar arasında işlevsel bir fark yoktur:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, $value)
    {
        // do something with $key and $value
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, mixed $value)
    {
        // do something with $key and $value
    }
}
</code></pre></div></div>

<p>İkinci versiyonda, add() için $value parametresinin mixed, başka bir deyişle array, bool, callable, int, float, null, object, resource veya string herhangi bir türü kabul edeceğini bildirdim. Bu nedenle, mixed $value bildirmek, parametre listesinde tip bildirimi olmadan $value kullanmakla aynıdır. Öyleyse neden mixed bildirimle uğraşıyorsunuz? Özünde, parametrenin kasıtlı olarak herhangi bir değeri kabul ettiğini beyan ediyorsunuz. Çıplak bir parametre, herhangi bir değeri kabul etmeye yönelik olabilir - veya yazılımcı tembel olduğu için tip bildirimi olmadan bırakılmış olabilir. Mixed şüphe ve belirsizliği ortadan kaldırır ve bu nedenle yararlıdır.</p>

<p>Özetlemek gerekirse, Tablo 3-3’te PHP tarafından desteklenen tip bildirimlerini listeliyorum.</p>

<p><strong>Tablo 3-3</strong>. Tip Bildirimleri</p>

<table>
  <thead>
    <tr>
      <th>Tip Bildirimi</th>
      <th>PHP Versiyon</th>
      <th>Tanım</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>array</td>
      <td>5.1</td>
      <td>Varsayılan olarak null veya bir dizi olabilir</td>
    </tr>
    <tr>
      <td>int</td>
      <td>7.0</td>
      <td>Varsayılan olarak null veya bir tamsayı olabilir</td>
    </tr>
    <tr>
      <td>float</td>
      <td>7.0</td>
      <td>Kayan noktalı sayı. Katı mod etkinleştirilmiş olsa bile tamsayı kabul edilecektir. Varsayılan olarak null, float veya integer olabilir</td>
    </tr>
    <tr>
      <td>callable</td>
      <td>5.4</td>
      <td>Çağrılabilir kod (anonim bir fonksiyon gibi). Varsayılan olarak null olabilir</td>
    </tr>
    <tr>
      <td>bool</td>
      <td>7.0</td>
      <td>Varsayılan olarak null veya boolean olabilir</td>
    </tr>
    <tr>
      <td>string</td>
      <td>5.0</td>
      <td>Varsayılan olarak null veya dize olabilir</td>
    </tr>
    <tr>
      <td>self</td>
      <td>5.0</td>
      <td>İçinde bulunduğu sınıfa başvuru</td>
    </tr>
    <tr>
      <td>[a class type]</td>
      <td>5.0</td>
      <td>Sınıf veya arayüz tipi. Varsayılan olarak null olabilir</td>
    </tr>
    <tr>
      <td>iterable</td>
      <td>7.1</td>
      <td>foreach ile gezilebilir (dizi olması gerekmez - Traversable uygulanabilir)</td>
    </tr>
    <tr>
      <td>object</td>
      <td>7.2</td>
      <td>Nesne</td>
    </tr>
    <tr>
      <td>mixed</td>
      <td>8.0</td>
      <td>Değerin herhangi bir türde olabileceğine dair açık bildirim</td>
    </tr>
  </tbody>
</table>

<h3 id="birleşik-union-tipler">Birleşik (Union) Tipler</h3>

<p>Mixed ile tip bildirimlerinin göreli katılığı arasında oldukça büyük bir uçurum vardır. Bir parametreyi iki, üç veya daha fazla tiple sınırlamanız gerekirse ne yaparsınız? PHP 8’e kadar, bunu başarabilmenin tek yolu, metot gövdesi içinde türü test etmekti. Storage sınıfına yeni bir gereksinimle dönelim. add(), yalnızca string veya boolean değer kabul etmelidir. İşte metot gövdesi içinde tipi kontrol eden bir uygulama:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, $value)
    {
        if (! is_bool($value) &amp;&amp; ! is_string($value)) {
            error_log("value must be string or Boolean - given: " .
            gettype($value));
            return false;
        }
        // do something with $key and $value
    }
}
</code></pre></div></div>

<p><strong>Not</strong>: Aslında, false döndürmek yerine büyük olasılıkla exception fırlatırdık. Bölüm 4’te istisnalar hakkında daha fazla bilgi edinebilirsiniz.</p>

<p>Bu manuel kontrol işimizi görsede, hantal ve okunması zor. Şanslıyız ki, PHP 8 yeni bir özellik getirdi: birleşik tip bildirimi yapmak için pipe sembolüyle ayrılmış iki veya daha fazla tipi birleştirmenize izin veren union type. İşte benim Storage’i yeniden uygulamam:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, string|bool $value)
    {
        // do something with $key and $value
    }
}
</code></pre></div></div>

<p>Şimdi $value parametresine float veya boolean dışında herhangi bir şey atamaya çalışırsam, artık tanıdık bir TypeError alacağım.</p>

<p>add() metodunu biraz daha toleranslı yapmak istersem, null değerine izin vermek için birleşik tip kullanabilirim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, string|bool|null $value)
    {
        // do something with $key and $value
    }
}
</code></pre></div></div>

<p>Birleşik tip bildirimleri, nesne tipi bildirimleriyle aynı şekilde çalışacaktır. Bu örnek, ShopProduct tipinde bir nesneyi veya null değeri kabul eder:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function setShopProduct(ShopProduct|null $product)
{
    // do something with $product
}
</code></pre></div></div>

<p>Birçok metot false’ı alternatif bir değer olarak kabul ettiğinden veya döndürdüğünden, PHP 8 birleşik tipler bağlamında false sözde türünü destekler. Dolayısıyla, bu örnekte, ShopProduct nesnesini veya false’ı kabul edeceğim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function setShopProduct2(ShopProduct|false $product)
{
    // do something with $product
}
</code></pre></div></div>

<p>Bu, <code class="language-plaintext highlighter-rouge">ShopProduct|bool</code> birleşiminden daha kullanışlı çünkü hiçbir senaryoda true kabul etmek istemiyorum.</p>

<p><strong>Not</strong>: Birleşik tipler PHP 8’de eklenmiştir.</p>

<h3 id="null-yapılabilir-tipler">Null Yapılabilir Tipler</h3>

<p>Birleşik tipin null değerini iki seçenekten biri olarak kabul ettiği durumlarda, kullanabileceğiniz eşdeğer bir parametre vardır. Null yapılabilir tip, soru işaretiyle başlayan bir tip bildiriminden oluşur. Storage’in bu sürümü string veya null kabul eder:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Storage
{
    public function add(string $key, ?string $value)
    {
        // do something with $key and $value
    }
}
</code></pre></div></div>

<p>Sınıf tipi bildirimlerini açıkladığımda, türlerin ve sınıfların eşanlamlı olduğunu ima ettim. Ancak ikisi arasında önemli bir fark vardır. Bir sınıf tanımladığınızda, tür de tanımlarsınız, ancak bir tür, tüm sınıf ailesini tanımlayabilir. Farklı sınıfların bir tür altında gruplandırılabileceği mekanizmaya kalıtım denir. Kalıtımı bir sonraki bölümde ele alacağım.</p>

<h2 id="dönüş-tipi-bildirimi">Dönüş Tipi Bildirimi</h2>

<p>Bir parametrenin tipini bildirebileceğimiz gibi, metotlarımızın döndürdüğü tipleri sınırlamak için dönüş tipi bildirimlerini kullanabiliriz. Doğrudan bir metodun veya fonksiyonun kapanış parantezinden sonra iki nokta üst üste eklenir ve onu dönüş tipi bildirimi takip eder. Dönüş tipi bildirilirken, parametre tiplerinde olduğu gibi aynı türler desteklenir. Burada getPlayLength() dönüş tipini kısıtlıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getPlayLength(): int
{
    return $this-&gt;playLength;
}
</code></pre></div></div>

<p>Bu metot çağrıldığında tamsayı değeri döndüremezse, PHP bir hata üretir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TypeError: popp\ch03\batch15\CdProduct::getPlayLength(): Return value must be of type int, none returned
</code></pre></div></div>

<p>Dönüş değeri bu şekilde zorlandığından, metodu çağıran herhangi bir kod, dönüş değerinin tamsayı olmasına güvenebilir.</p>

<p>Dönüş tipi bildirimleri, null yapılabilir ve birleşik türleri destekler:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getPrice(): int|float
{
    return ($this-&gt;price - $this-&gt;discount);
}
</code></pre></div></div>

<p>PHP 8’den itibaren desteklenen void dönüş tipi bildirimi ile bir metodun asla değer döndürmeyeceğini beyan edebilirsiniz. Örneğin, setDiscount() metodu bir değer sağlamak yerine set etmek için tasarlandığından, burada void dönüş tipi bildirimini kullanıyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function setDiscount(int|float $num): void
{
    $this-&gt;discount = $num;
}
</code></pre></div></div>

<h2 id="kalıtım">Kalıtım</h2>

<p>Kalıtım, temel bir sınıftan bir veya daha fazla sınıfın türetilebileceği anlamına gelir.</p>

<p>Bir başka sınıftan miras alan sınıfın onun alt sınıfı (subclass) olduğu söylenir. Bu ilişki genellikle ebeveynler ve çocuklar açısından tanımlanır. Bir alt sınıf (child), ebeveynden (parent) türetilir ve özelliklerini miras alır. Bu özellikler property ve metotlardan oluşur. Alt sınıf, tipik olarak ebeveyni (superclass olarak ta bilinir) tarafından sağlananlara yeni işlevler ekler; bu nedenle alt sınıfın ebeveynini genişlettiği söylenir.</p>

<p>Kalıtımın söz dizimine girmeden önce, onun çözmenize yardımcı olabileceği sorunları inceleyeceğim.</p>

<h3 id="kalıtım-problemi">Kalıtım Problemi</h3>

<p>ShopProduct sınıfına tekrar bakalım. Şu anda, oldukça genel. Her türlü ürün için kullanılabilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product1 = new ShopProduct("My Antonia", "Willa", "Cather", 5.99);
$product2 = new ShopProduct(
    "Exile on Coldharbour Lane", "The",
    "Alabama 3",
    10.99
);
print "author: " . $product1-&gt;getProducer() . "\n";
print "artist: " . $product2-&gt;getProducer() . "\n";

// author: Willa Cather
// artist: The Alabama 3
</code></pre></div></div>

<p>Yapımcı adını ikiye ayırmak hem kitaplar hem de CD’ler için uygundur. “The” ve “Willa”ya göre değil, “Alabama 3” ve “Cather”e göre sıralama yapabilmek istiyorum. Tembellik mükemmel bir tasarım stratejisidir, dolayısıyla bu aşamada ShopProduct’ın birden fazla ürün türü için kullanma konusunda endişelenmenize gerek yoktur.</p>

<p>Bununla birlikte, örneğime bazı yeni gereksinimler eklersem, işler hızla daha karmaşık hale gelebilir. Örneğin, kitaplara ve CD’lere özgü verileri temsil etmeniz gerektiğini düşünün. CD’ler için toplam çalma süresini kaydetmelisiniz; kitaplar için toplam sayfa sayısı. Daha başka farklar da olabilir, ancak bu, konuyu açıklamaya hizmet edecektir.</p>

<p>Bu değişiklikleri karşılamak için örneğimi nasıl genişletebilirim? İki seçenek hemen kendini gösterir. İlk olarak, tüm verileri ShopProduct sınıfına atabilirim. İkincisi, ShopProduct’ı iki ayrı sınıfa ayırabilirim.</p>

<p>İlk yaklaşımı inceleyelim. Burada, CD ve kitapla ilgili verileri tek bir sınıfta birleştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $numPages;
    public $playLength;
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $numPages = 0,
        int $playLength = 0
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
        $this-&gt;numPages = $numPages;
        $this-&gt;playLength = $playLength;
    }

    public function getNumberOfPages(): int
    {
        return $this-&gt;numPages;
    }

    public function getPlayLength(): int
    {
        return $this-&gt;playLength;
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<p>$numPages ve $playLength property’lerine metot erişimi sağladım. Bu sınıftan örneklenen nesne, fazladan bir metot içerecektir ve bir CD için, gereksiz bir kurucu metot parametresi kullanılarak örneklendirilmelidir: CD, kitap sayfalarıyla ilgili bilgileri depolayacak ve kitap oynatma uzunluğu verisini destekleyecektir. Bu muhtemelen şu anda birlikte yaşayabileceğiniz bir şey. Ancak, her biri kendine ait metotları olan ürün türleri eklemeye devam edersem ne olur? Sınıfımız giderek daha karmaşık ve yönetilmesi zor hale gelir.</p>

<p>Bu nedenle, tek bir sınıfa ait olmayan alanları zorlamak, gereksiz property ve metotlarla şişirilmiş nesnelere yol açar.</p>

<p>Sorun verilerle de bitmiyor. İşlevsellik konusunda da zorluklarla karşılaşıyorum. Ürünü özetleyen bir metot düşünün. Satış departmanı, faturalarda kullanılmak üzere net bir özet satırı talep etti. CD’ler için çalma süresi ve kitaplar için sayfa sayısı eklememi istiyorlar, bu yüzden her tür için farklı uygulamalar sağlamak zorunda kalacağım. Nesnenin formatını takip etmek için flag kullanmayı deneyebilirim.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getSummaryLine(): string
{
    $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
    $base .= "{$this-&gt;producerFirstName} )";

    if ($this-&gt;type == 'book') {
        $base .= ": page count - {$this-&gt;numPages}";
    } elseif ($this-&gt;type == 'cd') {
        $base .= ": playing time - {$this-&gt;playLength}";
    }

    return $base;
}
</code></pre></div></div>

<p>$type’ı ayarlamak için, $numPages parametresini kurucu metodta test edebilirim. Yine de ShopProduct sınıfı bir kez daha gereğinden fazla karmaşık hale geldi. Daha fazla farklılık  veya yeni formatlar eklendikçe, bu işlevsel farklılıkları yönetmek daha da zorlaşacak. Belki de bu sorun için başka bir yaklaşım denemeliyim.</p>

<p>ShopProduct bir arada iki sınıf gibi hissettirmeye başladığından, iki farklı tür oluşturabilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class CdProduct
{
    public $playLength;
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $playLength
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
        $this-&gt;playLength = $playLength;
    }

    public function getPlayLength(): int
    {
        return $this-&gt;playLength;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": playing time - {$this-&gt;playLength}";
        return $base;
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class BookProduct
{
    public $numPages;
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $numPages
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
        $this-&gt;numPages = $numPages;
    }

    public function getNumberOfPages(): int
    {
        return $this-&gt;numPages;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": page count - {$this-&gt;numPages}";
        return $base;
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }
}
</code></pre></div></div>

<p>Karmaşıklık sorununu ele aldım, ancak bunun bir bedeli var. Artık flag kullanmadan her format için bir getSummaryLine() metodu oluşturabilirim. Hiçbir sınıf, kendisiyle ilgili olmayan alanları veya metotları tutmaz.</p>

<p>Maliyet çoğaltmada yatmaktadır. getProducerName() metodu her sınıfta tamamen aynıdır. Her kurucu metot, aynı şekilde bir dizi özdeş property ayarlar.</p>

<p>GetProducer() metodunun her sınıf için aynı şekilde davranması gerekiyorsa, bir uygulamada yaptığım herhangi bir değişikliği diğeri içinde yapmam gerekir. Umursamazsanız, sınıflar kısa sürede senkronizasyondan çıkar.</p>

<p>Yinelemeyi sürdürebileceğimden emin olsam bile endişelerim bitmedi. Artık bir yerine iki türüm var.</p>

<p>ShopProductWriter sınıfını hatırlıyor musunuz? write() metodu, tek bir türle çalışacak şekilde tasarlanmıştır: ShopProduct. Bunu eskisi gibi çalışacak şekilde değiştirebilirim? Sınıf türü bildirimini metot imzasından kaldırabilirim, ancak o zaman doğru türde nesne iletildiği konusunda şansa güvenmeliyim. Metodun gövdesine kendi tip kontrol kodumu ekleyebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProductWriter
{
    public function write($shopProduct): void
    {
        if (
            ! ($shopProduct instanceof CdProduct) &amp;&amp;
            ! ($shopProduct instanceof BookProduct)
        ){
            die("wrong type supplied"); 
        }
        $str  = "{$shopProduct-&gt;title}: "
            . $shopProduct-&gt;getProducer()
            . " ({$shopProduct-&gt;price})\n";
        print $str;
    }
}
</code></pre></div></div>

<p>Örnekteki instanceof operatörüne dikkat edin; sol işlenendeki nesne sağ işlenen tarafından temsil edilen türdeyse, instanceof true olarak çözümlenir.</p>

<p>Bir kez daha, karmaşıklığı arttırmak zorunda kaldım. $shopProduct parametresini iki tür için test etmem ve her türün diğeriyle aynı alanları ve metotları desteklemeye devam edeceğine güvenmem gerekiyor. Sınıf tipi bildirimi kullandığımda ve tek bir tür talep ettiğimde - ShopProduct sınıfının belirli bir arabirimi desteklediğinden emin olabileceğim için - her şey çok daha düzenliydi.</p>

<p>ShopProduct sınıfı CD ve kitap yönleriyle birlikte iyi çalışmıyor ama ayrı da yaşayamıyor gibi görünüyor. Her format için ayrı bir uygulama sağlarken, kitap ve CD’leri tek bir tür olarak tanımlamak istiyorum. Tekrarı önlemek için ortak bir işlevsellik sağlamalıyım, ancak her formatın bazı metot çağrılarını farklı şekilde işlemesine izin vermek istiyorum. Kalıtım kullanmam gerekiyor.</p>

<h3 id="kalıtımla-çalışmak">Kalıtımla Çalışmak</h3>

<p>Bir kalıtım ağacı oluşturmanın ilk adımı, temel sınıfın birbirine uymayan veya farklı şekilde ele alınması gereken öğelerini bulmaktır. getPlayLength() ve getNumberOfPages() metotlarının birbirine ait olmadığını biliyorum. Ayrıca getSummaryLine() metodu için farklı uygulamalar oluşturmam gerektiğini de biliyorum. Bu farklılıkları türetilmiş iki sınıf için temel olarak kullanalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $numPages;
    public $playLength;
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $numPages = 0,
        int $playLength = 0
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
        $this-&gt;numPages = $numPages;
        $this-&gt;playLength = $playLength;
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        return $base;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class CdProduct extends ShopProduct
{
    public function getPlayLength(): int
    {
        return $this-&gt;playLength;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": playing time - {$this-&gt;playLength}";
        return $base;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class BookProduct extends ShopProduct
{
    public function getNumberOfPages(): int
    {
        return $this-&gt;numPages;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": page count - {$this-&gt;numPages}";
        return $base;
    }
}
</code></pre></div></div>

<p>Alt sınıf oluşturmak için, sınıf bildiriminde extends anahtar sözcüğünü kullanmalısınız. Örnekte, BookProduct ve CdProduct olmak üzere iki yeni sınıf oluşturdum. Her ikisi de ShopProduct sınıfını genişletir.</p>

<p>Türetilmiş sınıflarda kurucu metot tanımlamadığından, üst sınıfın kurucusu otomatik olarak çağrılır. Alt sınıflar, ebeveynin tüm public ve protected metotlarına erişebilir (ancak private metot veya property’lere erişemezler). Bu, ShopProduct sınıfında getProducer() tanımlanmış olsa bile, CdProduct sınıfından oluşturulan bir nesnede getProducer() yöntemini çağırabileceğiniz anlamına gelir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$product2 = new CdProduct(
    "Exile on Coldharbour Lane",
    "The",
    "Alabama 3",
    10.99,
    0,
    60.33
);

print "artist: {$product2-&gt;getProducer()}\n";
</code></pre></div></div>

<p>Böylece her iki çocuk sınıf, ebeveynin davranışlarını miras alır. BookProduct nesnesine ShopProduct nesnesiymiş gibi davranabilirsiniz. BookProduct veya CdProduct nesnesini ShopProductWriter sınıfının write() metoduna iletebilirsiniz ve her şey beklendiği gibi çalışacaktır.</p>

<p>CdProduct ve BookProduct sınıflarının getSummaryLine() metodunu geçersiz kılarak kendi uygulamalarını sağladığına dikkat edin. Türetilmiş sınıflar, ebeveynlerinin işlevlerini genişletebilir ama aynı zamanda değiştirebilir.</p>

<p>Üst sınıfın bu metodu uygulaması, alt sınıflar tarafından geçersiz kılındığı için gereksiz görünebilir. Bununla birlikte, yeni alt sınıfların kullanabileceği temel işlevleri sağlar. Metodun varlığı aynı zamanda, tüm ShopProduct nesnelerinin getSummaryLine() metodunu sağlayacağına dair bir garanti verir. Daha sonra, herhangi bir uygulama olmadan bunu temel sınıfta yapmanın nasıl mümkün olduğunu göreceksiniz. Her alt ShopProduct sınıfı, ebeveyninin özelliklerini devralır. Hem BookProduct hem de CdProduct, getSummaryLine() versiyonlarında $title property’sine erişir.</p>

<p>Kalıtım, ilk başta anlaşılması zor bir kavram olabilir. Diğerini genişleten bir sınıf tanımlayarak, ondan örneklenen nesnenin önce alt sınıfın, sonra üst sınıfın özellikleri tarafından tanımlanmasını sağlarsınız. Bunu düşünmenin başka bir yolu da aramadır. $product2-&gt;getProducer()’ı çağırdığımda, CdProduct sınıfında böyle bir metot olmadığı için metot çağrısı, ShopProduct’taki varsayılan uygulamaya düşüyor. $product2-&gt;getSummaryLine()’ı çağırdığımda ise, getSummaryLine() metodu CdProduct’ta bulunuyor ve çağrılıyor.</p>

<p>Aynı şey property erişimleri için de geçerlidir. BookProduct sınıfının getSummaryLine() metodunda $title’a eriştiğimde, property BookProduct sınıfında bulunamadı. Bunun yerine üst sınıftan, ShopProduct’tan alınır. $title her iki alt sınıfa da uygulanır ve bu nedenle üst sınıfa aittir.</p>

<p>Bununla birlikte, ShopProduct kurucu metoduna baktığımızda, alt sınıflar tarafından işlenmesi gereken verileri yönetmeye devam ettiğini gösteriyor. BookProduct sınıfı, $numPages parametre ve property’sini, CdProduct sınıfı ise $playLength parametre ve property’sini işlemelidir. Bunun işe yaraması için, alt sınıfların her birinde kurucu metotlar tanımlayacağım.</p>

<p><strong>Kurucular ve Kalıtım</strong></p>

<p>Bir alt sınıfta kurucu metot tanımladığınızda, üst sınıfa herhangi bir argümanı iletmekten sorumlu olursunuz. Bunu başaramazsanız, kısmen inşa edilmiş bir nesne elde edebilirsiniz.</p>

<p>Üst sınıftan bir metodu çağırmak için, önce sınıfın kendisine atıfta bulunmanın bir yolunu bulmalısınız. PHP bu amaç için bize parent anahtar sözcüğünü sağlar.</p>

<p>Nesne yerine sınıf bağlamında bir metoda başvurmak için -&gt; yerine :: kullanırsınız:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>parent::__construct()
</code></pre></div></div>

<p>Önceki kod parçası, “Ana sınıfın __construct() metodunu çağır” anlamına gelir. Burada, örneğimi her sınıfın yalnızca kendisine uygun olan verileri işlemesi için değiştiriyorum:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    public $title;
    public $producerMainName;
    public $producerFirstName;
    public $price;

    public function __construct(
        $title,
        $firstName,
        $mainName,
        $price
    ) {
        $this-&gt;title = $title;
        $this-&gt;producerFirstName = $firstName;
        $this-&gt;producerMainName = $mainName;
        $this-&gt;price = $price;
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )"; return $base;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class BookProduct extends ShopProduct
{
    public $numPages;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $numPages
    ) {
        parent:: __construct(
            $title,
            $firstName,
            $mainName,
            $price
        );
        $this-&gt;numPages = $numPages;
    }

    public function getNumberOfPages(): int
    {
        return $this-&gt;numPages;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( $this-&gt;producerMainName, ";
        $base .= "$this-&gt;producerFirstName )";
        $base .= ": page count - {$this-&gt;numPages}";
        return $base;
    }
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class CdProduct extends ShopProduct
{
    public $playLength;

    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        float $price,
        int $playLength
    ) {
        parent:: __construct(
            $title,
            $firstName,
            $mainName,
            $price
        );
        $this-&gt;playLength = $playLength;
    }

    public function getPlayLength(): int
    {
        return $this-&gt;playLength;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": playing time - {$this-&gt;playLength}";
        return $base;
    }
}
</code></pre></div></div>

<p>Her alt sınıf, kendi property’lerini ayarlamadan önce üst sınıfın kurucusunu çağırır. Temel sınıf artık yalnızca kendi verilerini tutuyor. Genel bir kural olarak, üst sınıflara alt sınıfları hakkında herhangi bir özel bilgi vermekten kaçınmalısınız.</p>

<p><strong>Not</strong>: PHP 5’ten önce, kurucular çevreleyen sınıfın adını alırdı. Yeni kurucular __construct() adını kullanır. Eski sözdizimini kullanarak, üst sınıfın kurucusuna yapılan çağrı sizi söz konusu sınıfa bağlar: parent::ShopProduct();. Eski kurucu sözdizimi PHP 7.0’da deprecated oldu ve PHP 8’de tamamen kaldırıldı.</p>

<p><strong>Geçersiz Kılınan Metodu Çalıştırmak</strong></p>

<p>parent anahtar sözcüğü, bir üst sınıftaki eşdeğerini geçersiz kılan herhangi bir metotla kullanılabilir. Bir metodu geçersiz kıldığınızda, üst sınıfın işlevselliğini ortadan kaldırmak yerine onu genişletmek isteyebilirsiniz. Bunu, geçerli nesne bağlamında üst sınıfın metodunu çağırarak başarabilirsiniz. getSummaryLine() metoduna tekrar bakarsanız, birçok kodun kopyalandığını göreceksiniz. ShopProduct sınıfında halihazırda geliştirilen işlevselliği yeniden oluşturmak yerine kullanmak daha iyi olacaktır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getSummaryLine(): string
{
    $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
    $base .= "{$this-&gt;producerFirstName} )";
    return $base;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getSummaryLine(): string
{
    $base = parent::getSummaryLine();
    $base .= ": page count - $this-&gt;numPages";
    return $base;
}
</code></pre></div></div>

<p>ShopProduct üst sınıfında getSummaryLine() metodu için temel işlevselliği oluşturdum.</p>

<p>Bunu CdProduct ve BookProduct alt sınıflarında yeniden oluşturmak yerine, özet dizesine daha fazla veri eklemeye devam etmeden önce ana metodu çağırıyorum.</p>

<p>Artık kalıtımın temellerini gördüğünüze göre, property ve metot görünürlüğünü resmin tamamı ışığında yeniden inceleyeceğim.</p>

<h3 id="public-private-and-protected-sınıflarınıza-erişimi-yönetmek">Public, Private, and Protected: Sınıflarınıza Erişimi Yönetmek</h3>

<p>Şimdiye kadar, tüm property’leri public olarak tanımladım. Property bildiriminizde eski var anahtar sözcüğünü kullandıysanız, metodlar ve property’ler için genel erişim varsayılan ayardı.</p>

<p><strong>Not</strong>: var, PHP 5’te kullanımdan kaldırılmıştır ve muhtemelen gelecekte dilden tamamen kaldırılacaktır.</p>

<p>Gördüğümüz gibi, sınıflarınızdaki öğeler public, private veya protected olarak bildirilebilir:</p>

<ul>
  <li>Public property ve metotlara herhangi bir bağlamdan erişilebilir.</li>
  <li>Private property veya metoda yalnızca çevreleyen sınıf içinden erişilebilir. Alt sınıfların bile erişimi yoktur.</li>
  <li>Protected property veya metoda yalnızca çevreleyen sınıf içinden veya bir alt sınıftan erişilebilir. Hiçbir harici koda erişim izni verilmez.</li>
</ul>

<p>Peki bu bizim için ne kadar faydalı? Görünürlük anahtar sözcükleri, bir sınıfın yalnızca yazılımcı tarafından gerekli görülen yönlerini açığa çıkarmanıza olanak tanır. Bu, nesneniz için net bir arayüz sağlar.</p>

<p>Erişim denetimi, yazılımcının belirli özelliklere erişmesini engelleyerek kodunuzdaki hataların önlenmesine de yardımcı olabilir. Örneğin, ShopProduct nesnelerinin indirimi desteklemesine izin vermek istediğinizi düşünün. $discount property’si ve setDiscount() metodu ekleyebilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// ShopProduct class
public $discount = 0;
//...
public function setDiscount(int $num): void
{
    $this-&gt;discount = $num;
}
</code></pre></div></div>

<p>Uygulanan indirimi dikkate alan bir getPrice() metodu oluşturabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public function getPrice(): int|float
{
    return ($this-&gt;price - $this-&gt;discount);
}
</code></pre></div></div>

<p>Bu noktada bir sorunumuz var. Yalnızca ayarlanmış fiyatı göstermek istiyorsunuz, ancak bir yazılımcı getPrice() metodunu kolayca atlayabilir ve $price özelliğine erişebilir:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print "The price is {$product1-&gt;price}\n";
</code></pre></div></div>

<p>Bu, sunmak istediğiniz iskontoya göre düzeltilmiş fiyatı değil, ham fiyatı yazdıracaktır. $price property’sini private yaparak buna hemen bir son verebilirsiniz. Bu, yazılımcıları getPrice() metodunu kullanmaya zorlayarak doğrudan erişimi engelleyecektir. ShopProduct sınıfının dışından $price property’sine erişme girişimleri başarısız olur. Daha geniş dünya söz konusu olduğunda, bu property’nin varlığı sona ermiştir.</p>

<p>Property’leri private olarak ayarlamak aşırı hevesli bir strateji olabilir. Private bir property’ye alt sınıf tarafından erişilemez. İş kurallarımızın yalnızca kitapların indirim için uygun olmaması gerektiğini belirttiğini hayal edin. GetPrice() metodunu, indirim uygulamadan $price property’sini döndürmesi için geçersiz kılabilirsiniz:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// BookProduct
public function getPrice(): int|float
{
    return $this-&gt;price;
}
</code></pre></div></div>

<p>Private $price property’si BookProduct’ta değil ShopProduct sınıfında bildirildiğinden, ona buradan erişme girişimi başarısız olur. Bu sorunun çözümü, $price değişkenini protected olarak bildirmek ve böylece alt sınıflara erişim sağlamaktır. Protected property veya metot, bildirildiği sınıf hiyerarşisinin dışından erişilemez. Yalnızca kaynak sınıfından veya kaynak sınıfın alt öğelerinden erişilebilir.</p>

<p>İlk başta property’leri private veya protected hale getirin ve kısıtlamanızı yalnızca gerektiğinde gevşetin. Sınıflarınızdaki çoğu (çoğu değilse de büyük kısmı) metot public olacak, ancak şüpheniz varsa bir kez daha kilitleyin. Sınıfınızdaki diğer metotlar için yerel işlevsellik sağlayan bir metodun, sınıfınızın kullanıcıları ile hiçbir ilgisi yoktur. Private veya protected yapın.</p>

<p><strong>Erişim (Accessor) Metotları</strong></p>

<p>Yazılımcıların sınıfınız tarafından tutulan değerlerle çalışması gerektiğinde, property’lere doğrudan erişimi reddetmek, bunun yerine gerekli değerleri aktaran metotlar sağlamak genellikle iyi bir fikirdir. Bu tür metotlar accessor veya getter ve setter olarak bilinir.</p>

<p>Erişim metotlarının sağladığı bir avantajı zaten gördünüz. getPrice() metodunda gösterildiği gibi, property değerini koşullara göre filtrelemek için accessor kullanabilirsiniz.</p>

<p>Property türünü zorlamak için setter kullanabilirsiniz. Tip bildirimleri, metot parametrelerini kısıtlamak için kullanılabilir, ancak property herhangi bir türde veri içerebilir. Liste verilerini çıkarmak için ShopProduct nesnesini kullanan ShopProductWriter sınıfını hatırlıyor musunuz? Bunu, bir defada herhangi bir sayıda ShopProduct nesnesini yazabilecek şekilde geliştirebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProductWriter
{
    public $products = [];

    public function addProduct(ShopProduct $shopProduct): void
    {
        $this-&gt;products[] = $shopProduct;
    }

    public function write(): void
    {
        $str = "";
        foreach ($this-&gt;products as $shopProduct) {
            $str .= "{$shopProduct-&gt;title}: ";
            $str .= $shopProduct-&gt;getProducer();
            $str .= " ({$shopProduct-&gt;getPrice()})\n";
        }
        print $str;
    }
}
</code></pre></div></div>

<p>ShopProductWriter sınıfı artık çok daha kullanışlı. Birçok ShopProduct nesnesini tutabilir ve hepsi için tek seferde veri yazabilir. Yine de, yazılımcılara sınıfı amacına uygun olarak kullanmaları konusunda güvenmeliyim. addProduct() metodunu sağlamış olmama rağmen, yazılımcıların $products property’sini doğrudan manipüle etmesini engellemedim. Birisi $products property’sine yanlış türde nesne eklemekle kalmaz, aynı zamanda tüm dizinin üzerine yazıp onu ilkel bir veri ile değiştirebilir. $products property’sini private yaparak bunu önleyebilirim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProductWriter
{
    private $products = [];
    //...
</code></pre></div></div>

<p>Harici kodun $products property’sine zarar vermesi artık imkansız. Tüm erişim addProduct() metodu aracılığıyla olmalıdır ve metot bildiriminde kullandığım sınıf tipi bildirimi, array property’sine yalnızca ShopProduct nesnelerinin eklenebilmesini sağlar.</p>

<h3 id="property-tipleri">Property Tipleri</h3>

<p>Böylece, metot imzalarındaki tip bildirimlerini property görünürlük bildirimleriyle birleştirerek, sınıflarınızdaki property tiplerini kontrol edebilirsiniz. İşte başka bir örnek: property tiplerini yönetmek için tip bildirimlerini ve property görünürlüğünü kullandığım Point sınıfı:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Point
{
    private $x = 0;
    private $y = 0;

    public function setVals(int $x, int $y)
    {
        $this-&gt;x = $x;
        $this-&gt;y = $y;
    }

    public function getX(): int
    {
        return $this-&gt;x;
    }

    public function getY(): int
    {
        return $this-&gt;y;
    }
}
</code></pre></div></div>

<p>$x ve $y property’leri private olduğundan, yalnızca setVals() metoduyla ayarlanabilirler ve setVals() yalnızca integer değerleri kabul edeceğinden, $x ve $y’nin her zaman tamsayı içerdiğinden emin olabilirsiniz.</p>

<p>Tabii ki, bu property’ler private olarak ayarlandığından, bunlara erişmenin tek yolu getter veya accessor metotlardır.</p>

<p>Property tiplerini tanıtan PHP sürüm 7.4’e kadar, property tiplerini sabitlemek için bu yönteme takılıp kalmıştık. Bu, property’leri için tip bildirmemizi sağlar. İşte bundan yararlanan Point sürümü:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Point
{
    public int $x = 0;
    public int $y = 0;
}
</code></pre></div></div>

<p>$x ve $y property’lerini public yaptım ve türlerini kısıtlamak için tip bildirimi kullandım. Bu nedenle, istersem kontrolden ödün vermeden setVals() metodundan kurtulmayı seçebilirim. Ayrıca getX() ve getY() metotlarına da ihtiyacım yok. Point artık son derece basit bir sınıftır, ancak her iki property public olsa bile, tuttuğu veriler hakkında dünyaya garantiler sunar.</p>

<p>Bu property’lerden birine dize ayarlamaya çalışalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$point = new Point();
$point-&gt;x = "a";
</code></pre></div></div>

<p>PHP bundan kaçmamıza izin vermeyecek:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TypeError: Cannot assign string to property popp\ch03\batch11\Point::$x of type int
</code></pre></div></div>

<p><strong>Not</strong>: Birleşik tipler, property tip bildirimlerinde de kullanılabilir.</p>

<p><strong>ShopProduct Sınıfları</strong></p>

<p>Bu bölümü, ShopProduct sınıfı ve alt öğelerin erişim denetimini kilitleyerek ve ele aldığımız diğer özelliklerden bazılarını dahil edecek şekilde değiştirerek kapatalım:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class ShopProduct
{
    private int|float $discount = 0;

    public function __construct(
        private string $title,
        private string $producerFirstName,
        private string $producerMainName,
        protected int|float $price
    ) {
    }

    public function getProducerFirstName(): string
    {
        return $this-&gt;producerFirstName;
    }

    public function getProducerMainName(): string
    {
        return $this-&gt;producerMainName;
    }

    public function setDiscount(int|float $num): void
    {
        $this-&gt;discount = $num;
    }

        public function getDiscount(): int
    {
        return $this-&gt;discount;
    }

    public function getTitle(): string
    {
        return $this-&gt;title;
    }

    public function getPrice(): int|float
    {
        return ($this-&gt;price - $this-&gt;discount);
    }

    public function getProducer(): string
    {
        return $this-&gt;producerFirstName . " "
            . $this-&gt;producerMainName;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        return $base;
    }
}
</code></pre></div></div>

<p>Görünürlüklerini private (veya $discount durumunda protected) olarak ayarlayarak property’lerin çoğuna erişimi kapatmanın yanı sıra, property bildirimlerimi kurucu metot imzasıyla birleştirdim. Aynı zamanda PHP 8’in yeni tip birleşim özelliğini gösteren $discount için property tip bildirimi kullandım. Int veya float değer atanabilmesi için $discount’u kısıtladım. $discount private olarak bildirildiğinden ve setDiscount() metodundaki tip bildirimi aynı koşulu uygulayacağından, bu kısıtlama gereksiz görünebilir. Bununla birlikte, bir tür zorunlu satır içi belgeleme işlevi gördüğü için ve kısmen de ShopProduct’ın geliştirilmesi sırasında yanlışlıkla $discount ile istenmeyen şekilde oynamamızı önleyeceği için property tipi bildirmek iyi bir uygulamadır.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class CdProduct extends ShopProduct
{
    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        int|float $price,
        private int $playLength
    ) {
        parent:: __construct(
            $title,
            $firstName,
            $mainName,
            $price
        );
    }

    public function getPlayLength(): int
    {
        return $this-&gt;playLength;
    }

    public function getSummaryLine(): string
    {
        $base = "{$this-&gt;title} ( {$this-&gt;producerMainName}, ";
        $base .= "{$this-&gt;producerFirstName} )";
        $base .= ": playing time - {$this-&gt;playLength}";
        return $base;
    }
}
</code></pre></div></div>

<p>Yine property tip bildirimini kurucu metot imzası ile birleştiriyorum. Bu sefer, yalnızca bir parametre için: $playLength. Kurucu parametrelerinin geri kalanını üst sınıfa aktarıyorum ve onlar için görünürlük ayarlamıyorum. Bunun yerine kurucunun gövdesinde kullanıyorum.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class BookProduct extends ShopProduct
{
    public function __construct(
        string $title,
        string $firstName,
        string $mainName,
        int|float $price,
        private int $numPages
    ) {
        parent:: construct(
            $title,
            $firstName,
            $mainName,
            $price
        );
    }

    public function getNumberOfPages(): int
    {
        return $this-&gt;numPages;
    }

    public function getSummaryLine(): string
    {
        $base = parent::getSummaryLine();
        $base .= ": page count - $this-&gt;numPages";
        return $base;
    }

    public function getPrice(): int|float
    {
        return $this-&gt;price;
    }
}
</code></pre></div></div>

<p>ShopProduct ailesinin bu sürümünde tüm property’ler private veya protected olarak tanımlandı. İşleri tamamlamak için bir dizi erişim metodu ekledim.</p>

<p><strong>Özet</strong></p>

<p>Bu bölüm, bir sınıfı boş bir uygulamadan tüm özeliklere sahip kalıtım hiyerarşisine götüren birçok konuyu kapsıyordu. Özellikle tür ve kalıtımla ilgili bazı tasarım konularını ele aldınız. PHP’nin görünürlük desteğini gördünüz ve bazı kullanımlarını incelediniz. Bir sonraki bölümde, size PHP’nin nesne yönelimli özelliklerinden daha fazlasını göstereceğim.</p>

<p><strong>Kaynak:</strong> “PHP 8 Objects, Patterns, and Practice”, Matt Zandstra</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Nesneler ve sınıflar bu kitabın merkezindedir ve on yıl önce PHP 5’in piyasaya sürülmesinden bu yana PHP’nin de kalbinde yer alıyor. Bu bölümde, PHP’nin nesne yönelimli özelliklerini inceleyerek nesneleri ve tasarımı daha derinlemesine ele almak için temel oluşturuyorum. Nesne yönelimli programlamada yeniyseniz, bu bölümü dikkatlice okumalısınız.]]></summary></entry><entry><title type="html">PHP ve Nesneler</title><link href="http://localhost:4000/2024/03/20/php-ve-nesneler.html" rel="alternate" type="text/html" title="PHP ve Nesneler" /><published>2024-03-20T00:00:00+03:00</published><updated>2024-03-20T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/20/php-ve-nesneler</id><content type="html" xml:base="http://localhost:4000/2024/03/20/php-ve-nesneler.html"><![CDATA[<p>Nesneler her zaman PHP projesinin önemli bir parçası değildi. Aslında, bir zamanlar PHP tasarımcıları tarafından “sonradan düşünülmüş” olarak tanımlanmışlardı. Bu bölümde, PHP’nin nesne yönelimli özelliklerinin gelişimini özetleyerek tanıtıyorum.</p>

<p>Aşağıdakilere bakacağız:</p>

<ul>
  <li>PHP/FI 2.0: PHP, ama bildiğimiz gibi değil.</li>
  <li>PHP 3: Nesneler ilk kez ortaya çıkıyor.</li>
  <li>PHP 4: Nesne yönelimli programlama gelişir.</li>
  <li>PHP 5: Dilin merkezindeki nesneler.</li>
  <li>PHP 7: Boşluğu kapatmak.</li>
  <li>PHP 8: Konsolidasyon devam ediyor.</li>
</ul>

<h2 id="php-nesnelerinin-kazara-başarısı">PHP Nesnelerinin Kazara Başarısı</h2>

<p>PHP’nin kapsamlı nesne desteği ve dolaşımdaki çok sayıda nesne yönelimli PHP kütüphanesi ve uygulamasıyla, PHP’de nesnenin yükselişi doğal ve kaçınılmaz bir süreç gibi görünebilir. Doğrusu, hiçbir şey gerçeklerden daha uzak olamaz.</p>

<p><strong>Başlangıç: PHP/FI</strong></p>

<p>Bugün bildiğimiz PHP’nin doğuşu, Rasmus Lerdorf tarafından Perl kullanılarak geliştirilen iki araca dayanmaktadır. PHP, Personal Home Page Tools anlamına geliyordu. FI, Form Interpreter anlamına gelir. Birlikte, veritabanlarına, formlara ve akış kontrolüne SQL ifadeleri gönderen makrolardan oluşuyorlardı.</p>

<p>Bu araçlar C’de yeniden yazıldı ve PHP/FI 2.0 adı altında birleştirildi. Bu aşamadaki dil, bugün tanıdığımız sözdiziminden farklı görünüyordu ama o kadar da farklı değildi. Değişkenler, ilişkisel diziler ve fonksiyonlar için destek vardı. Ancak nesneler ufukta görünmüyordu.</p>

<p><strong>Sentetik Şeker: PHP 3</strong></p>

<p>Hatta PHP 3 daha planlama aşamasındayken bile konu gündem dışıydı. PHP 3’ün başlıca mimarları Zeev Suraski ve Andi Gutmans idi. PHP 3 tamamen yeniden yazıldı, ancak nesneler yeni sözdiziminin gerekli bir parçası olarak görülmedi.</p>

<p>Zeev Suraski’ye göre, sınıf desteği neredeyse sonradan düşünülerek eklendi (kesin olarak 27 Ağustos 1997’de). Sınıflar ve nesneler aslında ilişkisel dizileri tanımlamanın ve bunlara erişmenin başka bir yoluydu…</p>

<p>Elbette, metotların ve kalıtımın eklenmesi, sınıfları ilişkisel dizilerden çok daha fazlası haline getirdi, ancak yine de sınıflarla neler yapabileceğiniz konusunda ciddi sınırlamalar vardı. Özellikle, bir üst sınıfın geçersiz kılınan metotlarına erişemezsiniz (bunun ne anlama geldiğini henüz bilmiyorsanız merak etmeyin; daha sonra açıklayacağım). Bir sonraki bölümde inceleyeceğim bir diğer dezavantaj, PHP scriptlerinde nesnelerin aktarılmasının optimal olmayan yoluydu.</p>

<p>Nesnelerin o zamanlar marjinal bir konu olduğu, resmi dökümantasyonda önem taşımamalarıyla vurgulanmaktadır. Dökümantasyon, nesnelere bir cümle ve bir kod örneği ayırdı. Örnek, kalıtımı veya özellikleri (property) göstermiyordu.</p>

<p><strong>PHP 4 ve Sessiz Devrim</strong></p>

<p>PHP 4, dil için bir başka çığır açan adım olsa da, temel değişikliklerin çoğu yüzeyin altında gerçekleşti. Zend Motoru (adı Zeev ve Andi’den türetilmiştir) dili güçlendirmek için sıfırdan yazılmıştır. Zend Engine, PHP’yi çalıştıran ana bileşenlerden biridir. Aramak isteyebileceğiniz herhangi bir PHP fonksiyonu, aslında üst düzey uzantı katmanının bir parçasıdır. Bunlar, veritabanı API’leriyle konuşmak veya dizelerde hokkabazlık yapmak gibi işleri yaparlar. Bunun altında, Zend Engine belleği yönetir, kontrolü diğer bileşenlere devreder ve her gün çalıştığınız tanıdık PHP sözdizimini çalıştırılabilir bayt koduna çevirir. Sınıflar gibi temel dil özellikleri için teşekkür etmemiz gereken Zend Engine’dir.</p>

<p>Nesnel bakış açımızdan, PHP 4’ün ana metotları geçersiz kılmayı ve bunlara alt sınıflardan erişmeyi mümkün kılması büyük bir avantajdı.</p>

<p>Ancak büyük bir dezavantaj devam etti. Bir değişkene nesne atamak, onu fonksiyona göndermek veya metottan döndürmek, bir kopyanın oluşmasıyla sonuçlandı. Bunun gibi bir atama düşünün:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$my_obj = new User('bob');
$other = $my_obj;
</code></pre></div></div>

<p>Bu kod çalıştırıldığında, aynı User nesnesine iki referans yerine, iki User nesnesinin varlığıyla sonuçlandı. Çoğu nesne yönelimli dilde, değere göre değil referansa göre atama beklersiniz. Bu, nesnelerin kendisini kopyalamak yerine nesne işaretçilerini atayacağınız anlamına gelir. Varsayılan değere göre atama davranışı, programcıların farkında olmadan komut dosyasının bir bölümündeki nesneleri değiştirerek, değişikliklerin başka bir yerdeki referanslar aracılığıyla görülmesini bekledikleri için pek çok hatayla sonuçlandı. Bu kitap boyunca, aynı nesnenin birden fazla referansını kullandığım birçok örnek göreceksiniz.</p>

<p>İyi ki, referansa dayalı atamayı zorlamanın bir yolu vardı, ancak bu, hantal bir yapı kullanmayı hatırlamak anlamına geliyordu.</p>

<p>Referans olarak nasıl atayacağınız aşağıda açıklanmıştır:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$other =&amp; $my_obj;
// $other and $my_obj point to same object
</code></pre></div></div>

<p>Bu, referansa göre geçişi zorunlu kılar:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>function setSchool(&amp; $school)
{
    // $school is now a reference to not a copy of passed object
}
</code></pre></div></div>

<p>Ve işte referansla geri dönüş:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>function &amp; getSchool()
{
    // returning a reference not a copy
    return $this-&gt;school;
}
</code></pre></div></div>

<p>İyi çalışsa da, &amp; işaretini eklemeyi unutmak kolaydı ve bu, hataların koda sızmasının çok kolay olduğu anlamına geliyordu. Hataların izini sürmek özellikle zordu, çünkü nadiren raporlanan hatalara neden oluyorlardı.</p>

<p>PHP kılavuzunda genel olarak sözdiziminin, özel olarak nesnelerin kapsamı genişletildi ve nesne yönelimli kodlama akımına doğru yönelim başladı. PHP’deki nesneler tartışmasız değildi (şimdi olduğu gibi) ve “Nesnelere ihtiyacım var mı?” sorusu, posta listelerinde yaygın olarak görülüyordu. Gerçekten de, Zend sitesi, nesne yönelimli programlamayı teşvik eden makalelerin yanı sıra bu konuda uyaran makalelere ev sahipliği yaptı. Referansla geçiş sorunları ve tartışmalara rağmen, birçok programcı kodlarını &amp; karakteriyle doldurdu. Nesne yönelimli PHP popülaritesini artırdı. Zeev Suraski bu konuyu DevX.com için bir makalede yazdı.</p>

<p>“PHP tarihindeki en büyük dönüm noktalarından biri - sınırlı işlevselliğe ve bir dizi sorun ve sınırlamaya rağmen - nesne yönelimli programlamanın gelişmesi ve artan sayıda kullanıma hazır PHP uygulamaları için en popüler paradigma haline gelmesiydi. Çoğunlukla beklenmedik olan bu eğilim, PHP’yi hazırlıksız yakaladı. Nesnelerin diğer dillerdeki nesneler gibi davranmadığı ve bunun yerine <em>ilişkisel</em> diziler gibi davrandığı ortaya çıktı.”</p>

<p>Önceki bölümde belirtildiği gibi, nesne yönelimli tasarıma olan ilgi çevrimiçi sitelerde ve makalelerde belirgin hale geldi. PHP’nin resmi yazılım reposu PEAR nesne yönelimli programlamayı benimsemiştir. Geriye dönüp bakıldığında, PHP’nin nesne yönelimli desteği benimsemesinin kaçınılmaz bir güce isteksiz bir teslimiyet olduğunu düşünmek kolaydır. Nesneye yönelik programlamanın 1960’lardan beri var olmasına rağmen, 1990’ların ortalarında gerçekten zemin kazandığını hatırlamak önemlidir. Nesne yönelimli programlamayı popülerleştiren Java, 1995’e kadar piyasaya sürülmedi. Prosedürel bir dil olan C’nin bir üst kümesi C++, 1979’dan beri ortalıkta dolaşıyor. Uzun bir evrimden sonra, 1990’larda tartışmasız büyük bir sıçrama yaptı. Perl 5, 1994 yılında piyasaya sürüldü, bu, kullanıcılarının nesneler üzerinde düşünmesini mümkün kılan, prosedürel bir dilde başka bir devrimdi. Küçük bir prosedürel dil için PHP, nesne desteğini oldukça hızlı geliştirerek, kullanıcılarının gereksinimlerine gerçek bir yanıt verdi.</p>

<p><strong>Benimsenen Değişim: PHP 5</strong></p>

<p>PHP 5, nesnelerin ve nesne yönelimli programlamanın açık onayını temsil ediyordu. Bu, nesnelerin PHP ile çalışmanın tek yolu olduğu anlamına gelmez (bu arada, bu kitap bunu da söylemiyor). Bununla birlikte, nesneler, enterprise sistemler geliştirmek için güçlü ve önemli bir araç olarak kabul edildi ve PHP, core tasarımında bunları tam olarak destekledi.</p>

<p>Muhtemelen, PHP 5’teki geliştirmelerin önemli etkilerinden biri, dilin daha büyük İnternet şirketleri tarafından benimsenmesiydi. Örneğin Yahoo ve Facebook, PHP’yi yoğun bir şekilde kullanmaya başladı. Sürüm 5 ile PHP, web geliştirme için standart dillerden biri haline geldi.</p>

<p>Nesneler dil sürücüsüne taşınmıştı. Belki de en önemli değişiklik, pass-by-reference davranışıydı. Ancak bu sadece başlangıçtı. Bu kitap boyunca ve özellikle de bu bölümünde, private ve protected metotlar ve properties (özellikler), static anahtar sözcüğü, namespace’ler, type hints (artık type declaration olarak adlandırılmaktadır) ve exceptions (istisnalar) dahil olmak üzere çok daha fazla geliştirmeyle karşılaşacağız. PHP 5 uzun bir süredir (yaklaşık 12 yıl) ortalıkta dolaşıyordu ve yeni özellikler aşamalı olarak piyasaya sürüldü.</p>

<p><strong>Not</strong>: PHP’nin, PHP 5’in tanıtılmasıyla referans olarak atama için tam anlamıyla hareket etmediğini ve bu durumun değişmediğini belirtmekte fayda var. Bunun yerine, varsayılan olarak, bir nesne atandığında, metoda iletildiğinde veya metottan döndürüldüğünde, o nesneye tanımlayıcı kopyalanır. Bu nedenle, &amp; işareti ile referansa göre atamayı zorunlu kılmadığınız sürece, yine de bir kopyalama işlemi gerçekleştiriyorsunuz. Bununla birlikte, pratik açıdan, bu tür kopyalama ile referans yoluyla atama arasında genellikle çok az fark vardır, çünkü aynı hedef nesneye başvuruda bulunursunuz.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class megaquiz_util_Conf
{
}
</code></pre></div></div>

<p>Bunun gibi sınıf adları, paketler arasındaki çakışmaları önlemenin bir yoludur, ancak dolambaçlı kodlar oluşturabilirler. Ayrıca closure, generator, trait, late static binding desteği de verildi.</p>

<p><strong>PHP 7: Boşluğu Kapatmak</strong></p>

<p>Programcılar zorlu bir gruptur. Design pattern’leri seven birçok kişi için, PHP’de hala eksik olan iki temel özellik vardı. Bunlar, skaler tip bildirimleri ve zorunlu dönüş tipleriydi. PHP 5 ile, yalnızca bir nesneye, diziye veya daha sonra çağrılabilir bir koda ihtiyacınız olduğu sürece, metoda veya fonksiyona iletilen değişkenin tipini zorlamak mümkündü. Skaler değerler (integer, string ve float gibi) hiç uygulanamadı. Ayrıca, bir metodun veya fonksiyonun dönüş tipini bildirme şansınız yoktu.</p>

<p>Göreceğiniz gibi, nesne yönelimli tasarım genellikle bir tür sözleşme olarak metot bildirimi kullanır. Metot, belirli girdiler talep eder ve karşılıklı olarak, size belirli bir veri tipini geri vermeyi taahhüt eder. PHP 5 programcıları, birçok durumda bu tür sözleşmeleri sürdürmek için yorumlara, kurallara ve manuel tip denetimine güvenmek zorunda kaldı. Geliştiriciler ve yorumcular genellikle bundan şikayet ediyordu.</p>

<p>PHP 7, skaler tip bildirimlerini (önceden type hint olarak biliniyordu) ve dönüş tipi bildirimlerini tanıttı. Dahası, PHP 7.4, typed property sunarak tip güvenliğini daha da ileri götürdü. Doğal olarak, tüm bunlar bu baskıda ele alınmıştır.</p>

<p>PHP 7 ayrıca, anonim sınıflar ve bazı namespace geliştirmeleri dahil olmak üzere başka güzel şeyler de sağladı.</p>

<p><strong>PHP 8: Konsolidasyon devam ediyor</strong></p>

<p>PHP, diğer dillerin en iyi özelliklerini ödünç almıştır. PHP 8, diğer dillerde annotation olarak bilinen nitelikler de dahil olmak üzere birçok yeni özellik sunar. Bu kullanışlı etiketler, bir sistemdeki sınıflar, metotlar, properties ve sabitler hakkında ek bağlamsal bilgiler sağlamak için kullanılabilir. Ayrıca PHP 8, tip bildirimleri için desteğini genişletmeye devam etmiştir. Bu alanda özellikle ilginç olanı, union tipi bildirimidir. Bu, bir özelliğin veya parametrenin tipinin belirtilen birkaç tipten biriyle sınırlandırabilmenize olanak tanır. PHP’nin tip esnekliğinden yararlanırken aynı zamanda tipleri kilitleyebilirsiniz.</p>

<p><strong>Nesne Tartışması</strong></p>

<p>Nesneler ve nesne yönelimli tasarım, tutku uyandırıyor gibi görünüyor. Pek çok mükemmel programcı, nesneleri kullanmadan yıllarca mükemmel kodlar üretti ve PHP, prosedürel web programlama için etkileyici bir platform olmaya devam ediyor.</p>

<p>Bu kitap doğal olarak baştan sona nesne yönelimli önyargı sergiliyor, bu önyargı benim nesneye bulaşmış bakış açımı yansıtıyor. Konumuz nesneler olduğundan, vurgunun kesinlikle nesne yönelimli olması kaçınılmazdır. Bununla birlikte, bu kitaptaki hiçbir şey, nesnelerin başarıya giden tek gerçek yol olduğunu öne sürmeyi amaçlamamaktadır.</p>

<p>Geliştiricilerin nesne yönelimli bir dil olarak PHP ile çalışmayı seçip seçmemesi bir zamanlar bir tercih meselesiydi. Bu, tamamen kabul edilebilir sistemler yaratılabileceği ölçüde hala geçerlidir. Bazı harika araçlar (örneğin, WordPress) temel mimarilerinde hala prosedüreldir. Bununla birlikte, PHP’nin nesne desteğini kullanmadan ve anlamadan bir PHP programcısı olarak çalışmak giderek daha zor hale geliyor, özellikle de projelerinizde kullanacağınız kütüphanelerin kendileri muhtemelen nesne yönelimli olacakları için.</p>

<p>Yine de, Perl’in şu ünlü mottosunu akılda tutmaya değer: “Bir şeyi yapmanın birden fazla yolu var.” Bu özellikle daha küçük scriptler için geçerlidir, hızlı bir şekilde çalışan bir örnek oluşturmak, daha büyük bir sisteme ölçeklendirilecek bir yapı oluşturmaktan daha önemlidir.</p>

<p>Kod esnek bir ortamdır. İşin püf noktası, konseptinizin ne zaman daha büyük bir geliştirmenin temeli haline geldiğini bilmektir. Artık büyüyen projenize tasarım odaklı bir yaklaşım benimsemeye karar verdiğinize göre, bu kitabın nesne yönelimli mimariler oluşturmaya başlamak için ihtiyaç duyduğunuz yardımı sağlayacağını umuyorum.</p>

<p><strong>Özet</strong></p>

<p>Bu kısa bölüm, nesneleri PHP dilindeki bağlamlarına yerleştirdi. PHP’nin geleceği, nesne yönelimli tasarıma bağlıdır. Sonraki birkaç bölümde, PHP’nin mevcut nesne desteğinin anlık görüntüsünü alacağım ve bazı tasarım konularını tanıtacağım.</p>

<p><strong>Kaynak:</strong> “PHP 8 Objects, Patterns, and Practice”, Matt Zandstra</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Nesneler her zaman PHP projesinin önemli bir parçası değildi. Aslında, bir zamanlar PHP tasarımcıları tarafından “sonradan düşünülmüş” olarak tanımlanmışlardı. Bu bölümde, PHP’nin nesne yönelimli özelliklerinin gelişimini özetleyerek tanıtıyorum.]]></summary></entry><entry><title type="html">PHP - Tasarım ve Yönetim</title><link href="http://localhost:4000/2024/03/20/php-tasarim-ve-yonetim.html" rel="alternate" type="text/html" title="PHP - Tasarım ve Yönetim" /><published>2024-03-20T00:00:00+03:00</published><updated>2024-03-20T00:00:00+03:00</updated><id>http://localhost:4000/2024/03/20/php-tasarim-ve-yonetim</id><content type="html" xml:base="http://localhost:4000/2024/03/20/php-tasarim-ve-yonetim.html"><![CDATA[<p>Temmuz 2004’te PHP 5.0 yayınlandı. Bu sürüm, bir dizi radikal geliştirmeyle geldi. Belki de bunlardan ilki, nesne yönelimli programlama için kökten geliştirilmiş destekti. Bu, PHP topluluğu içinde nesnelere ve tasarıma büyük ilgi uyandırdı. Aslında bu, PHP 4.0 ile  başlayan bir sürecin devamıydı.</p>

<p>Bu bölümde, nesnelerin çözebileceği bazı sorunları inceliyorum. Örüntülerin ve ilgili uygulamaların evriminin bazı yönlerini kısaca özetliyorum.</p>

<p>Ayrıca bu kitapta ele alınan konuları da özetledim.</p>

<ul>
  <li><em>Felaketin evrimi</em>: Bir proje kötüye gidiyor</li>
  <li><em>Tasarım ve PHP</em>: Nesne yönelimli tasarım teknikleri PHP topluluğunda nasıl kök saldı?</li>
  <li><em>Bu kitap</em>: Nesneler, Pattern’ler, Uygulama</li>
</ul>

<h2 id="sorun">Sorun</h2>

<p>Sorun şu ki PHP çok kolay. Fikirlerinizi denemeniz için sizi cezbeder ve iyi sonuçlarla gururunuzu okşar. PHP bunu desteklemek için tasarlandığından, kodunuzun çoğunu doğrudan web sayfalarınıza yazarsınız. Sayfadan sayfaya dahil edilebilecek dosyalara yardımcı işlevler (veritabanı erişim kodu gibi) eklersiniz ve siz farkına bile varmadan çalışan bir web uygulamanız olur.</p>

<p>Felakete doğru ilerliyorsun. Bunu elbette anlamıyorsunuz çünkü siteniz harika görünüyor. İyi performans gösteriyor, müşterileriniz mutlu ve kullanıcılarınız para harcıyor.</p>

<p>Yeni bir aşamaya başlamak için koda geri döndüğünüzde sorun çıkar. Artık daha büyük bir ekibiniz, daha fazla kullanıcınız ve daha büyük bir bütçeniz var. Ancak, hiçbir uyarı olmadan  işler ters gitmeye başlar. Sanki projeniz zehirlenmiş gibi.</p>

<p>Yeni programcınız kodu anlamakta zorlanıyor. Bir ekip üyesi olarak tam güce ulaşması beklediğinizden daha uzun sürüyor.</p>

<p>Bir gün olarak tahmin edilen basit bir değişiklik, sonuç olarak 20 veya daha fazla sayfayı  güncellemeniz gerektiğini keşfettiğinizde üç gün sürer.</p>

<p>Yazılımcılarınızdan biri, bir süre önce aynı kodda yaptığınız büyük değişikliklerin üzerine kendi sürümünü kaydeder. Kayıp üç gün boyunca fark edilmez, bu zamana kadar kendi yerel kopyanızı değiştirmiş olursunuz. Aynı zamanda dosya üzerinde çalışan üçüncü bir geliştiriciyi bekleterek karmaşayı çözmek bir gün sürer.</p>

<p>Uygulamanın popülaritesi nedeniyle, kodu yeni bir sunucuya taşımanız gerekir. Proje elle kurulmalıdır ve dosya yollarının, veritabanı adlarının ve parolaların birçok kaynak dosyaya sabit kodlanmış olduğunu keşfedersiniz. Taşıma sırasında çalışmayı durdurursunuz çünkü yapılandırma değişikliklerinin üzerine yazmak istemezsiniz. Birisinin Apache modülü ModRewrite ile ilgili akıllıca bir şey yaptığı ve uygulamanın artık düzgün çalışması için buna ihtiyaç duyduğu ortaya çıktığında, tahmini iki saat sekiz saat oluyor.</p>

<p>Sonunda 2. faza geçtiniz. Bir buçuk gün boyunca her şey yolunda. İlk hata raporu, siz ofisten çıkmak üzereyken gelir. Müşteri şikayet etmek için dakikalar sonra telefon eder. Rapor ilkine benziyor, ancak biraz daha incelendiğinde, benzer davranışlara neden olan farklı bir hata olduğu ortaya çıkıyor. Projenin geri kalanında kapsamlı değişiklikler gerektiren, fazın başlangıcındaki basit değişikliği hatırlıyorsunuz.</p>

<p>Gerekli değişikliklerin tümünün yerinde olmadığını anlıyorsunuz. Bunun nedeni, başlangıçta atlanmış olmaları veya söz konusu dosyaların üzerine yazılmasıdır. Hataları düzeltmek için gereken değişiklikleri aceleyle yaparsınız. Değişiklikleri test etmek çok acele ediyorsunuz, ancak bunlar basit bir kopyala ve yapıştır meselesi, öyleyse ne ters gidebilir?</p>

<p>Ertesi sabah, ofise geldiğinizde bir alışveriş sepeti modülünün bütün gece kapalı olduğunu görürsünüz. Yaptığınız son dakika değişiklikleri, baştaki tırnak işaretini atlayarak kodu kullanılamaz hale getirdi. Elbette, siz uyurken, diğer zaman dilimlerindeki potansiyel müşteriler tamamen uyanıktı ve mağazanızda para harcamaya hazırdı. Sorunu çözersiniz, müşterileri sakinleştirirsiniz ve ekibi başka bir günkü yangınla mücadele için bir araya getirirsiniz.</p>

<p>Kod yazan insanların bu günlük hikayesi biraz abartılı görünebilir, ancak tüm bunların tekrar tekrar olduğunu gördüm. Pek çok PHP projesi hayata küçük başlar ve canavarlara dönüşür.</p>

<p>Sunum katmanı aynı zamanda uygulama mantığını da içerdiğinden, veritabanı sorguları, kimlik doğrulama kontrolleri, form işlemleri ve daha fazlası sayfadan sayfaya kopyalanır. Bu kod bloklarından birinde değişiklik gerektiğinde, kodun bulunduğu her yerde yapılması gerekir.</p>

<p>Dokümantasyon eksikliği kodun okunmasını zorlaştırır ve test eksikliği belirsiz hataların dağıtıma kadar farkedilmesini engeller. Bir işin değişen doğası, genellikle kodun, temel olarak uygun olmadığı görevleri yerine getirene kadar asıl amacından uzaklaşması anlamına gelir. Bu tür bir kod genellikle arap saçına döndüğünden, yeni amaca uyacak şekilde kısımları değiştirmek ve yeniden yazmak imkansız değilse bile zordur.</p>

<h2 id="php-ve-diğer-diller">PHP ve Diğer Diller</h2>

<p>PHP’nin olağanüstü popülaritesi, sınırlarının erken ve zorlu bir şekilde test edilmesi anlamına geliyordu. Bir sonraki bölümde göreceğiniz gibi, PHP hayata kişisel web sayfalarını yönetmek için bir dizi makro olarak başladı. PHP 3’ün ve daha büyük ölçüde PHP 4’ün gelişiyle, dil hızla büyük kurumsal web sitelerinin arkasındaki başarılı güç haline geldi. Bununla birlikte, birçok yönden, PHP’nin başlangıç mirası, script tasarımı ve proje yönetimine taşındı. Bazı çevrelerde PHP, sunum (presentation) görevleri için en uygun olan hobi dili olarak haksız bir üne sahipti.</p>

<p>Bu sıralarda (milenyum başında), diğer topluluklarda yeni fikirler geçerlilik kazanıyordu. Nesne yönelimli tasarıma olan ilgi, Java topluluğunu harekete geçirdi. Java nesne yönelimli bir dil olduğu için bunun gereksiz olduğunu düşünebilirsiniz. Ancak sınıfları ve nesneleri kullanmak kendi başına belirli bir tasarım yaklaşımı sağlamaz.</p>

<p>Bir sorunu, çözümünün özüyle birlikte tanımlamanın yolu olarak design pattern (tasarım deseni) kavramı ilk kez 1970’lerde tartışıldı. Fikir - belkide en uygun şekilde - bilgisayar bilimi alanından değil, mimarlık alanından, Christopher Alexander’ın ufuk açıcı bir çalışmasıyla ortaya çıkmıştır: A Pattern Language (Oxford University Press, 1977). 1990’ların başlarında, nesne yönelimli programcılar, yazılım tasarımı problemlerini adlandırmak ve tanımlamak için aynı tekniği kullanıyorlardı. Erich Gamma, Richard Helm, Ralph Johnson ve John Vlissides’ın (bundan böyle bu kitapta “Gang of Four” takma adlarıyla anılacaklardır) yazdığı tasarım desenleri üzerine ufuk açıcı kitap Design Patterns: Elements of Reusable Object-Oriented Software bugün hala vazgeçilmezdir. İçerdiği pattern’ler, bu alana yeni başlayan herkes için gerekli bir ilk adımdır, bu nedenle bu kitaptaki pattern’lerin çoğu ondan alınmıştır.</p>

<p>Java dilinin kendisi, API’sinde birçok core pattern’i konuşlandırdı, ancak design pattern’lerin topluluğun genelinin bilincine sızması 1990’ların sonlarını buldu. Pattern’ler hızla Main Street kitapçılarının bilgisayar bölümlerine ulaştı ve ilk ateşli tartışmalar mail listelerinde ve forumlarda başladı.</p>

<p>İlgili konular da önem kazandı. Bunların arasında Kent Beck tarafından desteklenen Extreme Programming (XP) de vardı. XP, esnek, tasarım yönlü, yüksek odaklı planlama ve yürütmeyi teşvik eden bir proje yaklaşımıdır.</p>

<p>XP’nin ilkeleri arasında öne çıkan, testin bir projenin başarısı için çok önemli olduğu konusundaki ısrardır. Testler otomatikleştirilmeli, sık çalıştırılmalı ve tercihen kodlar yazılmadan önce tasarlanmalıdır.</p>

<p>XP ayrıca projelerin küçük (çok küçük) iterasyonlara bölünmesi gerektiğini de belirtir. Hem kod hem de gereksinimler her zaman incelenmelidir. Mimari ve tasarım, kodun sık sık gözden geçirilmesine yol açan, paylaşılan ve sürekli bir sorun olmalıdır.</p>

<p>XP, tasarım hareketinin militan kanadıysa, o zaman ılımlı eğilim, programlama hakkında şimdiye kadar okuduğum en iyi kitaplardan biri tarafından iyi bir şekilde temsil ediliyor: The Pragmatic Programmer: From Journeyman to Master by Andrew Hunt and David Thomas.</p>

<p>XP, bazıları tarafından biraz kült olarak kabul edildi, ama en üst düzeyde yirmi yıllık nesne yönelimli pratikle ortaya çıktı. Özellikle, yeniden düzenleme olarak bilinen code revision, pattern’lere güçlü bir yardımcı-tamamlayıcı olarak ele alındı.</p>

<p>Yeniden düzenleme 1980’lerden beri gelişti, ancak Martin Fowler’ın 1999’da yayınlanan ve alanı tanımlayan “Refactoring: Improving the Design of Existing Code” adlı kitabında  sistematikleştirildi.</p>

<p>XP’nin ve pattern’lerin öne çıkmasıyla birlikte test etme sıcak bir konu haline geldi. Otomatik testlerin önemi, Java programcılarının cephaneliğinde önemli bir silah haline gelen güçlü JUnit test platformunun piyasaya sürülmesiyle daha da vurgulandı. Konuyla ilgili dönüm noktası niteliğinde bir makale, Kent Beck ve Erich Gamma tarafından yazılan “Test Infected: Programmers Love Writing Tests”, konuya mükemmel bir giriş yapıyor ve büyük ölçüde etkili olmaya devam ediyor.</p>

<p>PHP 4, yaklaşık olarak bu sıralarda piyasaya sürüldü ve beraberinde verimlilikte iyileştirmeler ve en önemlisi, nesneler için gelişmiş destek getirdi. Bu geliştirmeler, tamamen nesne yönelimli projeleri bir olasılık haline getirdi. Programcılar, biraz şaşırtacak şekilde bu özelliği benimsedi. Bir sonraki bölümde göreceğiniz gibi, PHP’nin nesne desteği kesinlikle mükemmel değildi. Ancak disiplin ve sözdiziminin dikkatli kullanımı ile kişi aynı anda nesneler ve PHP hakkında düşünmeye başlayabilir.</p>

<p>Yine de, bu bölümün başında tasvir edilene benzer tasarım felaketleri yaygın olmaya devam etti. Tasarım kültürü olgunlaşmamıştı ve PHP ile ilgili kitaplarda neredeyse yoktu. Ancak çevrimiçi ortamda ilgi olduğu açıktı. Leon Atkinson, 2001’de Zend için PHP ve pattern’ler hakkında bir makale yazdı ve Harry Fuecks, 2002’de www.phppatterns.com’da (şu anda  kullanılmayan) kendi blog’unu başlattı. BinaryCloud gibi model tabanlı framework projelerinin yanı sıra otomatikleştirilmiş test ve belgeleme araçları ortaya çıkmaya başladı.</p>

<p>İlk PHP 5 beta sürümünün 2003 yılında piyasaya sürülmesi, nesne yönelimli programlama dili olarak PHP’nin geleceğini garantiledi. Zend Engine 2, büyük ölçüde geliştirilmiş nesne desteği sağladı. Aynı derecede önemli olan, nesnelerin ve nesne yönelimli tasarımın artık PHP projelerinin merkezinde yer aldığına dair bir sinyal gönderdi.</p>

<p>Yıllar geçtikçe PHP 5, namespace ve closure gibi önemli yeni özellikleri bünyesine katarak gelişmeye ve evrilmeye devam etti. Bu süre zarfında, sunucu taraflı web programlama için en iyi seçenek olarak ününü sağlamlaştırdı.</p>

<p>Aralık 2015’te yayınlanan PHP 7, bu eğilimin devamını temsil ediyordu. Özellikle, birçok geliştiricinin (bu kitabın önceki sürümleriyle birlikte) yıllardır yaygara kopardığı iki özellik olan hem parametre hem de dönüş tipi bildirimleri için destek sağladı. Anonim sınıflar, geliştirilmiş bellek kullanımı ve artırılmış hız dahil olmak üzere birçok başka özellik ve iyileştirme vardı. Yıllar geçtikçe dil, nesne yönelimli yazılımcının bakış açısından istikrarlı bir şekilde daha sağlam, daha temiz ve daha eğlenceli hale geldi.</p>

<p>Aralık 2020’de, PHP 7’nin yayınlanmasından neredeyse tam beş yıl sonra, PHP 8’in piyasaya sürülmesi gerekiyor. Bazı uygulama ayrıntıları değişebilir (ve bu kitabın yazılması sırasında biraz değişmiş olsa da), özellikler bu yazının yazıldığı tarihte (Ağustos 2020) zaten mevcuttur. Birçoğunu burada ayrıntılı olarak ele alıyorum. Tip bildirimlerinde iyileştirmeler, modern özellik ataması ve diğer birçok yeni özellik içerir.</p>

<h2 id="kitap-hakkında">Kitap Hakkında</h2>

<p>Bu kitap, nesne yönelimli tasarım alanında çığır açmaya çalışmıyor. Bunun yerine, PHP bağlamında, bazı yerleşik tasarım ilkelerini ve bazı temel pattern’leri (özellikle klasik Gang of Four kitabı olan Design Patterns’te yazılanları) inceliyorum. Son olarak, bir projenin başarısını sağlamaya yardımcı olabilecek araçlara ve tekniklere bakmak için katı kod sınırlarının ötesine geçiyorum. Giriş ve kısa bir sonuç dışında, kitap üç ana bölüme ayrılmıştır: nesneler, pattern’ler ve uygulama.</p>

<p><strong>Nesneler</strong></p>

<p>Bölüm 1’e PHP’nin ve nesnelerin tarihine hızlı bir bakışla başlıyorum, PHP 3’teki sonradan düşünülenden PHP 5’teki temel özelliğe geçişi çiziyorum.</p>

<p>Nesneler hakkında çok az bilginiz olsa da, hiç bilginiz olmadan da deneyimli ve başarılı bir PHP programcısı olabilirsiniz. Bu nedenle nesneleri, sınıfları ve kalıtımı açıklamaya temel ilkelerden başlıyorum. Bu erken aşamada bile, PHP 5, PHP 7 ve PHP 8’in getirdiği bazı nesne geliştirmelerine bakıyorum.</p>

<p>Temel bilgileri aldığınızda, PHP’nin daha gelişmiş nesne yönelimli özelliklerini inceleyerek konumuzun derinliklerine iniyorum. PHP’nin nesneler ve sınıflarla çalışmanıza yardımcı olmak için sağladığı araçlara da bir bölüm ayırdım.</p>

<p>Bununla birlikte, bir sınıfın nasıl tanımlanacağını bilmek ve onu bir nesneyi başlatmak için kullanmak yeterli değildir. Öncelikle sisteminiz için doğru katılımcıları seçmeli ve onlar için en iyi etkileşim yollarına karar vermelisiniz. Bu seçimleri açıklamak ve öğrenmek, nesne araçları ve sözdizimi hakkındaki çıplak gerçekleri öğrenmekten çok daha zordur. Bölüm 1’i PHP ile nesne yönelimli tasarıma girişle bitiriyorum.</p>

<p><strong>Pattern’ler</strong></p>

<p>Pattern, yazılım tasarımındaki bir sorunu tanımlar ve çözümün özünü sağlar. Buradaki “Çözüm”, bir cookbook’ta bulabileceğiniz türden kes-yapıştır kodu anlamına gelmez (yine de cookbook’lar programcı için birer kaynaktır). Bunun yerine, design pattern, bir sorunu çözmek için kullanılabilecek yaklaşımı tanımlar.</p>

<p>Bölüm 2, design pattern’leri tanımlayarak ve yapılarını açıklayarak başlar. Popülerliklerinin ardındaki bazı nedenlere de bakıyorum.</p>

<p>Pattern’ler, belirli temel tasarım ilkelerini destekleme ve takip etme eğilimindedir. Bunların anlaşılması, pattern’in motivasyonunun analiz edilmesine yardımcı olabilir ve tüm programlamaya faydalı bir şekilde uygulanabilir. Bu ilkelerin bazılarını tartışıyorum. Sınıfları ve etkileşimlerini tanımlamanın platformdan bağımsız bir yolu olan Unified Modeling Language (UML) de inceliyorum.
Bu kitap bir pattern kataloğu olmasa da, en ünlü ve faydalı pattern’lerden bazılarını inceliyorum. Her pattern’in ele aldığı sorunu anlatıyor, çözümü analiz ediyor ve PHP’de bir uygulama örneği sunuyorum.</p>

<p><strong>Uygulama</strong></p>

<p>Doğru yönetilmezse, doğru dengelenmiş bir mimari bile başarısız olur. 3. Bölümde, projenizin başarısını garanti altına alan bir framework oluşturmanıza yardımcı olacak mevcut araçlara bakıyorum. Kitabın geri kalanı tasarım ve programlama pratiği hakkındaysa, 3. Kısım kodunuzu yönetme pratiği hakkındadır. İncelediğim araçlar, bir proje için destek yapısı oluşturarak, ortaya çıkan hataları izlemeye yardımcı olabilir, programcılar arasında işbirliğini teşvik edebilir ve kurulum kolaylığı ve kod netliği sağlayabilir.</p>

<p>Otomatik testin gücünden daha önce bahsetmiştim. Bölüm 3’e, bu alandaki sorunlara ve çözümlere genel bir bakış sunan giriş bölümüyle başlıyorum.</p>

<p>Birçok programcı, her şeyi kendi başına yapma dürtüsüne yenik düşmekten suçludur. Composer, ana reposu olan Packagist ile birlikte, kolaylıkla projelere eklenebilecek binlerce  pakete erişim sunar.</p>

<p>Composer konusu açılmışken, bir paketin dağıtımını tek bir komut kadar basit hale getiren kurulum mekanizmasını inceliyorum.</p>

<p>Kod işbirliği ile ilgilidir. Bu gerçek ödüllendirici olabilir. Aynı zamanda tam bir kabus olabilir. Git, birçok programcının birbirlerinin çalışmalarının üzerine yazmadan aynı kod tabanı üzerinde birlikte çalışmasını sağlayan bir sürüm kontrol sistemidir. Geliştirmenin herhangi bir aşamasında projenizin anlık görüntülerini almanıza, kimin hangi değişiklikleri yaptığını görmenize ve projeyi birleştirilebilir dallara ayırmanıza olanak tanır. Git projenizi bir gün kurtaracak.</p>

<p>İnsanlar ve kütüphaneler işbirliği yaptıklarında, partiye genellikle farklı gelenekler ve stiller getirirler. Bu sağlıklı olmakla birlikte birlikte çalışabilirliği de baltalayabilir. Uymak ve kabul etmek gibi sözcükler tüylerimi ürpertiyor ama internetin yaratıcılığının standartlarla desteklendiği inkar edilemez. Belirli geleneklere uyarak, hayal edilemeyecek kadar geniş bir sanal alanda oynamak için özgürleşiriz. Bu yüzden PHP standartlarını, bize nasıl yardımcı olabileceklerini, nasıl ve neden uymamız gerektiğini keşfediyorum.</p>

<p>İki gerçek kaçınılmaz görünüyor. İlk olarak, hatalar genellikle aynı kod bölgesinde tekrarlanır ve bazı iş günleri déjà vu’da bir egzersiz haline getirir. İkincisi, genellikle iyileştirmeler düzelttikleri kadar veya daha fazla bozarlar. Otomatik test, kodunuzdaki sorunlar için bir erken uyarı sistemi sağlayarak bu sorunların her ikisini de ele alabilir. İlk önce Smalltalk için tasarlanmış, ancak şimdi başta Java olmak üzere birçok dile taşınan xUnit test platformunun güçlü bir uygulaması olan PHPUnit’i tanıtıyorum. Özellikle PHPUnit’in özelliklerine ve daha genel olarak test etmenin faydalarına ve maliyetine bakıyorum.</p>

<p>Uygulamanız dağınıksa dosyaların standart olmayan konumlara yüklenmesi gerekebilir, veritabanı kurmak veya sunucu yapılandırmasına yama uygulamak isteyebilir. Kısacası, uygulamaların kurulum sırasında yapılması gerekenler vardır. Phing, Ant adlı bir Java aracının bağlantı noktasıdır. Phing ve Ant, bir yapılandırma dosyasını yorumlar ve kaynak dosyalarınızı onlara söylediğiniz şekilde işler. Bu genellikle dosyalarınızı bir dizinden sisteminizdeki çeşitli hedef konumlara kopyalamak anlamına gelir, ancak ihtiyaçlarınız daha karmaşık hale geldikçe, Phing bunları karşılamak için kolayca ölçeklenir.</p>

<p>Bazı şirketler geliştirme platformlarını zorunlu kılar, ancak çoğu durumda ekipler bir dizi farklı işletim sistemi çalıştırır. Bunların tümü, kolaylıkla bir LAMP yığınını çalıştıracaktır. Ancak ideal olarak, geliştiricilerin kodlarını production’a çok benzeyen ortamlarda çalıştırmaları gerekir. Ekip üyelerinin kendilerine özgü geliştirme platformlarını koruyabilmeleri ancak production benzeri bir sistemde kodlarını çalıştırabilmeleri için sanallaştırma uygulaması olan Vagrant’ı inceliyorum.</p>

<p>Testler oldukça faydalıdır, ancak testlerinizi yüklemeniz, çalıştırmanız ve buna devam etmeniz gerekiyor. Build ve testlerinizi otomatikleştirmezseniz, rehavete kapılmak ve her şeyin kaymasına izin vermek kolaydır. Tam da bu konuda size yardımcı olacak “sürekli entegrasyon” kategorisinde bir araya toplanmış bazı araçlara ve tekniklere bakıyorum.</p>

<p><strong>Kaynak:</strong> “PHP 8 Objects, Patterns, and Practice”, Matt Zandstra</p>]]></content><author><name>Melih Kocatürk</name><email>melih.kocaturk@icloud.com</email></author><summary type="html"><![CDATA[Temmuz 2004’te PHP 5.0 yayınlandı. Bu sürüm, bir dizi radikal geliştirmeyle geldi. Belki de bunlardan ilki, nesne yönelimli programlama için kökten geliştirilmiş destekti. Bu, PHP topluluğu içinde nesnelere ve tasarıma büyük ilgi uyandırdı. Aslında bu, PHP 4.0 ile başlayan bir sürecin devamıydı.]]></summary></entry></feed>